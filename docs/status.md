# OPL 当前状态

Owner: `One Person Lab`
Purpose: `status`
State: `active_truth`
Machine boundary: 本文是核心人读真相面。机器真相继续归 `contracts/`、source、CLI/API 行为、runtime ledger、provider receipt、domain-owned manifest 和真实 workspace / App evidence。
更新时间：`2026-06-14`

Plugin native profile pointer: `contracts/opl-native-profile.json` 只声明 OPL Flow / OPL Doc 插件同步与 drift 检查所需的 repo-native profile；它不是 framework truth、runtime truth、domain truth、artifact authority、owner receipt 或 production-ready 证据。

OPL Flow closeout gate: `contracts/opl-framework/opl-flow-completion-audit-contract.json` 是目标态交付的机器化后验证合同。凡 claim scope 属于 `thorough_landing`、`complete_execution`、`end_to_end_acceptance`、`production_ready_claim` 或 `release_ready_claim`，closeout 前必须给出逐项 plan completion audit、完成度百分比、fresh evidence refs、missing refs、next action 和 subagent/worktree lane mapping。docs/catalog/plan/read-model/refs-only surface/contract/test/commit-only evidence 不能单独支撑 `100%` 或 complete claim；仍有非 100% 条目时只能继续推进，或在明确 owner / source of truth / 权限 / 外部依赖 / 验证边界 blocker 下输出 typed blocker。

## 读法

本文只保存当前角色、当前成熟度、完成边界、动态真相入口和仍未闭合的缺口类别。它不冻结 receipt id、attempt id、workorder 数字、open/closed counter、branch/SHA、provider tick、ledger verify 流水或某轮 closeout 细节。

需要当前事实时，先读本文给出的 live 命令、contracts/source/tests、runtime ledger、provider receipt、domain-owned manifest、App release/user-path evidence 和真实 workspace evidence。近期 read-model 重构后，主要 CLI 的 JSON 顶层均有 payload wrapper：`opl framework readiness --family-defaults --json` 读取 `.framework_readiness`，`opl framework operating-maturity --family-defaults --json` 读取 `.framework_operating_maturity`，`opl runtime app-operator-drilldown --json` 读取 `.app_operator_drilldown`，`opl brand-modules l5-status --json` 读取 `.brand_module_l5_status`，`opl family-runtime attempt list --json` 读取 `.family_runtime_stage_attempts`，`opl runway readiness --json` 读取 `.opl_runway_readiness`，`opl runway recovery-repair --json` 读取 `.opl_runway_recovery_repair`；不要沿用旧顶层字段路径判断计数、owner、repair guard 或 ready。`opl runtime app-operator-drilldown --json` 的 fast payload 不保证暴露所有 detail section；App release/user-path fast counters 当前读 `.app_operator_drilldown.summary.app_release_user_path_evidence_*`，release / production claim flags 读 `.app_operator_drilldown.summary.app_release_user_path_*_claimed` 与 `.app_operator_drilldown.summary.app_release_user_path_production_user_path_ready`；完整 refs-only evidence object 读 `opl runtime app-operator-drilldown --detail full --json` 的 `.app_operator_drilldown.app_release_user_path_evidence`；owner gate 聚合读 `.framework_operating_maturity.app_release_user_path` 和 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.app_release_user_path`。`opl agents default-callers --family-defaults --json` 当前仍是顶层对象，关键字段直接读 `deletion_evidence_worklist_count`、`owner_decision_status`、`default_caller_delete_ready` 和 `physical_delete_authorized`。Brand L5 独立 readout 的 runtime ledger 计数在 `.brand_module_l5_status.evidence_ledger`，不是旧的 `refs_only_ledger` 路径；`framework operating-maturity` 内的 Brand L5 聚合在 `.framework_operating_maturity.brand_module_l5`。需要历史过程时，读 `docs/history/**`、runtime ledger、提交历史或 domain-owned receipt/provenance。

2026-06-14 runtime readback discipline：`framework readiness` 当前读取 `.framework_readiness`；`status=framework_control_plane_available_with_open_production_tail` 仍表示 control plane 可读但 production tail 未闭合。`.attention_first_payload.summary` 的 attention / workorder / blocker counters、`.framework_readiness.summary.domain_manifest_*` workspace currentness arrays、以及 `.framework_readiness.current_owner_delta.task_or_study_ref` / `lineage_ref` / `source_fingerprint` 都是高漂移 runtime 派生值，只能 live-read，不在本文冻结。当前 owner delta 可能随 domain/App live owner、route-back 或 currentness identity 切换；本文只保留稳定结论：`.framework_readiness.current_owner_delta.latest_owner_answer_ref=null`、`.framework_readiness.current_owner_delta.hard_gate.domain_ready_authorized=false`、`.framework_readiness.summary.owner_delta_handoff_status=needs_domain_or_app_live_owner_payload` 时，readiness current pointer 仍未闭合。Workspace currentness 必须同时读 `opl workspace projects --json` 和 `.framework_readiness.summary.domain_manifest_*`。稳定真相是这些字段归 live command owner，而不是某一次数组或计数。任何 stale owner payload summary、refs-only typed blocker summary、attention counter、workspace sample 或已验证但未进入 readiness current pointer 的 summary 都不能借位写成 current pointer closeout、MAS paper ready、domain ready 或 production ready。

`framework operating-maturity` 当前 truth 读取 `.framework_operating_maturity`。稳定结论仍是 `status=evidence_required`、`.framework_operating_maturity.summary.ready_claim_authorized=false`，`.framework_operating_maturity.summary.current_owner_delta_owner_answer_missing=true` / `current_owner_delta_owner_answer_still_required=true`，且 owner gates 不能由 OPL 单独关闭。`.framework_operating_maturity.current_owner_delta_bridge` 必须跟随 `.framework_readiness.current_owner_delta` live-read；旧 owner payload summaries 可随新 identity 进入 stale refs。maturity bridge 的 `latest_owner_answer_ref` / `latest_owner_answer_kind` 可能指向当前 identity 的 refs-only domain-owner-payload-summary，例如 `typed_blocker_ref`，但它仍不是 `.framework_readiness.current_owner_delta.latest_owner_answer_ref`，也不是 readiness current pointer owner answer。只有 `readiness_current_pointer_owner_answer_ref` 进入 current pointer binding，且 owner/source/currentness/idempotency binding 满足时，才算 owner answer handoff 闭合；`owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim` 与 `observed_owner_answer_ref_shapes=["typed_blocker_ref"]` 的组合只能说明 OPL 已观察并验证 owner-shaped payload summary。当前 owner gate inventory 也会在 `.framework_operating_maturity.unresolved_owner_gates.gates[] | select(.gate_id=="owner-gate:current_owner_delta_owner_answer")` 直接暴露 `observed_ref_shapes`、`observed_owner_answer_refs`、`observed_owner_payload_summary_receipt_refs`、`verified_owner_payload_summary_receipt_refs`、`owner_payload_summary_closure_state`、`current_pointer_update_still_required` 和 `observed_refs_are_current_pointer_closeout=false`；自动化应读这些字段定位 owner handoff，不能把 observed refs 当 closeout。W7 owner-route work orders 机器消费优先读取 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.<lane>`；`owner_route_work_orders[]` 只保留数组兼容与人工扫描，不应被自动化转成 numeric-key map。Work order 的 `status` 区分 `open`、`owner_evidence_recorded` 和 `owner_acceptance_required`：`owner_evidence_recorded` 只表示 lane-specific open count 已清零且观察到 owner evidence，仍必须同时读 `owner_evidence_closure_state`、`owner_acceptance_required`、`ready_claim_authorized` 和 lane authority boundary。App drilldown 的 memory/artifact/lifecycle、cleanup-retirement、Brand L5 evidence ledger 和 default-callers cleanup worklist同样是 refs-only live projection：observed refs、deletion worklist、verified receipt、open count、handoff decision 等数字都会随 ledger 变化，不能写成 active truth completion。稳定边界是：这些读面只定位下一 owner、accepted ref shape、observed ref shape、closing source、verify path 和 false-authority boundary；不能授权 domain ready、App release ready、Brand L5、physical delete、memory accepted、artifact mutation、package/export readiness 或 production ready，也不能让 OPL 生成 owner receipt、typed blocker 或 quality/export/review verdict。

2026-06-13 maintenance foldback：App operator drilldown 的 core refs 构建逻辑已从 `src/runtime-tray-app-operator-drilldown.ts` 抽出到 `src/runtime-tray-app-operator-drilldown-parts/core-refs.ts`；当前入口约 `612` 行，新增语义 part 约 `420` 行。`family-runtime-enqueue` dedupe reconcile 也完成行为保持的语义拆分：`src/family-runtime-enqueue-parts/existing-dedupe-reconcile.ts` 从 `989` 行降到约 `712` 行，新增 `src/family-runtime-enqueue-parts/existing-dedupe-requeue.ts` 约 `118` 行封装 existing-task requeue apply 逻辑。fresh scoped `family:structure-advisory --repo one-person-lab=<current checkout>` 显示 `needs_design_pass=0`、`mechanical_residue=0`、`public_surface_risk=8`；`src/runtime-tray-app-operator-drilldown.ts` 与 `existing-dedupe-reconcile.ts` 都不再是 source-shape advisory 项。该改动只提升 Console / App operator read model 与 Runway queue/currentness reconcile 的维护边界，不改变上述 7 个 owner gates，不授权 ready、delete、L5 或 production claim。

2026-06-14 family-orchestration source-boundary foldback：`buildFamilyOrchestrationCompanion` 已从 `src/family-orchestration.ts` 抽出到 `src/family-orchestration-parts/companion.ts`，通用 helper 抽到 `src/family-orchestration-parts/shared.ts`；当前入口约 `738` 行，两个 part 分别约 `273` / `86` 行。focused `tests/src/family-orchestration.test.ts` 与 `npm run typecheck` 已通过。该项只证明 family orchestration companion builder 的源码边界更清楚，不改变 family orchestration schema、domain owner authority、current owner pointer、App release、Brand L5、provider long-soak、memory/artifact/lifecycle 或 production-ready 状态。

2026-06-14 Runway runtime refactor foldback：近期 runtime 重构已把 Temporal scheduler cadence 的实现边界收敛到 `src/family-runtime-temporal-provider-parts/scheduler-cadence.ts`，`src/family-runtime-scheduler.ts` 只保留 provider-gated command / SLO / queue pickup orchestration；worker readiness status resolver 已收为 `src/family-runtime-temporal-readiness.ts` 私有 helper，外部只通过 `buildTemporalWorkerReadiness`、`inspectTemporalWorkerLifecycle*`、`opl family-runtime worker status --provider temporal --json` 和 Runway read-model 消费状态。fresh readout 显示 `opl runway readiness --json` 的 `.opl_runway_readiness.readiness_status=blocked_provider_not_ready`，`.provider_runtime.degraded_reason=temporal_worker_source_stale`；`opl runway recovery-repair --json` 的 `.opl_runway_recovery_repair.repair_status=repair_blocked_by_worker_restart_guard`，`worker_restart_guard.guard_status=blocked`，`repair_blocker_ids=["active_stage_attempts_present"]`，`repair_can_mutate_worker=false`。`opl family-runtime worker status --provider temporal --json` 仍为 `readiness_status=worker_source_stale`，Temporal service reachable、worker dependency ready、visibility ready，但 managed worker source 不再 current；`opl family-runtime scheduler status --provider temporal --json` 因同一 provider blocker 返回 `status=blocked_provider_not_ready`。这次重构只收紧 Runway / provider 可维护边界和 public API 面，不关闭 current owner delta、domain owner-chain、Brand L5、App release、provider long-soak、memory/artifact/lifecycle 或 production-ready。

2026-06-14 durable objective update：原任务“持续推进 OPL family ideal operating model 在所有相关 repo 中落地”继续有效，但当前 active 解释必须更新为 `W7 owner-evidence / production-tail closure`，而不是继续用 OPL 基座改动、docs foldback、structure cleanup 或 refs-only ledger 代替完成。fresh `framework operating-maturity` 仍有 unresolved owner gates，且所有 gate 都是 `ready_claim_authorized=false` / `can_be_completed_by_opl=false`。当前 owner delta identity 必须从 `.framework_readiness.current_owner_delta` 与 `.framework_operating_maturity.current_owner_delta_bridge` live-read；本轮样本已证明旧 owner payload summaries 可随新 identity 进入 stale refs，当前 identity 的 refs-only payload summary 也可能已记录/验证但仍未进入 readiness current pointer。下一优先级是让对应 domain/App/route-back owner 把同一 live identity 的 domain-owned typed blocker、route-back、updated currentness evidence、domain owner receipt 或 quality gate receipt 折成 readiness 可消费的 current pointer owner answer，并把 `readiness_current_pointer_owner_answer_ref=null` 收口。其余 lane 继续按 owner repo 取得真实 receipt、typed blocker、human gate、release verdict、long-soak / recovery / dead-letter evidence、physical delete decision 或 memory/artifact/lifecycle receipt；OPL docs 和 read model 只记录、验证、投影和防误判。

2026-06-14 落地文档更新：本次 fresh readout 再次确认文档目标需要更新为“把已落地的 OPL 基座能力折回 active owner-evidence baton”，而不是继续把已验证的 read model 当成完成。`framework readiness` 当前仍是 `framework_control_plane_available_with_open_production_tail`，`current_owner_delta.current_owner=med-autoscience`，`current_owner_delta` 要求 `domain_owner_receipt_ref` / `quality_gate_receipt_ref` / `typed_blocker_ref`，且 readiness 层的 `latest_owner_answer_ref=null`。`framework operating-maturity` 当前仍是 `evidence_required` / `ready_claim_authorized=false`；它已经观察并验证了同一 current-owner identity 的 refs-only typed-blocker payload summary，bridge 层 `latest_owner_answer_ref` 可指向该 refs-only summary 且 `latest_owner_answer_kind=typed_blocker_ref`，但 `owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim`、`readiness_current_pointer_owner_answer_ref=null`、`owner_answer_missing=true`、`owner_answer_still_required=true` 仍表示 current pointer 未闭合。因此当前文档 closeout 只允许声明：OPL Framework 的 owner-delta-first readout、payload-summary intake、Runway repair guard、Brand L5 ledger、App release/user-path evidence lane、cleanup read model 和 memory/artifact/lifecycle projection 已能把后续工作准确路由给 owner；它们仍不能声明 current pointer closeout、MAS paper ready、domain ready、App release ready、Brand L5、physical delete、long-soak complete 或 production ready。

2026-06-14 MAS owner-route dry-run foldback：DM-CVD study `003-dpcc-primary-care-phenotype-treatment-gap` 的 MAS controller dry-run / `study progress` 继续显示当前 live owner route 不可由 OPL 文档或 read-model 直接闭合。Fresh readback 现在按 owner split 投影 `opl_execution_authorization_required`：canonical `current_work_unit.status=typed_blocker`、`current_execution_envelope.state_kind=typed_blocker`，canonical owner 是 OPL runtime owner `one-person-lab`；embedded typed blocker / PaperRecovery obligation owner 仍是 `gate_clearing_batch`，action 是 `run_gate_clearing_batch`，stage 是 `publication_supervision`，work unit 是 `publication_gate_replay`，work-unit fingerprint 是 `sha256:2c4793a4e41859fd21a0bc088459c85f298bacb7d06eea811b44beae568fbf9f`。Runtime health 同步显示 `canonical_runtime_action=external_supervisor_required`、`attempt_state=escalated`、`retry_budget_remaining=0`，worker liveness 为 `not_live` / `parked`，`active_run_id=null`，`running_provider_attempt=false`，`provider_admission_candidates=[]`。Study truth 仍有 `publication_supervisor_state.bundle_tasks_downstream_only`，medical publication surface 与 publication gate 均 blocked，因此 bundle/build/proofing 仍是下游工作，不能当作当前 owner delta。既有 closeout typed blocker ref `.../sat_e1063d97901cc3d70424fc5c.closeout.json#domain_blocker` 是 MAS domain-owned blocker / OPL execution authorization blocker evidence；maturity 可以把它观察为 `typed_blocker_ref` 形状，但它仍未进入 `.framework_readiness.current_owner_delta.latest_owner_answer_ref`，也不授权 OPL 执行 `owner-route-reconcile --apply`。`domain-health-diagnostic --request-opl-owner-route-reconcile --dry-run` 不是只读探针；除非 MAS/runtime owner 给出明确 developer-supervisor exact lane，否则保持 read-only baton。

2026-06-14 workspace currentness foldback：本轮用 `opl workspace bind` 把 MAG active binding 重绑到 `/Users/gaofeng/workspace/med-autogrant`，input 绑定到 repo-tracked `/Users/gaofeng/workspace/med-autogrant/examples/nsfc_workspace_p2c_critique.json`，并把 RedCube active binding 重绑到 `/Users/gaofeng/workspace/redcube-ai` / `workspace_root=/Users/gaofeng/workspace/redcube-ai`。fresh `opl workspace projects --json` 显示 MAG 与 RedCube active bindings 均有 `direct_entry.command` / `manifest_command`；fresh `opl domain manifests --json` 显示 `resolved_count=3`、`stale_binding_count=0`、`manifest_not_configured_count=0`、`failed_count=0`；fresh `framework readiness` 对应 `.framework_readiness.summary.domain_manifest_stale_binding_count=0`、`domain_manifest_not_configured_count=0`、`domain_manifest_live_failed_project_ids=[]`、`domain_manifest_projection_cache_used_count=0`。该修复只更新 OPL workspace registry currentness，不写 MAG/RCA domain truth，不构成 owner receipt、typed blocker、domain ready、App release ready、Brand L5 或 production-ready 证据；`framework operating-maturity` 仍是 `status=evidence_required`、`ready_claim_authorized=false`、`unresolved_owner_gates.gate_count=7`。

2026-06-14 provider capability evidence foldback：`opl runtime provider-long-soak-evidence record|verify` 已支持显式 `capability_requirement_ids`，真实 OPL state 中已验证 per-capability refs-only receipt，并新增当前 worker-source-stale restart guard blocker receipt：`provider-blocker:temporal/worker-source-stale-restart-guard/active-stage-attempts-present/2026-06-14`。fresh `framework operating-maturity` 仍是 `status=evidence_required`、`ready_claim_authorized=false`、`provider_long_soak_open_count=1`、`long_evidence_ready=false`、`provider_completion_counts_as_production_ready=false`；`provider_long_soak.verified_receipt_ref_count=4`，provider / typed blocker 计数从 `.framework_operating_maturity.provider_long_soak.observed_ref_counts` 或 App full drilldown 的 `.app_operator_drilldown.provider_long_soak_evidence.observed_ref_counts` 读取，本轮分别为 3 / 2，owner work order 仍 `open_count=1`。这关闭的是 per-capability owner evidence identity / blocker routing 与当前 restart guard blocker evidence intake 缺口，不是 provider long-soak、provider production ready、production ready 或 domain ready。

2026-06-14 memory/artifact/lifecycle owner work-order foldback：App drilldown 的 `lifecycle_owner_work_order` 现已被 `framework operating-maturity.memory_artifact_lifecycle` 和 `foundry_agent_os_production_evidence_gate.owner_route_work_orders[lane=memory_artifact_lifecycle_apply]` 消费。`observed_ref_count`、handoff decision count、missing owner action ids 和 next evidence action 都是 live projection counters；typed-blocker follow-through 状态在 `.app_operator_drilldown.memory_artifact_lifecycle.lifecycle_owner_work_order.typed_blocker_work_order.status`，不是 memory/artifact/lifecycle 顶层字段。本轮样本已证明这些数值会随 ledger 快速变化。稳定结论是该读面只把 MAS canonical-regeneration typed blockers、candidate refs 和 owner follow-through route 投影到 maturity；不代表 memory accepted、artifact mutated、package/export ready、physical delete authorized、domain ready 或 production ready。

2026-06-14 落地文档更新补充：近期 OPL 相关 repo 重构后，本文的任务目标不需要改成新路线；需要改的是完成解释。Fresh `framework readiness` 仍为 `framework_control_plane_available_with_open_production_tail`，current owner 仍为 `med-autoscience`，readiness 层 `latest_owner_answer_ref=null`，`owner_delta_handoff_status=needs_domain_or_app_live_owner_payload`。Fresh `framework operating-maturity` 仍为 `evidence_required`，`ready_claim_authorized=false`，`unresolved_owner_gates.gate_count=7`，gate ids 是 `current_owner_delta_owner_answer`、`domain_owner_chain_scaleout`、`brand_module_l5_operating_maturity`、`app_release_user_path`、`provider_long_soak`、`private_platform_retirement`、`memory_artifact_lifecycle_apply`。同一读数在 bridge 层已有 refs-only `latest_owner_answer_ref` 与 `latest_owner_answer_kind=typed_blocker_ref`，并观察到 `owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim`、`observed_owner_answer_ref_shapes=["typed_blocker_ref"]`，但 `readiness_current_pointer_owner_answer_ref=null`；这说明 owner-shaped typed blocker 可被审计看见，尚未成为 readiness current pointer closeout。Fresh MAS readback 同步显示 DM-CVD 003 当前 canonical `current_work_unit.status=typed_blocker`、`current_execution_envelope.state_kind=typed_blocker`、owner 为 `one-person-lab`、blocker type 为 `opl_execution_authorization_required`，embedded / obligation owner 为 `gate_clearing_batch`，且 `running_provider_attempt=false` / `provider_admission_candidates=[]`；这条 evidence 只能驱动 current pointer binding、route-back、execution authorization repair 或 updated currentness evidence，不能由 OPL 文档、payload-summary ledger 或 `owner-route-reconcile --apply` 代签。Fresh Runway readout 仍是 `blocked_provider_not_ready` / `temporal_worker_source_stale`，`recovery-repair` 因 `active_stage_attempts_present` 返回 `repair_blocked_by_worker_restart_guard` 和 `repair_can_mutate_worker=false`；provider repair 需要 runtime owner 处理 active attempts / restart guard，不是 domain progress。Fresh App release/user-path 需按三层读取：fast counters 使用 `.app_operator_drilldown.summary.app_release_user_path_evidence_*`，full detail 使用 `.app_operator_drilldown.app_release_user_path_evidence`，owner lane 使用 `.framework_operating_maturity.app_release_user_path` 与 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.app_release_user_path`。本轮 App evidence full detail 为 `open_gate_count=0`、`verified_ledger_receipt_ref_count=10`、`production_user_path_ready=true`、`release_ready_claimed=false`、`production_ready_claimed=false`，且 `authority_boundary.can_close_app_release_user_path=false`；maturity 聚合为 `release_owner_receipt_recorded_not_release_ready_claim` / `release_ready_authorized=false`，owner work order status 为 `owner_evidence_recorded`，不是 `open`，也不是 release-ready。Provider long-soak 计数读取 `.framework_operating_maturity.provider_long_soak.observed_ref_counts` 或 App full `.app_operator_drilldown.provider_long_soak_evidence.observed_ref_counts`，本轮 provider blocker / typed blocker 分别为 3 / 2，但 maturity 仍为 `provider_long_soak_open_count=1`。Private cleanup 与 memory/artifact/lifecycle 要从 `foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane` 读 owner work order；App drilldown 内 lifecycle typed-blocker follow-through 读取 `.memory_artifact_lifecycle.lifecycle_owner_work_order.typed_blocker_work_order.status`。`private_platform_retirement.open_count=0` 仍只是 `owner_evidence_recorded_not_ready_claim`，`memory_artifact_lifecycle_apply` 仍要求 typed-blocker follow-through；这些都不是 delete、memory、artifact、package/export、App release 或 production closure。

2026-06-14 StageRun execution authorization identity foldback：`opl runtime stage-run-authorization record` 已支持 `--dry-run --json`，用于在写 ledger 前验证 provider attempt / lease / execution authorization refs 与 exact identity。记录 payload 现在必须携带 `study_id`、`domain_context`、`stage_attempt_id`、`action_type`、`work_unit_id`、`work_unit_fingerprint`、`decision`、`reason` 和 `operator`，且 `domain_context.domain_id/study_id/stage_id` 必须与 payload root 一致，`work_unit_fingerprint` 必须匹配 `source_fingerprint`。合法 dry-run 返回 `status=planned`、`dry_run=true`、`writes_performed=false` 和 planned receipt；身份不一致返回 `status=blocked`、`blocker_id=stage_run_execution_authorization_identity_invalid`，例如 `domain_context_study_id_mismatch`，且不写 `stage-run-execution-authorization-ledger.json`。Temporal launch authorization builder 会从 workspace locator / task / stage / source fingerprint 派生这组 identity。该能力只关闭 OPL refs-only launch authorization preflight 和 currentness repair guard 缺口；它不创建 domain owner receipt、不创建 domain typed blocker、不关闭 readiness current pointer，也不声明 MAS paper ready、domain ready 或 production ready。

2026-06-14 文档落地验收边界：本次“落地文档更新”只关闭文档 currentness 与解释漂移，不关闭 runtime owner gates。完成条件是文档分层一致：本文作为 `active_truth` 只保存 fresh readback 入口、稳定不可误判边界和当前不能声明；`docs/active/current-state-vs-ideal-gap.md` 作为 `active_plan` 只保存 owner / gate / next action / Plan Completion Audit 口径；`docs/active/opl-family-ideal-operating-model-redesign.md` 作为 `active_reference` 只保存 north-star、审计标准和目标操作模型；`docs/active/foundry-agent-os-family-target-implementation-plan.md` 作为 `active_support` 只保存 W0-W7 目标 ABI、lane、source of truth 和 false-authority guard。任何后续文档更新如果只新增 readout 样本、receipt refs、counter 流水或 worktree closeout，必须进入 history / ledger / owner repo provenance，而不是继续堆在 active docs。文档更新可以声明 `docs_currentness_done`、`false_completion_guard_updated` 或 `owner_evidence_baton_updated`；不能声明 `current_pointer_closeout_done`、`owner_gate_closed`、`App_release_ready`、`Brand_L5_complete`、`provider_long_soak_complete`、`physical_delete_authorized`、`memory_or_artifact_ready` 或 `production_ready`。

## 品牌模块成熟度

OPL 顶层设计按当前十个品牌模块管理：`OPL Charter`、`OPL Atlas`、`OPL Workspace`、`OPL Pack`、`OPL Stagecraft`、`OPL Runway`、`OPL Vault`、`OPL Console`、`OPL Foundry Lab` 和 `OPL Connect`。品牌模块是 Framework 内部 bounded context 和成熟度语言，不是新的 runtime、第二 truth source 或 production-ready 证明；模块数量不作为硬约束，只有边界清楚的新 bounded context 才进入 registry。

成熟度读法固定为五级：

| 等级 | 含义 |
| --- | --- |
| `L1 conceptual` | 主要是理念或目标态。 |
| `L2 emerging` | 已有局部 docs/contracts/source。 |
| `L3 structural` | 合同、read model、conformance 或结构边界已经成型。 |
| `L4 executable baseline` | 具备品牌边界、schema/contract、CLI/App action、validate/doctor、docs foldback 和测试。 |
| `L5 production operating maturity` | 真实用户路径、跨 agent scaleout、长跑/恢复 evidence、release/install evidence、运维闭环和 owner acceptance 能持续证明。 |

当前十个品牌模块都达到 `L4 executable baseline` 的 structural/read-only baseline：`contracts/opl-framework/brand-module-registry.json` 持有品牌 registry，`contracts/opl-framework/brand-cli-governance.json` 持有 module-owned CLI command surface 和 `opl agents modules` 内部 branding spine，非 Workspace 模块走 `opl <brand-module> status|inspect|interfaces|validate|doctor --json`，Workspace 只新增 `opl workspace status|inspect --json` 品牌读面，`opl brand-modules validate --json`、`opl agents modules validate --json` 与 `opl contract validate --json` 提供机器守门。`OPL Runway` 的 L4 可执行读面已经扩展到 `opl runway readiness|reconcile|handoff-gates|recovery-repair|control-loop status --json`：这些读面只能表达 provider readiness、desired/current reconcile、handoff gate、repair plan、唯一下一 safe action 和 false-authority flags；Temporal 未配置、service down、worker not ready 或 scheduler missing 都读作 `provider_not_ready` / OPL repair action，不读作 domain ready。`OPL Pack` 是本轮从已有 machine surface 正式提升的第十模块，负责 Declarative Domain Pack、authority ABI、pack compiler、generated/hosted surfaces、standard authority functions，以及 Pack OS 通用 capability pack descriptor / install registry / content-addressed cache / refs-only distribution / lock / artifact lifecycle refs / review receipt refs transport。Pack OS 的机器入口是 `contracts/opl-framework/pack-os-contract.json`、`src/pack-os.ts` 和 `opl pack os inspect|install|registry|cache|distribute|lock|validate --json`；它只证明 descriptor/install/registry/cache/distribution/lock/lifecycle/review receipt refs transport 可用，不声明 domain ready、quality/export verdict、artifact authority、publication ready、App release ready 或 production ready。L5 evidence gate 已由 `contracts/opl-framework/brand-module-l5-operating-evidence.json`、`opl brand-modules l5-status|l5-validate|l5-interfaces --json`、`opl <brand-module> l5-status --json` 和 `opl runtime brand-module-l5-evidence record|verify|list --json` 落地；contract 现在按 13 个 evidence class 定义每个模块的 owner route / accepted ref shape / false-completion boundary，runtime ledger 只记录/验证本地 refs-only 证据并回投到 L5 status 的 route-level observed refs / receipt refs / ref shapes，不把 ledger receipt 计为 satisfied requirement。fresh `brand-modules l5-status` readout 显示 `.brand_module_l5_status.evidence_ledger.verified_receipt_count=3`，覆盖 Runway `long_soak_recovery`、Console `release_install_evidence` 和 Connect `release_install_evidence`；所有模块仍是 `evidence_required`，`l5_complete_module_count=0`，默认全模块 compact rows 每个模块 `open_requirement_count=12`。需要 per-requirement owner route 时使用 `opl brand-modules l5-status --module <module_id> --json`，不要用全模块 compact row 的空 `requirements=[]` 判定完成。L5 不能由 docs foldback、contract validation、CLI governance pass、conformance pass、verified ledger、provider completion、pack lock written、Runway repair action、owner-evidence refs-only route 或 App projection 单独关闭。

当前 runtime / product / policy 文档入口按十模块做 thin mapping：Workspace 是 resource root；Pack 是 Declarative Domain Pack、authority ABI、Pack OS、Agent Tool Arsenal / Capability Invocation ABI 和 generated/hosted surface 输入；Stagecraft 是 stage / transition authority、capability use policy 与 tool affordance boundary；Runway 是 provider durability；Atlas 是 topology / decision-map / resource telemetry、domain capability registry catalog 和 tool-card catalog；Vault 是 passive evidence telemetry；Console 是 App/operator cockpit、`current_owner_delta` default read root 和 invocation-plan projection；Foundry Lab 是 improvement control plane；Connect 是 module/skill/plugin/MCP/OpenAI/AI SDK descriptor discovery；Charter 是 owner split / surface budget / forbidden claim。这个 mapping 是维护入口，不是新成熟度结论；每个模块的 L5 仍必须由真实用户路径、跨 agent scaleout、长跑/恢复 evidence、release/install evidence、运维闭环和 owner acceptance 证明。Capability Invocation OS 保持在当前十模块协同边界内，不新增品牌模块。

Foundry Agent OS、multi-plane operating model、Temporal / StageRun transport、Runway current-control 和 Stage-route substrate 的当前读法统一回到 contracts、source、tests、read-model 与 [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)。`contracts/opl-framework/target-operating-architecture-contract.json#foundry_agent_os_standard` 定义 family-level target；`Console`、`Runway`、`Vault`、`Pack` 只证明 refs-only transport、owner-action projection、provider repair / reconcile、false-authority flags 和 read-model availability。本文不维护 W0-W7 work-order、commit、closeout、provider tick 或 attempt 流水。

Standard Agent landing acceptance：`contracts/opl-framework/standard-agent-landing-acceptance-contract.json` 是当前 OPL family 对“标准化智能体彻底落地”的机器验收定义，`contracts/opl-framework/standard-agent-landing-evidence-status.json` 是对应 evidence/status ledger。它把 MAS DM002/DM003 暴露的错误路径提升成七个 family-level gate：真实 agent pack semantics、generated surface production consumption、private-platform residue owner decision、`current_owner_delta` single ordinary route、stage-route arbiter / stop-loss、OMA target-agent work-order guard、cross-agent negative conformance。当前只声明 `acceptance_definition_landed` / `family_evidence_tail_open_not_complete`，不是 family completion：`descriptor_ready`、`generated_interface_ready`、`standard_pack_conformance_passed`、`suite_pass`、`Agent_Lab_pass`、`functional_structure_gap_count_zero` / `classification_zero`、`provider_completed`、`verified_refs_only_ledger`、`App_projection_ready` 和 `contract_landed` 都不能声明 domain ready、caller migration、physical retirement、target owner acceptance、Brand L5 或 production readiness。OMA / new-agent builder / takeover work order 现在必须在缺 target owner route、source morphology、generated surface consumption、private residue decision、no-forbidden-write proof 或 owner answer shape 时 fail closed 为 typed blocker / developer work order；OMA 仍不能签 target owner receipt 或创建 target typed blocker。ledger 明确 generated-surface production consumption、OMA target-agent samples、cross-agent negative conformance scaleout 和 long-soak / real-user-path owner evidence 仍是 `evidence_required`。

ordinary route 仍只有一条：fresh `current_owner_delta -> accepted owner answer shape -> domain/App/brand owner receipt、typed blocker、human gate、quality/export/review receipt、release verdict 或 route-back evidence -> next current_owner_delta`。`framework readiness`、`framework operating-maturity`、`agents conformance`、`brand-modules l5-status` 和 `agents default-callers` 是动态读面；`owner_evidence_intake` 与 live progress consumption 只能记录 observed refs 和 typed blocker refs，直到对应 domain / App / brand owner 关闭前都只能读作 `not_ready_claim`。

Runtime / currentness 修复只处理 OPL provider transport、queue/currentness identity、StageRun closeout binding 和 operator projection：Temporal closeout packet 保持 refs-only；`stage_progress_log` 只投影 domain closeout 已给出的摘要或显式 missing；MAS current-control queued admission refresh、terminal closeout reconcile、anti-loop stop-loss successor admission 和 `stage_run_currentness_identity` guard 只在 StageRun、source fingerprint、work unit、通用 idempotency、provider admission `route_identity_key` / `attempt_idempotency_key`、current pointer、selected dispatch / stage packet refs、明确 stop-loss successor policy 与 owner-answer binding 匹配时选择或解锁 provider work。通用 `idempotency_key` 不能兜底为 provider attempt identity；StageRun currentness 比较缺 route identity、attempt identity、selected dispatch、`stage_packet_ref` 或非空 `stage_packet_refs` 时必须 fail closed。MAS current-control provider admission 当前进一步收紧：`dispatch_ref` 只作为可选 dispatch 载荷和诊断 ref，不再作为 stage packet fallback；candidate 缺显式 `stage_packet_ref` / 非空 `stage_packet_refs`、`route_identity_key` 或 `attempt_idempotency_key` 时会记录 `current_control_provider_admission_stage_packet_ref_missing`、`current_control_provider_admission_route_identity_key_missing` 或 `current_control_provider_admission_attempt_idempotency_key_missing`，并把同 study stale sidecar default-executor pending task 一起压下，避免旧 pending task 绕过当前 blocker。`checkpointed + domain_handler_receipt_ref_only` 但缺 provider attempt / lease / execution authorization refs 时必须投影 `blocked_missing_launch_execution_authorization`；launch authorization refs 完整时也只能投影 `checkpointed_refs_only_domain_handler_receipt`，不能作为 running proof。同一 `anti_loop_budget_exhausted` lineage 的 same work-unit redrive 必须保持 blocked；合法出口只能是 identity-different `publishability_repair_sprint` successor、domain-owned typed blocker / owner answer、provider hard-gate clearance、provider-transport redrive 补齐 launch authorization refs，或稳定 human/operator gate，并在 queue event 与 `current_control_state.stop_loss_successor_admission` 中投影 route reason / identity difference / false-authority boundary。旧 selected dispatch、trace/span、queue residue、read-model refresh 或 provider completion 只能作为诊断，不能生成 domain authority。它们不签 MAS owner answer、不创建 MAS typed blocker、不生成 quality verdict、domain ready、App release ready、Brand L5 或 production-ready claim。currentness 相关结论必须从 source/tests/contracts 和 fresh live commands 读取，不能从旧日期、旧 attempt、旧 typed blocker 或 provider completion 外推。

## 当前公开角色

`OPL` 当前公开认知固定为三层：`OPL Framework -> One Person Lab App -> Foundry Agents`。

| 层 | 当前职责 | 不拥有 |
| --- | --- | --- |
| `OPL Framework` | Codex-default activation、Temporal-backed provider、typed queue、stage attempt、receipt/projection、shared contracts/indexes、Agent Lab、generated/hosted surface、safe action shell 和跨仓治理。 | domain truth、memory/artifact body、quality/export verdict、owner receipt authority、App release verdict。 |
| `One Person Lab App` | 面向人的工作台，消费 framework/provider 状态和 domain-owned projection，展示任务、阶段、阻塞、source/artifact/memory refs、SLO、repair、workorder 和 owner-aware action。 | OPL runtime/provider implementation、domain truth、artifact authority、quality/export verdict、长跑任务外围驱动。 |
| `Foundry Agents` | MAS/MAG/RCA/OMA 及后续 domain agent，持有 domain pack、stage semantics、quality/export verdict、artifact authority、memory body / accept-reject decision、owner receipt、typed blocker 和 direct/generated skill path。 | Framework runtime、generic queue/attempt ledger、App release truth、跨 domain shared primitive。 |

普通用户 App 形态按 `Codex App wrapper` 读取：固定 `Codex CLI` executor，内置 MAS/MAG/RCA/OMA task entry。AionUI 原生多 backend、多 Agent 选择、非默认 executor adapter 和 shell implementation 细节只能进入显式 developer/operator diagnostic，不是普通用户 product truth。`one-person-lab-app` 是 GUI product truth、active-shell contract、validator 和 release/user-path evidence owner；`opl-aion-shell` 与 `opl-agui-codex-shell` 是 App-owned implementation carrier / candidate，不是 OPL runtime owner。

`Codex CLI` 是当前第一公民 executor。标准 OPL Agent 的默认长跑路径是 `opl_temporal_hosted_autonomous`：任务启动后进入 OPL/Temporal 托管的 stage attempt runtime，由 OPL provider scheduler / typed queue / wakeup / resume-requery / retry-dead-letter / attempt ledger 持久在线推进；Codex App 只承担启动、观察、介入和投影入口。Temporal-backed provider 是 production online runtime 的必需 substrate；`local_sqlite` 只允许作为 dev/CI/offline diagnostic baseline。`hermes_agent`、`claude_code` 与 `antigravity_cli` 同属显式非默认 executor adapter/backend，以 request/stage binding、receipt/audit/fail-closed 证明连接，不承诺行为、质量、工具语义或 resume 与 `Codex CLI` 等价。

`MDS` 不进入 OPL 顶层 agent 列表。它只作为 MAS 显式声明的 source provenance、historical fixture、explicit archive import、backend audit、upstream intake 或 parity oracle reference。

## 当前真实状态

OPL 已具备 framework 主干：

- domain descriptor / stage / action / memory discovery；
- Temporal provider code、service / worker lifecycle、typed family queue、stage attempt ledger、typed closeout、retry/dead-letter、human gate 和 execution authorization / closeout binding guard；
- evidence worklist read model、provider proof / SLO projection、runtime snapshot、safe runtime action shell、App/operator drilldown read model 和 refs-only external evidence ledger；
- State Index Kernel、Stage Artifact / Workspace topology、workspace ensure/validate/doctor/adopt/upgrade/export-map/health/inspect/inventory surfaces；
- Agent Lab、Foundry Agent series contract、standard domain-agent scaffold / conformance、generated/hosted interface 和 default-caller / cleanup / no-resurrection guard；
- OPL-owned refs-only intake / projection for domain-dispatch、stage-production evidence、memory/artifact/lifecycle/no-regression receipt refs。

这些 surface 只证明 Framework 的 transport、ledger、projection、guard、recovery、admission 和 diagnostic 基础可用。它们不读取 memory/artifact body，不写 domain truth，不接受或拒绝 memory writeback，不授权 artifact/package/export readiness，不生成 owner receipt / typed blocker，也不声明 domain ready、App release ready 或 production ready。

Agent Lab、observability eval 和 mechanism improvement 继续是 refs-only control plane。OPL 只消费 refs，不写入 body、truth、artifact、owner receipt 或 quality verdict；domain truth、quality/export verdict、artifact authority、memory body 和 owner receipt 仍归 MAS/MAG/RCA。

当前状态摘要：

| 面 | 当前读法 |
| --- | --- |
| Framework runtime | `Codex CLI` first-class executor + Runway control-loop runtime L4 executable read surface + Temporal-backed production online substrate + local/dev diagnostic baseline；当前 Runway fresh readout 为 `blocked_provider_not_ready`，worker lifecycle 为 `worker_source_stale`，Temporal service reachable 但 managed worker source 不再 current。`provider_not_ready` / `worker_source_stale` 是 OPL runtime repair owner，不是 domain ready。 |
| App/operator | 默认 owner-delta-first；App 只启动、观察、介入和展示，不驱动外围长跑任务。 |
| Multi-plane operating model | `Console` 只消费 `current_owner_delta` 和 owner-action refs；`Runway` 只承载 provider attempt / lease / execution authorization / repair / reconcile refs；`Vault` 只保存 refs-only evidence、lineage、telemetry 和 audit packet。三者都不生成 domain owner answer、typed blocker、quality verdict 或 ready declaration。 |
| Foundry Agent structure | MAS/MAG/RCA/OMA 通过 contracts / descriptors / conformance 暴露标准 pack、stage、quality gate、authority function 和 direct/generated skill path。 |
| StageRun / owner answer | OPL 可签发 provider attempt、active lease、execution authorization decision 和 closeout binding refs；合法 closeout 仍必须来自 domain owner receipt、quality gate receipt、typed blocker、human gate 或 route-back evidence，并且必须匹配同一 StageRun/source/work-unit/idempotency、provider admission route identity / attempt idempotency 和 selected dispatch / stage packet identity。缺 binding 或缺完整 currentness identity 的 MAS typed blocker 只能作为 domain blocker evidence，不关闭 OPL current owner delta。 |
| Ordinary progress spine | `current_owner_delta` 机器读面已暴露 `ordinary_progress_spine`、T0 `progress_delta_receipt`、`artifact_tier_policy` 和 passive `audit_sidecar_policy`；App ordinary cockpit 与默认读面同步消费这些字段。它只让普通 owner delta 更短、更可接力，不授权 domain ready、App release ready、artifact/memory mutation、physical delete 或 production ready。 |
| Workspace / State Index | OPL 维护 workspace topology、workspace inspection、resource inventory、workspace report、workspace fleet report、Project lifecycle runtime、delete safe gate、Stage Artifact Unit、stage outputs index/current pointer 和 refs-only SQLite sidecar；workspace governance v2/v3 把 canonical generated roots 固定到 `control/opl/projections` 与 `control/opl/reports`，根层 `workspace_*.json` 只作兼容 mirror；新 workspace 的 Project Unit 默认物理集合是 `projects/<project-id>`，MAS `studies` 与 RCA/MAG/OMA `deliverables` 只作为 display / legacy alias；完成状态继续由 stage folder、manifest validity、owner receipt / typed blocker、current pointer 和 lineage 推导。 |
| Pack OS | OPL Pack 已有通用 refs-only pack descriptor/install/registry/cache/distribution/lock/lifecycle/review receipt transport：descriptor intake、registry entry、content-addressed cache manifest、distribution bundle manifest、lock projection、resource hash/ref、artifact lifecycle refs、review receipt refs 和 provenance 可被 OPL 读取、校验和写入。 | 该 surface 不存 artifact body 到合同、不改写 domain artifact、不签 owner receipt、不生成 typed blocker、不把 review receipt refs 当 quality verdict，也不授权 publication/grant/visual/App ready 或 production ready。 |
| External evidence | OPL 可记录/验证 body-free refs-only receipts；`runtime brand-module-l5-evidence` 同样只记录品牌模块 L5 evidence refs。verified ledger 只证明 refs transport 与 preflight 可用，不关闭 domain verdict、L5 completion 或 production evidence。 |

Fresh readiness / maturity / conformance readout 必须 owner-delta-first：当前 owner、accepted answer shape、Brand L5、App release/user-path、provider long-soak、cleanup、memory/artifact/lifecycle 和 blocker tail 都从 live commands 读取。历史 2026-06-09 cohort、canary、release evidence、ledger receipt、cleanup counter、provider proof 和 branch closeout 只作 provenance；它们不授权 domain ready、App release-ready、Brand L5、artifact ready、memory accepted、package/export ready、physical delete authorization 或 production ready。

## 未闭合项

具体数量和 drilldown 以 live CLI/read-model 为准，本文只保留类别和完成口径。

| 类别 | 仍缺什么 | 完成口径 |
| --- | --- | --- |
| `Current owner pointer closeout` | 当前 `framework readiness` / `framework operating-maturity` 都指向 `med-autoscience` 的 `domain_owner/default-executor-dispatch` owner answer gate。OPL 已记录/验证 refs-only typed-blocker payload summary，但 readiness current pointer 仍未绑定 owner answer；缺 `owner_answer_ref`、StageRun / manifest / current pointer / source fingerprint / idempotency binding。`.framework_operating_maturity.unresolved_owner_gates.gates[] | select(.gate_id=="owner-gate:current_owner_delta_owner_answer")` 现在直接暴露 observed owner answer refs、payload summary receipt refs、verified summary receipt refs、`current_pointer_update_still_required=true` 和 `observed_refs_are_current_pointer_closeout=false`，供 owner handoff 自动化消费。 | 只有同一 live identity 下的 MAS/domain owner receipt、quality gate receipt、typed blocker、human gate、route-back evidence，或 OPL runtime/currentness owner 给出的 execution-authorization / currentness repair receipt 被 current pointer 消费后，才能关闭该 gate。payload summary verified、provider completion、docs foldback、conformance pass、old closeout ref 和 current-owner-gate observed refs 都不能单独关闭。 |
| `App release / user path` | OPL maturity 已消费 App drilldown 的 verified same-cohort user-path refs：fast summary 显示 `production_user_path_ready=true`、`release_ready_claimed=false`、`production_ready_claimed=false`、verified ledger receipt 10 条；full detail 的 `.app_release_user_path_evidence` 显示 refs observed 且 `can_close_app_release_user_path=false`；maturity 聚合显示 `release_ready_authorized=false`。App release evidence payload 现在 fail-closed 地拆成 same-cohort user-path refs、release-owner verdict refs、typed blocker refs三条互斥 path，并暴露 `release_owner_verdict_handoff`。仍缺 App release owner 的 release-ready / production-ready verdict。 | App release-owner receipt / install evidence / typed blocker 或 release verdict 关闭 release-ready；OPL Framework 只记录 refs，不替 App 声明 release-ready。 |
| `Domain owner-chain scaleout` | MAS paper、MAG grant、RCA visual、OMA target-agent stage 在 OPL-hosted path 下持续返回 owner receipt、typed blocker、human gate、quality/export/review receipt 或 no-regression evidence。 | Domain-owned receipt / typed blocker 关闭对应 stage / transition / owner-chain 缺口；OPL 只承载 transport、ledger 和 projection。 |
| `StageRun closeout currentness` | 当前 owner delta、StageRun identity、manifest、current pointer、source fingerprint、work unit、通用 idempotency、provider admission `route_identity_key` / `attempt_idempotency_key`、selected dispatch / stage packet refs、provider attempt、active lease 和 execution authorization decision 必须绑定到同一个合法 owner answer。`runtime stage-run-authorization record --dry-run --json` 可在写 ledger 前验证 `study_id` / `domain_context` / `stage_attempt_id` / `action_type` / `work_unit_id` / `work_unit_fingerprint` / `decision` / `reason` / `operator` exact identity，并对 mismatch fail closed。 | 缺 provider / lease / authorization、缺完整 provider-admission / selected-stage-packet identity，或 execution authorization identity mismatch 时默认 owner 是 OPL runtime blocker；只缺 owner answer / closeout binding 时默认 owner 回到 domain owner。OPL 不借用旧 blocker、通用 idempotency、study-level decision 或 dry-run planned receipt 伪闭合。 |
| `Memory / artifact / lifecycle apply` | `operating-maturity` 已从 App drilldown 的 memory/artifact/lifecycle refs 与 reconcile counters 派生 `memory_artifact_lifecycle_open_count`；App drilldown 现在输出 `lifecycle_owner_work_order`、typed-blocker follow-through、accepted refs-only shapes 和 forbidden OPL claims。真实 memory retrieval/writeback、accepted/rejected receipt、artifact mutation receipt、package/export lifecycle receipt、cleanup/restore/retention 对账仍属 domain owner。 | Domain-owned surface 产生真实 receipts；OPL 只 intake / verify / project refs，count=0、refs observed、typed blocker ref 或 reconcile clean 都不代表 OPL 保存 memory body、修改 artifact body、授权 physical delete 或声明 package/export ready。 |
| `Provider long-soak` | `operating-maturity` 已从 provider cadence window / capability projection 与 provider long-soak refs-only ledger 派生 `provider_long_soak_open_count`；fresh `.framework_operating_maturity.summary.provider_long_soak_open_count=1` / `provider_long_soak.open_evidence_count=1`，ledger 状态为 `ledger_refs_verified`。当前 `long_evidence_ready=false`、`capability_status=capability_slo_blocked`；readout 现在把 capability SLO 子门折成 `capability_checklist`、`capability_missing_requirement_ids`、`capability_evidence_observed_requirement_ids` 和 `capability_next_evidence_action`。本轮已记录并验证 per-capability refs-only blocker以及当前 worker-source-stale restart guard blocker，`provider_long_soak.verified_receipt_ref_count=4`；provider / typed blocker 计数读取 `.framework_operating_maturity.provider_long_soak.observed_ref_counts`，本轮分别为 3 / 2；`restart_requery_ready`、`signal_history_ready`、`typed_closeout_required_ready`、`missing_closeout_block_ready`、`retry_dead_letter_boundary_ready` 均为 `refs_observed_not_ready_claim`，因此 `capability_missing_requirement_ids=[]` / `capability_open_requirement_count=0`。Temporal service/worker、provider cadence/capability、domain owner-chain dispatch、retry/dead-letter 和 repair loop 仍需在更长窗口内持续满足。 | Long-soak refs、recovery refs、dead-letter refs、provider blocker refs 或 typed blocker refs 可重复 record/verify；verified ledger、provider blocker、typed blocker、capability checklist 和 count 只说明 provider lane evidence 状态，不关闭 long-soak，不外推为 provider production ready、production ready 或 domain ready。 |
| `Runway control-loop runtime` | Runway 目标态按 L4/L4+ 结构强化读取：Temporal 只做 durable substrate，worker supervisor 只保 worker liveness，scheduler 只提供 cadence，Progress Reconciler 比较 desired/current 并输出唯一下一 safe action、owner/gate wait 或 OPL runtime blocker。`readiness`、`reconcile`、`handoff-gates`、`recovery-repair` 和 `control-loop status` 是已落地读面；Temporal 未配置、service down、worker not ready 或 worker source stale 时必须返回 provider repair owner。当前 Runway wrapper 必须读 `.opl_runway_readiness` 与 `.opl_runway_recovery_repair`；worker 与 attempt wrapper 分别读 `.family_runtime_worker` 和 `.family_runtime_stage_attempts`。fresh readout 仍显示 `worker_source_stale` / `blocked_provider_not_ready`；`recovery-repair` 现在把 restart guard 投影为 `repair_blocked_by_worker_restart_guard`、`repair_blocker_ids=["active_stage_attempts_present"]` 和 `repair_can_mutate_worker=false`。无过滤 `family-runtime attempt list --json` 保持 `view_mode=compact_timeline`，filtered `--status queued` 保持 full filtered view；本文不冻结 active attempt count、status breakdown、compact timeline total 或 omitted total。 | 该能力是 L5 evidence 前置，不是 L5 完成。真实 L5 仍需长跑/恢复、跨 agent scaleout、operator repair loop、release/install 和 owner acceptance 证据；handoff/gate/reconciler refs、provider repair、verified ledger、worker stale guard、bounded attempt readout 和 no-open-blocker 不能伪造 domain truth、receipt、typed blocker、verdict 或 production long-soak closure。 |
| `Private platform retirement` | Default-caller deletion evidence worklist 与 App drilldown cleanup-retirement ready-domain worklist 是两个不同 live read surfaces，计数可能随 refs-only ledger 变化。private residue cleanup lane 为空时也输出 `owner_decision_work_order.status=no_private_residue_classified_not_delete_ready`，`physical_delete_authorized=false`。 | 只有 domain owner 给出 physical delete authorization 才能物理删除；empty cleanup lane、structural evidence、tombstone/provenance refs、App 顶层 cleanup projection 或 OPL work order 都不授权删除；否则保持 authority adapter / tombstone / typed blocker 路径。 |
| `Brand module L5` | 当前十个品牌模块的 L5 evidence matrix、可执行 read/validate surface 和 refs-only runtime evidence ledger 已落地。fresh `brand-modules l5-status` 为 `evidence_required`、`evidence_required_module_count=10`、`l5_complete_module_count=0`、`evidence_ledger.verified_receipt_count=3`，默认 compact rows 显示每个模块 `open_requirement_count=12`；`--module <module>` 才展开 13 个 evidence requirements、owner routes、observed refs / typed blocker groups 和 command examples。`framework operating-maturity` 的 `owner_route_work_orders[lane=brand_module_l5_operating_maturity]` 现在把这 130 条 per-requirement work order 汇总成 10 个模块级 `owner_action_checklist` 和 130 个 `missing_owner_action_ids`，用于直接路由 brand owner evidence。真实 L5 运营证据仍缺。 | 各模块用真实用户路径、跨 agent scaleout、long-soak、release/install、owner acceptance 和运营闭环证明；L4/L3、contract validation、provider completion、verified ledger、contract refs-only evidence、owner-needed typed blocker refs、compact row 空 requirement 列表、top-level owner checklist 和 App projection 只能作为输入。 |
| `Workspace governance L5` | v3 已建立 profile binding、profile fingerprint、migration history、topology events、canonical generated projections/reports、root mirror 兼容、workspace report 用户检查面、workspace fleet report、workspace_norm 全量 projection drift gate、active/paused/archived/superseded/locked lifecycle、project delete safe gate 和 shared resource provenance refs-only inventory。 | 当前只声明 `L4_structural_baseline`：合同、初始化/ensure/upgrade/adopt 物化、validate/doctor drift gate、fleet report、lifecycle 操作和用户检查面可用。L5 仍需真实 App user path、跨 MAS/MAG/RCA/OMA scaleout、长跑迁移/恢复证据、release/install evidence 与 owner acceptance，不能由 workspace_norm pass、fleet report 或 generated projection currentness 单独关闭。 |

## Live 真相入口

当前状态、计数、receipt、attempt、workorder 和 owner-delta 必须从 live readout 读取：

```bash
rtk opl framework readiness --family-defaults --json
rtk opl framework operating-maturity --family-defaults --json
rtk opl stages readiness --family-defaults --json
rtk opl runtime app-operator-drilldown --json
rtk opl runtime app-operator-drilldown --detail full --json
rtk opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json
rtk opl family-runtime worker status --provider temporal --json
rtk opl family-runtime scheduler status --provider temporal --json
rtk opl family-runtime provider-slo tick --provider temporal --json
rtk opl family-runtime attempt list --json
rtk opl family-runtime attempt list --status queued --json
rtk opl app state --profile fast --json
rtk opl brand-modules validate --json
rtk opl brand-modules l5-status --json
rtk opl runtime brand-module-l5-evidence list --json
rtk opl runtime domain-owner-payload-summary list --json
rtk opl runtime app-release-evidence list --json
rtk opl runtime codex-app-runtime-evidence list --json
rtk opl runtime stage-run-authorization list --json
rtk opl charter status --json
rtk opl pack status --json
rtk opl runway doctor --json
rtk opl runway readiness --json
rtk opl runway reconcile --json
rtk opl runway handoff-gates --json
rtk opl runway recovery-repair --json
rtk opl runway control-loop status --json
rtk opl agents modules validate --json
rtk opl agents conformance --family-defaults --json
rtk opl agents default-callers --family-defaults --json
rtk opl index doctor --json
rtk opl workspace interfaces --json
rtk opl pack os inspect --descriptor <path> --json
rtk opl pack os install --descriptor <path> --registry <path> --json
rtk opl pack os registry --registry <path> --json
rtk opl pack os cache --descriptor <path> --cache-root <dir> --json
rtk opl pack os distribute --descriptor <path> --output <path> --json
rtk opl pack os lock --descriptor <path> --json
rtk opl pack os validate --descriptor <path> --json
rtk opl workspace fleet report --json
rtk opl workspace inspect --workspace <path> --json
rtk opl workspace inventory --workspace <path> --json
rtk opl workspace health --workspace <path> --json
rtk opl workspace project lifecycle --workspace <path> --project-id <id> --status paused --dry-run --json
rtk opl workspace project delete --workspace <path> --project-id <id> --dry-run --json
```

默认阅读顺序是 owner-delta-first：先从 `current_owner_delta` 看等待哪个 owner、需要什么 deliverable delta / receipt / typed blocker，以及该等待是否阻断 readiness。OPL provider / transport safe action 只有在没有 current owner delta 时才可成为默认下一步。raw refs-only counters、provider worker / redrive / scheduler route、evidence envelope、stage replay packet、typed blocker group、private residue inventory 和历史 receipt 计数都只作 drilldown。

Runtime / product / policy 入口的迁移读法同样服从 owner-delta-first：新增 App Console 页面、Agent Lab control-plane view、Atlas/Vault telemetry、route reconciler、artifact reconciler 或 generated surface 时，先证明它是否能折叠成 `current_owner_delta`、owner answer、typed blocker、hard gate、route-back 或 audit/ref drilldown。不能折叠的内容只作为 diagnostic、history、cleanup 或 support 入口，不进入默认 next action，也不改变 active gap owner。

`family-runtime provider-worker supervisor` 现在提供 provider worker process 的本机监督面：macOS 上安装 LaunchAgent 后由 `KeepAlive` / `RunAtLoad` 托管 resident Temporal foreground worker，并把 `--family-runtime-root` 绑定到当前 OPL state root。这个 supervisor 对齐 Temporal 的部署分层：Temporal Service 持有 workflow history、task queue、retry 和 activity timeout；Worker process 的存活由 deployment / supervisor 层保证，云端目标是 Kubernetes / systemd / container supervisor，本机目标是 launchd。旧 `provider-slo watchdog` 的 5 分钟 `StartInterval` tick 安装面已退役，安装新 supervisor 时会清理旧 `ai.opl.family-runtime.provider-slo` LaunchAgent。`provider-slo tick` 继续作为显式 health-check / production proof / fallback repair receipt，不作为主调度器、不消费 domain queue、不写 MAS/MAG/RCA truth、不生成 owner receipt 或 typed blocker，也不把 provider 恢复解释成 paper/grant/visual 进展。默认读面仍必须先看 current owner delta；supervisor 只负责让 OPL provider worker 不因进程退出而静默停止。

当 worker lifecycle 投影为 `worker_source_stale` 时，`provider-slo tick` / `provider repair` 只能在 `restart_guard.guard_status=ready` 时自动 stop/start worker。该 guard 需要同时满足：worker mutation guard 为 `allowed_explicit_developer_supervisor`，Temporal service reachable，stage attempt ledger 可读，且 `queued` / `running` / `checkpointed` / `human_gate` active stage attempt 数为 0。任一条件不满足时，worker repair receipt 必须返回 `repair_status=blocked` 与 `blocker_ids`，不得杀掉旧 worker。`opl runway recovery-repair --json` 当前会只读投影同一 restart guard；当 guard blocked 时输出 `repair_blocked_by_worker_restart_guard` 和 `repair_can_mutate_worker=false`，因此 operator 能看见 repair plan 与 mutation guard 的区别。这个规则把源码更新后的 worker stale 从“运维提醒”收敛为 fail-closed supervised restart 策略；它仍只修 OPL provider liveness，不生成 domain owner receipt、typed blocker、quality verdict 或 deliverable progress。

Runway control-loop runtime 的当前人读方向是：scheduler 只制造 reconcile 机会，worker supervisor 只维持 worker process liveness，Temporal 只保存 durable execution history 和消息语义，Progress Reconciler 才负责把 desired owner route / current queue-attempt-provider-gate-receipt refs 对账成唯一下一 safe action。`opl runway readiness|reconcile|handoff-gates|recovery-repair|control-loop status --json` 是当前已落地读面；`opl family-runtime scheduler status --provider temporal --json` 只读 scheduler cadence 与 provider blocker，不绕过 provider readiness。Temporal 未配置、service down、worker not ready、worker source stale 或 scheduler missing 必须投影为 `provider_not_ready` / OPL repair action。handoff、human gate、provider observation、scheduler cadence 和 reconciler output 只能携带 refs、typed blocker requirement、owner answer shape 或 OPL repair command；不能把 provider completed、worker healthy、scheduler ticked、gate passed、repair succeeded、no-open-blocker 或 chosen action 写成 domain ready、owner receipt、quality verdict、artifact ready、production ready 或 Runway L5 证据闭合。

## 当前默认入口

- 默认前门是 `opl`；`opl --help` / `opl help` 展示 OPL Framework 自有命令树，`opl exec` 负责一次性请求，`opl exec --help` 保留 Codex-compatible 执行器帮助边界，`opl resume` 负责续接会话。
- `opl install` 是当前一键安装入口，负责安装或复用 Codex CLI、Temporal-backed family runtime provider、MAS、MAG、RCA、OPL Meta Agent、推荐 skills 和 App 入口。
- `opl system` / `opl system initialize` / `opl system startup-maintenance` 管理 Codex CLI、provider profile/readiness、Connect module install/update、Connect skill sync、managed environment freshness、plugin cache freshness、reload prompt 和 local runtime state。
- `opl framework readiness --family-defaults --json` 是 family readiness 动态真相入口；它只输出 framework/operator 读面，不授权 domain ready、artifact authority 或 production ready。
- `opl framework operating-maturity --family-defaults --json` 是轻量 maturity gap aggregator，把 `current_owner_delta_bridge`、`owner_evidence_intake`、domain owner-chain scaleout、Brand module L5、App release/user-path、provider long-soak、private wrapper retirement 和 memory/artifact/lifecycle receipts 汇总为 refs-only 下一步读面；`owner_evidence_intake` 可读取 MAG production acceptance、RCA owner-chain live progress、OMA target-agent owner-chain evidence、`agents default-callers` private-platform refs 和 `runtime app-operator-drilldown` memory/artifact lifecycle refs，并只投影为 observed refs。它以 current owner delta 为默认 planning root，不跑全量 release/user-path proof，不替 App/domain owner 签 receipt 或 typed blocker，不声明 L5、App release ready、domain ready 或 production ready。
- `opl stages readiness --family-defaults --json` 是 stage readiness family drilldown 入口；单仓诊断继续使用 `opl stages readiness --domain <domain> --json`。
- `opl runtime app-operator-drilldown --json` 与 `opl runtime app-operator-drilldown --detail full --json` 是 App/operator drilldown 入口。
- `opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json` 是 runtime safe-action evidence worklist、stage evidence workorder packet、stage replay missing receipt attention packet 与 domain-dispatch evidence workorder packet 入口；cache-derived attention 仍不授权 domain truth、owner receipt、artifact authority 或 production ready。`family-runtime production-closeout` 已退役，不再是 active interface 或兼容 alias。
- `opl work-order execute --work-order <developer-patch-work-order.json> --json` 是 owner-gated developer patch work order 的唯一 canonical OPL 执行原语；Agent Lab 只消费该原语产出的 execution receipt、execution plan/report refs 与 re-evaluation refs，旧 `opl agent-lab execute-work-order` 兼容 alias 已退役。

## 当前不能声明

- 不能声明 OPL 已全量生产可用。
- 不能声明 Temporal provider proof 等于 MAS paper closure、MAG grant readiness 或 RCA visual ready。
- 不能把 StageRun authorization、stage evidence workorder、domain-dispatch evidence workorder、refs-only receipt verified、provider/SLO satisfied、ledger closed 或 open worklist 为 0 写成 domain owner-chain、App 用户路径、release/dist、artifact authority、expected receipt instance、monitor freshness、long-soak evidence、domain ready、App release ready 或 production ready。
- 不能把 Runway safe action / repair、Console owner-action projection、Vault verified refs、multi-plane health 或 ideal operating model foldback 写成 domain owner answer、domain typed blocker、quality/export/review verdict、artifact authority、App release verdict、Brand L5、physical delete authorization 或 ready declaration。
- 不能声明 private functional audit 分类完成就等于物理代码路径清零。
- 不能把 owner-delta summary、`next_forced_delta`、`goal_oracle_missing`、selected cohort 或 typed blocker verified 写成完成。
- 不能把 `agents legacy-cleanup apply` 的 dry-run / apply / verify ready 状态写成 OPL 已删除 domain repo 文件或 production evidence 已闭合。
- 不能为了兼容保留旧模块、旧接口、旧测试、旧 CLI alias、facade 或 wrapper；active caller 迁走后直接删除或进入 history/tombstone。

## 参考入口

- [文档索引](./README.md)
- [项目概览](./project.md)
- [架构](./architecture.md)
- [硬约束](./invariants.md)
- [关键决策](./decisions.md)
- [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)
- [OPL 与 Foundry Agents 理想目标态](./references/runtime-substrate/opl-family-agent-ideal-state.md)
