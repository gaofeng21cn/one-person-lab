# OPL 当前状态

Owner: `One Person Lab`
Purpose: `status`
State: `active_truth`
Machine boundary: 本文是核心人读真相面。机器真相继续归 `contracts/`、source、CLI/API 行为、runtime ledger、provider receipt、domain-owned manifest 和真实 workspace / App evidence。
更新时间：`2026-05-21`

## 当前公开角色

`OPL` 当前公开认知固定为三层：`OPL Framework -> One Person Lab App -> Foundry Agents`。

- `OPL Framework` 是完整智能体开发/运行框架，持有 Codex-default activation、Temporal-backed provider、typed queue、stage attempt、receipt/projection、shared contracts/indexes、Agent Lab、generated surface、safe action shell 和跨仓治理。
- `One Person Lab App` 是面向人的工作台，消费 framework/provider 状态和 domain-owned projection，展示任务、阶段、阻塞、source refs、artifact refs、memory refs、SLO、repair、workorder 和 owner-aware action。
- `Foundry Agents` 当前包括 `MAS`、`MAG`、`RCA`。它们持有各自的 domain truth、quality/export verdict、artifact authority、memory body / accept-reject decision、owner receipt 和 direct app skill path；OPL 只消费 refs，不写入 body、truth、artifact、owner receipt 或 quality verdict。

`Codex CLI` 是当前第一公民 executor。标准 OPL Agent 的默认长跑路径是 `opl_temporal_hosted_autonomous`：任务启动后默认进入 OPL/Temporal 托管的 stage attempt runtime，由 OPL provider scheduler / typed queue / wakeup / resume-requery / retry-dead-letter / attempt ledger 持久在线推进；Codex App 只承担启动、观察、介入和投影入口，不作为外围持续驱动任务的主体。Domain agent 不允许内置或恢复通用 daemon、scheduler、attempt loop。Temporal-backed provider 是 production online runtime 的必需 substrate；`local_sqlite` 只允许作为 dev/CI/offline diagnostic baseline。`hermes_agent`、`claude_code` 与 `antigravity_cli` 同属显式非默认 executor adapter/backend，只能以 receipt/audit/fail-closed 证明连接，不承诺行为、质量、工具语义或 resume 等价。

`MDS` 不进入 OPL 顶层 agent 列表。它只作为 MAS 显式声明的 source provenance、historical fixture、explicit archive import、backend audit、upstream intake 或 parity oracle reference。

## 当前真实状态

OPL 已具备 framework 主干：domain descriptor / stage / action / memory discovery、Temporal provider code、service / worker lifecycle、typed family queue、stage attempt ledger、typed closeout、evidence worklist read model、provider proof / SLO projection、runtime snapshot、OPL-owned provider scheduler cadence/tick surface、safe runtime action shell、lifecycle refs-only SQLite index、external evidence refs-only receipt ledger、App/operator drilldown read model、refs-only `domain_dispatch_evidence`、refs-only `stage_production_evidence` 和 refs-only functional privatization audit detail projection。`opl family-runtime status` 与 `opl agents scaffold` 现在都把 `opl_temporal_hosted_autonomous` 暴露为标准智能体默认运行策略，同时保持 `domain_agent_internal_daemon_allowed=false`、`domain_agent_internal_scheduler_allowed=false`、`domain_agent_internal_attempt_loop_allowed=false` 和 `codex_app_drives_long_running_tasks=false`。

`runtime app-operator-drilldown` 默认 summary-first，完整 refs/routes 只在 `--detail full` 展开。App execution bridge 统一走 `opl runtime action execute`；OPL CLI route 可创建 provider-backed stage attempt request、执行 provider scheduler safe action、写 OPL cleanup ledger 或记录/验证 refs-only evidence receipt。Domain route 只进入 typed queue / approval，不直接执行 domain action、不写 domain truth、不签 domain owner receipt。`functional_privatization_audit_refs` 也只在 full detail 中展开：它把 MAS/MAG/RCA 声明的 standard pack、minimal authority function、private platform residue、bridge exit gate、forbidden generic owner flags 和 cannot-absorb reason 作为 refs-only audit inventory 投给 App/operator。默认 summary 现在按 action-required 优先读取：`functional_privatization_action_required_count` 由 default watchlist、active private generic residue、semantic equivalence review 和 blocker 合成；`functional_privatization_hidden_cleared_count` 表示已折叠的 cleared/stable boundary；private platform residue inventory 数量只作为 full-detail 审计材料，不再当作默认待处理任务数。

Agent Lab、observability eval 和 mechanism improvement 继续是 refs-only control plane。OPL 只消费 refs，不写入 body、truth、artifact、owner receipt 或 quality verdict；domain truth、quality/export verdict、artifact authority、memory body 和 owner receipt 仍归 MAS/MAG/RCA。

`family-runtime intake|tick --hydrate` 的 MAS profile 路径已收口到 OPL module locator：即使 operator 只提供 `OPL_FAMILY_RUNTIME_MEDAUTOSCIENCE_PROFILE`，OPL 也会先解析 active MAS module checkout，再通过该 checkout 的 sidecar export hydrate pending family tasks，避免 PATH 上旧 `medautosci` 工具污染 DM002 等 live route。该路径仍只写 OPL queue / stage attempt / dispatch evidence；论文质量、owner receipt、typed blocker 和 artifact authority 继续由 MAS owner surface 给出。

2026-05-21 `family-runtime intake|tick|scheduler tick` 增加 OPL-owned task scope：`--domain <domain>`、`--study <study_id>` 与 `--payload-match <path=value>` 只过滤 OPL typed queue hydrate / dispatch 选择，便于把 DM002 这类单篇 study 的 owner-route 先推进；它不写 MAS study truth、不改 publication eval、不签 domain owner receipt，也不把 study-specific runtime/liveness/retry 逻辑下沉回 MAS。`opl module exec` 的输出 buffer 同步改为 OPL system-installation 层配置项，避免大 sidecar export 被 Node 默认 buffer 截断；domain repo 不需要也不应复制该 buffer/lifecycle 逻辑。

同日 `family-runtime intake|tick --hydrate` 对 domain export 注入 OPL-owned `opl_domain_export_context`，用于审计 sidecar export 的 owner/freshness 指纹。若 domain export 仍声明同一 pending task，而 OPL 队列中旧 dedupe item 已因旧 owner capability 进入 `dead_letter`，只有当新的 export context 或 domain `source_fingerprint` 证明 owner/freshness 已变化时，OPL 才会重置 attempts 并重排队；同一 owner 指纹不变时继续 `dedupe_noop`。该规则属于 OPL typed queue / retry / dead-letter control plane，不赋予 MAS 私有 runtime，不写 MAS truth、publication eval、current package 或 owner receipt。

2026-05-21 的 transition-runner closeout：`domain manifests` 与 `agents descriptor` 已能在发现 MAS `family_transition_spec_descriptor` 且缺少物化矩阵时，消费 MAS action catalog / product-entry shell 暴露的 read-only `study_state_matrix` command，按 descriptor locator refs 物化 `family_transition_spec` 与 `family_transition_matrix_cases`，再交给 OPL generic `family-transition-runner` 执行 matrix。`framework readiness` 的默认 summary 路径显式跳过该物化动作，避免把 operator readiness 变成昂贵 domain materialization。该能力只做 OPL transition runner transport/projection：不写 MAS study truth、不执行 MAS domain action、不授权 publication quality / submission readiness、不生成 domain owner receipt。

2026-05-21 的 current readout：Temporal provider cadence / capability SLO 已由 provider receipt 关闭；provider scheduler 与 MAS/MAG/RCA legacy cleanup 已由 OPL refs-only ledger receipt 关闭；MAG external evidence request、RCA evidence gate 和 MAS/MAG/RCA stage expected receipt / monitor freshness 已作为 refs-only verified request/gate/stage-evidence accounting 投到 App/operator。`opl framework readiness --family-defaults --json` 是当前动态读面：framework hard blocker、stage caller tail、App/operator evidence tail、stage diagnostic warning 与 evidence worklist open item 都从该 CLI、App drilldown 和 `family-runtime evidence-worklist` 读取，不在本文固化。Stage readiness warning 是 advisory / diagnostic warning，不是 framework hard blocker、domain ready blocker 或 open App workorder。

本轮 OPL evidence worklist 增加 `stage_evidence_workorder_packet` 与 `evidence_requirement_ledger`：`opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json` 会把 open `stage_production_evidence_receipt_record` route 按 domain/stage 聚合，显示 action id、payload owner、空 template、required refs、typed-blocker path、canonical `evidence_requirement.v1` 和 authority boundary。该 packet 与 ledger 只是 OPL workorder / requirement projection，不生成 domain owner receipt、不生成 monitor freshness、不声明 stage complete、domain ready 或 production ready。`family-runtime production-closeout` 已退役，不作为 active interface、兼容 alias 或文档入口保留；框架级 `opl framework production-closeout` 是另一条 framework-level 汇总命令。

同日 OPL App/operator drilldown 已开始 refs-only 消费 `opl-meta-agent` patch-loop closeout：`runtime app-operator-drilldown --detail full --json` 的 OMA sections 可投影 `blocked_suite_result_ref`、`developer_patch_work_order_ref`、`patch_traceability_matrix_ref`、`failure_evidence_refs`、`root_cause_refs`、`targeted_fix_refs`、`predicted_impact_refs`、`next_run_falsification_refs`、`target_repo_verification_refs`、`target_runtime_read_model_consumption_ref`、`workspace_environment_proof_ref`、`no_forbidden_write_proof_ref`、`target_owner_receipt_or_typed_blocker_ref`、`patch_absorption_ref`、`worktree_cleanup_ref` 和 `agent_lab_re_evaluation_ref`。App/operator 现在还把这些 refs 规整成 `self_evolution_cockpit`，让 operator 可直接按六个问题读取：证据是什么、根因是什么、改什么、预期改善什么、下一轮如何证伪、谁签收或谁阻塞。这只是 App/workbench 对 OMA work order 与 target owner closeout refs 的展示/审计入口，不写目标 truth、不读取 artifact 或 memory body、不签 owner receipt，也不把 patch-loop projection 写成 domain ready、quality verdict、artifact readiness 或 default promotion。

`opl agents scaffold --validate <repo>` 与 `opl agents conformance --family-defaults --json` 负责标准 OPL Agent 的结构 gate：`agent/` 必须持有真实 prompt/stage/skill/knowledge/quality gate 语义文件，generated surface owner、private generic-owner guard、physical morphology policy 和 active path scan 必须可被 OPL 审计。2026-05-21 新增的 `opl agents platform-surfaces --family-defaults --json` 是 OPL-owned generic platform surface classifier：它把 generated CLI/MCP/Skill/product shell、sidecar dispatch、status/workbench、workspace/source/artifact/memory locator、stage attempt queue/retry/dead-letter 和 generic transition runner 统一归 OPL Framework，同时把 domain truth、quality/export/publication/visual verdict、artifact body/mutation authority、source readiness、memory accept/reject、owner receipt、typed blocker 和 domain rubric 留给 MAS/MAG/RCA/OMA。该 classifier 只阻断 domain repo 明确声明 generic platform owner 的回流，不生成 production/default caller、domain ready、quality verdict 或 artifact authority。Conformance / readiness 的动态计数不在本文冻结；当前 truth 入口是 `opl framework readiness --family-defaults --json`，专题 drilldown 继续使用 `opl agents readiness --family-defaults --json`、`opl agents platform-surfaces --family-defaults --json`、`opl agents interfaces --domain <domain> --json`、`opl agents pack-compiler --json`、`opl stages readiness --domain <domain> --json` 和 App/operator drilldown。

同日 `opl agents conformance` 的实现边界也完成一次收薄：public conformance builder 保持为 scaffold、pack compiler、generated handoff、private audit、platform surface、physical morphology、workspace lifecycle 与 evidence tail 的薄聚合器；physical morphology policy、active path scan、legacy residue token 和 allowed tombstone/provenance 分类迁入 scoped module，并移除主文件的 line-budget baseline。这个改动只降低 OPL 自身 surface 混杂风险，不改变 conformance 的只读权威边界，也不声明 MAS/MAG/RCA/OMA production ready。

2026-05-21 OPL shared `product-entry-companions` 新增 `buildOplProductEntryLifecycleAdapterSurface` primitive，用于生成 refs-only `opl_family_lifecycle_adapter`：session shell、stage attempt、attempt ledger、owner route discovery、adoption cursor 和 lifecycle projection 的 generic owner 固定在 OPL / one-person-lab；domain truth、review/publication projection、canonical artifact、concrete executor 与 artifact/memory body 仍显式不由 OPL 持有。该 primitive 是 RCA/MAG/MAS 退役私有 product-entry/session lifecycle adapter 的替代能力来源；它本身不代表任一 domain repo 已完成 shared package pin 更新、generated/hosted default caller 切换、production owner-chain evidence 或 domain ready。

RCA physical morphology 的 legacy 名称口径已经按退役原则收口：`legacy_managed_runtime_gateway_names` 这类旧 managed/runtime/gateway surface id 必须通过 RCA `legacy_name_policy.forbidden_active_surface_ids` 和关闭 compatibility / generic owner flags 来证明退役，不能再要求它出现在 active surface classification 中。Conformance 只把它当负向 guard / tombstone proof 审计；这关闭的是结构 conformance blocker，不声明 RCA visual ready、artifact-producing owner receipt、long soak 或 App release/user path 完成。

## 当前差距

物理源码形态标准化现在作为独立治理口径读取：标准 OPL Agent 不只要 descriptor / generated interface ready，还要让 `agent/`、`contracts/`、`runtime/authority_functions/`、`src/` / `packages/` 和 `docs/` 的长期职责一眼清楚。MAS 的 supervisor / runtime transport / SQLite sidecar 命名仍是 physical morphology cleanup tail；MAG 的 product/status/sidecar/lifecycle/domain_runtime 路径继续按 handler/ref-only/authority 分类防回流；RCA 的旧 managed runtime 已物理删除，但 artifact-heavy/native-helper/product shell 仍不能被复制成新 Agent 通用 scaffold；OPL Meta Agent 的 scripts 已归 authority refs，并已用 MAS/MAG 两个真实目标完成 refs-only scaleout closeout，证明当前 materializer 不持有 generic runtime、domain truth、quality verdict 或 default promotion authority。这个口径吸收 OpenAI Agents SDK、LangGraph、AutoGen、CrewAI 等成熟项目的分层经验，不引入它们作为 OPL runtime dependency。

One Person Lab App 的产品运行路径默认以 OPL-managed environment 为真相：managed modules、managed skills、Codex plugin metadata、runtime tools 与 provider state 是普通用户入口的默认依赖。developer checkout 只作为显式开发/调试 override 进入运行路径；没有显式 override 时，workspace 源仓的 dirty、ahead 或实验分支不能定义 App 当前运行依赖。`opl system startup-maintenance` 现在提供 App startup maintenance action：默认处理 MAS/MAG/RCA 和 OPL Meta Agent 这四个 clean OPL-managed ecosystem modules，执行 install/update、repo bootstrap、health check、domain skill sync 和 plugin cache freshness 投影；dirty、ahead、diverged、no-upstream、env override、sibling workspace 或 invalid checkout 进入 `manual_required`，并输出 restart/reload 提示。AionUI shell 的 first-launch / app-version maintenance 源码路径已经从旧 `system reconcile-modules` 自动调用升级为 `system startup-maintenance`，并在 Full runtime 下跳过 git-backed maintenance；该 action 不写 domain truth、memory body、artifact body、quality/export verdict，也不安装 domain daemon。OPL Meta Agent 在这里是用于创建和检查 OPL-compatible agents 的 managed ecosystem module / default skill，不改变 MAS/MAG/RCA truth ownership。剩余缺口是发布包/截图证据、reload prompt 真实用户路径和长时 operator evidence。

OPL Developer Mode 已有系统配置和运行态 projection：`opl system developer-supervisor` 读写 OPL state 下的 developer supervisor 配置，并默认以 `gaofeng21cn` 作为安装期自动开启的 GitHub login 候选；`developer_mode` surface 会检测 GitHub identity、目标 repo permission，并计算 `direct_repo_fix`、`fork_pull_request`、`mixed_direct_and_pr`、observe-only 或 fail-closed route。App 设置页暴露 Developer Mode 开关，普通用户可手动开启或关闭；开启后任务默认可以启用外围 AI 巡检，Agent Lab 负责把问题归因、evidence、owner route、risk tier、independent AI reviewer refs、candidate fix、version ledger、canary / rollback 和 PR refs 组织成可审计控制面。repo developer / collaborator 可以走对应 repo 的修复、测试、canary 和 rollback-capable promotion 路径；非 repo developer 只能走 fork / PR 路径；高风险 domain truth、quality verdict、artifact mutation、memory accept/reject 或 credential/network/write policy 必须进入 owner/human gate。当前剩余缺口是 App 巡检自动挂载和真实 repo fix / PR route 的端到端 closeout 证据。

2026-05-20 fresh runtime evidence 显示默认 `opl family-runtime status` 选中 `temporal`，且 `provider_ready=true`、`full_online_ready=true`、`durable_online_ready=true`、`periodic_execution.status=provider_ready_scheduler_surface_available`、`selected_provider_can_replace_domain_daemons=true`。当前 readiness 真相必须以统一 lifecycle-aware provider payload 为准，并由 `opl family-runtime status`、`opl status runtime`、`opl runtime manager` 和 App provider projection 同源派生。`local_sqlite` 仍只允许作为 dev/CI/offline diagnostic path，不能替代 production provider 或 domain daemon；本机 `launchctl list` 与 `~/Library/LaunchAgents` fresh check 未发现 MAS / MedAutoScience supervision scheduler label 或 plist，MAS 旧 local scheduler 只能作为 repo-local cleanup diagnostic / tombstone 语境读取。MAS main 已进一步把显式 Hermes scheduler ensure path 收成 cleanup-only：`runtime-supervision-status --manager hermes` 与 `runtime-remove-supervision --manager hermes` 只读或清理旧 job/script/session/gateway evidence；`runtime-ensure-supervision --manager hermes` 不再是公开入口，controller direct-call 只返回 retired tombstone，不写 tick script、不 create/edit/resume/run cron job。

Fresh runtime evidence（2026-05-20）显示：`opl agents evidence apply` 已提供 OPL-owned refs-only external evidence receipt ledger，可对 domain 声明的 evidence request、evidence gate 和 stage production evidence request 记录或验证 receipt；`runtime app-operator-drilldown.domain_evidence_request_refs` 会把 request、remaining gate、replacement coverage、observed receipt status 和 verified gate receipt 投给 App/operator。App/operator 现在还会把 open external evidence request / remaining evidence gate 转成 `external_evidence_receipt_record`、`external_evidence_receipt_verify`、`evidence_gate_receipt_record` 或 `evidence_gate_receipt_verify` OPL safe action route；`runtime action execute` 只能用这些 route 记录或验证 OPL external-evidence refs-only ledger，record 时必须由 operator payload 提供 evidence refs、domain receipt refs、typed blocker refs、no-regression refs、release/dist refs、direct/hosted parity refs 或 owner-chain refs，verify 只会验证既有 OPL receipt。OPL 同时新增 `opl_meta_agent_registry` / `opl_meta_agent_workbench_refs` 消费面，把 OMA 的 `opl_domain_manifest_registration`、`app_workbench_projection` 和 `real_target_agent_scaleout_evidence` 作为 refs-only extension 暴露给 `domain manifests` 与 App/operator drilldown；summary 默认展示 `target_brief`、`candidate_package`、`agent_lab_results`、`developer_work_order`、`mechanism_patch_proposal`、`scaleout_evidence` 六个 section，完整 refs 仍通过 `--detail full` 显式展开，且 authority boundary 全部保持 no domain ready / no quality verdict / no default promotion。`runtime app-operator-drilldown.domain_dispatch_evidence` 现在会按 domain 汇总 stage attempt 中的 owner receipt refs、typed blocker refs、no-regression evidence refs、memory writeback receipt refs 和 domain-ready 非授权状态；`stage_production_evidence` 会按 domain/stage 汇总 selected executor binding、生产 caller attempt、expected receipt refs、source/artifact/workspace scope refs、runtime event refs、monitor refs、monitor freshness refs 与 observed evidence refs，并把 expected receipt / monitor 分成 declared、observed 和 unobserved 状态，避免把 stage pack obligation 缺口与 live receipt/monitor evidence 缺口混在一起。OPL proof bundle 会优先读取 `stage_contract.expected_receipt_refs`，让 workorder 指向 domain repo 声明的具体 receipt template / typed-blocker ref；App stage evidence workorder 会优先读取 `stage_contract.monitor_freshness_refs`，缺少显式 freshness refs 时才从 stage/cohort `monitor_refs` 派生 fallback。App/operator 会把 admitted-but-missing production caller stage 生成 `stage_production_attempt_request` safe action route，也会把 unobserved expected receipt / monitor freshness 生成 `stage_production_evidence_receipt_record|verify` safe action route；full detail stage evidence route 现在直接暴露 `payload_workorder`、`payload_ref_hints.required_any_payload_refs=[domain_receipt_refs,evidence_refs,typed_blocker_refs,source_scope_refs,runtime_event_refs]`、`copyable_runtime_action_execute_commands`、`payload_preflight_blocked_error_kind` 和结构化 `opl_cli_args`，record 前由 `stage_production_evidence_payload_preflight` 阻断空模板、声明型占位 ref、domain truth / artifact / memory body 或 ready verdict payload，verify route 会复用已记录的 OPL refs-only receipt ref。`runtime action execute` 可用 attempt route 调用 `opl family-runtime attempt create --provider temporal --executor-kind codex_cli --require-stage-admission` 创建 OPL-owned stage attempt request，也可用 evidence route 写入或验证 refs-only stage evidence ledger。provider scheduler 与 legacy cleanup 纳入同一 safe action shell；`runtime action execute` 可执行 scheduler status/install/trigger/tick 和 cleanup apply/verify，但它们只管理 OPL provider scheduler 或 OPL cleanup ledger，不安装 domain daemon、不删除 domain repo 文件。动态 summary 和 stage drilldown 一律读 `opl framework readiness --family-defaults --json`、`opl runtime app-operator-drilldown --json` 和 `opl runtime app-operator-drilldown --detail full --json`，本文不冻结 action route、receipt、missing stage 或 tail item 数字。两个 projection 都默认 summary-first，完整 attempt/stage evidence 与 route 只在 `--detail full` 展开，并持续声明 `provider_completion_is_domain_ready=false`、`projection_can_authorize_domain_ready=false`；attempt-create route 只请求 OPL stage attempt，不执行 domain action、不写 domain truth、不生成 domain owner receipt；stage evidence route 只记录或验证 body-free refs，不授权 stage completion、owner receipt 或 domain ready。verified receipt 或 gate receipt 只证明 refs-only roundtrip 可用，不声明 grant-ready、fundability-ready、generated/hosted caller default 化、App workbench consumption、stage completion 或 live soak 完成；OMA scaleout closeout 只证明 MAS/MAG 两目标 refs-only evidence loop 和 work order/proposal handoff 可被 OPL/App 消费，不声明 MAS/MAG domain ready；真实 default caller、release/dist、owner-chain、expected receipt instance、monitor freshness 和 long-soak 证据仍需由 MAS/MAG/RCA 或 App 生产并作为 receipt/ref 回填。

2026-05-20 safe-action 执行后的 fresh worklist 读法是：Temporal provider cadence / capability SLO 已由 provider receipt 证明；provider scheduler 与 legacy cleanup 已 closed by receipt；MAG external evidence request 与 RCA evidence gate 已由 domain-owned typed blocker refs record+verify 到 OPL refs-only ledger，并在 evidence worklist full detail 中显示为 `closed_by_domain_owned_typed_blocker`。随后 OPL 又把 stage expected receipt / monitor freshness 缺口升级为 `stage_production_evidence_receipt_record|verify` safe action route；这些 route 代表 OPL 现在具备可执行且 fail-closed 的 refs-only evidence record/verify transport，等待 domain/App/live receipt 回填；它不代表 provider scheduler、cleanup ledger、external evidence ledger 或 generated surface 回退，也不能被 OPL 伪造为 domain ready 或 production ready。

2026-05-21 fresh closeout 读法补充：RCA 6 个 stage 已用 RCA-owned `opl_expected_receipt_monitor_freshness_handoff` typed blocker refs 通过 OPL `stage-production-evidence:redcube:<stage>:record|verify` 记录/验证到 refs-only stage evidence ledger。随后 MAS 与 MAG stage evidence handoff 也已被记录/验证，因此 `runtime app-operator-drilldown --json`、`framework readiness --family-defaults --json` 与 `family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --json` 会把该类 workorder 按实时 ledger 状态投影；RCA stage 在 App tail ledger 中可显示为 `domain_owned_typed_blocker` 而不是 open。该对齐只说明 RCA 的 expected receipt / monitor freshness workorder accounting 已被 RCA-owned typed blocker 接管；它不声明 RCA controlled visual-stage long soak、真实 memory/lifecycle receipt instances、cross-family repeated no-regression、visual ready、exportable 或 handoffable 完成。

2026-05-21 后续 closeout 读法补充：MAS 6 个 stage 已用 MAS-owned `mas_stage_production_evidence_typed_blocker_handoff` typed blocker refs record+verify 到 OPL refs-only stage evidence ledger；MAG 6 个 stage 已用 MAG-owned `mag_opl_stage_evidence_receipt_handoff.v1` body-free owner receipt refs、App workorder 要求的 exact monitor freshness refs，以及 `package_and_submit_ready` 的 `human_gate:submission_ready_export_gate` record+verify 到同一 ledger。OPL stage evidence ledger 现在也接受并投影 body-free `source_scope_refs` 与 `runtime_event_refs`，App drilldown 会把 expected receipt、monitor freshness、source scope 与 runtime event 分别显示为 observed/unobserved，并把未观察项连接到同一条 `stage_production_evidence_receipt_record|verify` refs-only route；preflight 会阻断只覆盖 receipt/monitor 却漏掉 source scope 或 runtime event 的半闭合 payload。当前 open tail、evidence worklist open item、stage production caller tail、domain-ready authority 和 production-ready authority 都只从 `runtime app-operator-drilldown --json`、`framework readiness --family-defaults --json` 与 `family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --json` 读取。这个状态关闭的是 OPL refs-only transport 可用性；若 source scope 或 runtime event 未被 domain/App/live refs 覆盖，stage evidence workorder 仍会保持 open，不声明 MAS paper closure、MAG grant/fundability/export/submission readiness、RCA visual readiness、App release/user path、真实 long soak、memory/artifact/lifecycle scaleout 或 domain owner-chain production ready。

同日 App/operator stage production evidence read model 已改为证据视图读取完整 stage attempt ledger，同时保留 workbench 默认最近 25 条 attempt 的 UI 列表边界。MAG / MAS / RCA 的 stage evidence 归属匹配同时按 target domain id、project id 和 canonical command-domain id 解析，避免 `med-autogrant` stage plane 与 `medautogrant` attempt command id 这类合法别名造成生产 caller / executor binding 漏读。该修复关闭的是 App/operator evidence 读模截断与 domain id 匹配误报；不生成新的 domain owner receipt、expected receipt instance、monitor freshness、App release/user path 或 long-soak evidence。

2026-05-21 readiness accounting 也已把 agent readiness diagnostic 与 stage readiness diagnostic 分开：`framework readiness` summary 中 `agent_readiness_diagnostic_failure_count` 与 `stage_readiness_diagnostic_failure_count` 独立计算，`stage_readiness.diagnostic_failures` 只包含 stage readiness lens 的 diagnostic failure。这个口径防止 OMA / sibling repo 未绑定等 agent-readiness fallback 被误算成 stage readiness failure；它只是 OPL 读模型计数修正，不声明 domain ready、production ready 或真实 owner-chain evidence 完成。

同日 `framework readiness` 的 attention-first 摘要进一步把 `evidence_envelope_open_count`、`evidence_envelope_blocked_count`、`evidence_envelope_attention_count`、`domain_dispatch_attention_count`、`runtime_manager_mas_route_support_task_kind_count`、`runtime_manager_mas_aftercare_route_support_count`、`runtime_manager_mas_route_support_action_ref_count` 和 `total_operator_attention_tail_count` 暴露出来。`open_tail_count` 仍只统计 agent structural、App live 和 stage receipt freshness 三类 production open tail；agent structural evidence tail 现在按 production evidence tail ledger 的 open count 读取，closed tail 只保留为 total / closed drilldown 审计材料，不再重复计入 open tail。envelope attention 只提示 refs-only evidence envelope 中仍有 open / typed-blocker 项；domain dispatch attention 现在按 `typed_blocker_stage_count + uncovered_missing_owner_chain_count` 合成，已由 domain typed blocker 接管的 stage 不再同时计入 missing owner-chain；runtime manager route support 只提示 MAS route catalog 已能被 OPL/App 识别，不关闭 owner receipt、不授权 domain ready、不声明 production ready。这个改动避免 evidence-after-contract 的 closed tail、envelope 未闭合项、dispatch owner-chain 缺口和 route-support 已落地状态被单一 tail 小计误读，同时保持 authority boundary 为 no domain truth / no quality verdict / no artifact authority / no production ready。

同日 App/operator 默认 `attention_first_payload` 也新增 `evidence_after_contract` 摘要：默认面只暴露 evidence envelope attention、domain dispatch attention 和 MAS route-support catalog 的计数与 authority boundary，完整 envelope、dispatch、stage production 和 route support refs 仍必须通过 `--detail full` 显式读取。该摘要让 One Person Lab App / default caller 能在 open tail 为 0 时仍看到 domain/App/live evidence scaleout 的当前责任人是 domain repo 或 App live operator；它不把 attention 计数升格为 hard blocker，也不把 route catalog 写成 owner-chain、domain ready 或 production ready closure。

同一默认 payload 还会投影 `evidence_next_steps`：它把 domain dispatch attention、evidence envelope attention、stage missing evidence 和 domain-owned typed blocker 转成只读 operator guidance，列出需要回填的 owner receipt、typed blocker、no-regression、memory writeback、evidence、domain receipt 或 owner-chain refs，并指向 `domain_dispatch_evidence`、`evidence_envelope` 或 `stage_production_evidence` full-detail section。该列表不创建新的 safe action、不伪造 owner receipt、不执行 domain action、不关闭 domain ready；它只是让 default caller 在 provider scheduler 健康且 open tail 为 0 时仍能把下一步证据责任路由回 domain repo 或 App live operator。

同日 MAS `publication_aftercare/analysis-queue-progress` 与 `publication_aftercare/reviewer-refresh` 已进入 task-bound provider-hosted stage attempt 路径：hydrate / dispatch 会把 aftercare owner-route refs 映射到 OPL stage attempt ledger，保留 route/action refs、source fingerprint、workspace locator、retry/dead-letter 与 typed closeout 投影。该路径只证明 OPL owns queue / attempt / liveness / retry / dead-letter / projection，不写 MAS truth、publication quality、artifact gate 或 `current_package`。

同日 evidence envelope 的 owner summary 已收敛到 canonical owner id：`med-autoscience`、`med-autogrant`、`redcube-ai` 与 `one-person-lab`。`medautoscience`、`medautogrant`、`redcube`、`redcube_ai`、大小写 display owner 等来源别名只保留在 full-detail `owner_alias_diagnostics`、`owner_source_id` 或 `scope.source_domain_id` 中，供审计和 route 排障使用。默认 summary 不再把 command id、display id、project id 当成多个 owner 展示，避免 evidence-after-contract 读面形成多重语义；这个 canonicalization 只改变投影读法，不改 domain truth、owner receipt、typed blocker、artifact authority、quality verdict 或 production ready 口径。

Legacy cleanup evidence 以 `opl agents legacy-cleanup apply --domain <domain> --mode dry-run|apply|verify` 和 `opl runtime app-operator-drilldown --json` 为入口读取。App/operator cleanup plan 把 `agent_id` 与 `command_domain_id` 分开，并把 ready plan 派生为 `legacy_cleanup_apply` 与 `legacy_cleanup_verify` OPL safe action route；`runtime action execute` 可用这些 route 写入或验证 OPL cleanup ledger。该证据证明 OPL cleanup gate、refs-only ledger、App/operator 投影和 safe-action 执行面已能消费三仓 replacement / no-active-caller / provenance / tombstone / handoff refs；它不授权 OPL 删除 domain repo 文件，也不等于 production default caller、App 发布路径、owner-chain 或 long-soak evidence 已完成。具体 plan status、receipt 数和 route 数只从命令输出读取。

2026-05-19 的系统评估把当前完善主线收窄为 OPL 自身语义治理：provider readiness 多真相、generated surface drift/block、App/operator drilldown 默认过重、`family-runtime` parser 语义堆叠、stage launch guarantee 含糊、legacy vocabulary active leakage，以及 AI 原生专家判断容易被 readiness / scorecard / contract completeness 机械替代这七类污染点已在 main 落成机器守门面或本轮补齐 policy guard。当前 main 已吸收 runtime single truth、generated surface gate、App drilldown summary-first、CLI parser split、stage scope / `guarantee_mode` projection、stage production caller evidence projection、主文档 hygiene 和 contract-floor policy；这些属于 framework hygiene closeout，不等于 MAS/MAG/RCA production default caller、App 用户路径或 long-soak evidence 已闭合。后续只能在这些守门面之上补 production evidence，不把 evidence tail 写成框架结构缺口。

MAS/MAG/RCA 当前均可被 OPL 识别为 standard domain agent consumer。Conformance、generated-interface、proof-bundle、cohort-loop、transition-runner 和 MAS handoff 的当前状态只从 `opl framework readiness --family-defaults --json` 及对应 drilldown 命令读取；本文不冻结 pass count、stage count、blocker count、external request count、transition case count 或 verified receipt count。这个状态说明 descriptor、read model、handoff、generated/handler target proof、replacement surface、scaffold validation、physical morphology policy、active path scan 和 OPL generic transition runner consumption 可由 OPL structural gate 审计；production usable closure 只按 domain-owned `production_acceptance` receipt 或 typed blocker 读取，不能写成 OPL 已证明 production domain owner chain、App 真实用户路径或 long-soak evidence 全部闭合。

2026-05-21 后续 repo audit 更新了 remaining functional cleanup 读法：OPL 默认审计面已把 active private generic residue 作为动态 drilldown 计数读取，但这不等于三仓 active caller 已完全退役。MAS 默认 managed runtime backend 已切到 `opl_provider_backed_stage_runtime`，`mas_runtime_core` 现在是 delegated MAS domain owner receipt / typed blocker / diagnostic adapter；这关闭 default runtime/backend owner gap，但 `runtime_transport/`、turn runner、worker lease、SQLite lifecycle sidecar、runtime watch/supervision loop 和 product-entry/CLI/MCP/sidecar/workbench wrapper 仍需要在 no-active-caller、OPL provider parity、MAS receipt parity、no-forbidden-write 与 focused tests 同时成立后再物理删除、archive 或 tombstone。MAG 新增 `generated_hosted_default_caller_proof`，把 repo-local product/status/sidecar/grouped wrapper 明确标成 handler/ref-only/migration input，并把 direct/hosted parity 与 no-forbidden-write 变成外部 evidence request；这只证明 MAG handler boundary/read-model proof 可验，不声明 OPL generated/hosted production default caller 已完成迁移、App workbench 真实消费或 Temporal long soak 完成。RCA 没看到新的 generic owner blocker，`product-entry-session-store` 已物理改名为 `product-entry-session-snapshot-ref-adapter`；`product_entry_session_store` 仅保留为 semantic contract id。RCA 剩余是 continuity/runtime-loop projection、CLI/MCP wrappers、workspace/run envelope、runtimeWatch/sidecar/observability 和 executor adapter 的 naming/package hygiene、controlled visual-stage long soak、更多真实 artifact-producing owner receipt、visual memory body reuse、workspace receipt scaleout 和 cross-family repeated no-regression evidence。

Fresh substrate evidence（2026-05-18）显示：`opl substrate projection --domain med-autoscience --json` 返回 `projection_status=substrate_refs_resolved`，source refs、artifact refs、memory refs 与 lifecycle projection 均为 `resolved`。OPL 只索引 MAS manifest 暴露的 body-free source/artifact/memory refs；source truth、memory body、artifact body、publication quality verdict 和 artifact mutation authority 仍归 MAS。

当前 active gap 与实施顺序以 [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md) 和 [生产级框架闭环差距矩阵](./active/production-framework-closure-gap-matrix.md) 为准。dated proof、receipt 明细和阶段 closeout 流水保存在 [OPL family 文档过程归档 2026-05](./history/process/plans/2026-05-18-opl-family-doc-process-history.md)，不在本页展开。

## 当前功能/结构差距

OPL family 当前不能写成整体功能/结构差距归零。仍需关闭的功能/结构差距是：

0. `semantic_hygiene_guard_surface_closeout`
   OPL 自身语义污染点已在 main 落地为机器守门面：provider readiness 改为 lifecycle-aware 单一 payload 派生；generated surfaces 从 canonical action/stage metadata 与 pack compiler input 对齐，MAG transition oracle 降为 evidence gate 而非 generated drift；App/operator 默认面改为 summary-first，完整 refs/routes 走显式 full detail；`family-runtime` parser 按子域拆分；stage control plane 投影 source/artifact/workspace scope refs、runtime assumptions、monitor refs 与 `guarantee_mode`；旧 Hermes/Gateway/frontdoor/MDS/default-compat 词汇继续只留在 history/tombstone/provenance、fixture、negative guard 或 cleanup plan；readiness / conformance 机器面明确 AI 原生专家判断优先、contract 只保安全/审计/恢复下限。后续只作为回归守卫，不再作为新增功能缺口，也不代表 MAS/MAG/RCA production ready。

1. `generated_surface_production_consumption`
   OPL pack compiler / generated surface 已能输出 active-caller target proof；MAS/MAG/RCA repo-side generated/handler target consumption 已闭合。descriptor ready / classification closed 代表可生成、可分类和可路由；production domain owner chain、live caller evidence 和 domain ready 仍通过真实 provider / App / owner receipt evidence gate 验收。

2. `stage_launch_admission_gate`
   默认 operator / App 总入口已收敛为 `opl framework readiness --family-defaults --json`。`opl stages readiness --domain <domain>` 是 stage-level drilldown：它消费 Stage Kernel admission、scope refs、expected receipt refs、proof/replay refs、assumption/monitor refs 和 evidence gap，并把 Derived Diagnostic Lenses 折叠成 warning、recommendation、typed blocker、human gate 或 route-back ref。`family-stage-admission` 仍是 `opl stages list|inspect` 的读模型；`family-runtime attempt create` 会记录 `opl_family_stage_launch_admission_gate`，声明 stage 存在 admission hard blocker 时进入 blocked attempt；`--require-stage-admission` 在 attempt start 与 provider-hosted tick 路径阻断的是越权、effect-boundary/runtime-guard 缺 runtime event 记录、组合不满足、human gate hard blocker 或 executor binding 缺失等启动安全问题。`family-stage-cohort-loop`、`family-stage-assumption-lifecycle` 与 runtime-budget 等 lens 只提供 source scope、cohort query、trigger、monitor/metric/dashboard refs、runtime assumptions、success refs 和 minimal counterexample 的诊断解释；它们不是普通首屏，不是独立学习目标，也不授权 OPL 写 domain truth、domain verdict、artifact body、quality verdict、stage completion 或 owner receipt。Stage readiness、cohort-loop 和 blocker 的当前状态只从 framework readiness drilldown refs、`opl stages readiness --domain <domain> --json` 和专题 drilldown 读取；剩余功能项是把 selected executor binding、真实 consumed refs、expected receipt refs、scope freshness、monitor freshness 与 cohort-loop live execution 的 production caller scaleout 纳入 queue/provider/App 可见证据。

3. `app_workbench_drilldown`
   OPL CLI/runtime read model 已覆盖 route graph、review queue、artifact gallery、package/export lifecycle、memory refs、functional privatization audit、quality/readiness、SLO 和 owner-aware action routing；`app_execution_bridge` 已把 App 调用边界收口到 `opl runtime action execute`、`runtime lifecycle apply/reconcile` 和 provider scheduler / legacy cleanup safe action route。AionUI runtime 页面已把 external evidence、evidence gate receipt、legacy cleanup 和 provider cadence window summary-first counters 放入 `AppOperatorDrilldown` 默认卡片，并能按需调用 `opl runtime app-operator-drilldown --detail full --json` 加载完整 refs/routes；full detail 同时展开 `functional_privatization_audit_refs`，按 domain 列出 module refs、private platform residue inventory、authority function inventory、standard pack inventory、bridge exit gate 和 forbidden generic owner flags，且 App authority boundary 仍保持不写 domain truth、不读 memory/artifact body、不授权 quality/export verdict。默认 summary 的 `functional_privatization_action_required_count` 是日常待处理数，`private_platform_residue_inventory_count` 是 full-detail 审计账本数，不能把后者写成 open blocker。legacy cleanup detail 已输出可执行的 `command_domain_id` route，不再把 skeleton id 写成 CLI domain 参数；focused tests 已覆盖 summary 默认面、provider cadence window 计数、full-detail lazy load、functional privatization refs、provider scheduler action execution 和 legacy cleanup action execution。剩余是发布包/截图、真实用户路径和长时 operator evidence。domain repo 不应复制通用 workbench。

4. `domain_private_platform_residue`
   MAS 已把未分类 generic owner 回流和 5 个 structural follow-through gate 清零，并新增 `runtime_transport_handoff_projection` 约束 runtime runner / worker lease / supervisor / SQLite sidecar 只能作为 domain bridge、receipt、typed blocker、refs-only sidecar 或 diagnostic。MAG repo-side handler/ref-only/authority boundary 已闭合；RCA generated/hosted shell consumption、repo-local wrapper active-caller migration 和 legacy physical cleanup 也已按 OPL consumer 口径闭合。MAG/RCA 剩余不是继续迁移 repo-owned generic owner，而是外部 default caller、App、live owner-chain、production evidence tail 与 naming hygiene tail。所有新发现或既有残留都必须按 OPL 上收、generated surface 替换、refs-only 收薄、minimal authority function、diagnostic cleanup 或 tombstone 分类处理；结构闭合不能写成真实生产 owner chain、live evidence 或物理代码路径已清零。

5. `legacy_physical_cleanup`
   Hermes/Gateway/frontdoor/local-manager/MDS-default/default-compat 等旧面只有在 provenance、diagnostic、fixture、negative guard 或 history 语境中可见。MAS local LaunchAgent 当前没有本机 active label / plist；MAS Hermes scheduler ensure path 已退役为 tombstone，显式 Hermes 只保留 status/remove 的 legacy diagnostic cleanup；MAS repo 内相关路径只能保留 explicit cleanup diagnostic / tombstone 角色。MAS/MAG/RCA 的 OPL legacy cleanup plan、OPL refs-only lifecycle ledger receipts 与 App/operator cleanup routes 只从 `opl agents legacy-cleanup ...` 和 `opl runtime app-operator-drilldown --json` 读取。RCA 旧 managed runtime / repo-local generic wrapper residue 已闭合为 handler target、refs-only adapter、history/provenance 或物理删除。后续只保留新发现 residual 的 fail-closed cleanup gate、domain owner 物理删除 receipt 和 RCA naming hygiene tail。

6. `app_managed_environment_startup_maintenance`
   OPL 已提供 `opl system startup-maintenance` 作为 App startup maintenance 机器入口，MAS/MAG/RCA/OPL Meta Agent clean managed checkout 可受控 install/update、health check、skill sync 和刷新 plugin cache freshness；dirty/ahead/diverged/no-upstream/env override/sibling workspace/invalid checkout 会 fail closed 到人工处理，不静默覆盖 developer checkout 或 managed runtime。AionUI first-launch / app-version maintenance 源码路径已自动调用该 action。剩余是发布包/截图证据、reload prompt 用户路径和长时 evidence。

7. `developer_mode_agent_lab_repair_route`
   Developer Mode 的系统配置、App settings / initialize surface、GitHub login / repo permission projection、direct repo fix / fork PR route 计算、Agent Lab refs-only repair route read model 和动态 route builder 已落地。动态 builder 读取 developer mode projection、repo permission 与 patrol observation refs，输出 `blocked` / `observe-only` / `direct-fix` / `fork-PR` / `mixed`，并把 `developer_mode_projection_ref`、`route_eligibility`、`patrol_observation_ref`、`diff_ref`、`verification_refs`、`no_forbidden_write_ref`、`commit_ref`、`fork_repo_ref`、`pr_review_ref`、`owner_acceptance_ref` 作为 closeout refs 投影；`owner_acceptance_ref` 必须是外部 owner ref；没有真实外部 PR 权限时，non-owner fork/PR 只能用 repo contract fixture ref 表达 drill，并通过 `closeout_claim_status=fixture_drill_owner_acceptance_open` 与 `owner_acceptance_status=fixture_drill_not_owner_acceptance` 标记为非 owner receipt、非 owner acceptance closeout。OPL 仍不能写 owner receipt、domain truth 或 managed runtime truth。该路线应按 Agent Lab 风险分级运行：低风险 prompt/rubric/display/read-model/test metadata 只有在真实 failure/evidence delta、independent AI approve、promotion receipt、rollback 和 no-forbidden-write refs 齐全后才可 auto-promote；中风险 stage/tool/retry-dead-letter/memory-retrieval policy 还需 tests pass 和 canary 后自动推广；高风险 domain truth、quality verdict、artifact mutation、memory accept/reject 与 credential/network/write policy 必须 owner/human gate。剩余证据门是用真实 repo 问题跑一次完整 external non-owner fork/PR owner acceptance closeout；当前 repo fixture drill 只证明合同与测试路径，不证明 owner acceptance。该能力必须保持 managed runtime truth 不变，只授权开发修复、PR 路由和合规的低/中风险机制推广。

8. `agent_lab_external_research_absorption`
OPL Agent Lab 已把 ARIS 风格的日志驱动机制优化、独立 AI reviewer、自动 promotion / rollback、integration contract、review trace、effort/assurance 双轴、helper inventory/drift report、permission/current-date fail-closed invariant 与 MCP/stream reliability policy 变成 machine-readable refs-only surfaces。AHE-style 证据链和 Codex-first executor-first 读法继续收敛为 `executor_capability_lease`、Codex attempt trace / replay / fork refs、variant cockpit、independent reviewer refs 与 App/operator self-evolution cockpit：OPL 只给 Codex 等 selected executor 发放可审计 stage lease、工作区/工具/预算/回执边界和 replay/falsification refs，不枚举或限制 AI 内部推理。MAS 侧负责把 research wiki、reviewer direct evidence、analysis queue manifest、runtime event ledger、provider/executor switch hygiene 和 claim assurance 投影进 `mechanism_evolution_inputs`；OPL 只消费 refs、typed graph/queue/ledger/hygiene/assurance metadata 和验证结果，不接管 paper memory body、analysis queue body、runtime event body、provider/executor transcript body、claim text body、publication verdict、owner receipt 或 artifact authority，也不引入 ARIS、AHE、OpenAI Agents SDK、LangGraph、AutoGen 或 CrewAI runtime dependency。

## 当前测试/证据差距

下面是目标结构正确后的证据门，不能替代功能/结构迁移；MAS/MAG/RCA 都不能把 descriptor ready、read model、replacement proof 或 provider proof 写成功能/结构迁移完成：

- MAS 多条真实 paper line 的 provider-hosted guarded apply、progress delta、AI reviewer update、artifact delta、human gate、stop-loss 或 stable typed blocker。
- MAG 真实 OPL-hosted grant-stage controlled soak、owner receipt、typed blocker 或 no-regression evidence。
- RCA 真实 artifact-producing owner receipt、visual memory body reuse、workspace receipt scaleout 和 repeated no-regression evidence。
- MAS/MAG/RCA 的 memory / artifact / lifecycle 在真实 workspace 中形成 accepted/rejected writeback、cleanup/restore/retention 和 artifact mutation receipt。
- Temporal provider 长时 SLO 已有 `cadence_window` 机器投影，可区分 `window_cadence_satisfied`、`window_evidence_incomplete`、`window_repair_receipt_observed` 和 `no_window_evidence`，并把 expected / observed / missing / blocked repair receipt counters 暴露给 runtime snapshot、App/operator read model 和 AionUI 默认卡片。`provider_capability_slo` 现在把 restart re-query、signal history、typed closeout、missing-closeout block、retry/dead-letter boundary 和 domain truth boundary preservation 投到 runtime snapshot / App summary；当前窗口与计数只从 `opl framework readiness --family-defaults --json` 和 `opl runtime app-operator-drilldown --json` 读取，不在本文固化。`domain_dispatch_evidence` 已让 App/operator 可见 owner-chain refs 是否出现；仍需真实 domain owner-chain dispatch scaleout、长时 operator evidence 与 no-forbidden-write proof。

当前未闭合项固定按下面四类读取，具体数量和 drilldown 以 `opl framework readiness --family-defaults --json` 为入口：

- App release / user path：发布包、截图、reload prompt 真实用户路径、provider state 联动和长时 operator evidence。
- Domain owner-chain 与 live evidence scaleout：MAS/MAG/RCA 在真实 workspace 中持续产出 owner receipt、typed blocker、no-regression evidence、expected receipt instance、monitor freshness、memory/artifact/lifecycle receipt、long-soak refs 和 no-forbidden-write proof；OPL worklist / workorder accounting 闭合不等于这些真实生产证据已经完成。
- MAS physical thinning：retained runtime transport / SQLite / runner / workbench sidecar 只在 no-active-caller、OPL parity、domain receipt parity、focused tests 与 provenance/tombstone refs 同时成立后删除、archive 或 tombstone。
- RCA naming hygiene tail：历史 `managed` / runtime / gateway / session / sidecar 命名继续降到 provenance、semantic-id 或 tombstone，不恢复 compatibility alias。

## 当前默认入口

- 默认前门是 `opl`；`opl exec` 负责一次性请求，`opl resume` 负责续接会话。
- `opl install` 是当前一键安装入口，负责安装或复用 Codex CLI、Temporal-backed family runtime provider、MAS、MAG、RCA、OPL Meta Agent、推荐 skills 和 App 入口。
- `opl system` / `opl system initialize` / `opl system startup-maintenance` 管理 Codex CLI、provider profile/readiness、module install/update、skill sync、managed environment freshness、plugin cache freshness、reload prompt 和 local runtime state。
- `opl framework readiness --family-defaults --json` 是 family readiness 动态真相入口；它只输出 framework/operator 读面，不授权 domain ready、artifact authority 或 production ready。
- `opl runtime app-operator-drilldown --json` 与 `opl runtime app-operator-drilldown --detail full --json` 是 App/operator drilldown 入口。
- `opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json` 是 runtime safe-action evidence worklist 与 stage evidence workorder packet 入口；`family-runtime production-closeout` 已退役，不再是 active interface 或兼容 alias。

## 当前不能声明

- 不能声明 OPL 已全量生产可用。
- 不能声明 Temporal provider proof 等于 MAS paper closure、MAG grant readiness 或 RCA visual ready。
- 不能把 `stage_evidence_workorder_packet`、refs-only receipt verified、provider/SLO satisfied 或 tail item closed 写成 domain owner-chain、App 用户路径、release/dist、artifact authority、expected receipt instance、monitor freshness 或 long-soak evidence 已全部闭合。
- 不能声明 private functional audit 分类完成就等于物理代码路径清零。
- 不能把 `agents legacy-cleanup apply` 的 dry-run / apply / verify ready 状态写成 OPL 已删除 domain repo 文件或 production evidence 已闭合。
- 不能为了兼容保留旧模块、旧接口、旧测试、旧 CLI alias、facade 或 wrapper；active caller 迁走后直接删除或进入 history/tombstone。

## 参考入口

- [文档索引](./README.md)
- [项目概览](./project.md)
- [架构](./architecture.md)
- [硬约束](./invariants.md)
- [关键决策](./decisions.md)
- [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)
- [OPL 与 Foundry Agents 理想目标态](./references/runtime-substrate/opl-family-agent-ideal-state.md)
