# OPL 当前状态

Owner: `One Person Lab`
Purpose: `status`
State: `active_truth`
Machine boundary: 本文是核心人读真相面。机器真相继续归 `contracts/`、source、CLI/API 行为、runtime ledger、provider receipt、domain-owned manifest 和真实 workspace / App evidence。
更新时间：`2026-05-31`

## 当前公开角色

`OPL` 当前公开认知固定为三层：`OPL Framework -> One Person Lab App -> Foundry Agents`。

- `OPL Framework` 是完整智能体开发/运行框架，持有 Codex-default activation、Temporal-backed provider、typed queue、stage attempt、receipt/projection、shared contracts/indexes、Agent Lab、generated surface、safe action shell 和跨仓治理。
- `One Person Lab App` 是面向人的工作台，消费 framework/provider 状态和 domain-owned projection，展示任务、阶段、阻塞、source refs、artifact refs、memory refs、SLO、repair、workorder 和 owner-aware action。
- `Foundry Agents` 当前包括 `MAS`、`MAG`、`RCA`。它们持有各自的 domain truth、quality/export verdict、artifact authority、memory body / accept-reject decision、owner receipt 和 direct app skill path；OPL 只消费 refs，不写入 body、truth、artifact、owner receipt 或 quality verdict。

普通用户 App 形态当前按 `Codex App wrapper` 读取：App 把 OPL-managed environment、固定 `Codex CLI` executor 和 MAS/MAG/RCA Foundry Agent 入口组合成一个桌面工作台。用户选择工作类型、任务和 Foundry Agent 入口；AionUI 原生多 backend、多 Agent 选择、非默认 executor adapter 或 shell implementation 细节只能进入显式 developer/operator diagnostic，不是普通用户产品 truth。

`Codex CLI` 是当前第一公民 executor。标准 OPL Agent 的默认长跑路径是 `opl_temporal_hosted_autonomous`：任务启动后默认进入 OPL/Temporal 托管的 stage attempt runtime，由 OPL provider scheduler / typed queue / wakeup / resume-requery / retry-dead-letter / attempt ledger 持久在线推进；Codex App 只承担启动、观察、介入和投影入口，不作为外围持续驱动任务的主体。Domain agent 不允许内置或恢复通用 daemon、scheduler、attempt loop。Temporal-backed provider 是 production online runtime 的必需 substrate；`local_sqlite` 只允许作为 dev/CI/offline diagnostic baseline。`hermes_agent`、`claude_code` 与 `antigravity_cli` 同属显式非默认 executor adapter/backend，只能以 receipt/audit/fail-closed 证明连接，不承诺行为、质量、工具语义或 resume 等价。

非生产证据层已经落到 App/operator 默认读面。`runtime app-operator-drilldown` 的 summary 与 `attention_first_payload.codex_app_runtime_role` 直接投影 `runtime_policy=opl_temporal_hosted_autonomous`、`codex_app_roles=[start, observe, intervene, display]`、`codex_app_drives_long_running_tasks=false`、`long_running_task_driver_owner=one-person-lab`、`long_running_task_driver_substrate=temporal`，并保持 `production_long_soak_claimed=false`。`codex_app_runtime_role.production_evidence_followthrough` 把 `temporal_hosted_long_soak_refs` 作为 refs-only production-evidence gate 投影到 `evidence_next_steps`，并给出 `app_live_operator_or_opl_provider_owner` 回填真实 Temporal-hosted long-soak refs、provider state linkage、operator evidence refs 或 typed blocker refs 的 payload workorder。该 workorder 对接专用 body-free ledger：App/default caller 可通过 `codex_app_runtime_evidence:codex_app_runtime_role:record` / `verify` safe-action route，或 direct `opl runtime codex-app-runtime-evidence record|verify|list`，记录、验证和读取 Codex App runtime evidence refs。同一 surface 还提供 `opl runtime codex-app-runtime-evidence long-soak start|event|finish` 本地观察入口：`start` 只写 workorder 和 operator log，`event` 只追加受控 event，`finish` 在观察窗口和 required events 满足后只物化 manifest 与可提交 record payload，仍需后续 `record` / `verify` 才能进入 refs-only ledger。当前本机 ledger 已验证 1 条真实 `temporal_hosted_long_soak_refs` receipt；fresh App/operator drilldown 读为 `codex_app_runtime_evidence_open_gate_count=0`、`codex_app_runtime_evidence_verified_ledger_receipt_ref_count=1`、`codex_app_production_long_soak_claimed=false`、`codex_app_production_evidence_gate_remains_open=false`。recorded 但未 verify 的 receipt 只投影为 verify follow-through；App/default caller 的 success path 必须包含真实 `temporal_hosted_long_soak_refs`，`provider_state_linkage_refs` 与 `operator_evidence_refs` 只是支撑审计 refs，单独记录或验证不会关闭该 gate；typed blocker refs 保持 gate open / blocked 语义。该 surface 只给 Codex App / operator 展示启动、观察、介入和展示边界，不新增 domain action、不写 domain truth、不创建 owner receipt、不生成 typed blocker、不把 verified refs 写成 production-ready verdict。

`MDS` 不进入 OPL 顶层 agent 列表。它只作为 MAS 显式声明的 source provenance、historical fixture、explicit archive import、backend audit、upstream intake 或 parity oracle reference。

## 当前真实状态

OPL 已具备 framework 主干：domain descriptor / stage / action / memory discovery、Temporal provider code、service / worker lifecycle、typed family queue、stage attempt ledger、typed closeout、evidence worklist read model、provider proof / SLO projection、runtime snapshot、OPL-owned provider scheduler cadence/tick surface、safe runtime action shell、lifecycle refs-only SQLite index、external evidence refs-only receipt ledger、App/operator drilldown read model、refs-only `domain_dispatch_evidence`、refs-only `stage_production_evidence` 和 refs-only functional privatization audit detail projection。external evidence ledger 现在也能记录并验证 memory writeback receipt、artifact mutation receipt、package lifecycle receipt、lifecycle receipt、restore proof 与 no-regression 的 body-free refs；即使这类 memory/artifact/lifecycle/no-regression receipt 没有绑定 domain-declared external request 或 evidence gate，只要属于当前 resolved domain，也会作为 standalone verified receipt 投影到 App/operator external receipt、family-runtime evidence-worklist 和对应 summary 计数。verified standalone `memory_writeback_receipt_refs` 也进入 App/operator `memory_writeback_refs`、ref family memory refs 和 runtime visualization 的 `memory_writeback_receipt` 节点，让 operator 能从 summary 计数下钻到同一 body-free receipt ref；standalone no-regression refs 进入 `domain_external_verified_no_regression_ref_count` 和 closed refs-only worklist item，不进入 memory/artifact/lifecycle 分项计数。该投影仍只是 OPL refs-only intake / projection，不读取 memory body、不读取或变更 artifact body、不接受/拒绝 memory writeback、不授权 package/export readiness、domain artifact mutation 或 visual readiness，也不创建或关闭 domain request/gate；`domain_dispatch:*` 与 `stage_production_evidence:*` receipt 继续归各自专用 read-model，不重复塞进 standalone external receipt。`opl family-runtime status` 与 `opl agents scaffold` 现在都把 `opl_temporal_hosted_autonomous` 暴露为标准智能体默认运行策略，同时保持 `domain_agent_internal_daemon_allowed=false`、`domain_agent_internal_scheduler_allowed=false`、`domain_agent_internal_attempt_loop_allowed=false` 和 `codex_app_drives_long_running_tasks=false`。

`stage_progress_log` 是当前 OPL family-runtime attempt/progress projection。它只从 OPL SQLite attempt ledger、Temporal provider history/status/update refs、human-gate/dead-letter state、closeout refs、domain-owned receipt refs 和 typed blocker refs 派生，并投给 App/operator、runtime tray、attempt query 与 Agent Lab refs-only improvement inputs。其 `user_stage_log` 已升级为标准 OPL Agent 的用户透明度要求：OPL 投影 stage identity、耗时、token/cost、refs 和显式 missing/null，并保存 domain closeout 返回的 stage 问题、目标、做了什么、改了哪些 surface、结果、剩余 blocker 和证据 refs；MAS/MAG/RCA/OMA 等 domain agent 必须在 typed closeout 里提供 `stage_work_done` / `changed_stage_surfaces` 等人话语义，缺失时显示 typed blocker 或 `missing_domain_semantic_summary`，不能静默当作透明完成，不能由 OPL 推断、补写或读取 artifact body 生成。标准 OPL Agent scaffold / admission 已要求 `user_stage_log_contract`，MAG、RCA、OMA 等 Foundry Agent 需要由各自 owner 提供 grant-facing、visual-facing 或 agent-building-facing 的语义摘要。Agent Lab 只消费这些 refs 做 eval、root-cause、candidate fix、promotion/follow-up projection；它不拥有 runtime log、不写 provider history 或 SQLite ledger、不执行 domain action，也不生成 owner receipt、domain truth、quality verdict、artifact authority 或 production-ready claim。历史 `stage_execution_log` 名称不再是 active surface，只能按 retired/tombstone/provenance 读取。

Progress-First 已上收为 OPL family shared contract，而不是 MAS/DM002 个案补丁。`stage_progress_log` 和 `user_stage_log_contract` 现在要求每个 stage 明确 `progress_delta_classification`、`deliverable_progress_delta` 与 `platform_repair_delta`，并保持 platform repair、refs-only ledger、projection hygiene、currentness 修复和 typed-blocker accounting 单独列账，不能显示成交付物实质进展。`effective_current_context.v1` 是 owner route、source fingerprint、stage packet、workspace/session identity、latest closeout、running attempt 与 superseded lineage 的统一 currentness packet；`family-stall-lineage.v1` 记录 blocker family、attempt refs、repeat count、last deliverable delta、next forced delta、escalation owner 与 terminal flag。`contracts/opl-framework/foundry-agent-series-policy-release.json` 是当前 Progress-First policy bundle release surface，带 `sha256:stable-json` policy bundle fingerprint；domain `contracts/foundry_agent_series.json` 必须 pin `shared_policy_release`，只能引用 release ref/fingerprint，不能复制 policy body 作为 domain authority。OPL App/operator、evidence-worklist、readiness、runtime tray 和 Agent Lab 只能消费这些 shared projection；MAS/MAG/RCA/OMA 保留各自 paper/grant/visual/target-agent alias，但 alias 必须映射到 OPL generic deliverable/platform delta，不能各自发明并行 schema。Progress-First 的 runtime liveness 口径现在把 Temporal worker blocker 放在 provider/scheduler read-model 的首要 actionable blocker。若 Temporal service 已可达但 worker `worker_not_ready`、`worker_source_stale` 或 dependency blocked，`opl family-runtime status` 与 `opl family-runtime scheduler status --provider temporal` 会投影 `worker_lifecycle_status`、`next_repair_action`、`next_repair_command`、Temporal service 状态和 `liveness_blocker_first=true`，避免被 stale/currentness、dedupe、queue 或 scheduler cadence 噪音掩盖。`opl family-runtime scheduler tick --provider temporal` 在这种 worker liveness blocker 存在时先返回 `blocked_provider_not_ready`、`provider_liveness_blocker` 与 `queue_tick=null`；worker liveness 解除后才进入 provider SLO tick 和 queue dispatch。该行为只属于 OPL provider/runtime-manager liveness 暴露，不执行 domain action、不写 domain truth、不签 owner receipt、不声明 provider SLO / domain ready / production ready / App release ready。

Fallow 现在是 OPL repo-local JS/TS 结构 hygiene 守门面：`.fallowrc.json` 把 `bin/opl`、CLI 主入口、package exports、OPL runtime / product / system 入口和动态加载的 `src/cli/cases/**`、`src/cli/modules/**` 显式登记为分析入口，`node:sqlite` 按 Node 内置模块忽略，测试目录不参与 duplicate / health 结论。`npm run hygiene:fallow -- --format json --summary` 会通过 `npx --yes fallow@latest` 运行，是当前人工检查入口。本轮已删除无 active caller 的 `src/domain-manifest.ts`、`src/domain-manifest/surface-normalizers.ts`、`src/opl-web-paths.ts` 和 `src/management/domain-manifest-catalog.ts` 历史 facade；管理层、runtime tray 与测试调用方直接读取 canonical `src/domain-manifest/catalog-builder.ts` / `src/domain-manifest/types.ts`。Fresh fallow production 读为 `unused_files=0`、`unlisted_dependencies=0`、`unresolved_imports=0`。剩余 fallow tail 仍包括 public-export 面、重复 helper 与 import-cycle tail；这些只作为源码 hygiene / owner-surface 收薄信号，不声明 domain ready、production ready、App release ready 或 Temporal long-soak closeout。

当前 `scripts/verify.sh` 的 line-budget gate 已恢复为可执行结构门：Temporal provider scheduler cadence 逻辑迁入 `family-runtime-temporal-provider-parts/scheduler-cadence.ts`，MAS default-executor single-flight 测试 helper 和 system engine action 测试拆成独立 case 文件，并删除已回到默认行数以下的 retired baseline。Fresh line-budget 与 `scripts/verify.sh` smoke 通过。该 closeout 只恢复 OPL repo 结构验证，不改变 provider SLO、App release、domain owner-chain、memory/artifact/lifecycle receipt、MAS physical thinning、RCA naming hygiene 或 family production ready 判据。

本机 OPL external evidence ledger 已验证 MAG 与 RCA refs-only lifecycle / workspace evidence。`opl://external-evidence/med-autogrant/mag-lifecycle-external-evidence-scaleout-20260526` 消费既有 MAG domain-owned artifact-mutation refs 与 restore proof refs；`opl://external-evidence/med-autogrant/mag-package-lifecycle-receipt-scaleout-20260526` 消费 MAG `product receipt-readiness` 生成的 package lifecycle receipt refs 与 cleanup / restore / retention lifecycle receipt refs，source ref 固定到 `/Users/gaofeng/.codex/projects/med-autogrant/runtime-state/receipts/lifecycle/20260526-opl-scaleout-receipt-readiness.json`。

RCA workspace receipt scaleout 当前读取为 `rca.workspace_receipt_scaleout.2026-05-28.6-workspaces`，已作为 `opl://external-evidence/redcube_ai/rca-workspace-receipt-scaleout-20260528-6-workspaces` verified standalone external evidence 进入 OPL ledger，消费 6 条 artifact-producing owner receipt refs、12 条 accepted/rejected visual memory writeback receipt refs、18 条 cleanup/restore/retention lifecycle receipt refs 与 6 条 restore proof refs；旧 `opl://external-evidence/redcube_ai/rca-workspace-receipt-scaleout-20260527-5-workspaces` 保留为累计 provenance，不作为当前 RCA snapshot closeout。RCA export 继续保持 `workspace_receipt_scaleout_claimed=false` 与 `declares_production_soak_complete=false`。

RCA repeated visual-stage no-regression refs 已作为 `opl://external-evidence/redcube_ai/rca-cross-family-repeated-no-regression-20260528-4-refs` 验证并进入 App/operator 和 evidence-worklist 的 standalone external receipt 投影，消费 `rca-no-regression:visual-stage:2026-05-27-opl-family-cross-family-repeat-a`、`rca-no-regression:visual-stage:2026-05-27-opl-family-cross-family-repeat-b`、`rca-no-regression:visual-stage:2026-05-28-opl-family-ppt-deck-window2` 与 `rca-no-regression:visual-stage:2026-05-28-opl-family-xiaohongshu-window2` 四条 RCA domain-owned no-regression refs；该 receipt 只证明 no-regression refs 可被 OPL refs-only ledger record/verify/project，不改变 App/operator memory/artifact/lifecycle external summary counters。

App/operator external evidence、no-regression、memory writeback、artifact mutation、package/lifecycle、restore proof 与 evidence-worklist closed refs-only 数量都是动态 summary 计数，只从 `opl runtime app-operator-drilldown --detail full --json` 与 `opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json` 读取，不在本文固化。Fresh live read 只用于判断当前读面是否仍能下钻到同一 body-free receipt refs，以及 `domain_ready_authorized=false`、`production_ready_authorized=false` 是否继续保持。Standalone receipts 只消费 domain-owned body-free refs，不生成 owner receipt、不写 grant/visual truth、不读取 memory body 或 artifact body、不授权物理删除、grant ready、visual ready、package/export ready、RCA visual ready、Temporal visual-stage long soak 或 production ready；当前 framework readiness 仍把 blocked refs-only envelope 作为 operator attention，而不是 closeout verdict。

`runtime app-operator-drilldown` 默认 summary-first，完整 refs/routes 只在 `--detail full` 展开。App execution bridge 统一走 `opl runtime action execute`；OPL CLI route 可创建 provider-backed stage attempt request、执行 provider scheduler safe action、写 OPL cleanup ledger 或记录/验证 refs-only evidence receipt。Domain route 只进入 typed queue / approval，不直接执行 domain action、不写 domain truth、不签 domain owner receipt。默认 summary 也暴露 current-control-state 中真实 running provider attempt 的 bounded liveness refs，包括 domain、task kind、stage attempt id 与最新 heartbeat，用于让 operator 看到 MAS default-executor 等 live control-plane 状态；最新 heartbeat 优先来自 provider activity 的 supervision / runner-progress event，缺失时才回落到 stage attempt ledger 的 provider run 时间，并通过 `liveness_source` / `ledger_last_heartbeat_at` 保留来源边界。current-control-state 同时携带当前 attempt 的 `stage_progress_log` refs-only summary 与 `active_stage_attempt_stage_progress_log_ref`，让 domain read-model 能看到 stage log 是否存在、attempt 数、runner-progress event、usage/duration 可观测性和 authority boundary；它不读取 domain artifact body，也不生成领域语义。accepted typed closeout 若在 `closeout_refs` 中携带 domain-owned typed blocker ref，current-control-state 会同时归类到 `typed_blocker_refs`，让 operator 看到“AI 执行已回执但仍等待 owner receipt / live paper-line closeout”的阻塞来源；这不改变 `accepted_typed_closeout` 状态，也不把 provider completion 写成 readiness。该投影保持 refs-only，不发布 domain ready、publication ready 或 artifact ready。`functional_privatization_audit_refs` 也只在 full detail 中展开：它把 MAS/MAG/RCA 声明的 standard pack、minimal authority function、private platform residue、bridge exit gate、forbidden generic owner flags 和 cannot-absorb reason 作为 refs-only audit inventory 投给 App/operator。默认 summary 现在按 action-required 优先读取：`functional_privatization_action_required_count` 由 default watchlist、active private generic residue、semantic equivalence review 和 blocker 合成；`functional_privatization_hidden_cleared_count` 表示已折叠的 cleared/stable boundary；private platform residue inventory 数量只作为 full-detail 审计材料，不再当作默认待处理任务数。

`functional_privatization_audit` 现在把 semantic equivalence、private residue、owner receipt / typed blocker 和 cannot-absorb reason 放进同一 refs-only evidence gate。它只作为 App/operator 的 current drilldown，不授权 OPL 写 domain truth、memory body、artifact body 或 owner receipt，也不把机械 completion、watchlist 清零或读模型自身写成 private residue 已删除、domain ready、quality/export ready。

`functional_privatization_audit` 的 semantic-equivalence 判断现在优先消费 domain-owned explicit fields：`semantic_equivalence_status`、`semantic_equivalence_evidence_refs`、`semantic_equivalence_typed_blocker_refs` 与 `semantic_equivalence_no_regression_refs`。只有 domain 未给出显式状态时，OPL 才按 active caller / migration text 推导 review-required。该规则让 RCA 这类 refs-only executor adapter 能用 domain-owned boundary proof 关闭语义等价歧义，同时保留 bridge-exit、typed blocker、no-regression 和物理删除尾项；它不声明 private residue 已删除、RCA visual ready、domain ready 或 production ready。

Agent Lab、observability eval 和 mechanism improvement 继续是 refs-only control plane。OPL 只消费 refs，不写入 body、truth、artifact、owner receipt 或 quality verdict；domain truth、quality/export verdict、artifact authority、memory body 和 owner receipt 仍归 MAS/MAG/RCA。

`family-runtime intake|tick --hydrate` 的 MAS profile 路径已收口到 OPL module locator：即使 operator 只提供 `OPL_FAMILY_RUNTIME_MEDAUTOSCIENCE_PROFILE`，OPL 也会先解析 active MAS module checkout，再通过该 checkout 的 sidecar export hydrate pending family tasks，避免 PATH 上旧 `medautosci` 工具污染 live route。active MAS workspace binding 路径也保持 export / dispatch checkout 一致：当 pending task 来自 `workspace_binding` export，OPL 会把 `command_cwd` 写入 `opl_domain_export_context`，随后 dispatch 继续在同一个 bound workspace checkout 中调用 MAS `domain-handler dispatch`。该路径仍只写 OPL queue / stage attempt / dispatch evidence；论文质量、owner receipt、typed blocker 和 artifact authority 继续由 MAS owner surface 给出。

`family-runtime intake|tick --hydrate --profile <profile>` 是 MAS profile 的显式 operator override，优先于 `OPL_FAMILY_RUNTIME_MEDAUTOSCIENCE_PROFILE` 和 active workspace binding。该参数只用于选择 MAS sidecar export 的 profile，并继续通过 OPL module locator 调用 active MAS checkout；它不写 MAS study truth、不改变 publication verdict、不签 owner receipt，也不把 workspace-specific binding 逻辑下沉回 MAS。

`family-runtime intake|tick|scheduler tick` 的 scoped hydrate / dispatch 只过滤 OPL typed queue 选择，不写 domain truth、不改 verdict、不签 domain owner receipt，也不把 study-specific runtime/liveness/retry 逻辑下沉回 domain repo。scope 支持 `--domain` / `--study` / `--payload-match`，也支持按 sidecar export 的 top-level `task_kind` 使用 `--task-kind <kind>` 过滤；`--payload-match task_kind=...` 只匹配 payload body，不适合过滤 top-level task kind。`opl module exec` 的输出 buffer 属于 OPL system-installation 层配置项，domain repo 不复制该 buffer/lifecycle 逻辑。

`family-runtime intake|tick --hydrate` 会对 domain export 注入 OPL-owned `opl_domain_export_context`，用于审计 sidecar export 的 owner/freshness 指纹。若 domain export 仍声明同一 pending task，而 OPL 队列中旧 dedupe item 已因旧 owner capability 进入 `dead_letter`，只有当新的 export context 或 domain `source_fingerprint` 证明 owner/freshness 已变化时，OPL 才会重置 attempts 并重排队；同一 owner 指纹不变时继续 `dedupe_noop`。该规则属于 OPL typed queue / retry / dead-letter control plane，不赋予 MAS 私有 runtime，不写 MAS truth、publication eval、current package 或 owner receipt。

MAS `domain_owner/default-executor-dispatch` 的 single-flight 现在分清 intake 与 provider admission 两层。若同一 `workspace_root` / `study_id` / `action_type` / `dispatch_ref` 或同 study 已有 live provider-started `codex_cli` attempt，后续 MAS owner 输出新的 dedupe/work-unit/source fingerprint task 时，OPL hydrate/enqueue 会保留新的 queued task、刷新 live task lease，并记录 deferred single-flight evidence；provider admission / tick 继续阻止并发启动，直到 live attempt terminal sync、blocked/redrive 或完成 closeout 后再选择当前 queued task。这个语义保证 current-manuscript AI reviewer / writer handoff 不会被 live attempt 纯 noop 丢弃，同时仍不并发启动同 study 默认 executor。该状态仍只是 OPL queue/provider lifecycle currentness，不代表 MAS owner receipt、论文修复、AI reviewer verdict 或 package freshness 已关闭。

`domain manifests` 与 `agents descriptor` 已能在发现 MAS `family_transition_spec_descriptor` 且缺少物化矩阵时，消费 MAS action catalog / product-entry shell 暴露的 read-only `study_state_matrix` command，按 descriptor locator refs 物化 `family_transition_spec` 与 `family_transition_matrix_cases`，再交给 OPL generic `family-transition-runner` 执行 matrix。transition materialization 有独立短预算：`study_state_matrix` 超时只投影到 `family_transition_materialization.status=failed`，不把已经成功解析的 domain manifest 变成 `command_timeout`。`framework readiness` 的默认 summary 路径显式跳过该物化动作，避免把 operator readiness 变成昂贵 domain materialization。该能力只做 OPL transition runner transport/projection：不写 MAS study truth、不执行 MAS domain action、不授权 publication quality / submission readiness、不生成 domain owner receipt。

`workspace bind` 对 MAS/MAG/RCA structured binding 的派生面已从退役的 repo-local default wrapper 改为 OPL generated product-entry materializer：OPL 通过 bound `workspace_path` 与 locator refs 调用 MAS `build_product_entry_manifest` / `build_product_entry_status`、MAG `MedAutoGrantProductEntry().build_product_entry_manifest` / `build_product_status`、RCA `@redcube/domain-entry` `getProductEntryManifest` / `getProductStatus`，并让 `domain manifests` 执行真实 domain-owned manifest。当前本机 active MAS/MAG/RCA binding 已按该 materializer 重绑并 live resolved；这只关闭 workspace binding / manifest live refresh 漂移，不声明 MAS paper closure、MAG grant ready、RCA visual ready、owner receipt、physical delete 或 production ready。

Temporal provider cadence / capability SLO、provider scheduler、legacy cleanup、external evidence request、evidence gate、stage expected receipt / monitor freshness 和 domain-dispatch owner-chain accounting 都已进入 OPL refs-only ledger / App/operator drilldown / `framework readiness` 动态读面。`opl framework readiness --family-defaults --json` 是当前默认总读面；framework hard blocker、stage caller tail、App/operator evidence tail、stage diagnostic warning 与 evidence worklist open item 都从 CLI、App drilldown 和 `family-runtime evidence-worklist` 读取，不在本文固化。Stage readiness warning 是 advisory / diagnostic warning，不是 framework hard blocker、domain ready blocker 或 open App workorder。

`stage_evidence_workorder_packet`、`domain_dispatch_evidence_workorder_packet`、`owner_handoff_packet` 和 `evidence_requirement_ledger` 是当前 evidence worklist 的默认组织方式：full detail 按 canonical owner/stage 聚合需要 domain/App/live operator 回填的 receipt refs、typed blocker refs、owner-chain refs、no-regression refs 或 evidence refs；默认 App/operator 面只投影 bounded group guidance 和 next action。它们只汇总 refs、closure gate 和 authority boundary，不生成 receipt、typed blocker、owner-chain、monitor freshness 或 no-regression ref，不执行 domain action，也不声明 stage complete、domain ready 或 production ready。`family-runtime production-closeout` 已退役，不作为 active interface、兼容 alias 或文档入口保留；框架级 `opl framework production-closeout` 是另一条 framework-level 汇总命令。

`framework readiness` 的默认实现现在把 App/operator dispatch summary、embedded `evidence_worklist`、`stage_receipt_freshness_tail` 和 attention packet 从同一个 full runtime snapshot 派生；同一 payload 内的 `domain_dispatch_evidence_receipt_*` route count 与 `domain_dispatch_evidence_workorder_packet_summary.workorder_count` 必须一致。该 single-snapshot 约束只减少 read-model 内部漂移和多重语义污染，不冻结瞬时 live workorder 数，也不改变 OPL 非授权边界。

`family-runtime evidence-worklist` 在未传入外部 `runtimeSnapshot` 时，会先用 preliminary App/operator snapshot 识别当前已暴露的 domain-dispatch record workorder 与 live Temporal attempts，再执行 bounded terminal observation sync；若本地 stage attempt ledger 被 provider terminal observation 更新，则重建 final snapshot 后再派生 worklist。该 currentness sync 只消费 Temporal provider 的 terminal status / typed closeout refs，把本地 attempt projection 拉到 provider terminal currentness，避免 stale local closeout 继续暴露 open domain-dispatch workorder；它不扫描历史 completed gap 生成新 workorder，不生成 owner receipt、typed blocker、owner-chain 或 no-regression ref，不执行 domain action，也不改变传入 snapshot 的纯投影路径。

domain-dispatch workorder 已把 target identity、stage attempt source fingerprint、domain source freshness 和 identity binding policy 纳入 preflight。默认 worklist / App default caller 能先判断 payload 应绑定 OPL stage attempt key 还是 domain sidecar freshness key，再交给 `runtime action execute` fail closed 校验。同一 bound dispatch supersession identity 的默认 actionable route 只来自最新且已有 `accepted_typed_closeout` 的 stage attempt；未形成 accepted typed closeout 的 running / blocked / failed / provider non-completion attempt 只作为 blocked envelope 或 superseded provenance 投影，不开放 owner receipt / typed blocker record workorder。精确 dispatch identity 仍保留 `dispatch_authority` 维度用于审计，但 authority cutover 后的较旧 attempt 始终保留为 superseded provenance，即使最新 attempt 已记录/验证 domain-owned typed blocker 或 receipt refs，也不会把旧 attempt 重新暴露成 record workorder。该能力只提升 refs-only payload 可操作性和身份审计强度，不读取 domain body、不新增 receipt authority、不执行 domain action、不生成 typed blocker 或 owner receipt，也不把 verified receipt 写成 owner-chain closeout。

provider history re-query 与 queue inspect 能把 provider completed observation 同步为 OPL stage attempt typed closeout refs ledger。同步逻辑只把 closeout refs、consumed refs、writeback refs、next owner、route impact 和 `domain_ready_verdict` 作为 transport metadata 投到 OPL attempt ledger，并保持 `provider_completion_is_domain_ready=false`；它不生成 MAS owner receipt、不写 MAS truth、不授权 paper ready、artifact ready、publication ready 或 production ready。

`family-runtime attempt query|inspect` 的 operator visibility 现在直接投影 Temporal `codex_stage_activity_timeout_policy`：`CodexStageActivity` 使用长 `start_to_close`、短 heartbeat timeout、周期 supervision heartbeat、60 分钟 runner 总窗口和独立 no-output watchdog；`local_sqlite` 不投影该 Temporal policy。Temporal activity 运行期间会把 supervision heartbeat 和 runner-progress heartbeat 作为 OPL provider liveness metadata 同步进 stage attempt ledger，供 current-control-state / App/operator summary 读取；该写入只更新 `provider_run.last_heartbeat_at` 与 activity event，不改 closeout、owner receipt、domain truth 或 route verdict。该字段只让 App/default caller 与 operator 在 attempt 读面上看清 Codex stage 的 provider 监督策略，不改变 workflow 执行语义，不证明 runner progress、owner receipt、domain ready 或 production ready。

每个 stage attempt 现在还投影统一 `stage_progress_log`：`attempt query`、`queue inspect`、operator visibility 与 runtime tray `stage_attempt_workbench` 都能看到 intended work、actual work、timeline、usage、Temporal visibility refs、evidence refs 和 authority boundary。Temporal 负责 durable workflow history、activity heartbeat 和 searchable visibility substrate；OPL 负责把 attempt ledger、provider run、activity events、usage projection 与 closeout / blocker refs 派生成 OPL 语义 log。usage / cost / duration 继续采用显式 observed 或 missing/null 语义，避免把未知误写成 0；该 log 是 OPL 统一可观测面，不替 domain owner 做 quality、ready、artifact 或 package authority 判断，也不把 Temporal Web UI fork 成 OPL App 主状态页。`attempt_true_path_proof` 是同一 attempt 的 refs-only 证明面，用来把 `attempt query`、`queue inspect`、App full drilldown、`stage_progress_log`、Temporal visibility 和 Temporal Web UI debug ref 绑定到同一 `stage_attempt_id/task_id/workflow_id/run_id`；它只证明当前真路径可追踪，不声明 long-soak、domain-ready、artifact authority 或 quality verdict。

MAS default-executor 的 OPL-owned provider retry-budget dead-letter 现在有显式 operator redrive 路径。`opl family-runtime queue redrive <task_id> --reason <operator_reason>` 可在同一 task 具备 `dead_letter/retry_budget_exhausted` queue state 和 `dead_lettered` / `retry_budget_exhausted` Temporal `codex_cli` stage attempt evidence 时，把 task 放回 `queued` 并创建新的 provider attempt。该路径用于 OPL provider/runtime 修复后的恢复，不会由 hydrate dedupe 自动触发，也不写 MAS study truth、publication quality、artifact gate、paper package 或 `current_package`。

OPL queue/attempt owner 现在有 durable 显式暂停面。`opl family-runtime queue hold --study <study_id> --reason <operator_reason>` 会写入 scoped admission hold，把已存在的 scoped `queued` / `retry_waiting` / `running` task 移到 `waiting_approval`，清除 lease，并把 linked active stage attempt 投影为 `human_gate` / `operator_hold_requested`；后续 hydrate、enqueue 或 provider redrive 命中同一 scope 时也进入 `waiting_approval`，不会在 operator 明确暂停期间重新排成 worker 可取任务。`opl family-runtime attempt cancel <stage_attempt_id> --reason <operator_reason>` 用于已经启动的 Temporal attempt cancellation；发出 cancel transport 后，本地 read model 立即投影为 `cancel_requested` / `operator_cancel_requested`，linked MAS default-executor task 立即进入 provider-only cancellation blocker，随后再由 Temporal `CANCELED` / `CANCELLED` terminal observation 收敛。两者都只写 OPL queue/attempt ledger 与 provider transport projection，不写 domain truth、quality verdict、artifact gate、paper package 或 `current_package`。暂停 live study 线时先处理 active attempt/worker，再 hold scoped queued/retry/running work，避免旧 worker 竞态取走新任务。

payload path guidance 已进入默认接力面。operator 必须选择真实 success refs path，或提交 domain-owned typed blocker path；domain-dispatch 的 success refs path 只接受 `domain_receipt_refs`、`owner_chain_refs` 或 `no_regression_refs`，`evidence_refs` 只是 supplemental context，不能单独记录 refs-only closeout，也不能关闭 owner chain。空模板、占位 ref、OPL 自造 owner receipt / typed blocker / owner-chain / no-regression ref 都不会被当成成功 evidence。默认 selected `next_safe_action` 只从当前未闭合且可行动的 App/domain/live evidence route、provider route 或 cleanup route 中选择；已由 provider SLO / lifecycle ledger receipt 闭合的 framework route、no-op cleanup route、`stage_attempt_query` 这类 diagnostic query，以及 verified receipt + domain-owned typed blocker 已关闭且 open obligation 为 0 的 stage production evidence record route，只保留在 full detail / provenance / execution bridge 中，不再占用 App/default caller 的下一步。typed-blocker-closed stage record route 仍可由显式 action execution 接收后续 domain/App/live owner success refs，以便 supersede blocker；默认过滤本身不声明 stage complete、domain ready 或 production ready。selected action 在需要 App/domain/live payload 时直接携带 `opl runtime action execute --payload-file <payload.json>` 参数、copyable command 和 refs-only `payload_workorder`，让 App/default caller 不必跳 full detail 才知道如何提交真实 payload；当没有 open selected route 时，默认面返回 `next_safe_action=null`，由 `evidence_next_steps`、`owner_handoff_packet` 和 `framework readiness.next_safe_actions` 承接 owner refs 回填 guidance。Codex App runtime evidence workorder 直接暴露 `codex-app-runtime-evidence long-soak start|event|finish`、`record --payload-file` 和 `verify --receipt-ref` command set，让默认 App/operator caller 可先创建本地 Codex App runtime observation workorder、追加 Temporal/provider/operator 受控 events，再由 `finish` 物化 hash-bound `temporal_hosted_long_soak_refs` 与支撑用 `provider_state_linkage_refs`、`operator_evidence_refs` record payload，最后走 refs-only ledger intake / verify；safe-action success path 会 fail closed 阻断只有 provider/operator 支撑 refs 的 payload。这些 path 和 command 不授权 long-soak closeout、production ready 或长跑任务驱动权。App release/user-path record route 现在携带 `opl_app_release_user_path_evidence_payload_workorder`，明确同 cohort release package、screenshot、reload prompt user path、provider state linkage、long operator evidence refs path 与 release-owner typed blocker path；该 workorder 也直接暴露 `app-release-evidence long-operator start|event|finish`、`record --payload-file` 和 `verify --receipt-ref` observation command set，让默认 App/operator caller 可先创建本地 observation workorder、追加受控 operator events，再由 `finish` 物化 manifest / payload，最后走 refs-only ledger intake / verify。这些 path 和 command 都不授权 release ready、production ready 或 App release/user path closeout。`opl runtime app-release-evidence long-operator start` 只写本地观察 workorder 和 operator log，`event` 只追加受控 required event，`finish` 只有在观察窗口和 required operator events 满足后才 materialize hash-bound `long_operator_evidence_ref` 和可提交 record payload，仍需随后 `record` / `verify` 进入 refs-only ledger。`owner_handoff_packet` 的 owner entry、owner payload attention group 与 framework `review_owner_handoff_packet` / `review_owner_payload_group_scaleout` next action 也携带 `opl_owner_handoff_payload_workorder`，明确 success refs path、domain-owned typed blocker path、空模板阻断和 false authority flags。该接力面仍只投影 workorder、return-shape guidance 和 boundary，不把 handoff/next action 升级成 domain action、owner receipt、typed blocker、owner-chain closeout 或 readiness verdict。

Provider worker lifecycle repair 和 Provider SLO production proof 现在作为独立 safe-action route 进入 App/operator 默认面。当 canonical provider lifecycle inspection 或 stage workbench 读到 Temporal worker `worker_source_stale` 时，默认 selected action 先指向 `provider-worker:temporal:restart`，通过 `opl runtime action execute` 执行 OPL-managed worker stop/start；当 Temporal service 可达但 worker 未就绪、repair action 为 `start_temporal_worker` 时，默认面会暴露 `provider-worker:temporal:start`，同样通过 safe-action shell 执行 `opl family-runtime worker start --provider temporal`。`provider_slo tick` 在执行 production proof 或 queue admission 前也会自动执行 OPL-owned worker lifecycle repair：worker not-ready 时 start，worker source-stale 时 stop/start，并记录 `temporal_provider_worker_repair_receipt`，让 OPL-managed worker 掉线或 checkout source 更新后不再只停在外部巡检提示；如果 lifecycle mutation guard 判定当前 CLI 是未隔离的 git developer checkout 且目标是默认 shared state root，则 repair 返回 blocked receipt，要求改走 managed runtime/current、显式 `OPL_STATE_DIR` 隔离或显式 developer override。App/operator 也会把同一 guard 投影到 `provider-worker:*` 和受该 worker repair 阻塞的 `provider_slo_cadence_execution` route：二者 `route_status=blocked_by_provider_worker_mutation_guard`、`default_actionable=false`、`can_submit_to_safe_action_shell=false`，`runtime action execute` 对该 route 返回 `execution_status=blocked` 而不执行 proof/start 命令。`framework readiness` 现在同时保留 provider long-window SLO raw open tail 和 guarded/operator-actionable 计数；当 provider SLO route 被 worker mutation guard 阻塞时，raw tail 仍留在 App full production tail ledger，但默认 `open_tail_count`、`operator_actionable_attention_tail_count` 和 payload-free action 会扣除该 guarded tail，读成 blocked refs-only attention。`family-runtime provider repair --provider temporal` 也会在 OPL provider lifecycle 内消费 `start_temporal_worker` / `restart_temporal_worker` repair action并写出 `temporal_worker_repair` receipt，因此 source-stale managed worker 不再只停在 `attention_needed`。Temporal attempt query 已区分 connect/describe 失败和 describe 成功后的 workflow query 不可用，后者投影为 `temporal_stage_attempt_query_unavailable`，避免把 worker/query problem 误报成 Temporal service downtime。`worker stop` 会等待 managed worker 真实退出，必要时在短 grace window 后 force stop，并把 signal actions 与 `force_stopped` / `stop_incomplete` 投到 lifecycle receipt。detached worker start 会携带 `--family-runtime-root`，`worker stop` 会按当前 provider module path 以及同 root-tagged foreground orphan worker查找清理；即使 `temporal-worker.json` 已缺失、只记录单一 stale PID，或此前错误 source 留下同 root worker，也会清理这些仍持有 task queue 的旧进程，并把 `orphan_stopped_pids`、`orphan_stop_incomplete_pids` 与 `orphan_stop_actions` 投到 stop receipt。worker lifecycle 当前后，`provider_slo_cadence_execution` 只在需要重新产生 Temporal provider SLO execution receipt、执行 provider proof repair command 且底层 worker mutation guard 允许时占用默认 next action。若该 route 已读为 `provider_slo_dispatch_status=cadence_current` 或 `blocked_by_provider_worker_mutation_guard`，App/default caller 会把它视为当前 provider proof provenance / blocked provenance，仍保留在 full detail / bridge route 中，但不再抢占下一步，让 domain-dispatch / App/live payload workorder 重新浮出。二者都优先于 generic scheduler install/status/trigger/tick；scheduler install 只保留为 cadence substrate action。Provider worker / SLO route 仍只作用于 OPL provider lifecycle/evidence，不执行 domain action、不写 domain truth、不生成 owner receipt、不声明 domain ready 或 production ready。

Temporal scheduler cadence 现在还有独立的 stale-overlap 释放边界：schedule action 启动的 `SchedulerTickWorkflow` 带 `workflowRunTimeout` / `workflowExecutionTimeout`，短 activity 带 `scheduleToCloseTimeout`。如果 worker 在 workflow task pickup 或 scheduled activity pickup 阶段消失，tick 会在有界时间内失败并释放 `overlap=SKIP` 的 running action，而不是持续阻断后续 scheduler tick。该边界只修复 OPL provider cadence/liveness，不执行 domain action、不重写 queue、不授权 MAS/MAG/RCA truth、quality verdict、artifact gate、paper package 或 production ready。

Temporal worker source currentness 现在按 OPL family-runtime worker 运行代码指纹读取，而不是按整个 repo git HEAD。文档、ledger 或叙述性 contract 提交不会让正在运行的 worker 变成 stale；`src/family-runtime-*` 运行代码和 parts helper 变化仍会触发 fail-closed restart。这一口径避免 MAS/MAG/RCA 长期队列被无关 OPL 文档提交打断，同时继续阻止旧 worker 承载新的 runtime admission、single-flight、queue 或 provider lifecycle 逻辑。

`opl system update` 与 `opl system startup-maintenance` 现在包含 OPL Framework runtime self-update target。只有配置了显式 `OPL_FRAMEWORK_UPDATE_SOURCE`，且当前 CLI target 是非 git 的 managed runtime root 时，该 target 才会把 source checkout 的 tracked framework files 刷新到 `runtime/current/opl`，必要时在 target root 执行 `npm install --include=optional --ignore-scripts=false`，并写 `.opl-framework-source.json` refs-only metadata。开发 checkout 或未配置 source 时只报告 skipped；该路径只维护 OPL Framework runtime source/dependency currentness，不写 MAS/MAG/RCA truth、quality verdict、artifact gate、paper package 或 `current_package`。

Temporal managed worker 现在已进入 production-bundle 启动路径：OPL 在 family-runtime state root 下物化 workflow bundle 和 manifest，managed worker 通过 `workflowBundle.codePath` 启动，并在 worker state / readiness / lifecycle contract 中投影 bundle path、bundle hash 和 bundle source version。StageAttemptWorkflow 的 replay compatibility gate 也使用同一 bundle跑 `Worker.runReplayHistory`，覆盖 completed history 与 signal history，避免 workflow command sequence 改动绕过 replay 检查。Codex activity 现在把 Temporal cancellation signal 传入 Codex runner，workflow input 对超阈值 inline 字符串生成 `payload_ref:sha256:*` 和 `payload_guard`；Codex activity result 写入 workflow history 的部分也压成 refs-only small summary，避免 raw stdout/stderr、large runner event、agent receipt body 或 domain payload body 污染 history。以上只关闭 OPL provider hardening / history hygiene 差距；完整 payload/closeout 仍通过 OPL ledger ref 与 domain/workspace/source owner ref 承载；外部 production Temporal service provisioning、生产 retry tuning 和真实 MAS/MAG/RCA domain soak 仍按 domain owner-chain 和 production evidence gate 单独证明。

DM002 暴露的 silent spin 恢复入口已按 owner split 进入 App/operator 默认面：当同一 attention set 同时包含 Temporal worker repair、blocked transport redrive 和 MAS owner handoff payload record 时，默认 selected action 先选 `provider_worker_start|provider_worker_restart`，再选 `blocked_transport_redrive`，最后才暴露 `domain_dispatch_evidence_receipt_record` 这类需要 MAS/App/live owner refs 的 payload workorder。这个顺序让 operator 先恢复 OPL 可修的 worker/liveness，再 redrive provider transport，最后记录 MAS-owned owner receipt / typed blocker / owner-chain refs；它不执行 MAS domain action、不写 MAS truth、不关闭 publication quality、不刷新 artifact/package authority，也不把 handoff refs record 当成 paper progress。

DM002 暴露的假 live-skip 已收敛到 OPL provider/queue 层：MAS default-executor scheduler / tick / admission 现在会在同 dispatch live-skip 前同步可观测 Temporal terminal state；旧 workflow 已失败或 provider non-complete 时，linked queue task 会先变成 provider blocker，再由 OPL tick 在 retry budget 内自动 redrive 同 source fingerprint 的 provider attempt。若 MAS 已导出更新 source 的同 dispatch handoff，旧 blocked task 保持 audit residue，不再自动重驱动。这只保证 OPL 不因 stale Temporal workflow 永久卡住论文 owner handoff；论文是否更新、AI reviewer verdict、publication gate 和 package freshness 仍以 MAS owner surface 为准。

MAS default-executor Codex stage runner 现在在 Codex live process 未输出最终 typed closeout 且 session recovery 也未恢复 closeout 时，可按当前 dispatch stage packet 的 `study_id`、`action_type` 与 `idempotency_key` 或 `action_fingerprint`，从 MAS domain-owned `default_executor_execution/latest.json` 的 `executions` / `execution_ledger` 恢复 `domain_stage_closeout_packet`；缺少这些 owner-route 身份或身份不匹配时 fail closed。该恢复只把 MAS-owned execution receipt 作为 OPL transport closeout metadata 消费，不生成 MAS owner receipt、不写 MAS truth、不授权论文质量、artifact/package 或 production ready。

Framework readiness 与 App/operator drilldown 的 attention summary 现在同时暴露 `operator_payload_required_attention_*` 与 `operator_payload_free_attention_*` 子计数；`family-runtime evidence-worklist` summary 与顶层 payload 也同步暴露 `open_safe_action_payload_required_item_count`、`open_safe_action_payload_free_item_count` 和 `open_safe_action_payload_requirement_semantics`。`open_safe_action_*` 是 worklist 全量 open safe-action route 的 payload split；`operator_actionable_attention_*` 是 framework readiness 当前纳入默认 attention tail 的 payload split。两者只有在 open safe-action route 集合与 readiness operator-attention 集合相同时才会一一相等；当 worklist 同时含 provider scheduler、install/verify 或其他非 owner-payload attention route 时，readiness 保持自己的 attention 范围，不把这些 route 误读成 owner evidence payload-required workorder。payload-required 子集只表示 route 可通过 safe-action shell 记录真实 App/domain/live refs 或 typed blocker，并不表示 OPL 可以自己生成 owner payload、关闭 owner chain、授权 domain ready 或 claim production ready。`domain_blocked_attention_*` 表示已有 domain-owned typed blocker、route-back blocker 或 refs-only blocked envelope，默认状态会读成 `framework_control_plane_available_with_blocked_refs_only_attention`，不再混同为 operator-actionable workorder。`domain_blocked_unique_typed_blocker_ref_count` 是 evidence envelope 与 next-action ledger 可见 typed blocker refs 的跨来源 union；`next_action_unique_typed_blocker_ref_count` 仍只描述 next-action ledger 自身。`domain_blocked_typed_blocker_group_count` 和 `next_action_typed_blocker_group_count` 只用于把重复出现的 domain-owned typed blocker refs 按 owner / domain / claim scope / ref 去重成 attention group；raw tail / envelope / next-action item counts 继续保留为审计数量，去重字段不关闭 blocker、不生成 receipt、不减少真实生产证据差距。

`family-runtime evidence-worklist` 的 `open_worklist_item_count` 是动态读数，只从 fresh CLI/read-model 读取；当它为 0 时，只表示当前没有 OPL 可执行的 open safe-action route，当它大于 0 时，payload-required 子集仍必须由 domain/App/live owner 提供真实 refs 或 typed blocker。payload-free verify route 只验证先前已记录的 OPL refs-only receipt，不能生成 domain owner receipt、typed blocker、owner-chain ref 或 no-regression ref。summary 与顶层 guard 会继续暴露 `zero_open_worklist_is_completion_claim=false`、`zero_open_worklist_is_domain_ready=false`、`zero_open_worklist_is_production_ready=false` 和 blocked refs-only envelope count，防止把“没有 open worklist”误读成 domain ready、production ready、owner-chain 完成或全局 closeout。该入口现在也透传 App/operator 的 `domain_owner_payload_summary_attention`，并在 summary 暴露 `domain_owner_payload_summary_naming_hygiene_blocker_count`；该字段只让 evidence-worklist、App/operator 与 framework readiness 对 RCA naming hygiene no-resurrection attention 读法一致，不创建 safe-action worklist item，也不关闭 owner payload、visual/export 或 production evidence。

Provider scheduler route 的默认读法已经收窄：`provider-scheduler:temporal:status` 是显式可执行的只读 diagnostic query，可通过 `opl runtime action execute` 查询，但不计入 App/default `next_safe_action`、open evidence-worklist 或 provider scheduler next-action ledger。`provider-scheduler:temporal:install|trigger|tick` 是 provider scheduler mutation；当 provider worker repair 被 developer-checkout shared-state mutation guard 阻塞时，这些 route 继承 `blocked_by_provider_worker_mutation_guard`，并从默认可提交 safe action 中移除。该口径只避免误导 operator，不绕过 worker guard，不安装 scheduler，不关闭 provider SLO 或 production evidence tail。

New Agent / OMA production consumption follow-through 已进入机器读面。`opl_meta_agent_workbench_refs.production_consumption_followthrough` 把 managed install/update、App live path、owner receipt / typed blocker scaleout 和 long-soak 作为 refs-only production-consumption gate 消费；`opl system startup-maintenance`、`opl runtime oma-app-live-path record/list` 和 `opl runtime oma-production-consumption record/verify/list` 只记录、验证或读取 OPL-managed module、App-live、typed blocker、operator evidence 或 long-soak evidence refs，record 命令支持 `--payload <json>` 或 `--payload-file <path>`。`opl runtime oma-production-consumption long-soak start|event|finish` 可生成本地 OMA long-soak observation workorder、追加受控 operator event、物化 manifest、hash-bound `long_soak_ref` 和可提交 record payload；`start` 不写 ledger，`event` 只追加 body-free `event_kind` / `observed_at` / optional `evidence_ref`，`finish` 只有在观察窗口和 required operator events 满足后才 materialize ref，仍需随后 `record` / `verify` 进入 refs-only ledger。当前本机 App drilldown 已观察到 managed install/update、App live path、owner receipt / typed blocker scaleout refs 和 verified long-soak ref；`framework readiness` 读为 OMA `production_consumption_ready=true`、`open_gate_ids=[]`、active `typed_blocker_refs=[]`，历史 typed blocker 只保留为 provenance。Production-consumption ledger 只提供真实 evidence 的 intake / verify surface，recorded 但未 verify 的 receipt 只投影为 pending verify；typed blocker refs 只说明 owner 显式阻塞，operator evidence refs 只保留审计/监控引用。App `evidence_next_steps`、App execution bridge 和 `framework readiness.next_safe_actions` 会把未来 open、pending verify 或 manual-required 的 OMA production-consumption gate 暴露为 refs-only operator follow-through；空模板、占位 ref、record-only receipt、event-only operator log、observation workorder 本身或 OPL 自造 receipt 仍不能关闭任何 gate。该 surface 说明 OMA 已从 structural consumption 进入 production-consumption follow-through 并闭合当前 OMA evidence gate，不生成 owner receipt、typed blocker 或 domain truth，也不声明 MAS、MAG、RCA、family production ready 或未来 agent production ready。

MAS/MAG/RCA domain-dispatch payload 已多轮进入 OPL refs-only external evidence ledger：MAS guarded-apply、owner-route、aftercare、default-executor typed blocker payload 与逐 paper-line `paper_line_owner_chain_results`，MAG grant-stage / lifecycle / legacy route-back payload，以及 RCA controlled-soak / visual-stage typed blocker payload，都已证明可被 OPL safe-action shell、identity preflight 和 refs-only ledger 消费。MAS per-line result 只被归一化为 domain-owned owner receipt refs、owner-chain support refs、no-forbidden-write proof refs 或 stable typed blocker refs；OPL 不读取 body、不生成 owner receipt、不授权 artifact mutation，也不把 per-line result 写成 paper closure；若 payload 显式携带 body 或 paper/publication/artifact readiness claim，domain-dispatch preflight 会 fail closed。默认 action 面只按每个 bound dispatch identity 暴露最新 actionable workorder，较旧 attempt 折叠为 superseded provenance，只在 full detail / provenance 中可见，不作为默认 actionable workorder 或 receipt route；当 live workorder 重新出现时，attention queue 使用 canonical domain owner 指向 domain/App/live payload owner，同时保留 OPL safe-action shell owner 与 false authority flags。domain-dispatch payload preflight 会把 generic `evidence_refs` 作为 supplemental refs 保留计数，但只允许真实 owner receipt、owner-chain、no-regression 或 typed blocker refs 进入可记录路径；因此 evidence-only payload 会 fail closed，继续留在 worklist，直到 domain/App/live owner 给出 closeout refs 或 typed blocker。OPL 只消费 domain owner surface 生成的 refs-only success / typed-blocker payload，不自造 owner payload；具体 attempt、receipt ref、worklist counter、blocked envelope counter 和 same-day foldback 流水只从 ledger、App/operator full detail、`framework readiness` 与 `family-runtime evidence-worklist` fresh 读取，不在 active status 固化。zero open 只表示当前 refs-only worklist 没有可执行 safe-action route；它不能写成 OPL 可自行关闭 owner-chain，也不能写成 global complete、domain ready、production ready、paper closure、artifact authority、expected receipt instance、monitor freshness、App release/user path 或 long-soak evidence 已全部形成。

`opl system semantic-hygiene --json` 已把 `public_surface_budget_conformance` 纳入机器守门面。该 gate 直接读取 `contracts/opl-framework/public-surface-index.json` 中的 active public surfaces，检查每个默认 surface 都有 allowed reason、promotion evidence、至少两个 consumer refs 和 false authority flags，并把 `contracts/opl-framework/surface-budget-policy.json` 作为治理证据投影。该结论只说明 default navigation / App / operator / framework discovery surface 受 Surface Budget 约束，不声明 domain ready、quality verdict、artifact authority、production ready，也不能替代 AI executor planning 或 domain owner。
上述 payload path guidance 已上收进默认接力面。`domain_dispatch_evidence_workorder_group_attention_items` 现在携带 group-level `payload_preflight_policy`、`payload_preflight_blocked_error_kind`、`required_return_shapes`、bounded `sample_record_action_ids` / `sample_record_command_refs`、空 `payload_template`、`payload_ref_hints` 与 `payload_template_policy`；App `evidence_next_steps` 的 `domain_dispatch_evidence_group_workorder`、`app_release_user_path_evidence`、`oma_production_consumption_followthrough`、`owner_handoff_packet` 的 owner entry，以及 `framework readiness.next_safe_actions` 的 `review_owner_handoff_packet` / `review_domain_dispatch_group_workorder` / `review_oma_production_consumption_followthrough` 会直接透传 accepted payload paths、preflight gate、return-shape guidance 和 safe-action shell record hints。`family-runtime evidence-worklist --detail full` 的 open domain-dispatch `worklist_items` 也按 `action_id` 从同一 full App/operator route 继承 `payload_workorder`、accepted payload paths、identity binding guidance、copyable record commands 和 false authority flags；默认 summary `next_safe_actions` 同步透传这些 payload handoff 字段，避免 default caller 只在 packet/group/full detail 面能看到 handoff，而单项 route 或默认 next action 仍显示 payload workorder 缺失。`app_release_user_path_evidence` 使用专用 `opl_app_release_user_path_evidence_payload_workorder`，把真实 App release/user-path refs path 与 typed blocker path 写成机器字段，而不是只暴露 generic selected-action workorder。owner payload group 与 owner handoff review 现在使用同一个 `opl_owner_handoff_payload_workorder` 边界包，让默认 caller 能在 owner 级别看到 required refs、accepted paths、payload template、empty-template policy 和不能写 owner receipt / typed blocker / owner-chain / readiness 的 authority flags。默认 App/operator 的 selected `next_safe_action` 也会在 route 需要 App/domain/live payload 时带出 `--payload-file <payload.json>`、`copyable_runtime_action_execute_commands.record_with_payload` 和 refs-only `payload_workorder`，让 operator 可以直接替换空模板为真实 refs 或 typed blocker。默认 App/default caller 因此可以先判断应回填 success refs path 还是 typed blocker path，并直接拿到 bounded `opl runtime action execute` record command 样例；需要完整 refs 时再跳 full detail。该接力面仍只投影 workorder、空模板、样例命令和 boundary，不把 handoff/next action 升级成 domain action、owner receipt 或 readiness verdict。

RCA 的 `operator_evidence_readiness_projection` 也进入 App/operator full drilldown 的 `domain_owner_payload_summary_refs`。该 read model 只保留 domain-owned `owner_payload_item_summary` 与 `stage_expected_receipt_payload_summary` 的 refs-only payload 模板、success refs path、typed blocker path、required return shapes 和 false authority flags，让 OPL/App/operator 能看到 RCA production evidence tail 的逐 work item / 逐 stage 输入面。默认 summary 给出 `domain_owner_payload_summary_*` 计数和 ledger / record / verify route 计数，完整 payload 需要 `--detail full`；full detail 的 `operator_action_routing_refs` 会为未记录的 owner payload / stage expected receipt target 暴露 `domain_owner_payload_summary_receipt_record` safe-action route，为 recorded receipt 暴露 verify route，verified target 不再占用 route。Stage production evidence record route 和 `stage_evidence_workorder_packet` 现在也会把同 domain/stage 的 `stage_expected_receipt_payload_summary` 投影为 `domain_owner_payload_candidate_refs`，给 operator 显示可拷贝的 success refs path、typed blocker path、已覆盖 refs 与仍缺 refs；candidate 只是定位和回填辅助，不参与 preflight 覆盖计算，不自动 record / verify，不关闭 stage evidence route，也不生成 owner receipt、monitor freshness 或 typed blocker。对应 direct CLI 是 `opl runtime domain-owner-payload-summary record --target-identity <json> --payload-file <payload.json>`、`verify [--receipt-ref <ref>]` 和 `list`。该 ledger 只接受 body-free success refs path 或 domain-owned typed blocker path，`receipt_ref` 只表示 OPL ledger receipt id，不作为 domain/stage receipt evidence；同一 target 当前 receipt 唯一，重录会替换当前 target receipt。payload preflight 会 fail closed 阻断空模板、占位 ref、success refs 与 typed blocker refs 混用、body 字段和 readiness / artifact authority claim。App `attention_first_payload.evidence_after_contract.domain_owner_payload_summary_attention`、`framework readiness` 的 `domain_owner_payload_summary_attention` 和 `family-runtime evidence-worklist` 的顶层 `domain_owner_payload_summary_attention` 仍只投影 bounded domain / work-item / stage attention、naming-hygiene blocker count 和 full-detail 指针；ledger record/verify 不读取 RCA visual truth / artifact body / memory body，不生成 RCA owner receipt 或 typed blocker，不关闭 owner chain，也不声明 RCA visual ready、exportable、domain ready、production ready 或 Temporal long-soak 完成。

MAS 的 `real_paper_autonomy_guarded_apply_proof.paper_line_provider_canary_closeout` 现在也可进入同一 `domain_owner_payload_summary_refs`，但只在 domain manifest 明确暴露 `paper_line_owner_payload_summary` 与 `paper_line_domain_dispatch_evidence_record_payloads` 时消费。若 MAS closeout 同时暴露 `stage_expected_receipt_payload_summary`，App/operator 会把其中的 stage expected receipt / monitor freshness / typed-blocker guidance 规范化成 `stage_expected_receipt` target，并生成同一个 refs-only `domain_owner_payload_summary_receipt_record` route；当前该路径只消费 MAS 已观测 payload 支撑的 `finalize_and_publication_handoff`，不会从 contract-only surface 合成未观测 stage。当前本机 OPL `domain-owner-payload-summary` ledger 已按 MAS recommended typed-blocker path 记录并验证该 `finalize_and_publication_handoff` stage receipt：typed blocker refs 为 `004-dpcc-longitudinal-care-inertia-intensification-gap`、`idea-idea-3839d99b` 和 `paper-run-dfcc79d2` 的 MAS owner apply receipt missing refs。独立的 `stage-replay-missing-receipt` ledger 另按 MAS success refs path 验证了同一 stage 的 `owner_receipt:finalize_and_publication_handoff` replay target，receipt ref 为 `opl://stage-replay-missing-receipt/med-autoscience%2Ffinalize_and_publication_handoff%2Fowner_receipt%3Afinalize_and_publication_handoff`；该 receipt 只关闭对应 replay attention，不改变 domain-owner-payload-summary 的 typed-blocker guidance。`provider_guarded_soak_read_model.paper_line_guarded_apply_evidence` 仍只是 contract/discovery surface，不能被当成 owner-chain evidence。App/operator 与 `framework readiness` 展示 MAS paper-line success refs path、typed blocker path、stage expected receipt refs path、required return shapes、bounded domain/work-item/stage 计数、refs-only record/verify route 计数和 false authority flags；record route 只能接收 domain-owned owner receipt / no-regression / owner-chain refs、stage receipt / monitor freshness refs 或 typed blocker refs，verify route 只验证已记录的 OPL refs-only receipt。不读取 paper body，不生成 MAS owner receipt 或 typed blocker，不关闭 paper/publication/artifact authority，也不声明 MAS domain ready、production ready 或 paper closure。

MAG 的 `manifest_consumer_evidence.sustained_consumption_followthrough_workorder` 现在进入 App/operator full drilldown 的 `mag_manifest_sustained_consumption_followthrough_refs`。该 read model 只把 MAG owner manifest 中的 sustained-consumption followthrough workorder 投影成 OPL refs-only action route：未记录时暴露 `mag_manifest_sustained_consumption_followthrough_receipt_record`，record 后暴露 verify route，direct CLI 是 `opl runtime mag-manifest-sustained-consumption record --target-identity <json> --payload-file <payload.json>`、`verify [--receipt-ref <ref>]` 和 `list`。record payload 必须选择完整 success refs path（`app_operator_consumption_ref`、`default_caller_consumption_ref`、`owner_payload_response_ref`、`workspace_receipt_scaleout_evidence_ref`、`no_forbidden_write_ref`、`long_soak_or_typed_blocker_ref`）或 `typed_blocker_refs` path；空模板、占位 ref、success refs 与 typed blocker 混用、未知顶层 payload 字段、body 字段和 sustained-consumption / grant / quality / export / submission / provider-long-soak readiness claim 都 fail closed。MAG `manifest_sustained_consumption_payload_response.opl_runtime_action_execute_payload` 已收窄为 OPL 可直接消费的 evidence refs payload：身份保留在 action route target identity，success path 不再混入顶层 `typed_blocker_refs`。当前本机 `opl runtime mag-manifest-sustained-consumption list --json` 读为 `receipt_count=1`、`verified_receipt_count=1`，verified receipt ref 为 `opl://mag-manifest-sustained-consumption/medautogrant%2Fmag_manifest_sustained_consumption_followthrough_workorder%2Fmanifest_sustained_consumption_followthrough`，payload path 为 `sustained_consumption_refs_path`，其中 `long_soak_or_typed_blocker_refs` 仍指向 MAG provider long-soak window typed blocker。App/operator summary 读为 `ledger_receipt_ref_count=1`、`verified_ledger_receipt_ref_count=1`、`pending_verify_receipt_ref_count=0`，且 sustained-consumption / grant / submission / provider-long-soak claim counts 均为 0。该 surface 只记录并验证 App/default caller refs-only payload，不提交 MAG operator payload、不创建 MAG owner receipt或 typed blocker、不读取 grant truth / memory body / artifact body，也不声明 MAG grant ready、fundability ready、submission ready、provider long-soak complete、App sustained consumption complete、production ready 或 global closeout。

`opl framework readiness --family-defaults --json` 的默认 `attention_first_payload` 带有 bounded `semantic_hygiene_contract_floor`，从 `opl system semantic-hygiene --json` 透传 gate count、guarded count、gate id 与 `functional_privatization_evidence_gate` 状态。该投影只给 App/default caller 展示当前 contract-floor 已覆盖 functional privatization evidence gate、Surface Budget、evidence envelope single semantics 等守门面，不新增 warning/action，不声明 domain ready、production ready、artifact authority，也不替代 AI executor planning 或 domain owner。

App/operator 与 `framework readiness` 的默认 `attention_first_payload.memory_artifact_lifecycle_evidence` 现在把 memory refs、memory writeback receipt refs、domain-dispatch memory writeback refs、artifact refs、package/export refs、lifecycle index refs、restore proof refs、domain artifact mutation receipt refs，以及 resolved domain 的 standalone verified memory/artifact/lifecycle external receipt refs 统一投影为 refs-only 计数，并保留 full detail sections 指向 `memory_writeback_refs`、`artifact_gallery_refs`、`package_export_lifecycle_refs`、`domain_dispatch_evidence`、`lifecycle_ledger_refs` 和 `domain_evidence_request_refs.external_receipts`。该字段只改善 Codex/default caller 对已观察 memory/artifact/lifecycle evidence 的可见性；OPL 仍不能读 memory body 或 artifact body，不能 accept/reject memory writeback，不能 mutate artifact body，不能授权 package/export readiness，也不能执行 domain physical delete。计数为 observed 只说明相关 refs 已进入读面，不说明 MAS/MAG/RCA 的真实 memory/artifact/lifecycle receipt scaleout、owner-chain、quality verdict、artifact authority 或 long-soak 已完成。

RCA controlled visual-stage 的 refs-only ledger 过程也归入同一过程归档；当前 status 只保留结论：6 条 RCA visual delivery stage attempt 已通过 RCA-owned typed blocker path record / verify，preflight 为 `ready_to_record`，identity binding 为 `matched`，并把 domain-dispatch workorder accounting 清零。RCA production visual-stage long soak、visual ready、exportable、handoffable、真实 memory/lifecycle receipt scaleout、artifact-producing owner receipt 与 App release/user path 仍是 open evidence gate；OPL 不执行 RCA visual stages、不写 RCA truth / artifact / memory、不生成 RCA owner receipt。

OPL App/operator drilldown 已开始 refs-only 消费 `opl-meta-agent` patch-loop closeout：`runtime app-operator-drilldown --detail full --json` 的 OMA sections 可投影 `blocked_suite_result_ref`、`developer_patch_work_order_ref`、`patch_traceability_matrix_ref`、`failure_evidence_refs`、`root_cause_refs`、`targeted_fix_refs`、`predicted_impact_refs`、`next_run_falsification_refs`、`target_repo_verification_refs`、`target_runtime_read_model_consumption_ref`、`workspace_environment_proof_ref`、`no_forbidden_write_proof_ref`、`target_owner_receipt_or_typed_blocker_ref`、`patch_absorption_ref`、`worktree_cleanup_ref` 和 `agent_lab_re_evaluation_ref`。App/operator 还把这些 refs 规整成 `self_evolution_cockpit`，让 operator 可直接按六个问题读取：证据是什么、根因是什么、改什么、预期改善什么、下一轮如何证伪、谁签收或谁阻塞。这只是 App/workbench 对 OMA work order 与 target owner closeout refs 的展示/审计入口，不写目标 truth、不读取 artifact 或 memory body、不签 owner receipt，也不把 patch-loop projection 写成 domain ready、quality verdict、artifact readiness 或 default promotion。

`opl work-order execute` 现在支持可选 `target_owner_closeout_hook`。OPL 仍持有 target worktree lifecycle、Codex CLI dispatch、target verification、absorb 和 cleanup，但 source patch 吸收后会调用目标仓声明的 domain-owner closeout command，由目标仓返回 refs-only `domain_receipt`、`typed_blocker` 或 `no_regression_evidence`。OPL 只把该返回值嵌入 execution receipt 的 `target_owner_receipt_or_typed_blocker`；缺 hook、hook 失败或返回非 refs-only owner shape 时继续记录 typed blocker。旧 `opl agent-lab execute-work-order` alias 已退役，Agent Lab 只消费 canonical primitive 的 receipt/ref，不保留兼容入口。该 hook 不授予 OPL owner receipt authority，不允许写 domain truth、artifact body、memory body、quality/export verdict，也不改变 MAS/MAG/RCA owner 边界。

`opl agents scaffold --validate <repo>` 与 `opl agents conformance --family-defaults --json` 负责标准 OPL Agent 的结构 gate：`agent/` 必须持有真实 prompt/stage/skill/knowledge/quality gate 语义文件，generated surface owner、private generic-owner guard、physical morphology policy 和 active path scan 必须可被 OPL 审计。`opl agents platform-surfaces --family-defaults --json` 是 OPL-owned generic platform surface classifier：它把 generated CLI/MCP/Skill/product shell、sidecar dispatch、action metadata / command registration shell、status/workbench、workspace/source/artifact/memory locator、stage attempt queue/retry-dead-letter 和 generic transition runner 统一归 OPL Framework，同时把 domain truth、quality/export/publication/visual verdict、artifact body/mutation authority、source readiness、memory accept/reject、owner receipt、typed blocker、domain action ids / handler refs 和 domain rubric 留给 MAS/MAG/RCA/OMA。`opl agents interfaces --family-defaults --json` 是 family-wide generated interface drilldown；`--domain` 与 `--repo-dir` 继续保留单仓诊断形状。该 classifier 与 generated interface report 只阻断或定位 domain repo 明确声明 generic platform owner、generated surface 缺口或接口生成缺口，不生成 production/default caller、domain ready、quality verdict 或 artifact authority。Conformance / readiness 的动态计数不在本文冻结；当前 truth 入口是 `opl framework readiness --family-defaults --json`，专题 drilldown 继续使用 `opl agents readiness --family-defaults --json`、`opl agents platform-surfaces --family-defaults --json`、`opl agents interfaces --family-defaults --json`、`opl agents pack-compiler --json`、`opl stages readiness --family-defaults --json`、按需 `opl stages readiness --domain <domain> --json` 和 App/operator drilldown。

New Agent 模板消费 lane 已进入 repeat structural consumption evidence，并已有 durable refs-only ledger：`opl agents scaffold --consumption-evidence` 会对默认样本执行 generate -> scaffold validation -> standard conformance -> agent readiness -> App/operator projection -> cleanup，并在 App/operator summary 暴露每个样本消费的 surface 数量、readiness surface 与 App/operator surface。2026-05-26 fresh proof 对 `award-foundry`、`thesis-foundry`、`review-foundry` 三个临时样本均读为 `passed`：每个样本写入 30 个模板文件、消费 5 个 pack path、观察 1 个默认 `codex_cli` executor binding、解析 1 个 quality gate ref，并穿过 scaffold validation、standard conformance、agent readiness 和 App/operator projection 四类 surface，临时目录按 `ephemeral_removed_after_validation` 清理。生成模板本身已包含可被 conformance/readiness 消费的 action catalog、generated-surface handoff targets、functional audit modules 和 physical source morphology policy；不再依赖测试 fixture 后处理才能通过 structural consumption。`opl runtime standard-agent-template-consumption record|verify|list` 现在可以把该 replayable evidence ref、fingerprint、cohort ref、sample refs 与 consumed surface refs 写入并验证 body-free OPL ledger；App/operator summary 和 full drilldown 会投影 recorded / verified / pending verify receipt 计数。2026-05-28 当前本机 ledger 已有 3 条 verified receipt：`opl://standard-agent-template-consumption-ledger/default-state-2026-05-28T08-20-35-619Z`、`opl://standard-agent-template-consumption-ledger/repeat-replay-2026-05-28T13-00Z` 和 `opl://standard-agent-template-consumption-ledger/repeat-replay-2026-05-28T21-02Z`；fresh App/operator summary 与 full drilldown 读为 ledger receipt `3`、verified receipt `3`、pending verify `0`，且 `can_claim_domain_ready=false`、`can_claim_artifact_authority=false`、`can_claim_production_ready=false`。本轮 repeat receipt 复用了同一 deterministic replay evidence fingerprint，证明标准 scaffold consumption 可重复复验并形成独立 durable receipt；它不是新 domain 内容差异、App live rendering、managed install/update、owner receipt、domain ready、artifact authority 或 production ready。

`opl agents conformance` 的实现边界已经收薄：public conformance builder 保持为 scaffold、pack compiler、generated handoff、private audit、platform surface、physical morphology、workspace lifecycle 与 evidence tail 的薄聚合器；physical morphology policy、active path scan、legacy residue token 和 allowed tombstone/provenance 分类迁入 scoped module，并移除主文件的 line-budget baseline。这个改动只降低 OPL 自身 surface 混杂风险，不改变 conformance 的只读权威边界，也不声明 MAS/MAG/RCA/OMA production ready。

OPL shared `product-entry-companions` 提供 `buildOplProductEntryLifecycleAdapterSurface` primitive，用于生成 refs-only `opl_family_lifecycle_adapter`：session shell、stage attempt、attempt ledger、owner route discovery、adoption cursor 和 lifecycle projection 的 generic owner 固定在 OPL / one-person-lab；domain truth、review/publication projection、canonical artifact、concrete executor 与 artifact/memory body 仍显式不由 OPL 持有。该 primitive 是 RCA/MAG/MAS 退役私有 product-entry/session lifecycle adapter 的替代能力来源；它本身不代表任一 domain repo 已完成 shared package pin 更新、generated/hosted default caller 切换、production owner-chain evidence 或 domain ready。

RCA physical morphology 的 legacy 名称口径已经按退役原则收口：`legacy_managed_runtime_gateway_names` 这类旧 managed/runtime/gateway surface id 必须通过 RCA `legacy_name_policy.forbidden_active_surface_ids` 和关闭 compatibility / generic owner flags 来证明退役，不能再要求它出现在 active surface classification 中。Conformance 只把它当负向 guard / tombstone proof 审计；这关闭的是结构 conformance blocker，不声明 RCA visual ready、artifact-producing owner receipt、long soak 或 App release/user path 完成。

## 当前差距

物理源码形态标准化现在作为独立治理口径读取：标准 OPL Agent 不只要 descriptor / generated interface ready，还要让 `agent/`、`contracts/`、`runtime/authority_functions`、`src` / `packages` 和 `docs` 的长期职责一眼清楚。Fresh `opl agents conformance --family-defaults --json` 读为 4 个 repo passed、0 blocked、structural conformance passed；fresh `opl agents default-callers --family-defaults --json` 读为 32 个 generated/default caller surfaces、0 blocked surfaces、0 missing owner/typed-blocker、0 missing no-forbidden-write、0 missing tombstone/provenance refs。`domain_handler` 是 default-caller gate 名；其 canonical target set 只允许显式 domain handler target 机器面（`domain_handler`、`domain_action_adapter_export_dispatch`、`domain_action_adapter`），不能用 generic wrapper、compat facade 或 repo-local default caller 补证据。App/operator 的 default-caller deletion 投影在 active binding 有 repo path 时优先消费 repo-native `agents default-callers` readiness；manifest projection 只作为无 repo contract 的 fixture / diagnostic fallback，避免 App/read-model 重新生成第二套删除证据语义。MAS wrapper tail 已收成 former tail / deletion-gate provenance，剩余是 live provider paper-line、memory/artifact/lifecycle receipt 和 provider SLO evidence gates。MAG 当前顶层合同读为 `standard_agent_source_shape_status=landed`、`claims_opl_replacement_exists=true`、`claims_domain_repo_physical_delete_authorized=false`；`attempt_ledger` 等旧词只允许作为合同 policy/provenance/negative guard 中的非 owner claim 出现，不是 active forbidden residue。RCA 当前 bridge gate 读为 `source_shape_status=landed`、`functional_structure_gap_count=0`、`remaining_bridge_module_ids=[]`、`declares_no_active_bridge_modules=true`。OPL Meta Agent 读为 `source_shape=landed`、`functional_structure_gap_count=0`、`domain_repo_retained_generic_surface_count=0`。这个口径不引入 OpenAI Agents SDK、LangGraph、AutoGen、CrewAI 等作为 OPL runtime dependency，也不把 structural conformance、default-caller readiness 或 zero open deletion requirements 写成 physical delete authorization、domain ready 或 production ready。

One Person Lab App 的产品运行路径默认以 OPL-managed environment 为真相：managed modules、OPL-generated Codex plugin metadata、managed companion skills、runtime tools 与 provider state 是普通用户入口的默认依赖。developer checkout 只作为显式开发/调试 override 进入运行路径；没有显式 override 时，workspace 源仓的 dirty、ahead 或实验分支不能定义 App 当前运行依赖。`opl system startup-maintenance` 提供 App startup maintenance action，维护 clean OPL-managed ecosystem modules，并把 dirty、ahead、diverged、no-upstream、env override、sibling workspace、invalid checkout 或 timeout 投影为 `manual_required` / typed blocker。OPL Meta Agent 的 fallback health check 使用 OMA 仓声明的 `scripts/verify.sh smoke`，不要求该仓提供 OPL 自身 `fast` lane alias。AionUI first-launch / app-version maintenance 走该 action；它不写 domain truth、memory body、artifact body、quality/export verdict，也不安装 domain daemon。OPL Meta Agent 在这里是用于创建和检查 OPL-compatible agents 的 managed ecosystem module；其 Codex 可见入口由 OPL 从 OMA contract pack 生成 `opl-meta-agent-local` Codex plugin，而不是把 OMA 作为 standalone companion skill 同步进 `~/.codex/skills`。这不改变 MAS/MAG/RCA truth ownership。

App release / user-path 证据现在有 refs-only ledger 和 CLI intake，记录 App/live/release owner 回填的 release package、screenshot、reload prompt user path、provider state linkage、long operator evidence 或 typed blocker refs，并要求这些 refs 落在同一个 release/user-path cohort；跨版本 refs 默认投影为 `cohort_ambiguous`，不会拼接关闭 gate。若多个候选 cohort 并存，read model 只会在最新候选 cohort 自身完整覆盖五类 gate 时选择它；如果更新候选只提供部分 refs，旧完整 cohort 不能遮蔽新 cohort 的缺口。该入口只写 OPL refs-only ledger，并保持 `can_claim_release_ready=false`、`can_claim_production_ready=false`、`can_close_app_release_user_path=false`；CLI roundtrip、ledger receipt observed / verified、selected cohort 或 typed blocker recorded 都不能替代同一 cohort 的真实发布包、截图、reload prompt 用户路径、provider state 联动或 long operator evidence。

App release long-operator observation 现在有单独 CLI：`opl runtime app-release-evidence long-operator start --cohort <version> --minimum-duration-minutes <n> --evidence-dir <path>` 只生成本地观察 workorder 和 operator log，`event --workorder-file <path> --event-kind <kind> [--evidence-ref <ref>]` 只追加受控 required event，`finish --workorder-file <path>` 在最小时长与必要 operator events 满足后只物化可提交的 `long_operator_evidence_refs` payload 文件。该路径不自动写 ledger、不生成 release artifact、不截屏、不声明 release ready 或 production ready；operator 仍需把 payload 经 `record` 写入 refs-only ledger，并经 `verify` 后才可能被 App release/user-path read model 计入 observed refs。

OPL Developer Mode 已有系统配置和运行态 projection：`developer_mode` 会检测 GitHub identity、目标 repo permission，并计算 direct repo fix、fork PR、mixed direct/PR、observe-only 或 fail-closed route。App 设置页暴露 Developer Mode 开关；Agent Lab 负责把问题归因、evidence、owner route、risk tier、independent reviewer refs、candidate fix、version ledger、canary / rollback 和 PR refs 组织成可审计控制面。当前 live projection 已在真实 `gh` 权限查询下恢复 `direct_repo_fix` 路由，5 个目标 repo 均可判定 direct write ready；`opl runtime developer-mode-closeout record|verify|list` 现在可记录、验证和列出 body-free Developer Mode live repair closeout refs，Agent Lab 只消费 verified ledger receipt 来投影 live closeout route，recorded receipt 继续显示 verify-pending。`opl app state --profile fast|full` 的 `developer_mode.live_closeout_evidence` 现在消费同一 Agent Lab closeout read model 的 bounded summary，让 App/default caller 可见 ledger status、verified/pending counter、live owner-acceptance missing counter、fixture negative-guard counter 和 refs-only authority boundary，但不投影 drill body、不写新 receipt；App/operator drilldown 和 `framework readiness` 默认 attention payload 也会投影 `developer_mode_live_closeout_evidence`，显示 missing direct-fix / fork-PR live ledger route、pending verify receipt、required external owner acceptance refs 和 full-detail drilldown section。Fork/PR ledger intake 和 App/operator workorder 现在要求 fork repo、pull request、owner acceptance 三者都绑定可解析 GitHub refs，并且 owner acceptance 必须是 `github-pr-owner-acceptance-ref:` typed ref；普通 PR URL 或 `github-pr-ref:` 只能证明 PR 存在，不能证明 owner acceptance。当前本机 ledger 已验证 4 条 direct-fix receipts 和 1 条 RCA cross-repository fork-PR receipt：`gflab/redcube-ai` fork -> `gaofeng21cn/redcube-ai` PR #2，head commit `56ea90fb204e1beef9e144c24e78007373121ebd`，CI 与 Sentrux Advisory success，PR 于 `2026-05-28T11:53:22Z` merge。Fresh framework readiness 读为 Developer Mode `closeout_refs_ready`、verified fork-PR count 1、missing route count 0、route repetition ref count 1、risk-tier auto-promotion ref count 1、App patrol mount ref count 1、`scaleout_followthrough_open_gate_count=0`。Risk-tier auto-promotion follow-through 已收成 first-class Agent Lab ledger：operator 先用 `opl agent-lab risk-tier-promotion record|verify` 记录带真实 failure/evidence delta、独立 AI review、rollback target、canary observation、no-forbidden-write 和 verification refs 的 receipt，Developer Mode ledger 只接受已 verified 的 `risk_tier_auto_promotion_refs` receipt ref；recorded/unverified receipt、fixture、scorecard pass、provider completion、单独 rollback/canary ref 或占位字符串都会 fail closed。当前 verified Agent Lab receipt 为 `agent-lab-risk-tier-auto-promotion-ref:one-person-lab/developer-mode-risk-tier-verified-ledger-gate-20260529`，对应 Developer Mode closeout receipt 为 `opl://developer-mode-closeout/one-person-lab/risk-tier-auto-promotion-2026-05-29`。该 ledger、App state summary、App/operator projection 和 framework readiness 都只是 refs-only operator guidance，不等同 owner receipt 本身，也不写 domain truth、managed runtime truth、memory body、artifact body 或 quality/export verdict；scaleout follow-through verified 也不声明 App release ready、domain ready、production ready 或 global goal complete。

当前 runtime readiness 真相以统一 lifecycle-aware provider payload 为准，并由 `opl family-runtime status`、`opl status runtime`、`opl runtime manager` 和 App provider projection 同源派生。`local_sqlite` 只允许作为 dev/CI/offline diagnostic path，不能替代 production provider 或 domain daemon；MAS 旧 local / Hermes scheduler 只能作为 cleanup diagnostic / tombstone 语境读取。

2026-05-27 fresh DM002 巡检暴露出 family-runtime status 读 queue SQLite 时会遇到短时 `database is locked`，导致外环把正常并发写锁误读成 runtime unhealthy 或需要人工恢复。当前修复已把 queue、lifecycle index、operator tray、production closeout 和 domain memory 的 SQLite 打开路径统一到 `family-runtime-sqlite`，所有连接设置 bounded busy timeout；后续 provider/read-model 巡检应等待短写锁释放，再按真实 queue/attempt/owner receipt 状态判断是否需要 redrive。

OPL-owned refs-only external evidence ledger 可记录或验证 domain evidence request、evidence gate、stage production evidence request、domain-dispatch evidence 和 resolved-domain standalone memory/artifact/lifecycle external evidence receipt。`runtime action execute` 只能通过对应 safe action route 记录或验证 refs-only ledger；record payload 必须由 domain repo、App 或 live operator 提供真实 refs，verify 只验证既有 OPL receipt。Standalone memory/artifact/lifecycle receipt 只进入 App/operator read model、evidence envelope 和 worklist closed-receipt projection，不新增 open request、record/verify action route，也不关闭 domain-declared request/gate；专用 `domain_dispatch:*` 与 `stage_production_evidence:*` receipt 不在此处重复计数。App/operator 和 framework readiness 默认 summary-first；完整 attempt / stage evidence / route / envelope 只在 `--detail full` 展开，并持续声明 provider completion、projection、verified receipt 或 gate receipt都不能授权 domain ready、owner receipt、quality verdict、artifact authority、App release ready 或 production ready。

`opl framework readiness --family-defaults --json` 现在在同一聚合内复用一份 5s bounded domain manifest catalog，并把该 catalog 传给 pack compiler、stage readiness 与 full App/operator snapshot。`runtime app-operator-drilldown --detail full --json` 也使用同一 5s live manifest refresh policy；full detail 只展开 refs/routes，不放宽 manifest command timeout。Domain manifest command 的 managed shell env 按 bound workspace 隔离 `OPL_DOMAIN_COMMAND_TMP_ROOT`、uv project env、pycache 与 cache root，避免 MAS/MAG/RCA 或同仓不同 worktree 共用同一个 uv venv 并互相删除/重建。当前 MAS main 的 import-light manifest discovery 在预热 managed env 下 5s live refresh 可 resolved 且 `domain_manifest_projection_cache_used_count=0`、`domain_manifest_live_failed_project_ids=[]`；首次 clean-runner uv project venv 建立和依赖下载仍属于 startup-maintenance / managed environment 预热尾项。若 live domain manifest 仍超时，read model 只能使用 projection cache 并暴露 `domain_manifest_projection_cache_used_count`、`domain_manifest_live_failed_project_ids` 和 timeout 诊断字段；cache fallback 仍是 non-authoritative operator visibility，不授权 domain ready、production ready、artifact authority、owner receipt 或 App release ready。

External evidence receipt projection 现在按 domain-owned closure ledger 的显式语义分类，防止仅凭 OPL ledger 字段名制造多重语义污染。若 domain workspace 中对应 closure object 声明 `closure_state=closed_by_verified_external_receipt_ref`，且没有 `typed_blocker_ref` / `typed_blocker_refs` / `domain_owned_typed_blocker_refs`，OPL 会把历史上误写进 `typed_blocker_refs` 的 closure ref 投影为 `domain_owned_receipt_refs`；只有没有这种 domain closure 证明、或 receipt 显式声明 typed blocker semantics 时，才投影为 `domain_owned_typed_blocker_refs`。该规则只修正 refs-only read-model 分类，不修改 domain truth，不签 owner receipt，也不把 external evidence closure 写成 domain ready、fundability ready、artifact authority 或 production ready。

Stage production evidence、domain-dispatch evidence、evidence envelope 和 owner handoff 现在按 canonical owner / payload kind / stage 聚合，并把 expected receipt、monitor freshness、source scope、runtime event、owner-chain、typed blocker、no-regression、memory writeback 和 App/live evidence 作为 refs-only workorder 或 attention item 投影给 operator。该投影只减少默认面噪声和多 owner 语义漂移，不创建 safe action 之外的新 authority，不伪造 owner receipt，不执行 domain action，也不关闭 domain ready。具体 route、receipt、计数和 payload 字段只从 `opl framework readiness --family-defaults --json`、`opl runtime app-operator-drilldown --json`、`opl runtime app-operator-drilldown --detail full --json` 与 `opl family-runtime evidence-worklist --detail full --json` 读取。

Stage replay missing receipt ledger 已开始消费真实 domain-owned replay answer，而不是只展示 handoff template。2026-05-31 fresh 本机 OPL `opl runtime stage-replay-missing-receipt` ledger 已有 15 条 verified receipts：1 条 MAS `success_refs_path` 和 14 条 `typed_blocker_path`，其中 typed blocker 分布为 MAG 1 条、MAS 8 条、RCA 4 条、OMA 1 条；MAS 3 条 `human_gate:study_user_decision_gate` target 由 `contracts/production_acceptance/mas-production-acceptance.json#/stage_replay_human_gate_blocker_summary` 的 MAS-owned typed blocker refs 覆盖，OMA `human_gate:oma_baseline_owner_review` 由 OMA production acceptance 的 domain-owned typed blocker ref 覆盖。MAS `finalize_and_publication_handoff` 的 `owner_receipt:finalize_and_publication_handoff` target 已由 success refs path 验证并从 replay attention 中移除；fresh evidence-worklist / framework readiness 仍保留 14 个 stage replay missing receipt workorder、14 个 missing receipt refs 和 9 个 human-gate refs，当前 `open_worklist_item_count=0`、`open_safe_action_item_count=0`，但 `zero_open_worklist_is_completion_claim=false`、`zero_open_worklist_is_domain_ready=false`、`zero_open_worklist_is_production_ready=false` 且 blocked refs-only attention 仍存在。Compact attention 仍有 `stage_replay_missing_receipt_workorder_attention_summary`，并通过 full packet 暴露完整 workorder、target identity、copyable direct ledger record/verify/list handoff 与 success / typed-blocker payload template。Verified success refs 只抑制精确覆盖的 replay target；verified typed blocker 只表示 domain-owned answer 已被记录和验证，workorder 仍按 blocked attention 保留。该面不是 human gate approval、MAS paper closure、grant ready、submission/export ready、RCA visual ready、exportable、handoffable、Temporal visual-stage long soak、OMA production ready、domain ready 或 production ready。

MAS guarded-apply owner summary 现在给 6 个 runtime-guard stage 提供 refs-only candidate guidance：`direction_and_route_selection`、`baseline_and_evidence_setup`、`bounded_analysis_campaign`、`manuscript_authoring`、`review_and_quality_gate` 与 `finalize_and_publication_handoff`。当前 App/operator full drilldown 只暴露前 4 个 MAS stage-production safe-action routes，且每个 route 都能看到 MAS-owned `typed_blocker_path` candidate；未出现在当前 route list 的 stage 仍以 MAS contract / owner summary 为准。该 guidance 只关闭 App/operator 候选 payload 可见性缺口，不生成 expected receipt instance、monitor freshness receipt、MAS owner receipt、paper closure、domain ready 或 production ready。

Legacy cleanup evidence 以 `opl agents legacy-cleanup apply --domain <domain> --mode dry-run|apply|verify` 和 `opl runtime app-operator-drilldown --json` 为入口读取。App/operator cleanup plan 把 `agent_id` 与 `command_domain_id` 分开，并把 ready plan 派生为 `legacy_cleanup_apply` 与 `legacy_cleanup_verify` OPL safe action route；`runtime action execute` 可用这些 route 写入或验证 OPL cleanup ledger。当前 OPL normalizer 会保留 MAS `functional_consumer_boundary`，并允许 `source_provenance` 的 refs-only `{ref}` / `{command}` 简写通过字段名推导 surface kind；MAS current standard-agent evidence 因此能关闭 no-active-caller、replacement parity、provenance 和 history/tombstone checklist，fresh App/operator drilldown 读为三仓 cleanup plan ready、blocked plan count 为 0、domain physical delete can execute 为 0。Blocked cleanup plan 仍只作为 route-back blocker 投影：evidence envelope status 为 `blocked`，`blocked_reasons` 保留 no-active-caller、replacement parity、provenance 或 tombstone 缺口，且 `typed_blocker_observed=false`，不污染 open envelope 或 domain typed-blocker 计数。该证据证明 OPL cleanup gate、refs-only ledger、App/operator 投影和 safe-action 执行面已能消费三仓 replacement / no-active-caller / provenance / tombstone / handoff refs；它不授权 OPL 删除 domain repo 文件，也不等于 production default caller、App 发布路径、owner-chain 或 long-soak evidence 已完成。具体 plan status、receipt 数和 route 数只从命令输出读取。

当前完善主线已收窄为 OPL 自身语义治理：provider readiness 多真相、generated surface drift/block、App/operator drilldown 默认过重、`family-runtime` parser 语义堆叠、stage launch guarantee 含糊、legacy vocabulary active leakage，以及 AI 原生专家判断容易被 readiness / scorecard / contract completeness 机械替代这七类污染点已在 main 落成机器守门面或 policy guard。当前 main 已吸收 runtime single truth、generated surface gate、App drilldown summary-first、CLI parser split、stage scope / `guarantee_mode` projection、stage production caller evidence projection、主文档 hygiene 和 contract-floor policy；这些属于 framework hygiene closeout，不等于 MAS/MAG/RCA production default caller、App 用户路径或 long-soak evidence 已闭合。后续只能在这些守门面之上补 production evidence，不把 evidence tail 写成框架结构缺口。

MAS/MAG/RCA 当前均可被 OPL 识别为 standard domain agent consumer。Conformance、generated-interface、proof-bundle、cohort-loop、transition-runner 和 MAS handoff 的当前状态只从 `opl framework readiness --family-defaults --json` 及对应 drilldown 命令读取；本文不冻结 pass count、stage count、blocker count、external request count、transition case count 或 verified receipt count。这个状态说明 descriptor、read model、handoff、generated/handler target proof、replacement surface、scaffold validation、physical morphology policy、active path scan 和 OPL generic transition runner consumption 可由 OPL structural gate 审计；production usable closure 只按 domain-owned `production_acceptance` receipt 或 typed blocker 读取，不能写成 OPL 已证明 production domain owner chain、App 真实用户路径或 long-soak evidence 全部闭合。

Repo audit 的 current 读法是：OPL 默认审计面把 active private generic residue 作为动态 drilldown 读取，但这不等于三仓所有 production caller / App / long-soak evidence 已完成。MAS 默认 managed runtime backend 已切到 `opl_provider_backed_stage_runtime`，旧 `runtime_transport/`、`mas_runtime_core*`、turn runner、worker lease、`lifecycle_refs_adapter.py` 和 lifecycle refs SQLite writer 已按 no-alias physical retirement 关闭；当前只能作为 tombstone/provenance、OPL provider-backed handoff refs、MAS owner receipt、typed blocker、domain authority refs 或 no-forbidden-write proof 被读取，不再写成 delegated runtime adapter、diagnostic fallback 或 retained compatibility surface。MAS 剩余是仍有 caller 的 product/status/workbench、owner-route handoff 和 progress/domain-ref projection 删除门，以及真实 paper owner-chain、memory/artifact/lifecycle receipt 与 provider long-soak evidence；这些不是 MAS generic runtime owner 复活。MAG 的 `generated_hosted_default_caller_proof` 把 repo-local product/status/sidecar/grouped wrapper 明确标成 handler/ref-only/migration input，并把 direct/hosted parity 与 no-forbidden-write 变成外部 evidence request；OPL structured workspace binding 的 MAG direct entry / manifest 现在通过 MAG `MedAutoGrantProductEntry` status / manifest builder 生成，OPL 只负责 workspace/input locator、managed shell 执行和 refs-only 投影，不再硬编码退役 grouped CLI 语义。这只证明 MAG handler boundary/read-model proof 可验，不声明 OPL generated/hosted production default caller 已完成迁移、App workbench 真实消费或 Temporal long soak 完成。RCA `product-entry-session-store` 已物理改名为 `product-entry-session-snapshot-ref-adapter`；active semantic id 是 `product_entry_session_snapshot_refs_adapter`，旧 `product_entry_session_store` 只允许作为 retired/provenance id 出现在 RCA 合同或历史记录中。RCA 剩余是 continuity/runtime-loop projection、CLI/MCP wrappers、workspace/run envelope、runtimeWatch/sidecar/observability 和 executor adapter 的 naming/package hygiene、controlled visual-stage long soak、更多真实 artifact-producing owner receipt、visual memory body reuse、workspace receipt scaleout 和 cross-family repeated no-regression evidence。

Substrate projection 证据显示：`opl substrate projection --domain med-autoscience --json` 可解析 MAS 暴露的 body-free source/artifact/memory refs 与 lifecycle projection。OPL 只索引 MAS manifest 暴露的 body-free source/artifact/memory refs；source truth、memory body、artifact body、publication quality verdict 和 artifact mutation authority 仍归 MAS。

当前 active gap 与实施顺序以 [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md) 和 [生产级框架闭环差距矩阵](./active/production-framework-closure-gap-matrix.md) 为准。dated proof、receipt 明细和阶段 closeout 流水保存在 [OPL family 文档过程归档 2026-05](./history/process/plans/2026-05-18-opl-family-doc-process-history.md)，不在本页展开。

## 当前功能/结构差距

OPL family 当前不能写成整体功能/结构差距归零。仍需关闭的功能/结构差距是：

0. `semantic_hygiene_guard_surface_closeout`
   OPL 自身语义污染点已在 main 落地为机器守门面：provider readiness 改为 lifecycle-aware 单一 payload 派生；generated surfaces 从 canonical action/stage metadata 与 pack compiler input 对齐，MAG transition oracle 降为 evidence gate 而非 generated drift；App/operator 默认面改为 summary-first，完整 refs/routes 走显式 full detail；`family-runtime` parser 按子域拆分；stage control plane 投影 source/artifact/workspace scope refs、runtime assumptions、monitor refs 与 `guarantee_mode`；旧 Hermes/Gateway/frontdoor/MDS/default-compat 词汇继续只留在 history/tombstone/provenance、fixture、negative guard 或 cleanup plan；readiness / conformance 机器面明确 AI 原生专家判断优先、contract 只保安全/审计/恢复下限。后续只作为回归守卫，不再作为新增功能缺口，也不代表 MAS/MAG/RCA production ready。

1. `generated_surface_production_consumption`
   OPL pack compiler / generated surface 已能输出 active-caller target proof；MAS/MAG/RCA repo-side generated/handler target consumption 已闭合。descriptor ready / classification closed 代表可生成、可分类和可路由；production domain owner chain、live caller evidence 和 domain ready 仍通过真实 provider / App / owner receipt evidence gate 验收。

2. `stage_launch_admission_gate`
   默认 operator / App 总入口已收敛为 `opl framework readiness --family-defaults --json`。`opl stages readiness --family-defaults --json` 是 stage-level family drilldown；`opl stages readiness --domain <domain> --json` 是单仓 stage drilldown。它们消费 Stage Kernel admission、scope refs、expected receipt refs、proof/replay refs、assumption/monitor refs 和 evidence gap，并把 Derived Diagnostic Lenses 折叠成 warning、recommendation、typed blocker、human gate 或 route-back ref。`family-stage-admission` 仍是 `opl stages list|inspect` 的读模型；`family-runtime attempt create` 会记录 `opl_family_stage_launch_admission_gate`，缺少 `family_stage_control_plane`、stage 不在声明 pack 内，或声明 stage 存在 admission hard blocker 时都进入 blocked attempt，不再保留未注册旧 stage 的兼容 attempt 路径；`--require-stage-admission` 在 attempt start 与 provider-hosted tick 路径继续阻断越权、effect-boundary/runtime-guard 缺 runtime event 记录、组合不满足、human gate hard blocker 或 executor binding 缺失等启动安全问题。`family-stage-cohort-loop`、`family-stage-assumption-lifecycle` 与 runtime-budget 等 lens 只提供 source scope、cohort query、trigger、monitor/metric/dashboard refs、runtime assumptions、success refs 和 minimal counterexample 的诊断解释；它们不是普通首屏，不是独立学习目标，也不授权 OPL 写 domain truth、domain verdict、artifact body、quality verdict、stage completion 或 owner receipt。Stage readiness、cohort-loop 和 blocker 的当前状态只从 framework readiness drilldown refs、`opl stages readiness --family-defaults --json`、按需 `opl stages readiness --domain <domain> --json` 和专题 drilldown 读取；剩余功能项是把 selected executor binding、真实 consumed refs、expected receipt refs、scope freshness、monitor freshness 与 cohort-loop live execution 的 production caller scaleout 纳入 queue/provider/App 可见证据。

3. `app_workbench_drilldown`
   OPL CLI/runtime read model 已覆盖 route graph、review queue、artifact gallery、package/export lifecycle、memory refs、functional privatization audit、quality/readiness、SLO 和 owner-aware action routing；`app_execution_bridge` 已把 App 调用边界收口到 `opl runtime action execute`、`runtime lifecycle apply/reconcile` 和 provider scheduler / legacy cleanup safe action route。AionUI runtime 页面已把 external evidence、evidence gate receipt、legacy cleanup 和 provider cadence window summary-first counters 放入 `AppOperatorDrilldown` 默认卡片，并能按需调用 `opl runtime app-operator-drilldown --detail full --json` 加载完整 refs/routes；full detail 同时展开 `functional_privatization_audit_refs`，按 domain 列出 module refs、private platform residue inventory、authority function inventory、standard pack inventory、bridge exit gate 和 forbidden generic owner flags，且 App authority boundary 仍保持不写 domain truth、不读 memory/artifact body、不授权 quality/export verdict。默认 summary 的 `functional_privatization_action_required_count` 是日常待处理数，`private_platform_residue_inventory_count` 是 full-detail 审计账本数，不能把后者写成 open blocker。legacy cleanup detail 已输出可执行的 `command_domain_id` route，不再把 skeleton id 写成 CLI domain 参数；focused tests 已覆盖 summary 默认面、provider cadence window 计数、full-detail lazy load、functional privatization refs、provider scheduler action execution 和 legacy cleanup action execution。App/operator 默认 `next_safe_action` 现在只暴露仍 open / pending 且属于 evidence、provider 或 cleanup 的 selected route；provider SLO 已满足、lifecycle ledger 已有 receipt、cleanup plan 没有 actions 或 route 只是 diagnostic query 时，不会把该 route 选成默认下一步。domain-dispatch evidence 默认面按 bound dispatch identity 只保留最新、已 accepted typed closeout 且缺 evidence 的 attempt 为 actionable workorder；superseded attempt 和未完成 closeout 的 attempt 只在 full detail / blocked provenance 中保留，不生成默认 receipt route。需要真实 App/domain/live payload 的 selected action 会直接携带 `--payload-file <payload.json>`、copyable command 和 `payload_workorder`；若当前没有 open selected route，`next_safe_action=null`，生产 evidence 尾项继续通过 `evidence_next_steps` 与 `framework readiness.next_safe_actions` 投影给 owner。该优先级与 payload workorder 只改善真实 payload 接力，不把空模板、record-only receipt、typed blocker、operator attention、diagnostic query、已闭合 framework route、未 closeout attempt 或 superseded provenance 写成 release ready、OMA domain truth、owner receipt、domain ready 或 family production ready。当前 `26.5.19` release package、provider state linkage、真实已安装 App 窗口截图、startup-maintenance / first-run reload-check refs 和同 cohort long operator evidence 已进入 verified ledger；App release/user-path active open gate 与 active typed blocker 清零，历史 typed blocker 只保留为 provenance。domain repo 不应复制通用 workbench。

4. `domain_private_platform_residue`
   MAS 已把未分类 generic owner 回流和 5 个 structural follow-through gate 清零，并新增 `runtime_transport_handoff_projection` 约束 runtime runner / worker lease / supervisor / SQLite sidecar 只能作为 domain bridge、receipt、typed blocker、refs-only sidecar 或 diagnostic。MAG repo-side handler/ref-only/authority boundary 已闭合；RCA generated/hosted shell consumption、repo-local wrapper active-caller migration 和 legacy physical cleanup 也已按 OPL consumer 口径闭合。MAG/RCA 剩余不是继续迁移 repo-owned generic owner，而是外部 default caller、App、live owner-chain、production evidence tail 与 naming hygiene tail。所有新发现或既有残留都必须按 OPL 上收、generated surface 替换、refs-only 收薄、minimal authority function、diagnostic cleanup 或 tombstone 分类处理；结构闭合不能写成真实生产 owner chain、live evidence 或物理代码路径已清零。
   RCA production evidence scaleout active surface 已不再暴露 `legacy_payload_field_aliases`；`domain_receipt_refs` / `no_regression_refs` 只按当前 refs payload path 保留，不作为兼容映射。OPL App/operator consumer 现在带 no-resurrection guard：若 RCA active manifest 重新暴露 `production_evidence_scaleout_refs.legacy_payload_field_aliases`，默认 summary、framework readiness 和 `family-runtime evidence-worklist` summary 会暴露 `domain_owner_payload_summary_naming_hygiene_blocker_count`，`attention_first_payload.evidence_after_contract.domain_owner_payload_summary_attention`、framework readiness attention、evidence-worklist 顶层 attention 和 full `domain_owner_payload_summary_refs` 会投影 `blocked_legacy_payload_field_aliases_resurrected`，并且不生成 `domain_owner_payload_summary_receipt_record` route 或 open worklist item。该 guard 只阻止旧 alias 复活，不声明 RCA visual ready、exportable、handoffable 或 production soak complete。

5. `legacy_physical_cleanup`
   Hermes/Gateway/frontdoor/local-manager/MDS-default/default-compat 等旧面只有在 provenance、diagnostic、fixture、negative guard 或 history 语境中可见。MAS local LaunchAgent 当前没有本机 active label / plist；MAS Hermes scheduler ensure path 已退役为 tombstone，显式 Hermes 只保留 status/remove 的 legacy diagnostic cleanup；MAS repo 内相关路径只能保留 explicit cleanup diagnostic / tombstone 角色。MAS current standard-agent evidence 已由 OPL normalizer 消费为 cleanup ledger readiness：`functional_consumer_boundary.standard_agent_purity` 提供 no-active-caller 证据，generated default caller readiness 提供 replacement parity，`source_provenance` 与 history-suppressed default read model 提供 provenance / tombstone 证据；这只让 OPL cleanup ledger plan ready，不授权 OPL 物理删除 MAS 文件。MAS/MAG/RCA 的 OPL legacy cleanup plan、OPL refs-only lifecycle ledger receipts 与 App/operator cleanup routes 只从 `opl agents legacy-cleanup ...` 和 `opl runtime app-operator-drilldown --json` 读取。RCA 旧 managed runtime / repo-local generic wrapper residue 已闭合为 handler target、refs-only adapter、history/provenance 或物理删除。后续只保留新发现 residual 的 fail-closed cleanup gate、domain owner 物理删除 receipt 和 RCA naming hygiene tail。

6. `app_managed_environment_startup_maintenance`
   OPL 已提供 `opl system startup-maintenance` 作为 App startup maintenance 机器入口，MAS/MAG/RCA/OPL Meta Agent clean managed checkout 可受控 install/update、health check、skill sync 和刷新 plugin cache freshness；dirty/ahead/diverged/no-upstream/env override/sibling workspace/invalid checkout 会 fail closed 到人工处理，不静默覆盖 developer checkout 或 managed runtime。Module step timeout 会把对应 target 投影为 `manual_required` / `module_*_blocked`，并让其他 clean managed modules 继续完成维护和 receipt 写入，避免单个 domain health check 阻塞整个 App startup maintenance。AionUI first-launch / app-version maintenance 源码路径已自动调用该 action。OMA production-consumption follow-through 现在把 managed install/update receipt 缺失与当前 module inspection 连接起来：developer/sibling/dirty/ahead 等非 managed-root 状态会在 App/default caller 中显示为 manual-required gate、blocker 列表和 review command，而不是被误读成普通 missing ref 或由 App 自动覆盖开发 checkout。当前 App release/user-path 已用 startup-maintenance artifact 和 first-run `already-prepared` 日志切片验证 reload-check refs，并已用同 cohort long operator evidence 关闭当前 evidence gate；OMA clean managed-root receipt 与 verified long-soak ref 也已进入当前 production-consumption read model。上述 refs 仍不授权 release ready、domain ready 或 family production ready。

7. `developer_mode_agent_lab_repair_route`
   Developer Mode 的系统配置、App settings / initialize surface、GitHub login / repo permission projection、direct repo fix / fork PR route 计算、Agent Lab refs-only repair route read model 和动态 route builder 已落地。动态 builder 读取 developer mode projection、repo permission 与 patrol observation refs，输出 `blocked` / `observe-only` / `direct-fix` / `fork-PR` / `mixed`，并把 `developer_mode_projection_ref`、`route_eligibility`、`patrol_observation_ref`、`diff_ref`、`verification_refs`、`no_forbidden_write_ref`、`commit_ref`、`fork_repo_ref`、`pr_review_ref`、`owner_acceptance_ref` 作为 closeout refs 投影；direct-fix 的 `owner_acceptance_ref` 必须是外部 owner ref，fork/PR 的 `owner_acceptance_ref` 必须绑定 `github-pr-owner-acceptance-ref:` typed ref。没有真实外部 PR 权限时，non-owner fork/PR 只能用 repo contract fixture ref 表达 drill，并通过 `closeout_claim_status=fixture_drill_owner_acceptance_open` 与 `owner_acceptance_status=fixture_drill_not_owner_acceptance` 标记为非 owner receipt、非 owner acceptance closeout。Live repo permission probe 已用真实 `gh api` 验证到 `gaofeng21cn/one-person-lab`、MAS、MAG、RCA 和 OMA 均为 direct write ready；Developer Mode 默认 probe 也能容忍正常 GitHub CLI API 延迟，不再把约 1.6-1.8s 的成功权限响应误判为 blocked。`opl runtime developer-mode-closeout record --payload <json>` 可记录真实 route eligibility、patrol observation、diff、verification、no-forbidden-write、commit 或 fork/PR、external owner acceptance refs；fork-PR ledger intake 现在要求 URL-backed GitHub fork repo ref、URL-backed GitHub pull request ref 和 `github-pr-owner-acceptance-ref:`，会拒绝 `fixture://` / `repo-contract-fixture-ref:` 这类 drill refs，也会拒绝没有可解析 GitHub URL 的弱 typed ref、普通 PR URL、`github-pr-ref:` 或弱 owner acceptance ref；同一 payload 可附带 `route_repetition_refs`、`risk_tier_auto_promotion_refs` 和 `app_patrol_mount_refs`，用于 machine-visible scaleout follow-through。`risk_tier_auto_promotion_refs` 已收紧为 verified Agent Lab receipt：operator 必须先通过 `opl agent-lab risk-tier-promotion record|verify` 写入带真实 failure/evidence delta、独立 AI review、promotion receipt、rollback target、canary observation、no-forbidden-write 和 verification refs 的 ledger receipt，Developer Mode intake 只接受 verified receipt ref；recorded/unverified receipt、fixture、scorecard pass、provider completion、单独 rollback/canary ref 或占位字符串都会 fail closed。Agent Lab 也可从多个 verified live ledger closeout receipts 的 target repo 或 patrol observation 覆盖派生 body-free route repetition ref。App/operator payload workorder 会在 fork-PR success path 暴露 URL-backed ref policy、accepted prefixes、rejected fixture prefixes 和 typed PR owner acceptance requirement，并在 base direct-fix / fork-PR closeout refs ready 后继续把 route repetition、verified risk-tier auto-promotion 和 App patrol mounting 作为 scaleout open gates 投给默认 caller，避免 fixture、普通 PR ref、弱 owner acceptance、record-only risk-tier receipt 或空模板被提交或记录成 verified live route。`verify` 后 Agent Lab read-model 会把 verified ledger receipt 合并为 live closeout route，record-only receipt 保持 `ledger_refs_recorded_verify_pending`；scaleout refs 只在 verified receipt 或派生 route repetition ref 中计入 `scaleout_followthrough`，缺失时读为 `scaleout_refs_incomplete`。`opl app state` 现在把该 read model 的 closeout summary 收窄投影到 `developer_mode.live_closeout_evidence`，只暴露状态、计数、scaleout follow-through、owner acceptance policy、non-authority outputs 和 refs-only authority boundary，不包含 full drill 列表；App/operator summary、`attention_first_payload.evidence_after_contract`、`evidence_next_steps` 和 framework readiness 也会读取同一 Agent Lab live closeout evidence，给出 `developer_mode_live_closeout_status`、`ledger_evidence_status`、direct-fix / fork-PR verified ledger counts、missing live ledger route count、pending verify count、scaleout follow-through open gate count 和 no-owner-authority payload workorder，full detail 才展开 drills 与 receipt refs。OPL 仍不能写 owner receipt、domain truth 或 managed runtime truth。2026-05-23 的 `family-runtime --task-kind` direct-fix 已推送到 `gaofeng21cn/one-person-lab@38ccb4bb4320e0429d43dd46faf755c03d45f95e`，并记录/验证为 Developer Mode direct-fix refs-only ledger receipt `opl://developer-mode-closeout/one-person-lab/patrol-observation-ref%3Aone-person-lab%2Ffamily-runtime-task-kind-scope%2F2026-05-23`；另有 direct-fix currentness receipt `opl://developer-mode-closeout/one-person-lab/patrol-observation-ref%3Aone-person-lab%2Fdeveloper-mode-fixture-currentness-live-count%2F2026-05-28`。2026-05-28 新增 RCA cross-repository fork-PR verified receipt `opl://developer-mode-closeout/redcube-ai/patrol-observation-ref%3Arca%2Fdeveloper-mode-cross-repo-fork-pr-20260528`，对应 `gflab/redcube-ai` fork -> `gaofeng21cn/redcube-ai` PR #2，head commit `56ea90fb204e1beef9e144c24e78007373121ebd`，GitHub CI 与 Sentrux Advisory success，PR 由 owner merge。2026-05-29 已有 App patrol mounting verified receipt `opl://developer-mode-closeout/one-person-lab/app-patrol-mount-2026-05-29`，把 `app-patrol-mount-ref:one-person-lab-app/developer-mode-live-closeout-evidence-fast-full-20260529` 投给 App/default caller 可见的 follow-through；同日新增 risk-tier follow-through verified receipt `opl://developer-mode-closeout/one-person-lab/risk-tier-auto-promotion-2026-05-29`，引用 `agent-lab-risk-tier-auto-promotion-ref:one-person-lab/developer-mode-risk-tier-verified-ledger-gate-20260529`。Fresh readiness 读为 route repetition 1、risk-tier auto-promotion 1、App patrol mount 1、scaleout open gate 0。当前 repo-contract fork-PR sample 仍保留为 fixture currentness guard：`fixture:redcube-ai/fork-pr-drill` / `fixture://redcube-ai/developer-mode-fork-pr-drill` 继续通过 `fixture_repo_currentness.status=repo_contract_fixture_not_live_repo` 与 `repo_contract_fixture_not_live_repo_count` 暴露为 repo-contract fixture tail；`live_external_owner_acceptance_count` 只统计 verified Developer Mode closeout ledger 中的 live external owner refs，`external_owner_acceptance_missing_count` 只统计 live / non-fixture route 缺口，repo-contract fixture drill 的负向守门进入 `fixture_drill_external_owner_acceptance_missing_count` 与 `fixture_drill_owner_acceptance_open_count`。fixture repo 或 drill coverage 不能被 consumer 当作 live GitHub repo、fork/PR owner acceptance 或 external owner closeout。该路线应按 Agent Lab 风险分级运行：低风险 prompt/rubric/display/read-model/test metadata 只有在真实 failure/evidence delta、independent AI approve、promotion receipt、rollback 和 no-forbidden-write refs 齐全后才可 auto-promote；中风险 stage/tool/retry-dead-letter/memory-retrieval policy 还需 tests pass 和 canary 后自动推广；高风险 domain truth、quality verdict、artifact mutation、memory accept/reject 与 credential/network/write policy 必须 owner/human gate。verified Developer Mode ledger receipts、派生 route repetition、verified risk-tier auto-promotion receipt 和显式 App patrol refs 只证明对应 repo repair closeout / follow-through refs 可被 Agent Lab / App / readiness 消费，不证明 domain ready、owner receipt、managed runtime truth、release ready、production ready 或 global goal complete。

8. `agent_lab_external_research_absorption`
OPL Agent Lab 已把 ARIS 风格的日志驱动机制优化、独立 AI reviewer、自动 promotion / rollback、integration contract、review trace、effort/assurance 双轴、helper inventory/drift report、permission/current-date fail-closed invariant 与 MCP/stream reliability policy 变成 machine-readable refs-only surfaces。AHE-style 证据链和 Codex-first executor-first 读法继续收敛为 `executor_capability_lease`、Codex attempt trace / replay / fork refs、variant cockpit、independent reviewer refs 与 App/operator self-evolution cockpit：OPL 只给 Codex 等 selected executor 发放可审计 stage lease、工作区/工具/预算/回执边界和 replay/falsification refs，不枚举或限制 AI 内部推理。MAS 侧负责把 research wiki、reviewer direct evidence、analysis queue manifest、runtime event ledger、provider/executor switch hygiene 和 claim assurance 投影进 `mechanism_evolution_inputs`；OPL 只消费 refs、typed graph/queue/ledger/hygiene/assurance metadata 和验证结果，不接管 paper memory body、analysis queue body、runtime event body、provider/executor transcript body、claim text body、publication verdict、owner receipt 或 artifact authority，也不引入 ARIS、AHE、OpenAI Agents SDK、LangGraph、AutoGen 或 CrewAI runtime dependency。

9. `pilotdeck_clean_room_intake`
   OPL 已 clean-room 吸收 PilotDeck 的三类模式为 active framework surfaces，不复制 AGPL 代码、不接入 PilotDeck runtime、不改变 domain owner 边界。`memory_trace_projection` 现在作为 body-free OPL projection 出现在 attempt query、stage progress log、memory locator index、stage attempt workbench 和 App/operator drilldown，只投影 consumed memory refs、recall/retrieval trace refs、writeback receipt refs、rejected write refs 和 source refs，且显式 `can_read_memory_body=false` / `can_write_domain_memory_body=false` / `can_accept_or_reject_memory_writeback=false`。`model_route_cost_projection` 现在把 selected model/executor route refs、route reason/tier/fallback refs 与 observed token/cost telemetry 关联到 attempt、operator visibility、stage progress log 和 workbench summary；它不改 executor、不自动降级、不替代 quality gate。`opl work-order execute` 每次执行都会物化 OPL-owned `execution-plan.md` 与 `execution-report.md`，并把 `execution_plan_ref` / `execution_report_ref` 写入 execution receipt；报告覆盖 changed files、verification、absorption、cleanup 以及 typed blocker / owner hook 状态，失败路径也会生成 failure report 与 failure cleanup receipt。MAS/MAG/RCA 继续持有 domain truth、memory body、artifact body、quality verdict 和 owner receipt。

10. `work_order_execution_primitive`
   OPL 已把可执行 developer work order 的运行面收成 canonical Framework primitive：`opl work-order execute --work-order <developer-patch-work-order.json> ... --json`。该入口由 OPL 持有 target worktree open、Codex CLI dispatch、target verification、patch commit、fast-forward absorption、cleanup、failure cleanup receipt、refs-only execution receipt、OPL-owned execution plan/report artifact 和可选 target owner closeout hook invocation；旧 `opl agent-lab execute-work-order` alias 已退役，当前只保留负向 guard 防止兼容入口和 alias 字段复活。Agent Lab self-evolution / external-suite 只消费 canonical execution receipt、execution plan/report refs 和 re-evaluation refs。这只明确执行原语 owner，不授予 OPL/Agent Lab domain truth、artifact body、memory body、quality/export verdict 或 owner receipt authority，也不改变 Codex CLI 默认 executor 地位。

## 当前测试/证据差距

下面是目标结构正确后的证据门，不能替代功能/结构迁移；MAS/MAG/RCA 都不能把 descriptor ready、read model、replacement proof 或 provider proof 写成功能/结构迁移完成：

- MAS 多条真实 paper line 的 provider-hosted guarded apply、progress delta、AI reviewer update、artifact delta、human gate、stop-loss 或 stable typed blocker。
- MAG 真实 OPL-hosted grant-stage controlled soak、owner receipt、typed blocker 或 no-regression evidence。
- RCA 真实 artifact-producing owner receipt、visual memory body reuse、workspace receipt scaleout 和 repeated no-regression evidence。RCA stage control plane 已补齐 OPL standard Progress-First `progress_delta_policy` 与 `family-stall-lineage.v1` typed blocker lineage policy；fresh family `agents conformance` 读为 4/4 passed。RCA `stages replay-certification --domain rca` 现在会消费 stage control plane declared replay refs：append-only event log、attempt ledger、runtime events 与 owner receipts 已 observed，4 条 `human_gate:redcube_operator_review_gate` closeout replay refs仍没有 success/human approval ref；RCA production acceptance 已为这 4 条 workorder 输出 domain-owned typed blocker refs，OPL `stage-replay-missing-receipt` ledger 已 record/verify，并让 evidence-worklist 把它们读为 `blocked_by_domain_owned_typed_blocker_ref`。MAS production acceptance 现在也为 3 条 `human_gate:study_user_decision_gate` workorder 输出 MAS-owned `stage_replay_human_gate_blocker_summary` typed blocker refs；OPL ledger 已 record/verify `baseline_and_evidence_setup`、`bounded_analysis_campaign` 与 `manuscript_authoring` 三条 missing-replay receipt refs，fresh evidence-worklist 不再有 `blocked_by_missing_replay_receipt_ref`。OMA synthetic stage-decomposition plane 也会消费 declared machine replay refs；OMA production acceptance 现在为 `human_gate:oma_baseline_owner_review` 输出 domain-owned typed blocker ref，OPL ledger 已 record/verify `opl://stage-replay-missing-receipt/opl-meta-agent%2Fstage-decomposition%2Fhuman_gate%3Aoma_baseline_owner_review`，fresh evidence-worklist 把该 workorder 读为 `blocked_by_domain_owned_typed_blocker_ref`。`stages readiness --domain rca|opl-meta-agent --detail full` 和 family defaults replay packet 会把这些缺口投影为 refs-only `opl_stage_replay_missing_receipt_workorder`，只接受真实 human-gate / owner receipt ref 或 domain-owned typed blocker ref，不 requery human、不创建 owner receipt、不声明 domain/production ready。`family-runtime evidence-worklist --family-defaults --detail full` 现在也会把同类 `payload_workorder` 汇总成 `stage_replay_missing_receipt_workorder_packet` 与 attention items，并在自建 family stage readiness 时使用 bounded live refresh + projection cache fallback，避免 slow/failed live manifest 让 direct worklist 与 `framework readiness` 的 replay attention 口径分叉；cache fallback 只提供 operator visibility 与 timeout diagnostics，不提升为 domain truth、owner receipt 或 readiness claim。Compact attention 只列默认上限内的 workorder，另由 `stage_replay_missing_receipt_workorder_attention_summary` 显式暴露 full packet 总数、省略数量、omitted domain/status/kind 分组和 full-detail ref。Replay-missing item 不进入 open safe-action worklist，不可由 OPL 执行或签收；每个 workorder / compact attention item 都携带 `target_identity`、copyable direct ledger record/verify/list handoff 与 success / typed-blocker payload template，domain / human owner 可通过 `opl runtime stage-replay-missing-receipt record|verify|list` 记录 body-free success receipt refs 或 typed blocker refs。verified success ref 只有精确覆盖同一 `domain_id/stage_id/missing_ref` 时才从 packet 中抑制该 attention；verified typed blocker ref 只标注并计数，workorder 仍按 blocked attention 保留。该结构 closeout 只移除 MAS/RCA/OMA stage replay missing-ref 的“无 domain-owned answer”尾项，不声明 MAS study-user decision approval、MAS paper closure、RCA human approval、visual ready、OMA baseline owner approval、OMA production ready、artifact authority、Temporal controlled visual-stage long soak 或 production ready。
- MAS/MAG/RCA 的 memory / artifact / lifecycle 在真实 workspace 中形成 accepted/rejected writeback、cleanup/restore/retention 和 artifact mutation receipt；OPL 当前已能 refs-only intake / verify / project 这些 receipt refs，但 receipt body、accept/reject、artifact mutation authority 和 final verdict 仍必须来自 domain owner。
- Temporal provider 长时 SLO 已有 `cadence_window` 机器投影，可区分 `window_cadence_satisfied`、`window_evidence_incomplete`、`window_repair_receipt_observed` 和 `no_window_evidence`，并把 expected / observed / missing / blocked repair receipt counters 暴露给 runtime snapshot、App/operator read model 和 AionUI 默认卡片。`provider_capability_slo` 现在把 restart re-query、signal history、typed closeout、missing-closeout block、retry/dead-letter boundary 和 domain truth boundary preservation 投到 runtime snapshot / App summary；当前窗口与计数只从 `opl framework readiness --family-defaults --json` 和 `opl runtime app-operator-drilldown --json` 读取，不在本文固化。`domain_dispatch_evidence` 已让 App/operator 可见 owner-chain refs 是否出现；仍需真实 domain owner-chain dispatch scaleout、长时 operator evidence 与 no-forbidden-write proof。

当前未闭合项按下面类别读取，具体数量和 drilldown 以 `opl framework readiness --family-defaults --json` 为入口：

- App release / user path：默认 App/operator 与 framework readiness 现在暴露 `release_package_refs`、`screenshot_refs`、`reload_prompt_user_path_refs`、`provider_state_linkage_refs` 和 `long_operator_evidence_refs` 五类 refs-only evidence gate，并把 App release/user-path evidence record/verify 纳入 `runtime action execute` safe action route；当前本机 ledger 已验证 `opl://app-release-user-path-evidence/26.5.19-package-provider`、`26.5.19` 真实已安装 App 窗口截图 receipt、`opl://app-release-user-path-evidence/26.5.19-startup-maintenance-reload-check-20260523`、同 cohort verified long operator receipt，以及 historical typed blocker receipt `opl://app-release-user-path-evidence/typed_blocker_ref%3A%2F%2Fone-person-lab-app%2F26.5.19%2Flong-operator-evidence-pending%3Fgate%3Dlong_operator_evidence_refs%26reason%3Dno_same_cohort_long_operator_soak_ref_observed`。2026-05-28 fresh GitHub Actions draft candidate `26.5.28-draft.20260527235839` 已跑通标准/Full 发布包、remote standard+Full verification、标准 clean VM smoke、Full clean VM smoke、one-shot installer smoke 和 Docker WebUI smoke；这些 refs 可作为更新 cohort 的 App/live release evidence 进入同一 ledger，但仍只授权 refs-observed 读面，不声明 release ready 或 production ready。App drilldown 只在最新候选 cohort 自身完整时选择它；历史 typed blocker 不再阻塞当前 gate。first-run 日志中的 `already-prepared` 只证明 reload-check / GUI preparation user path 已检查，不证明实际 reload 发生；ledger verified、selected cohort、open gate 清零或 historical typed blocker provenance 都不等于 App release ready 或 production ready。
- Domain owner-chain 与 live evidence scaleout：MAS/MAG/RCA 在真实 workspace 中持续产出 owner receipt、typed blocker、no-regression evidence、expected receipt instance、monitor freshness、memory/artifact/lifecycle receipt、long-soak refs 和 no-forbidden-write proof；OPL worklist / workorder accounting 闭合不等于这些真实生产证据已经完成。当前 MAS guarded-apply / owner-route / aftercare / default-executor / stage-evidence payload、MAG grant-stage / lifecycle / legacy route-back、RCA visual-stage typed blocker payload 均已能按真实 owner refs 或 typed blocker path 进入 OPL refs-only ledger，并由 identity preflight 防止跨 attempt / stale payload 误闭合。App/operator `domain_owner_payload_summary_refs` 现在可消费 MAS、RCA 与 MAG full owner-payload response：MAS 的 guarded-apply canary closeout 会投影 paper-line owner refs 与已观测 `finalize_and_publication_handoff` stage expected receipt refs，MAG 的 `mag_opl_owner_payload_response` / `owner_payload_response` 会投影 owner refs 与 per-stage expected receipt refs；count-only scaleout snapshot 不会被扩成 stage route；grant/quality/export/submission readiness claim 会在 refs-only preflight fail closed。Stage evidence workorder 会把同 stage 的 domain-owner payload summary refs 作为 candidate refs 暴露给 operator，用于定位可提交 payload和剩余缺口；candidate 不等于 receipt，也不关闭 stage evidence route；verified stage typed blocker receipt 只按 blocked closure 关闭对应 worklist item，不声明 success 或 readiness。MAG `manifest_consumer_evidence.sustained_consumption_followthrough_workorder` 另有专用 `mag_manifest_sustained_consumption_followthrough_refs` 和 `opl runtime mag-manifest-sustained-consumption record|verify|list` ledger；它只接受完整 sustained-consumption success refs path 或 typed blocker path，并拒绝未知字段、body、占位 ref 与 readiness claim。当前本机 MAG sustained followthrough ledger 已有 verified refs-only receipt，payload path 为 `sustained_consumption_refs_path`，且 long-soak 字段仍是 provider long-soak window typed blocker；这只证明 App/default caller refs payload 已被 OPL 记录/验证，不关闭 sustained App consumption 或 provider long-soak。`domain-owner-payload-summary`、stage-production-evidence 和 domain-dispatch receipt 数量只从 App/operator drilldown、OPL ledger 与 evidence-worklist fresh read 读取；这些计数增长只证明 refs-only transport 和 preflight 可用，不生成 MAS/MAG/RCA owner receipt、fundability/export/submission readiness、paper closure、grant stage closeout 或 long-soak closeout。当 `domain_dispatch_evidence_workorder_count` 为 0 时，只能说明当前 refs-only workorder accounting 没有 open domain-dispatch record route，不能说明真实 owner-chain、memory/artifact/lifecycle receipt 或 long-soak 证据已经完成；当 `stage_receipt_freshness_open_workorder_count` 为 0 时，只能说明当前 stage evidence workorder 已被 verified refs 或 domain-owned typed blocker 收口，不能说明 expected receipt instance、monitor freshness 或 paper closure 全部 observed。framework readiness 的 `open_tail_count=0` 只表示当前没有 operator-actionable payload route；被 provider worker mutation guard 阻塞的 provider SLO raw tail 和 domain-owned typed-blocker attention 仍按 blocked refs-only attention 保留。具体 attempt、receipt、fingerprint 和命令流水只从 OPL ledger / read-model / history 读取；当前 status 只保留结论：refs-only transport 和 accounting 可用，默认面只按每个 bound dispatch supersession identity 暴露最新可记录 workorder，App release/user-path、Codex App runtime evidence、Developer Mode live route、OMA production-consumption 和 MAG sustained followthrough ledger 已有 verified refs，但不声明 guarded apply、default executor dispatch、reviewer refresh、MAG sustained App consumption、MAG grant stage、RCA visual stage、domain ready、App release ready 或 family production ready。
- MAS owner-chain refresh provenance：此前多轮 same-day MAS owner-chain refresh 暴露并闭合了一组 `domain_owner/default-executor-dispatch` payload-required workorder；MAS owner surface 均返回 typed-blocker path、owner-chain refs 与 no-forbidden-write proof，OPL 只记录/验证 body-free refs-only receipt。具体 attempt id、receipt ref 与当时计数已经是 ledger/provenance 信息，不再在 active status 中声明为 current counter。
- Current live worklist snapshot（2026-05-31）：fresh main `family-runtime evidence-worklist`、`framework readiness` 与 App/operator drilldown 的 worklist、payload-required / payload-free、operator-actionable、evidence-envelope、domain-dispatch receipt 和 stage freshness 计数都是动态审计数，只从 fresh CLI/read-model 读取，不在本文冻结。当前 `open_worklist_item_count=0` 和 `open_safe_action_item_count=0` 只说明没有 OPL 可执行 safe-action route；stage replay packet 仍有 14 个 workorder、14 个 missing receipt refs 和 9 个 human-gate refs。Stage replay ledger 当前 15 条 verified receipts 中有 1 条 success refs path，覆盖 MAS `finalize_and_publication_handoff/owner_receipt:finalize_and_publication_handoff`；其余 14 条为 typed blocker path，并继续形成 blocked refs-only attention。`zero_open_worklist_blocked_refs_only_attention_remains=true`、`zero_open_worklist_is_completion_claim=false`、`zero_open_worklist_is_domain_ready=false`、`zero_open_worklist_is_production_ready=false` 是固定授权边界；provider SLO cadence / capability 是否 satisfied 也以 fresh readiness 为准。若 fresh read-model 没有 OPL 可执行 safe-action route，只说明当前 workorder accounting 没有可执行 route；若 fresh read-model 暴露 payload-free verify route，只说明已有 refs-only ledger receipt 等待 verify。blocked refs-only attention 和 domain-owned typed blocker refs 仍可继续存在，不生成 MAS owner success receipt、paper-line closeout、artifact/memory/lifecycle authority、human-gate approval、domain-ready、production-ready 或 global completion。
- MAS source-purity cleanup：旧 `runtime_transport` / SQLite / runner / worker lease 已按 no-alias retirement 读取；当前 MAS 合同读为 `functional_structure_gap_count=0`、`repo_local_wrapper_tail_count=0`、`source_purity_cutover_status=standard_agent_source_shape_landed`。原 product/status/workbench、owner-route handoff 与 progress/domain-ref projection wrapper tail 只作为 former tail / deletion-gate provenance；仍不能声明 `domain_repo_physical_delete_authorized`、paper closure、artifact authority ready 或 production ready。剩余是 live paper owner-chain、memory/artifact/lifecycle receipt 和 provider long-soak evidence。
- MAG/RCA physical-delete authority：MAG source shape landed，RCA bridge gate landed 且无 remaining bridge module；二者当前都按 standard-agent structural/source-purity clean 读取。仍不能写成物理删除完成或 production ready；后续只按真实 owner receipt / typed blocker、no-forbidden-write、no-regression 和 production evidence 扩面。
- OMA source hygiene：OMA structural/source-purity clean；default-caller deletion evidence 已通过 `domain_handler` canonical target 消费。后续只做防回归、真实 target-owner receipt / typed blocker 样本和 production evidence，不授权目标 domain ready、family production ready 或默认 promotion。
- RCA naming hygiene tail：RCA 已移除 production evidence scaleout 的 `legacy_payload_field_aliases` active alias map；OPL consumer 已加 no-resurrection guard，发现该 alias map 复活会在默认 App/operator summary 暴露 naming-hygiene blocker count，并阻断 owner-payload summary record route。历史 `managed` / runtime / gateway / session / sidecar 命名继续降到 provenance、semantic-id 或 tombstone，不恢复 compatibility alias。
- New Agent / OMA production consumption：scaffold cohort 与 OMA structural consumption 已有 proof；默认 App/framework 读面已把 managed install/update、App live path、owner receipt / typed blocker scaleout 和 long-soak 固定为 4 个 production-consumption follow-through gate。OPL-hosted OMA `stage-decomposition` fallback plane 现在复用 standard Progress-First delta policy 与 `family-stall-lineage.v1` policy，避免 OMA repo contract 已更新而 OPL hosted/generated stage readiness 仍报告 `missing_progress_delta_policy` / `missing_typed_blocker_lineage_policy` 的漂移；该修复只关闭 OPL hosted plane 的 launch kernel blocker，不写 OMA domain truth、不生成 target-owner receipt，也不声明默认 promotion。`standard_agent_template_consumption_refs` 在 full drilldown 中暴露 replayable `evidence_contract`，把 `opl agents scaffold --consumption-evidence` 的期望输出 root、sample cohort success path、消费 surfaces 和 forbidden claim fields 明确为可复验 contract；该读面不隐式生成临时仓，也不把 sample pass 写成 domain ready、artifact authority、quality/export 或 production ready。`runtime standard-agent-template-consumption record|verify|list` 已成为同一 replay evidence 的 durable refs-only ledger，App/operator summary 会显示 ledger receipt、verified receipt 与 pending verify 计数；当前 verified receipt `opl://standard-agent-template-consumption-ledger/default-state-2026-05-28T08-20-35-619Z`、`opl://standard-agent-template-consumption-ledger/repeat-replay-2026-05-28T13-00Z` 与 `opl://standard-agent-template-consumption-ledger/repeat-replay-2026-05-28T21-02Z` 让默认 App/operator 读面从 replayable contract 继续前进到 3 次 verified durable receipt。verified ledger receipt 仍只证明 scaffold consumption replay refs 已被 OPL 记录/验证。当前 owner receipt / typed blocker scaleout、managed install/update、App live path 和 verified long-soak ref 已由 observed refs 退出 open gate；`framework readiness` 读为 OMA `production_consumption_ready=true`。OMA `stage-decomposition / oma_baseline_owner_review` replay human-gate 缺口现在有 OMA-owned typed blocker verified receipt，状态是 blocked-by-domain-owned answer，不是 owner approval 或 success receipt。Managed install/update gate 仍能在未来 OMA 不是 clean managed-root 时给出 typed manual-required route；App live path gate 通过 OPL refs-only receipt ledger 消费真实 App/live evidence；production-consumption gate 有 `opl runtime oma-production-consumption record/verify/list` refs-only intake，也有 `long-soak start|event|finish` 本地 observation workorder 入口。Recorded receipt、typed blocker、operator evidence ref、event-only operator log 或未 record/verify 的 observation manifest 不能替代 verified long-soak ref；当前 OMA gate 关闭和 replay typed blocker 也不声明 MAS/MAG/RCA domain ready、family production ready 或默认 promotion。

## 当前默认入口

- 默认前门是 `opl`；`opl --help` / `opl help` 展示 OPL Framework 自有命令树，`opl exec` 负责一次性请求，`opl exec --help` 保留 Codex-compatible 执行器帮助边界，`opl resume` 负责续接会话。
- `opl install` 是当前一键安装入口，负责安装或复用 Codex CLI、Temporal-backed family runtime provider、MAS、MAG、RCA、OPL Meta Agent、推荐 skills 和 App 入口。
- `opl system` / `opl system initialize` / `opl system startup-maintenance` 管理 Codex CLI、provider profile/readiness、module install/update、skill sync、managed environment freshness、plugin cache freshness、reload prompt 和 local runtime state。
- `opl framework readiness --family-defaults --json` 是 family readiness 动态真相入口；它只输出 framework/operator 读面，不授权 domain ready、artifact authority 或 production ready。
- `opl stages readiness --family-defaults --json` 是 stage readiness family drilldown 入口；单仓诊断继续使用 `opl stages readiness --domain <domain> --json`。
- `opl runtime app-operator-drilldown --json` 与 `opl runtime app-operator-drilldown --detail full --json` 是 App/operator drilldown 入口。
- `opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json` 是 runtime safe-action evidence worklist、stage evidence workorder packet、stage replay missing receipt attention packet 与 domain-dispatch evidence workorder packet 入口；其 direct stage readiness 聚合使用 bounded live refresh 与 projection cache fallback 维持 operator visibility，一切 cache-derived attention 仍不授权 domain truth、owner receipt、artifact authority 或 production ready。`family-runtime production-closeout` 已退役，不再是 active interface 或兼容 alias。
- `opl work-order execute --work-order <developer-patch-work-order.json> --json` 是 owner-gated developer patch work order 的唯一 canonical OPL 执行原语；Agent Lab 只消费该原语产出的 execution receipt、execution plan/report refs 与 re-evaluation refs，旧 `opl agent-lab execute-work-order` 兼容 alias 已退役。

## 当前不能声明

- 不能声明 OPL 已全量生产可用。
- 不能声明 Temporal provider proof 等于 MAS paper closure、MAG grant readiness 或 RCA visual ready。
- 不能把 `stage_evidence_workorder_packet`、`domain_dispatch_evidence_workorder_packet`、refs-only receipt verified、provider/SLO satisfied 或 tail item closed 写成 domain owner-chain、App 用户路径、release/dist、artifact authority、expected receipt instance、monitor freshness 或 long-soak evidence 已全部闭合。
- 不能声明 private functional audit 分类完成就等于物理代码路径清零。
- 不能把 `agents legacy-cleanup apply` 的 dry-run / apply / verify ready 状态写成 OPL 已删除 domain repo 文件或 production evidence 已闭合。
- 不能为了兼容保留旧模块、旧接口、旧测试、旧 CLI alias、facade 或 wrapper；active caller 迁走后直接删除或进入 history/tombstone。

## 参考入口

- [文档索引](./README.md)
- [项目概览](./project.md)
- [架构](./architecture.md)
- [硬约束](./invariants.md)
- [关键决策](./decisions.md)
- [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)
- [OPL 与 Foundry Agents 理想目标态](./references/runtime-substrate/opl-family-agent-ideal-state.md)
