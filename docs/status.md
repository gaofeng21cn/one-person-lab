# OPL 当前状态

Owner: `One Person Lab`
Purpose: `status`
State: `active_truth`
Machine boundary: 本文是核心人读真相面。机器真相继续归 `contracts/`、source、CLI/API 行为、runtime ledger、provider receipt、domain-owned manifest 和真实 workspace / App evidence。

Plugin native profile pointer: `contracts/opl-native-profile.json` 只声明 OPL Flow / OPL Doc 插件同步与 drift 检查所需的 repo-native profile；它不是 framework truth、runtime truth、domain truth、artifact authority、owner receipt 或 production-ready 证据。

Stage 内质量循环当前结构读法：Framework 已新增真正的 Temporal `StageRunWorkflow` 父级 controller，并以独立 `StageAttemptWorkflow` 动态执行 `producer/reviewer/repairer/re_reviewer`；正式 Review/Repair/Re-review 均要求 fresh Codex thread/session，Re-review按 finding closure，默认最多三轮 Repair + Re-review。StageRun 只由 `family-runtime attempt create` 消费 pack compiler 生成的 `opl_pack_bound_stage_quality_runtime_binding` 创建，manifest SHA参与 identity；raw `stage-run start` 已退役且 `stage-run` 只保留 query。same-thread `protocol_closeout_resume` 只在 read-only sandbox 中补既有 Attempt closeout，不算 Review 或质量轮次。producer / repairer 的本地 artifact 由 OPL transport 按最终 bytes 生成 content-addressed identity receipt，外部 artifact 使用独立 domain identity receipt；二者的 exact ref/SHA 才能进入 review receipt。质量预算、provider/activity/structured-output/runtime retry分账；有可消费产物的预算耗尽投影为 `completed_with_quality_debt`，真实 hard-stop evidence 保留 class、typed blocker/human gate 与来源 Attempt，不得降级为 quality debt。`stage_quality_cycles` 只是可重建 SQLite drilldown，Temporal history/query仍是 durable truth。MAS/MAG/RCA/OMA/OBF 的 domain adoption、顶层独立 Meta Review 和领域特定 route-back 由各自仓 source/contracts/tests 持有；这些非 live 结构证据不声明真实项目已运行、domain ready、artifact ready、publication/export ready、App release ready 或 production ready。

Standard Agent 边界收敛当前读法：Console project progress 已收薄为 normalized domain operator projection consumer，不再从 MAS workspace cockpit、paper path、clinical field 或专用 gate 推导进度；domain-specific summary 必须由 domain manifest projection 提供。Runway default-executor recovery 不再包含 MAS fallback，owner 只能由 execution / stage packet / attempt 显式字段或 standard-agent registry 解析，缺失时保持 `owner_unresolved`。Temporal production proof 的 synthetic workflow 使用 `example-domain` fixture。`AgentExecutionRequest.required_capabilities` 当前只支持 Codex `image_generation` activation；未知 capability 和非 Codex executor 均 fail closed，receipt 回读 requested/activated capability，但图片 prompt、产物落点、digest、figure manifest 和 artifact authority 仍归消费该 transport 的 domain owner。上述均为 source/contract/test 结构口径，不声明 provider ready、domain ready、artifact ready 或 production ready。

Standard Agent hosted action 当前读法：`family-action-catalog.v2` 将 action 执行绑定固定为 closed `handler_ref` / `stage_binding` union，`domain-handler-registry.v1` 只声明 TypeScript export 或 Python callable；`entry_command_template`、`manifest_command_template`、`runtime.dispatch_command` 与 action `source_command` 已退出标准接口。`opl agents run --domain --action --workspace` 与每个新 child Attempt 都静默解析最新可运行 generation：普通用户从 `latest-stable` 更新渠道取得，Developer Mode 从可信本地 checkout 取得；新 generation 的发布/下载 admission 仍严格校验 ABI、payload 和 digest，失败时不采用该 generation，而是自动继续使用最近验证可运行的 LKG。version、Git SHA、digest、lock、receipt 与 currentness 只作 provenance/观察，不因相互漂移而拒绝执行；只有 current 与 LKG 都不可运行、必需 ABI/export/module/Skill 缺失、路径/权限/安全失败或 health/handler probe 真失败才硬停。每个 Attempt 绑定自己解析到的不可变 generation，运行中不热换；后续 package 更新不会使既有论文、图表、artifact 或历史证据失效。Standard Agent 的 workspace `direct_entry.manifest_command` 只作为 migration diagnostic 投影，过期或已删除的命令不能覆盖 managed contract，也不能让 action list 归零。Handler 输入/输出均按 repo schema 校验并在只读 sandbox 执行；Stage action 先固化 canonical request bytes，再把 request ref/SHA 绑定到 Temporal StageRun。Workspace 保存 exact request/output bytes，Ledger 只保存 ref/SHA/byte-size；Stage `started` 不等于 domain completion，任何 hosted result 也不授权 OPL 写 domain truth、签 owner receipt、创建 typed blocker 或声明 domain/quality/export/production ready。

OPL Flow closeout gate: `contracts/opl-framework/opl-flow-completion-audit-contract.json` 是目标态交付的机器化后验证合同。凡 claim scope 属于 `thorough_landing`、`complete_execution`、`end_to_end_acceptance`、`production_ready_claim` 或 `release_ready_claim`，closeout 前必须给出逐项 plan completion audit、完成度百分比、fresh evidence refs、missing refs、next action 和 subagent/worktree lane mapping。docs/catalog/plan/read-model/refs-only surface/contract/test/commit-only evidence 不能单独支撑 `100%` 或 complete claim；仍有非 100% 条目时只能继续推进，或在明确 owner / source of truth / 权限 / 外部依赖 / 验证边界 blocker 下输出 typed blocker。

Currentness / CI hygiene 当前读法：调研或历史 closeout 中的本地 ahead/behind、dirty file、queued workflow、failed workflow、run id、SHA 和 fetch failure 都不是可继承事实。每轮 currentness、CI、latest、synced、release 或 sibling-repo 状态 claim 必须 fresh-read owning repo：`git fetch` / `git status --short --branch`、可用 remote（Aion shell 需要在 SSH 失败时读取 `gh-https`）、GitHub Actions 最新同 surface run、repo-native focused tests，以及 App/tap/release owner readback。历史 readback 只进入 `docs/history/**`、runtime ledger、owner repo provenance 或提交历史；`docs/status.md` 只保留 owner route、false-ready guard 和 live-read 入口。MAS、MAG、RCA、OMA、OPL Book Forge、App、Homebrew tap、Aion shell 和 MAS Scholar Skills 的 currentness / release / domain readiness 仍回各自 repo。OPL status 不把 sibling repo 的 clean-current、focused tests、docs foldback 或 push 成功外推为 release-ready、domain-ready、paper-progress、visual-ready、Brand L5 或 production-ready。

GUI route policy 当前读法：One Person Lab App 的 GUI 主线固定为 `one-person-lab-app` 消费 `opl-aion-shell` 的 OPL-branded AionUI shell；`opl-native-workbench` 是 App-owned foreground alternative GUI candidate；Hermes Desktop / `hermes-codex` 是 retained explicit reference candidate；AG-UI/CopilotKit / `agui-codex` / `opl-agui-codex-shell` 只作为 archived technical proof 与显式 replay surface 保留。除非用户明确要求 AGUI replay 或历史技术验证审计，OPL 主仓、App 仓、AionUI shell 仓和 AGUI shell 仓都不应继续把 AGUI 纳入默认开发、验证、polish、release、adoption 或功能面落地路线。App GUI product truth、candidate policy、active-shell validation 和 release gate 继续归 `one-person-lab-app`，OPL Framework 只记录消费边界和 runtime/action projection。AionUI shell 是上游 AionUI fork 的实现 carrier：OPL 可维护 App adapter、OPL overlay、packaging/readback hooks 和 owner 明确的最小验证，但 upstream fork body 默认只读，不再进入测试瘦身、结构重写或实现清理。AionUI 和 Native Workbench 的功能面完成口径是消费同一套 App / Framework canonical `app_state`、`action_refs` 和 package/runtime/task projection；二者都不能把 fallback 推断成 runtime ready、package installed/executable、task truth 或新的 action id。缺 App bridge 或缺 action ref 时只能显示 `unavailable`、`preview_only`、`payload_required` 或等价不可执行状态。

Live Evidence 后置 / 功能结构优先是当前 OPL family 日常开发原则。普通开发优先关闭功能面与结构面缺口：source / contract / CLI / API / readback / App shell 可用，generated / hosted surface 消费清楚，私有 wrapper / facade / compat residue 退役或 tombstone，no-second-truth 和 false-ready guard 可执行。Live Evidence、production evidence 和 L5 evidence 仍是最终 readiness / release / production / 品牌成熟度验收所必需的后置 lane，包括 owner-chain live scaleout、provider long-soak、Brand L5 operating evidence、App release cohort、真实用户路径、跨 agent scaleout、大型 live ledger refresh、真实论文 / 项目运行证据。它们不得在默认开发中阻塞可独立完成的功能/结构清理，也不得被 docs、refs-only ledger、contract pass、test pass 或 projection clean 替代为 ready claim。

OPL / Domain 边界收口当前读法：generic Framework surface 必须使用 `domain_*`、`stage_*`、`owner_evidence_*`、`artifact_*` 等通用词汇；MAS / MAG / RCA 名字只允许出现在 first-party fixture、compatibility profile、domain manifest mirror、professional Skill pointer 或 history / provenance。`contracts/family-orchestration` 的通用 stage / replay / graph / proof / registry / event / checkpoint / conflict / runtime-budget examples 已按 `example-domain` generic vocabulary 收口，并由 focused guard 防止 MAS / paper / publication / medical / RCA residue 回到 generic examples。Fixture-aligned product-entry / human-gate / domain-memory / runtime-supervision 示例和 MAS Scholar Skills packaging mirror 不能再被误判成 OPL 缺少 domain Skill，也不能被反向写成 OPL 持有 domain truth、quality verdict、owner receipt 或 readiness authority。

Runway / runtime environment 当前读法：OPL 当前环境策略分三档，默认档是 `Fast Local Env`，用于当前 R / Python / MAS 画图等本机高频路径；它通过 environment profile、compiler、doctor 和 run-context 管理语言依赖、OPL-managed R library、OPL-managed Python uv environment、runtime refs 与 no-host-fallback consumer preflight，不要求 Docker 作为全局硬依赖。第二档 `Local Sandbox / Docker` 是需要更强隔离或复现时的后置 provider，显式 profile/preflight 后才承担 local workspace / process / network isolation。第三档 `Remote Sandbox / E2B` 是当前唯一已实现的后置 remote provider，必须显式选择并保留 credential-ref、provider-receipt-ref 与 no-secret-log guard；其他外部 compute provider只作为 Connect discovery / generic external adapter候选，不构成 Runway executor support。Temporal 继续作为 production online durable workflow / wakeup / retry / human-gate substrate；sandbox provider只承接 selected stage executor 的隔离执行环境，不替换 OPL Framework、Runway 或 Temporal。`OPL Connect` 只在需要时承担 provider discovery、configuration、package/install 或 connector分发辅助，不拥有 stage execution sandbox。Runtime environment substrate 收敛为 env profile / compiler / doctor / run-context / descriptor / lock / bundle / receipt / cache 边界，不扩张成自研 VM/container sandbox。Fast Local Env doctor/run-context、Docker/devcontainer run、E2B credential run、provider long-soak、App release cohort 和真实用户路径仍是不同证据层；docs foldback、contracts、focused tests、materialization receipt、cache hit、doctor pass、run-context exists、mocked local Docker path 或 mocked E2B path 都不能替代为 runtime ready、provider ready、domain ready、App release ready 或 production ready。

Docs gap lifecycle 当前读法：理想态文档继续定义目标和不变量，不因实现已落地而降低标准；`docs/active/current-state-vs-ideal-gap.md` 是 active work tracker，不是完成史。默认 OPL family maintained repo 的非 live 功能/结构基线折回 `functional_structure_baseline_landed` 后，active gap 文档只保留当前 no-gap 判断、后置 live evidence 指针、forbidden claims 和下一轮 fresh-audit baton。已完成的 gap 调研、规划、closeout、branch/worktree、receipt 流水和验证过程进入 `docs/history/**`、runtime ledger、owner repo provenance 或提交历史，不再污染 active gap / ideal-state / active development 文档。

标准 Agent MCP / Codex carrier 当前读法：MAS/MAG/RCA/OMA/OBF 不默认暴露 per-agent standalone MCP server，Codex App 可见面统一来自 repo-local plugin carrier、primary skill 和 OPL-generated interface surface。标准 agent 的 Codex plugin 物理结构固定为 `agent/primary_skill/SKILL.md` rich source 加 `plugins/<plugin_name>/skills/<plugin_name>/SKILL.md` full-copy carrier；OPL Connect 只校验、复制到本机 marketplace/cache 并注册 Codex config，不生成第二份 skill body。`opl connect skills --json` 的 `skill_catalog.packs[].mcp_projection` 是当前机器读面；关键字段应显示 `standard_agent_standalone_mcp_default_enabled=false`、`unified_mcp_projection_owner=one-person-lab` 和 `cli_mcp_relationship_policy.all_cli_commands_are_mcp_tools=false`。未来 unified MCP server 属于 OPL Connect 的后续 runtime/server lane，必须先证明 toolsets、progressive discovery、lazy schema、read-only default 和 owner-gated mutation；当前 contract/test/readback 只证明 policy 和 projection landed，不声明 MCP server runtime ready。

Standard Agent AI-first Principle Pack 当前读法：OPL 只持有跨标准 Agent 通用原则，Domain 持有领域特化。`docs/policies/standard-agent-ai-first-principles.md` 与 `contracts/opl-framework/standard-agent-principles.json` 分别固定人读原则和机器边界；标准 domain 仓通过 `contracts/standard-agent-principles-adoption.json`、`agent/principles/opl-standard-agent-principles.md` 和 `agent/principles/domain-specialization.md` 声明 adoption 与领域映射。原则包固定 AI-first / contract-light、stage 主提示词优先、capability_kind、workspace/source intake shell、owner answer shape、generated/hosted surface、no-second-truth 和 false-ready guard；MAS/MAG/RCA/OMA/OBF 继续持有 domain intake mapping、source semantics、专业 Skill / knowledge / rubric、quality/export/review verdict、artifact authority、owner receipt、typed blocker 和 human gate。`intake` 不成为独立 OPL Skill；通用 intake shell 只定位 refs、scope、fingerprint、projection 和 owner route。`opl workspace source ingest` 现在承担用户文件/PDF/参考设计的 domain-neutral refs-only intake：复制 source material、计算 sha256、写 receipt，并把 source refs 交给 Codex/domain stage；文件语义提取、论文架构理解、医学/写作判断仍归 OMA/MAS/BookForge 等 domain stage。docs、read-model、contract 或测试通过都不能声明 standard-agent complete、target-agent ready、domain ready、Brand L5、App release ready 或 production ready。

Evidence-Grounded Decision Agent Profile 当前读法：这是标准 Foundry Agent 的横切 decision-support profile，不是医学/血液病 agent、不是第 11 个品牌模块，也不是 production/live ready 声明；profile catalog、standard scaffold 和 refs-only template 只作为 lower-bound guardrail、physical skeleton 与 refs-only shape，不是 target-agent design source 或模板库上限。它的非 live 功能/结构基线从 profile contract、module-owned source、CLI/readback 和 focused conformance evidence fresh-read。Conformance 缺口只关闭 profile accepted/promotion/ready 声明；目标 stage 的 raw、partial、negative、failed、损坏或零输出都以 artifact 或 diagnostic 进入后续 declared stage。无证据、低置信、冲突证据或缺 provider/tool receipt 形成质量债；只有 selected executor 不可用、sensitive external egress 未批准、wrong-target identity/currentness、安全/权限/authority、不可逆动作或明确 human decision 才硬停。最终 decision、domain verdict、quality/export verdict、artifact authority、owner receipt、typed blocker、human gate、App release ready、Brand L5 和 production ready 仍回对应 owner。

Source-derived build 当前非-live 功能闭环进一步要求：`AgentPackPlan` 中论文迁移 stage 与 target-only stage 全部独立物化且恰好一次，DesignAdmissionReceipt 覆盖两类准入，stage control plane 拒绝未计划或 origin 不一致的 stage；正式 `AgentBuildReceipt` 在写盘后生成并携带完整 materialized stage ids、计划文件存在性和 SHA-256 digests。预物化 draft 不再携带 receipt body。该证明只说明结构物化与 source-use conformance，不等于 target domain ready、owner acceptance 或 Live Evidence。

Profile capability planning 当前已落为 `opl profiles capability-plan --selection-file <path> [--catalog-repo <owner-repo>] --json`：它复用 Connect 的 exact capability resolver，只读取 canonical selection receipt、Framework-owned `profile_capability_plan_input` 固定投影与显式 owner repo 的 schema-valid `contracts/capability_map.json`，并把 owner contract/root fingerprint、exact resolution、optional miss、route-required blocker candidate，以及 dependency / environment / descriptor / Pack lock 条件化 action refs写入同一 refs-only readback。专业能力包通过 owner capability map 和 Connect package/source refs进入，不把 owner module body、legacy id catalog、固定 commit/fingerprint 或 parity validator复制回 OPL。默认不扫描本机 Skill/HOME，不自动 search/sync/install/download/cache，不写 descriptor/lock，不声明 dependency、environment、target、domain 或 production ready。OMA 若不提供 catalog repo仍可得到无副作用计划与 optional miss；若显式提供 owner repo，则 exact refs与 provenance进入 plan fingerprint。

JavaScript Framework 只有 OPL 根包 `opl-framework`：`foundry-agent-series-policy`、`reference-design-pattern-packet`、`json-schema-registry`、`reference-build-proof` 和 `standard-agent-pack-abi` 均为 OPL 本体的 public subpath；标准 Agent action 通过 `opl agents run` 进入 package-managed hosted runtime，不再暴露离线 readback evaluator subpath。标准智能体由 OPL 安装流程托管解析，不声明 Framework dependency、不持有 lock receipt，也不维护独立 implementation release channel；policy fingerprint 只约束合同语义。

同一 profile 的配套结构读法是：Runway route handoff 和 owner-wait projection 必须显式返回 domain progress、domain ready 与 quality verdict 均不可 claim；research-frontier / frontier-board compatibility carrier 只能投影成 `stage_candidate_portfolio_refs_projection`；Connect scientific / PubMed / references verify 输出必须标明 optional scientific connector profile、provider receipt candidate、metadata-only verification role 和 citation/claim-support no-authority flags；dependency doctor 只作为 generic dependency profile envelope，profile 与依赖清单来自 package / domain descriptor，不把环境 ready 写成 domain ready、publication ready 或 final export authority。

Domain private platform tail matrix 当前读法：Phase 7 的机器入口是 `contracts/opl-framework/domain-private-platform-tail-matrix.json`。它只把 MAS/MAG/RCA/OMA/OBF/MAS Scholar Skills 的 private tail class、replacement OPL primitive、retained authority、delete/tombstone gate、forbidden claims 和 verification surface 做成 refs-only seed；focused test 只验证 seed 完整性和 false-authority boundary。物理删除、owner acceptance、domain ready、runtime ready、publication/export ready、App release ready 或 production ready 仍必须回 fresh `opl agents default-callers --family-defaults --json` / `opl agents conformance --family-defaults --json`、domain repo-native readback、owner decision ref、tombstone/provenance ref 与 no-forbidden-write proof。

标准化智能体迁移主题的当前人读入口是 `docs/active/standard-agent-migration-owner-map.md`，它只保留 Framework generic substrate 与 MAS/MAG/RCA/OMA/OBF domain authority 的 owner 路由，并把当前 gap 与 baton 指回唯一 Active Truth。2026-07-13 的 26 项范围、计数、逐项 Plan Completion Audit 和 closeout readback 已归 `docs/history/process/plans/2026-07-13-standard-agent-migration-owner-map.md`，只能作为 provenance；当前机器真相必须从 contracts、source、tests、`opl agents * --json` 与各 domain owner surface fresh 读取，不能由 redirect 或历史计划声明 live evidence、owner acceptance、runtime ready、domain ready、App release ready 或 production ready。

标准 Agent repo 结构当前读法：标准 OPL 智能体 repo 的目标形态由 `docs/policies/standard-agent-repo-structure.md` 固定为 `Declarative Domain Pack + OPL generated/hosted surfaces + minimal authority functions`。Domain repo 的 `agent/principles`、`agent/prompts`、`agent/stages/manifest.json`、`agent/skills`、`agent/tools`、`agent/knowledge` 与 `agent/quality_gates` 是 Stage 能力的 canonical source；OPL Pack 从这些声明生成 `family_stage_control_plane`，不读取或回退到 repo-tracked `contracts/stage_control_plane.json`。workspace / quest 下的 `.codex/skills/` 只是 Codex discovery/兼容投影，不是 canonical source，也不是 hosted Attempt 的执行真相；Attempt 从自己绑定的只读 `.agents/skills` generation 读取 Skills。尚未迁移到 manifest 的 first-party Agent 必须由各自 owner 明确 stage graph、trust lane、transition 与 domain-only refs 后再删除 legacy plane；OPL 不做启发式转换。该读法不声明 live domain progress、domain ready、quality/export ready、production ready、App release ready 或 owner accepted。

Standard Agent Interface 当前结构已落地：`contracts/opl-framework/standard-agent-interface.schema.json` 和 `src/kernel/standard-agent-interface.ts` 只定义/校验统一 ABI；domain-owned descriptor 提供 workspace defaults/locator、runtime registration ref、progress alias 与 routing signal，不再携带 entry/manifest/dispatch command template。package-managed discovery 只接受 `runtime_source_readiness=current`、tree digest readback 一致且 package operational ready 的 managed source；stale tree、缺 lock、dependency/scope 不闭合或 descriptor identity 不符时不消费。Atlas 不再硬编码医学/grant/visual/agent-building/book 关键词；Workspace 不再维护 MAS/MAG/RCA 私有 materializer，也不再从 descriptor 派生命令；progress/closeout 默认字段只保留 `deliverable_progress_delta` 与 `platform_repair_delta`，domain alias 从 descriptor 读取，attempt/Temporal contract 不再内置 `paper_stage_log` 或 MAS/MAG/RCA alias 表。registry 已收薄为 identity/package locator，Workspace 缺 descriptor 时统一使用 generic baseline。以上是结构/功能关闭，不是 Live Evidence、domain ready、owner acceptance、App release ready 或 production ready。

OPL Cloud / Workspace 产品语义当前读法：当前必要用户工作面是 App desktop + Docker/WebUI。`OPL Cloud`、在线 Workspace、Console/Gateway 和托管运行体验是长期、条件启用的用户可见产品包装，只有真实 account、storage、isolation、backend 与 owner policy 齐备时才出现，不是 Framework 第 11 个源码模块，也不是当前 runtime/App release gate。条件启用的在线 `OPL Workspace` 可以消费 `workspace`、`console`、`connect`、`ledger`、`runway` 等多个 Framework 模块；Framework `OPL Workspace` 模块只持有 workspace protocol、Project Unit、Stage Artifact Unit 和文件生命周期投影，不持有 Cloud product truth、artifact body、quality verdict 或 owner receipt。

OPL Connect / Fabric 当前读法：Fabric 是通用资源底座语义，不是当前 source-module owner；Connect 是 Fabric 上可独立调用的连接能力，不是 Console 私有后端。Connect core 只拥有 connector interface、credential boundary、provider receipt、cache/retry metadata、package registry / manifest / lock / lifecycle receipt 和 no-authority flags。`opl connect scientific search --provider crossref|openalex --query <query> --json` 与 `opl connect references verify --references-file <json> --providers crossref,openalex,pubmed,pmc,semantic-scholar,crossmark,publisher --json` 提供 provider evidence、cache/retry metadata 和 normalized refs；PubMed 使用 NCBI ESummary，PMC 使用 Europe PMC metadata，并把 provider 的全文可用声明与实际 XML body 校验分开；Publisher 只做 DOI resolver landing page metadata lookup。OPL 不恢复 `connect pubmed` compatibility command。provider invocation、retry/cache 与 receipt transport 归 Connect；医学 query strategy、metadata 解释、引用取舍和 publication authority 归 MAS / MAS Scholar Skills。这些 connector 不伪装成 reference truth，也不替代 literature retrieval judgment、citation support map、claim-evidence review 或 owner-gated publication authority。`opl packages registry refresh --registry-url <url> --json`、`opl packages validate-manifest ... --json`、`opl packages install ... --json` 和 `opl packages list --json` 是专业智能体 package registry / manifest / lock / receipt 的 Framework 入口：registry 只做 discovery，安装 authority 来自 validated manifest + Framework package lock receipt，输出固定 no-domain-authority boundary，并写入 `OPL_STATE_DIR` 下的 registry cache、package lock 和 lifecycle ledger。`opl app action execute --action install_from_manifest_url` 只委托该 Framework writer。它们可以被本机 OPL App、在线 Workspace、CLI 或 MAS 等 domain agent 消费；Console 只做组织治理、权限、配额、审批和展示。引用质量、claim-evidence map、review ledger、论文写作质量、agent prompt/workflow、owner receipt、typed blocker、publication-ready 和 domain-ready 仍归 MAS / domain owner。

OPL foundation boundary cleanup 当前读法：OPL core 应只保留 stage、candidate refs、stage-run evidence refs、owner route、receipt、replay、artifact lineage、connector receipt、workspace/source/artifact/memory locator 和 operator projection。任何以 `research`、`hypothesis`、`publication`、`fundability`、`visual-quality`、`medical` 等 domain 语义命名的 core surface 都必须被收薄、改名或迁出：能泛化为基座投影的，改回 `stage-*` / `domain-* refs` vocabulary；不能泛化的，迁回 MAS/MAG/RCA/OMA/OBF 或对应外置 Skill / connector pack。新增 canonical API、contract、readback 和 docs 必须使用 `domain_*`、`stage_*`、`owner_evidence_*` 词汇；domain-specific profile 可作为 active-caller compatibility 暂留，但不得重新定义 OPL core ontology。2026-07-08 的实现级收口已把 `connect external-skills` 泛化为 external specialist registry，把 Runway MAS route/autonomy 读回 profile 化为 `domain_route` / `domain_progress`，把 Stagecraft owner-answer / visual transition 和 Console MAS current-work-unit / workbench projection normalize 到 generic domain profile projection；旧 MAS/RCA/K-Dense 名字只作为 compatibility surface/source/profile 保留。PubMed/Crossref/OpenAlex、MAS Scholar Skills thin mirror、stage-candidate portfolio 和 owner-evidence sustained consumption 的收口清单见 [OPL Domain Boundary Cleanup 2026-07-07](./references/current-support/opl-domain-boundary-cleanup-20260707.md)：这些都不是缺 domain capability 或 domain truth 的证据。当前 docs/read-model 只能声明这一功能结构边界，不声明 domain ready、publication ready、grant ready、visual ready、Brand L5 或 production ready。

2026-07-09 追加读法：`stage-selection-vocabulary` 的 canonical vocabulary 只能保存 generic / profile-driven stage words；`grant_direction_assessment`、`grant_ops`、`medautogrant`、`redcube`、`publication` 等旧 domain 值只能出现在 compatibility alias 或 profile example。Kernel managed-shell clean-runner 的 MAS/MAG/RCA profile 也只能作为 package / descriptor / profile 数据读取，不能重新写回 OPL core 固定数组或被解释成 OPL 拥有 domain execution semantics。`canonicalOwnerId` / owner-id normalization 是 repo/package owner locator 归一化，不是 standard-agent canonical id 规则；standard domain agent canonical ids 仍固定为 `mas`、`mag`、`rca`、`oma`、`obf`。

Standard Agent boundary adjustment 当前读法：标准 domain agent 集合从 `src/kernel/standard-agent-registry.ts` 派生，canonical ids 统一为 `mas`、`mag`、`rca`、`oma`、`obf`；`opl-meta-agent` 与 `opl-bookforge` 只作为 OMA / OBF 的 repo、package、plugin、alias 或 carrier 名保留，不能替代 `oma` / `obf` 成为 standard-agent canonical id。`mas-scholar-skills` 是 `framework_capability_package`，不是 standard domain agent，也不是 domain-agent physical-delete 或 default-caller gate 对象。2026-07-08 边界调整已修正旧 root-level registry / projection pointer，并记录每个标准智能体的上收 / 留存矩阵，见 [OPL 标准智能体边界调整 2026-07-08](./references/current-support/opl-standard-agent-boundary-adjustment-20260708.md)。`opl agents default-callers --family-defaults --json` 的空 deletion worklist、refs-only evidence 或 closed retirement gate 只表示当前没有 OPL 可执行删除项，不等于 delete-ready；physical delete 仍需 domain owner 的 delete / keep / typed-blocker decision。该调整只证明 source pointer 与人读边界收口，不声明任何 domain ready、owner acceptance、physical delete authorization、App release ready 或 production ready。

Agent Package Manager 当前读法：OPL Agent Package 不是 Codex Plugin 的同义词，Codex Plugin 只是 carrier 之一。OPL Connect 的 package core 持有 `id/version/digest/dependencies/trust/lock/lifecycle receipt/exposure/shortcut`，但除 package id 外，version/digest/lock/receipt/currentness 都是 provenance；标准 Agent 的 Codex carrier、runtime source carrier、Capability Pack 和 App shortcut属于同一 `opl packages` 闭包事务。每个 hosted action 和新 child Attempt 静默解析 latest runnable generation，`runtime_source_readiness` 以所选 current/LKG generation 的真实 ABI、必需模块/Skill、health 与只读 handler probe 为准，不要求 provenance 与后来 source 相等。source 换代先写 content-addressed immutable generation 与 durable marker，再原子发布 pointer；既有 generation 和 developer checkout 不覆盖、不删除，事务期间不自动 GC，显式 maintenance 只能清理无活跃引用且非 current/LKG 的 generation。package lock/receipt 是 provenance commit point；下次 status/action 自动 rollback 未提交 mutation或 finalize 已提交 cleanup，`cleanup_pending` 可重入且不把 durable success 报成失败。runtime preparation env 位于 OPL state root，不进入 source checkout。Agent manifest / lock 不声明 Framework dependency，也不安装第二份 Temporal tree。`opl packages link-framework --agent-root <repo> --check --json` 是诊断，去掉 `--check` 才执行修复。OPL App 可以管理、安装、更新和展示 package，但只消费 Framework refs 与 action refs，不拥有 package truth、domain semantics、domain truth、owner receipt、typed blocker、quality verdict 或 artifact authority。Package core 的收薄遵循删除无语义 wrapper、复用标准库与既有 Framework primitive 的稳定原则；完成度只从 source、contracts、tests 与 fresh `opl packages ... --json` readback 判断，不维护静态计数或 blocker 快照，也不退回 Codex Plugin-only。

Agent Package 公共目录与 App status 当前读法：`opl packages list --json#opl_agent_packages.directory.entries[]` 与 `opl app state --profile fast|full --json#app_state.agent_packages.directory.entries[]` 是唯一可浏览目录集合，不另造 installed-only collection。directory `available_actions[]` 才是严格五字段 `{action_id,action_ref,payload,required_payload_fields,confirmation_required}` 的可执行 action；direct-manifest install 同时声明 `manifest_url` 与 `trust_tier` 为 required payload。canonical first-party identity 只归 Framework-owned Release Set catalog；外部 registry/catalog collision typed fail，stale cache collision 不覆盖 built-in metadata/version/manifest/trust。`app_state.agent_packages.status_index.packages[package_id]` 把 raw readiness 归一为 App-facing `dependency_readiness`，并投影 provenance observations、真实 hard-stop reason、capability exposure、receipt 与 rollback refs；version/SemVer、closure digest、carrier lineage、receipt、reload 或 policy currentness 漂移不得生成 `repair_required`、`attention_needed` 或 activation recommendation。receipt/rollback refs 只用于 ordinary provenance/recovery 展示，不证明 reload 或 rollback 已执行；closure 报告 current 与 LKG generation，不伪造 identity。单个 status read 失败投影为 readback unknown/unavailable，但不能撤销已有 LKG 或成为 hosted execution 拒绝；disabled package 保持不可执行。fast/full 共享这些 canonical 字段，fast 对尚未 live 验证的正向 readiness 保持 `verification_deferred`，full 只增加 owner detail；App/Shell 必须按 package id 消费这些投影，并仅通过 directory action ref 执行 mutation，不得从 status `command_ref`、raw status、旧 workspace binding 或静态 allowlist 推断。该功能面已有 source/focused-test 证据，但不代表本机 Stable 安装、真实 HOME migration、package launch、live rollback 或 release promotion 已完成。

OPL Flow package 当前读法：`opl packages install|update|rollback opl-flow` 复用唯一 Agent Package registry/materializer/lock/lifecycle ledger。manifest 的 `managed_policy_surface` 直接消费 owner policy/schema；policy requires 只校验 Base prerequisite，online recommends 进入内部 dependency/integration sync，compatible optional 不默认安装，conflicts/retires 通过 fresh typed inventory 备份并移出 discovery。status 的 `materializer.managed_policy_migration` 投影 policy/inventory digest、dependency ids、migration actions、backup 与 model recommendation role；不存在 workflow 专属 checkout、state root、receipt、rollback 或第二 readback。该状态只证明 package transaction currentness，不声明 Base/runtime/domain/release/production ready。

Framework source-module 当前读法：`src/modules/<module_id>/` 是十个 Framework 模块的物理 source owner；`OPL Fabric`、`OPL Cloud`、在线 Workspace、Console 页面和 Gateway/API 是产品/Cloud 语义，不形成第 11 个源码模块。源码边界的机器入口是 `contracts/opl-framework/source-module-map.json`、`contracts/opl-framework/module-dependency-policy.json` 和 `scripts/source-module-boundary.mjs`；维护者说明见 [OPL Framework 源码模块边界](./references/source-module-boundary.md)。Fresh `npm run source:modules -- --strict-imports --strict-cycles` 通过时，完成口径是“物理归位 + public entrypoint 硬门 + deep import 清零 + public-entry dependency cycle 清零”。第一批 owner-alignment 已把通用 helper、source-ref normalization helper、Framework shared error vocabulary 与 family action catalog shared contract 收进 `kernel`，App release / user-path evidence ledger 收进 `Ledger`，stage replay missing receipt workorder 收进 `Stagecraft`，stage attempt 通用投影与 runtime snapshot provider injection 收进 `Runway`，并把 `ledger -> runway`、`stagecraft -> runway`、`workspace -> console` 等第一批方向穿透写入 forbidden dependency policy。当前 Runway / Foundry Kernel 不再在 source-module readback 中依赖 Console；非 Charter 模块也不再因为只需要 `FrameworkContractError` 而依赖 Charter public entrypoint。Console 仍拥有 App/operator snapshot projection，由 CLI/App/Console caller 显式注入给 Runway / Foundry consumer。2026-07-04 的模块内聚收薄已把 Console、Connect、Runway、Charter、Ledger、Foundry Kernel 第一批超 1000 行目标文件拆入各自 owner 的 parts / types / helper 文件，当前 `src/modules/**/*.ts` 行数读回已无超过 1000 行的文件；仓库级 `line-budget:strict` 对剩余非模块源码 / 测试长文件使用 reviewed baseline 做 ratchet，防止继续增长。该口径证明 source owner、import gate、依赖环硬门和第一批文件边界治理已进入可执行维护状态；public API 最小化、pair-count 热区和测试 fixture 继续作为后续质量治理，不声明 runtime ready、release ready、Brand L5 或 production ready。

MAS Scholar Skills 当前读法：外部 `mas-scholar-skills` package manifest 是完整 export catalog 与专业内容正文的唯一 owner；当前发布包声明 35 个 `skills/*/SKILL.md` exports，并单独声明 10 个 module contract ids。Framework 通过唯一 `opl packages` 生命周期把它作为 MAS required dependency 解析、安装、更新和保留 LKG；普通 UI 不提供独立安装入口，也不会在未安装 MAS 时把它作为全局默认 package。每个 hosted action 与新 child Attempt 在首次启动边界静默解析最新可运行的 MAS + provider generation，并把完整 35 Skills 写入该 Attempt 自有的只读 `.agents/skills` generation；workspace / quest 的 `.codex/skills/` 只作 Codex discovery/兼容投影。11 core + 10 modules 是 hard readiness floor，不是安装上限，module ids 不物化。新 generation admission 失败自动回退 LKG；同一 Attempt 重试不热换，新 Attempt 再追最新。只有 current 与 LKG 都真实缺少必需 ABI/export/module/Skill，或路径、权限、安全、health/handler probe 失败时才阻断；version、SHA、digest、lock、receipt 和 currentness 漂移只作 provenance。MAS stage prompt 与 runtime truth 留在 MAS；专业 Skill truth 留在 package owner。OPL package/use receipt 记录本次实际来源，不能声明 MAS paper progress、quality verdict、clinical data readiness、domain-ready、runtime-ready、owner receipt、typed blocker、artifact authority 或 publication/export readiness。

OPL Foundation Skills 当前读法：`plugins/opl-foundation-skills/skills/` 是 OPL-owned base / support Skill 的 source-only canonical 位置；当前 `plugins/opl-foundation-skills/exposure.json` 声明 25 个 active Framework support Skill 和 21 个 `no_regression_redirects`。原 progress-transition reviewer 已随第二控制面物理退役，不以 redirect、alias 或兼容 Skill 保留。


MAS route / currentness 的执行细节只从 live source、contracts、tests、runtime ledger、provider receipt、MAS truth 和 `family-runtime` / `runway` readback 读取。进入 OPL stage-attempt request/projection 的 PaperMission route request 必须 fail-closed 校验 study、transaction、route command、route target、route identity、request / NextAction idempotency 和 attempt idempotency；StageAttempt / provider running 状态必须有对应 provider proof。`stage_run_currentness_identity.idempotency_key` 表示 MAS request / NextAction identity，`attempt_idempotency_key` 表示 OPL StageAttempt / provider attempt identity；二者不得混用。domain-handler export timeout、route handoff、legacy consumption ledger、旧 workspace supersede、stale running projection 和 identity hardening 的 dated closeout 过程只作 history/provenance，不再复制到 status 主入口。历史归档见 `docs/history/**` 与提交历史。

OPL StageRun closeout readback 当前读法：`family-runtime attempt query|inspect` 与 current-control 投影只属于 passive transport/read-model projection；它不接受或拒绝 Codex route，不选择 next owner，不创建 owner receipt / typed blocker / human gate，也不声明 paper/domain/publication/submission/runtime/production ready。

OKF context bundle 当前已作为 OPL-owned advisory context / interchange surface 落地。机器入口是 `contracts/opl-framework/okf-context-bundle-contract.json`、`src/okf-context-bundle.ts`、`opl okf validate --bundle <dir> --json`、`opl okf inspect --bundle <dir> --json`、`opl okf project-pack --pack <pack_compiler_input.json> --output <dir> --json`、`opl okf project-repo --repo <domain_repo> --output <dir> --json` 和 `opl okf native-frontmatter inspect --repo <domain_repo> --json`。`project-pack` 只投影单个 Foundry Agent pack compiler input；`project-repo` 从 domain repo 的 `contracts/pack_compiler_input.json` 和可选 `contracts/memory_descriptor.json` 生成同一个 body-free OKF bundle；`native-frontmatter inspect` 只读取 domain-owned `agent/**/*.md` 的 OKF-compatible frontmatter readiness。当前 fresh readback 已覆盖 `med-autoscience`、`med-autogrant`、`redcube-ai`、`opl-meta-agent` 和 `opl-bookforge`：五个 bundle 都可物化并通过 validator，memory locator 都是 refs-only，`can_write_domain_truth=false` 且 `can_schedule_runtime=false`。OKF 只投影 Foundry Agent pack refs、memory locator refs、index/log/crosslink 和 no-authority metadata，帮助 AI executor 更快获得上下文；它不写 domain truth、不复制 memory / artifact / prompt / skill / quality gate body、不调度 runtime、不签 owner receipt、不创建 typed blocker、不授权 quality/export/domain-ready/App-release/production-ready claim。OKF warning（broken link、unknown field/type、missing optional metadata）默认不阻断 ordinary progress；只有被绑定到 source/data authority、owner identity、forbidden write、irreversible mutation 或 hard readiness / final export / submission claim 时，才回到既有 authority gate。

OKF native frontmatter 当前不是默认 source of truth，而是 `contracts/opl-framework/okf-context-bundle-contract.json` 内的 opt-in advisory migration lane；可执行入口是 `opl okf native-frontmatter inspect --repo <domain_repo> --json`。稳定 domain-owned `agent/**/*.md` 可以逐步携带 OKF-compatible frontmatter，但必须声明 `body_owner` 与 `domain_authority`，并保持 OPL 只 preserve / validate / inspect / project refs-only metadata。该 inspect 入口只输出 `ready` / `advisory_gaps` / `agent_root_missing` 和缺失字段、禁止 authority claim 的读回，不写 domain repo，不迁移正文，不能让 OPL 成为 Markdown body owner，不能复制或改写 prompt / skill / knowledge / quality gate / artifact / memory body，不能把 unknown frontmatter 或 missing optional metadata 变成普通 stage progress blocker，也不能授权 Runway、framework readiness 或 domain readiness claim。

Runtime environment substrate 当前作为 OPL-owned primitive 上收入口落到 contract、materializer/cache implementation 和 false-ready readback。机器入口是 `contracts/opl-framework/runtime-environment-substrate-contract.json`、`src/modules/runway/runtime-environment-substrate.ts` 与 `opl runtime env inspect|lock|build|prepare|materialize|verify|cache status|cache inventory|cache prune|doctor|run-context|contract --json`。这些 direct owner 入口固定 `Domain Environment Intent -> OPL Runtime Lock -> Content-Addressed Layers -> Runtime Bundle Manifest -> Materialized Runtime Root -> Receipt / Cleanup / Rollback` 的 owner boundary，并已能返回 descriptor / lock / bundle manifest / layer key / filesystem cache inventory / cleanup plan / materialization receipt / verify readback；`materialize --apply` 只写 `${OPL_STATE_DIR}/runtime-environment` 下的 runtime root、lock、manifest、env 和 receipt，并可更新 current / rollback pointer；`cache prune --apply` 只删除未被 current / rollback pointer 保护且带 receipt 的 stale runtime root；`prepare --apply` 只把缺失 R 包安装进 OPL-managed R library、把缺失 Python 包安装进 OPL-managed uv environment，并向显式 artifact root 写 dependency lock / receipt / run-context。`run-context --artifact-root <path>` 现在输出 consumer preflight：缺 `dependency_run_context.json`、target identity 不匹配或未提供 artifact root 时 fail closed，route hint 指向 `opl runtime env prepare`，且 `host_environment_fallback_allowed=false`；`--paper-root` 只保留为 compatibility alias，由 `root_vocabulary` 标注。当前仍不调度 domain stage、不写 domain truth / memory body / artifact body、不签 owner receipt / typed blocker，也不能声明 domain ready、App release ready 或 production ready；runtime materialization/verification 只能声明指定 OPL runtime environment root 已物化并可读，prepared run-context 只能证明指定 dependency profile 的 managed R/Python bindings 可消费。下一步证据尾项是 App release cohort、provider long-soak、真实用户路径和真实项目运行证据；它们继续后置，不能由 contract、docs、focused tests、run-context 或 cache hit 替代。

Codex runtime toolchain updater 是 runtime environment 结构面的 App-owned concrete executor payload 入口，机器读面在 `opl system --json` 的 `.system.core_engines.codex.runtime_substrate_updater`。该 readback 现在显式包含 `platform_package_materialization_policy`：当 root `@openai/codex` package 存在但 npm optional platform package 未物化时，`opl engine install|update|reinstall --engine codex` / `opl system startup-maintenance` 只允许在 App-owned stage prefix 内根据 root package optional dependency 显式安装 `@openai/codex-darwin-arm64`，再复制平台 vendor binary；`global_toolchain_mutation_allowed=false`，且 `can_claim_domain_ready=false`、`can_claim_app_release_ready=false`、`can_claim_production_ready=false`。失败 receipt 必须返回 `codex_platform_package_install_failed` 或 `codex_vendor_binary_not_found`，不能回落到 package bin wrapper、全局 npm/Homebrew/system Codex mutation，不能把 runtime payload 物化写成 App release、domain、provider long-soak 或 production readiness。

`dependency_prepare_policy` 当前接受 `cran`、`github`、`bioconductor` 三类 R package source；Bioconductor package 由 `BiocManager` 安装到同一 OPL-managed `R_LIBS_USER`，不回落到 host library。

## 读法

本文只保存当前角色、当前成熟度、完成边界、动态真相入口和仍未闭合的缺口类别。它不冻结 receipt id、attempt id、workorder 数字、open/closed counter、branch/SHA、provider tick、ledger verify 流水或某轮 closeout 细节。

需要当前事实时，先读本文给出的 live 命令、contracts/source/tests、runtime ledger、provider receipt、domain-owned manifest、App release/user-path evidence 和真实 workspace evidence。近期 read-model 重构后，主要 CLI 的 JSON 顶层均有 payload wrapper：`opl framework readiness --family-defaults --json` 读取 `.framework_readiness`，`opl framework operating-maturity --family-defaults --json` 读取 `.framework_operating_maturity`，`opl runtime app-operator-drilldown --json` 读取 `.app_operator_drilldown`，`opl brand-modules l5-status --json` 读取 `.brand_module_l5_status`，`opl family-runtime attempt list --json` 读取 `.family_runtime_stage_attempts`，`opl runway readiness --json` 读取 `.opl_runway_readiness`，`opl runway recovery-repair --json` 读取 `.opl_runway_recovery_repair`，`opl agents residue-decisions --family-defaults --json` 读取 `.private_platform_residue_owner_decisions`；不要沿用旧顶层字段路径判断计数、owner、repair guard 或 ready。`opl app state --profile fast --json` 的普通 Settings WebUI 入口读 `.app_state.settings_control_center.app_settings_read_model.docker_webui`，该块只投影一键安装、API key 配置、seed/image 选择、runtime proxy、失败恢复和下一步动作；Docker/runtime 诊断仍读 `opl system docker-webui doctor --json`。`opl runtime app-operator-drilldown --json` 的 fast payload 不保证暴露所有 detail section；App release/user-path fast counters 当前读 `.app_operator_drilldown.summary.app_release_user_path_evidence_*`，release / production claim flags 读 `.app_operator_drilldown.summary.app_release_user_path_*_claimed` 与 `.app_operator_drilldown.summary.app_release_user_path_production_user_path_ready`；完整 refs-only evidence object 读 `opl runtime app-operator-drilldown --detail full --json` 的 `.app_operator_drilldown.app_release_user_path_evidence`；owner gate 聚合读 `.framework_operating_maturity.app_release_user_path` 和 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.app_release_user_path`。`opl agents default-callers --family-defaults --json` 当前仍是顶层对象，关键字段直接读 `deletion_evidence_worklist_count`、`active_deletion_evidence_worklist_count`、`surface_retirement_gate_count`、`closed_surface_retirement_gate_count`、`owner_decision_status`、`default_caller_delete_ready`、`physical_delete_authorized` 和 `active_legacy_caller_deletion_gate`；其中 `deletion_evidence_worklist_count` 只表示仍开放的 active delete tail，`surface_retirement_gate_count` 才是全量 no-resurrection gate 数；closed gate 默认只保留计数和 `closed_surface_detail_policy`，不再暴露逐 surface 明细。`residue-decisions` 的 `physical_delete_authorized` 必须读 wrapper 内字段，顶层缺失字段不能解释为 delete state。Brand L5 独立 readout 的 runtime ledger 计数在 `.brand_module_l5_status.evidence_ledger`，不是旧的 `refs_only_ledger` 路径；`framework operating-maturity` 内的 Brand L5 聚合在 `.framework_operating_maturity.brand_module_l5`。需要历史过程时，读 `docs/history/**`、runtime ledger、提交历史或 domain-owned receipt/provenance。

Runtime readback discipline 当前读法：`framework readiness`、attention/workorder counters、workspace currentness arrays 与 `current_owner_delta` 都是高漂移被动投影，只能 live-read。它们不授权 stage progression，也不能把缺 packet、receipt、review 或 provider refs 变成 runtime blocker。可读 artifact 由 Codex 继续消费；transport identity 仅用于 stale reuse 与 wrong-target guard；quality/export/publication/ready 仍需对应 owner evidence。

`framework operating-maturity` 当前 truth 读取 `.framework_operating_maturity`。稳定结论仍是 `status=evidence_required`、`.framework_operating_maturity.summary.ready_claim_authorized=false`，且 production-tail owner gates 不能由 OPL 单独关闭。当前 owner-delta bridge、owner-answer missing / still-required flags、unresolved owner gate count 与 gate ids 都必须从 fresh maturity payload live-read；本文只保留字段路径和 false-authority 读法，不冻结某轮 MAS closeout ref、gate count 或 gate id 列表。`.framework_operating_maturity.current_owner_delta_bridge` 必须跟随 `.framework_readiness.current_owner_delta` live-read；旧 owner payload summaries 可随新 identity 进入 stale refs。`owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim` 是 refs-only summary 分类，不能覆盖 readiness owner-evidence identity / provider currentness 的 fresh 判定；同理，current pointer 只说明 owner-answer ref 已观察，不授权 domain ready 或 production ready。W7 owner-route work orders 机器消费优先读取 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.<lane>`；`owner_route_work_orders[]` 只保留数组兼容与人工扫描，不应被自动化转成 numeric-key map。Work order 的 `status` 区分 `open`、`owner_evidence_recorded` 和 `owner_acceptance_required`：`owner_evidence_recorded` 只表示 lane-specific open count 已清零且观察到 owner evidence，仍必须同时读 `owner_evidence_closure_state`、`owner_acceptance_required`、`ready_claim_authorized` 和 lane authority boundary。Brand L5 的 owner route 现在由 `contracts/opl-framework/brand-module-l5-operating-evidence.json` 的 per-requirement `owner_route_ref` / `owner_repo_ref` 显式驱动，不再根据 `owner` 文案推断；per-requirement work order、`missing_owner_action_ids`、`observed_refs_not_l5_claim_action_ids`、ledger receipt、observed ref 和模块完成计数都必须从 `.brand_module_l5_status` 与 maturity owner-route work order live-read，不在本文冻结样本数字。模块级下一步优先读 `.brand_module_l5_status.modules[].owner_followthrough_summary`，class-level 分布读 `.brand_module_l5_status.evidence_class_followthrough_summary`，maturity 聚合读 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.brand_module_l5_operating_maturity.owner_action_checklist[]`；不要再把 `open_requirement_count`、class aggregate distribution 或 `supporting_guard_can_be_completed_by_opl=true` 误读成 L5 完成。Brand L5 的稳定边界是：ledger verified receipt、observed evidence class、typed-blocker follow-through、owner acceptance requirement、class-level owner route aggregate 和下一 evidence class 都只是 refs-only projection / owner-route input；当前仍必须读为 `evidence_required` 与 `ready_claim_authorized=false`，且每个模块下一 requirement 必须 live-read。App drilldown 的 memory/artifact/lifecycle、cleanup-retirement、Brand L5 evidence ledger 和 default-callers cleanup worklist同样是 refs-only live projection：observed refs、deletion worklist、verified receipt、open count、handoff decision 等数字都会随 ledger 变化，不能写成 active truth completion。稳定边界是：这些读面只定位下一 owner、accepted ref shape、observed ref shape、closing source、verify path 和 false-authority boundary；不能授权 domain ready、App release ready、Brand L5、physical delete、memory accepted、artifact mutation、package/export readiness 或 production ready，也不能让 OPL 生成 owner receipt、typed blocker 或 quality/export/review verdict。

Console Brand L5 owner-evidence intake 已进入 runtime ledger / history provenance；当前 Console 和全局 Brand L5 状态只从 `opl brand-modules l5-status --json` 与 `opl brand-modules l5-status --module console --json` 读取。稳定结论是 observed Console cross-agent refs 只关闭对应 owner-evidence 输入，`l5_completion_status=evidence_required`、`l5_can_be_claimed=false` 仍由 live readout 判定；下一 owner action、ledger receipt count、observed classes 和 release/App evidence cohort 都不得在 active status 冻结为当前 truth。该 intake 不关闭 Console L5、App release ready 或 production ready。

维护性源码边界读法：Console / App operator drilldown、OKF context bundle、public command specs、runtime environment substrate、Runway queue/currentness reconcile、family orchestration companion、Temporal scheduler cadence、worker readiness、CLI pipe fluency 和 scheduler history payload 都由各自 source、contracts、package scripts、focused tests 与 `docs/runtime/README.md` 等 direct owner 持有当前 truth。Source-structure 维护信号直接读取 `opl framework source-structure --family-defaults --json` 与 `--strict` 模式的 `.source_structure_operator_readback`；该 readback 从 `contracts/opl-framework/source-structure-budget.json`、tracked source line count 和 strict ratchet policy 派生，只输出 advisory / strict finding、near-limit / oversized / reviewed-baseline 状态和 false-ready guard，不替代 `scripts/line-budget.mjs` validator，不创建第二源码真相，不声明 domain ready、quality verdict、App release ready、production ready、physical delete 或 full goal complete。`runtime-environment-substrate` 的 contract loader、target/state/hash/cache/materialization、prepare / managed R library、managed Python uv environment、run-context preflight 和 base-readback helpers 归 `src/modules/runway/runtime-environment-substrate-parts/{contract,target-state,package-profile,sandbox-provider-plan,projection-cache}.ts`；该拆分只服务 source boundary，不改变 runtime env CLI/API payload、OPL-managed runtime root 写入边界、run-context fail-closed 或 cache prune protection。本文不保存行数、advisory counter、pipe transcript、incident sample 或某次测试清单；需要确认当前形态时 fresh-read 对应 source/test/direct CLI，而不是沿用 status 页里的历史样本。

Source-module boundary completion audit 当前读法：机器真相仍归 `scripts/source-module-boundary.mjs`、`contracts/opl-framework/source-module-map.json`、`contracts/opl-framework/module-dependency-policy.json` 和 fresh `npm run source:modules -- --strict-imports` / `--strict-cycles` 输出。当前可标 `done` 的范围只包括十模块物理归位、target CLI/root `src/*.ts` 归零、deep cross-module import 清零、dependency cycle 清零、已配置 forbidden dependency 无违规；`partial` 范围是 public entrypoint 仍有 broad re-export、下一批 forbidden candidates 仍需 pair-count 和 owner 判断后才能升级。2026-07-04 latest readback 中 `--strict-imports --strict-cycles` 输出 `status=ok`、`dependency_cycles.count=0`、`deep_import_violations.count=0`、`forbidden_dependency_violations.count=0`；product-entry handoff bundle、developer-mode closeout ledger、generated interface bundle 和 contract validators 也已回到 Console / Foundry Kernel / Pack / Charter owner。strict import / strict cycle pass 不能外推为低耦合、minimal API、runtime ready、release ready、Brand L5 或 production ready。维护者说明见 [OPL Framework 源码模块边界](./references/source-module-boundary.md)。

Runway / provider 当前状态必须 live-read：`runway readiness`、`runway recovery-repair`、`family-runtime scheduler status`、`family-runtime worker status`、`attempt list --status running` 和 Temporal reachability 共同决定 scheduler status、worker lifecycle、source version、visibility、selected action、attempt repair worklist、mutation guard 与 repair blockers。`runway recovery-repair` 的 `attempt_repair_queue.breakdown` 是 bounded refs-only 分类读面，用于把 blocked / failed attempt backlog 按 status、reason、stage 分给 operator / owner route；它不 redrive、不修改 ledger、不签 owner receipt、不创建 typed blocker，也不把历史 backlog 清零写成 runtime ready。Retry / repair worklist 是 owner 接力面，不是 domain progress、provider long-soak closure、Brand L5、App release 或 production-ready。

Progress closeout 当前读法：`StructuredCloseoutGate`、Codex `--output-schema` 和 same-session closeout enforcement 已退役。`Codex CLI` 的任意可读 final message、部分草稿、负结果或实际文件都形成 raw artifact 和最小 progress envelope；格式、schema、receipt、review 与 normalizer 缺口只形成质量债务，不触发自动 redrive，不阻断下一 stage。质量债务仍不能声明 domain ready、owner acceptance、publication/export ready 或 production ready；这些必须 fresh-read owning domain/authority surface。


Active gap 与 closeout 的唯一维护入口是 [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)。动态运行事实分别从 `opl framework readiness --family-defaults --json`、`opl framework operating-maturity --family-defaults --json` 和对应 direct owner command 读取；source structure、App shell、memory/artifact/lifecycle、private residue、generated surface 与 owner-route 状态不再经过 tranche facade 聚合。具体 round id、branch/worktree、origin SHA、counter、lane closeout 和 follow-through 只进入 `docs/history/**`、runtime ledger、owner repo provenance 或提交历史。

Source structure 直接读取 `opl framework source-structure --family-defaults --json`；private residue 与 physical-delete boundary 直接读取 `opl agents residue-decisions --family-defaults --json` 和 `opl agents default-callers --family-defaults --json`；standard-agent 结构与 owner gate 分别读取 `opl agents conformance`、`framework readiness` 和 `framework operating-maturity`。这些 readback 只提供结构、currentness、owner route 与 false-ready evidence，不是 full Plan Completion Audit，也不能把 focused tests、remote SHA 或 clean readback写成 runtime ready、domain ready、App release ready 或 production ready。

App shell / Native Workbench / Hermes / AGUI 读法只保留当前 owner boundary：AionUI 是 App mainline，`opl-native-workbench` 是 foreground alternative，Hermes Desktop 是 retained explicit reference candidate，AGUI 只作 archived technical proof / explicit replay provenance；App release、active-shell adoption、packaged GUI acceptance、real user path、sustained App/operator consumption、Live Evidence 和 production readiness仍回 App owner / release owner。

Memory / artifact / lifecycle、OPL primitive/runtime/owner-route、strict source-purity/private-wrapper retirement 和 generated/hosted surface 的 active 口径只保存结构边界和 forbidden claim：这些 readback 可以证明 refs-only intake、owner work order projection、ordinary owner route、typed blocker / human gate shape、source/script classification、no-resurrection policy、generated surface consumption 和 stale-ref fail-closed 可读；它们不能读取或保存 body、写 domain truth、执行 physical delete、签 owner receipt、创建 typed blocker instance、作出 human decision、声明 default-caller cutover、App/live readiness、domain ready、provider long-soak complete、Plan Completion Audit 通过或 production ready。

已吸收的 P0 `domain_pack_generated_hosted_surfaces` 继续按 `closed_structure_gate` 读取：OPL-only `opl-generated-hosted-consumption-guard-20260621` lane 把 `domain-pack-compiler-contract.json#generated_interface_bundle`、`opl agents pack-compiler --family-defaults --json`、`opl agents interfaces --family-defaults --json`、`generated_surface_consumption_bundle` 与 `active_caller_cutover_proof` 折进同一个 `generated_hosted_surface_boundary.generated_surface_consumption_guard`。该 guard 显示标准 family repo pack compiler readback 可用、generated surface 40/40 ready、generated interface consumption 40/40 ready、blocked=0、domain generated-surface owner claim=0，并把 active-caller cutover / generated wrapper bundle 状态作为同源 API/CLI readback refs 暴露。结构面可作为 generated/hosted surface 消费基线，但 Live Evidence、real default-caller scaleout、direct skill path、App/operator functional path、App GUI live rendering、domain owner acceptance 和 physical delete owner receipt 仍是后置 evidence lane。

同一读面中的 `generated_hosted_surface_boundary` 仍只帮助 tranche 选择和跨仓 guard 对齐；它不能把 generated consumption bundle、pack compiler family-default feed、App projection、support profile、default-caller evidence worklist 或 refs-only consumption 写成 domain ready、App live rendering complete、production ready、default caller cutover、physical delete authorized、owner receipt signed、typed blocker created 或 Foundry Agent truth ownership。

同一读面也包含 `cross_repo_ref_integrity_guard`：它只校验 tranche backlog 中的跨 repo refs 是否使用 owning repo 的 canonical path，或是否已被 owning repo 显式标记为 forbidden legacy ref。当前 `support_repo_profile_no_resurrection` 只消费 `opl-doc:contracts/support-repo-policy.json`；旧 `contracts/support_repo_policy.json` 只能作为 forbidden legacy ref 出现在 guard 中，不创建 alias、不作为第二 support-policy 合同真相。该 guard 不能替代目标 repo truth、support repo readiness、Foundry Agent truth set membership、Plan Completion Audit 或 Live Evidence。


同一读面也包含 `memory_artifact_lifecycle_boundary_guard`：它从 `src/memory-artifact-lifecycle-evidence-ledger.ts`、`src/memory-artifact-lifecycle-readback.ts`、App drilldown memory/artifact/lifecycle evidence builder 和 `runtime memory-artifact-lifecycle-evidence` CLI spec 派生 refs-only evidence intake、readback projection、owner work-order projection、workspace review-repair transport 和 accepted ref shapes。该块把 memory / artifact / lifecycle 的功能接口边界折进 P0 tranche readback，并带 `structural_closeout_guard.cannot_claim` 防止 false-ready；不能把 refs observed、verified refs-only ledger、owner acceptance ref、typed blocker ref、open_count=0、reconcile clean、review-repair transport passed 或 OPL cleanup apply 写成 memory accepted、artifact mutated、repair accepted、package/export ready、physical delete authorized、domain ready 或 production ready，也不能让 OPL 读取 / 保存 memory body、修改 artifact body、签 owner receipt 或创建 typed blocker。

同一读面也包含 `runtime_environment_substrate_guard`：它从 `contracts/opl-framework/runtime-environment-substrate-contract.json` 派生 runtime environment ordinary path、materialization/cache/dependency prepare policy、run-context consumer policy、CLI readback commands、forbidden claims 和 false-ready flags。稳定口径是 OPL 只拥有 descriptor / lock / bundle / runtime root / receipt / cleanup / run-context substrate；domain agents 只声明 dependency intent。该块不能把 cache hit、descriptor exists、run-context exists、materialization receipt、verification receipt 或 cleanup receipt 写成 provider ready、domain ready、App release ready、owner receipt、typed blocker、provider long-soak 或 production ready，也不能让缺失/错配 run-context 回落到宿主机环境，不能授权 OPL 调度 domain stage。

同一读面也新增 `ordinary_progress_guard`：它从 `contracts/family-orchestration/family-owner-route.schema.json`、`contracts/opl-framework/owner-answer.schema.json`、`contracts/opl-framework/stage-typed-blocker.schema.json` 和 current owner-delta / readiness source refs 派生 single ordinary route guard。稳定口径是 `current_owner_delta` 仍是唯一默认 planning root，owner route 只承接 handoff / allowed actions / idempotency / refs-only projection；`owner_answer_admission_gate` 只说明 OPL 可以消费 owner answer / typed blocker / human decision / route-back refs 并重新派生 current owner delta；typed blocker 必须由 domain 或 human owner 签出，human gate 只能由人类决策关闭。raw worklist、provider trace、evidence ledger、App operator projection 和 private-residue cleanup 都不能覆盖 current owner delta，也不能声明 domain ready、artifact ready、App release ready、production ready 或全目标完成。


Wrapper-aware readout discipline：`framework readiness`、`operating-maturity`、App operator drilldown、Brand L5、Runway 与 attempt list 只做被动投影。缺 selected stage packet、route identity、attempt identity、transition request 或 receipt 时，OPL 记录 advisory/currentness debt 并禁用旧 attempt 复用，但不得阻止新 stage；identity 冲突才阻止错误目标 mutation。OPL 不根据 action_queue/read-model 形状推导下一步 domain action。Repeated-lineage/no-progress 只作 advisory diagnostic，不能冻结 launch、current pointer、terminal state 或 successor admission。




稳定结论是：这些 foldback 可以关闭 OPL-controlled transport、readout、guard、preflight、source-boundary 或 owner-handoff support gap，但不能让 OPL 写 MAS/MAG/RCA/OMA truth、签 domain owner receipt、创建 domain typed blocker、授权 quality/export/review verdict、声明 App release ready、Brand L5 complete、provider long-soak complete、physical delete authorized、memory/artifact/package ready 或 production ready。任何后续文档更新如果只新增 readout 样本、receipt refs、counter 流水或 worktree closeout，必须进入 history / ledger / owner repo provenance，而不是继续堆在 active docs。

## 品牌模块成熟度

OPL 顶层设计按当前十个品牌模块管理：`OPL Charter`、`OPL Atlas`、`OPL Workspace`、`OPL Pack`、`OPL Stagecraft`、`OPL Runway`、`OPL Ledger`、`OPL Console`、`OPL Foundry Kernel` 和 `OPL Connect`。品牌模块是 Framework 内部 bounded context、源码物理 owner 和成熟度语言，不是新的 runtime、第二 truth source 或 production-ready 证明；模块数量不作为硬约束，只有边界清楚的新 bounded context 才进入 registry。当前 App 与条件启用的 Cloud 产品语义可以和这十个模块对齐，方便把 Framework 能力包装成用户可见产品能力；`OPL Cloud`、在线 Workspace、Console 页面或 Gateway/API 体验不反向改写 `src/modules/<module_id>/` 的源码 owner，也不成为当前 App desktop + Docker/WebUI 的必要 gate。

成熟度读法固定为五级：

| 等级 | 含义 |
| --- | --- |
| `L1 conceptual` | 主要是理念或目标态。 |
| `L2 emerging` | 已有局部 docs/contracts/source。 |
| `L3 structural` | 合同、read model、conformance 或结构边界已经成型。 |
| `L4 executable baseline` | 具备品牌边界、schema/contract、CLI/App action、validate/doctor、docs foldback 和测试。 |
| `L5 production operating maturity` | 真实用户路径、跨 agent scaleout、长跑/恢复 evidence、release/install evidence、运维闭环和 owner acceptance 能持续证明。 |

当前 registry 内品牌模块都达到 `L4 executable baseline` 的 structural/read-only baseline：`contracts/opl-framework/brand-module-registry.json` 持有品牌 registry，`contracts/opl-framework/brand-cli-governance.json` 持有 module-owned CLI command surface 和 `opl agents modules` 内部 branding spine，非 Workspace 模块走 `opl <brand-module> status|inspect|interfaces|validate|doctor --json`，Workspace 只新增 `opl workspace status|inspect --json` 品牌读面，`opl brand-modules validate --json`、`opl agents modules validate --json` 与 `opl contract validate --json` 提供机器守门。`OPL Runway` 的 L4 可执行读面已经扩展到 `opl runway readiness|reconcile|handoff-gates|recovery-repair|control-loop status --json`：这些读面只能表达 provider readiness、desired/current reconcile、handoff gate、repair plan、唯一下一 safe action 和 false-authority flags；Temporal 未配置、service down、worker not ready 或 scheduler missing 都读作 `provider_not_ready` / OPL repair action，不读作 domain ready。`OPL Pack` 是本轮从已有 machine surface 正式提升的独立模块，负责 Declarative Domain Pack、authority ABI、pack compiler、generated/hosted surfaces、minimal authority functions，以及 Pack OS 通用 capability pack descriptor / install registry / content-addressed cache / refs-only distribution / lock / artifact lifecycle refs / review receipt refs transport。Pack OS 的机器入口是 `contracts/opl-framework/pack-os-contract.json`、`src/modules/pack/pack-os.ts` 和 `opl pack os inspect|install|registry|cache|distribute|lock|validate --json`；它只证明 descriptor/install/registry/cache/distribution/lock/lifecycle/review receipt refs transport 可用，不声明 domain ready、quality/export verdict、artifact authority、publication ready、App release ready 或 production ready。L5 evidence gate 已由 `contracts/opl-framework/brand-module-l5-operating-evidence.json`、`opl brand-modules l5-status|l5-validate|l5-interfaces --json`、`opl <brand-module> l5-status --json` 和 `opl runtime brand-module-l5-evidence record|verify|list --json` 落地；contract 现在按 evidence class 定义每个模块的 owner route / accepted ref shape / false-completion boundary，runtime ledger 只记录/验证本地 refs-only 证据并回投到 L5 status 的 route-level observed refs / receipt refs / ref shapes，不把 ledger receipt 计为 satisfied requirement。当前 ref shape 投影会把 release/install、scaleout/per-agent 等 owner evidence 识别为合同专属 shape，例如 `release_evidence_ref`、`install_evidence_ref`、`scaleout_receipt_ref` 和 `per_agent_receipt_ref`，避免 owner handoff 被降成泛型 `evidence_ref`；`workspace/cross_agent_scaleout` 额外投影 MAS/MAG/RCA/OMA 的 `supporting_domain_owner_chain_refs` 与 target coverage，但该字段只作 owner-handoff context，不计入 Brand L5 scaleout evidence，也不改变 typed-blocker follow-through。当前 10 个模块的 `operator_repair_loop` requirement 已有 `repair_loop_ref`、`current_owner_delta_ref`、`safe_action_ref` 和 `owner_acceptance_ref`，`owner_acceptance` requirement 已有 route-specific owner acceptance refs，所以这两个 class 在 fresh L5 aggregate 中应读作 observed refs not L5 claim，而不是 typed-blocker follow-through。`owner_followthrough_summary` 进一步把每个模块的 missing owner evidence、typed-blocker follow-through 和 observed refs not L5 claim 拆开，避免把 `open_requirement_count` 写成纯缺证据。Brand L5 的模块数、完成数、required 数、ledger receipt count、observed classes、模块下一 evidence class 与 owner follow-through 都以 `opl brand-modules l5-status --json` / `--module <module_id>` 的 `.brand_module_l5_status` 为唯一当前读面；本文只保留稳定边界：L5 仍是 `evidence_required`、`ready_claim_authorized=false`，per-requirement owner route 必须 live-read，且不能用全模块 compact row 的空 `requirements=[]` 判定完成。L5 不能由 docs foldback、contract validation、CLI governance pass、conformance pass、verified ledger、provider completion、pack lock written、Runway repair action、operator repair owner-evidence refs-only route、owner acceptance refs-only input 或 App projection 单独关闭。

当前 runtime / product / policy 文档入口按十模块做 thin mapping：Workspace 是 Framework workspace protocol / resource root 和 workspace/source intake shell，不等同在线 Workspace 产品；Pack 是 Declarative Domain Pack、authority ABI、Pack OS、Agent Tool Arsenal / Capability Invocation ABI、Standard Agent principle pack 的 generated/hosted surface 输入；Stagecraft 只承载 declared stage、stage prompt、capability use policy 与 tool affordance boundary，不持有 semantic transition authority；Runway 是 provider durability、Codex-selected route transport 和 stage attempt projection；Atlas 是 topology / decision-map / resource telemetry、domain capability registry catalog、source/catalog locator 和 tool-card catalog；Ledger 是 passive evidence / receipt telemetry，并已新增 `opl_substrate_provenance_surface` 作为 OpenScience-inspired refs-only substrate：只承载 project-local artifact graph refs、ledger pointer/hash、claim warning descriptor、annotation-to-source regeneration receipt descriptor 和 native viewer watch-only descriptor，不保存 artifact body、不写 domain truth、不签 owner receipt、不授权 quality/readiness；Console 是 App/operator cockpit、`current_owner_delta` default read root、governance 和 invocation-plan projection，不是 Connect 私有后端；Foundry Kernel 是 improvement control plane；Connect 是可独立调用 connector、module/skill/plugin/MCP/OpenAI/AI SDK descriptor discovery；Charter 是 owner split / AI-first 通用原则 / surface budget / forbidden claim。终局 Codex Attempt 独占 semantic stage route judgment；Framework 必须校验 Attempt role/终局资格、route ABI、legacy field absence、finding-closure 与 declared target，但不得依据 receipt、review 或 quality score判断、排序或重写 ABI 合法路线。这个 mapping 是维护入口，不是新成熟度结论；每个模块的 L5 仍必须由真实用户路径、跨 agent scaleout、长跑/恢复 evidence、release/install evidence、运维闭环和 owner acceptance 证明。Capability Invocation OS 和 Standard Agent AI-first Principle Pack 都保持在当前十模块协同边界内，不新增品牌模块；domain intake mapping 与专业判断继续留在 domain agent。源码状态的维护口径同样固定：`src/modules/<module_id>/` 是真实物理组织；`source-module-map.json`、registry 和历史 root 记录只服务归属校验与历史迁移 readback；`entrypoints/`、`kernel/` 是非品牌技术层；新增或迁入后的模块代码都从 owning module public index 进入。当前 source-module 边界进一步固定：Runway / Foundry Kernel 都不直接依赖 Console implementation；需要 Console-owned runtime tray snapshot 时由 CLI/App caller 显式注入 provider，Foundry readiness / maturity 只消费 provider 输出和 Ledger refs-only evidence projection；FrameworkContractError 与 owner id normalization 由 `kernel` 持有，Charter 持有 contract validator，Workspace 持有 workspace topology profile，Console 持有 product-entry handoff bundle，Foundry Kernel 持有 developer-mode closeout ledger，Pack 持有 generated interface bundle。fresh `source:modules -- --strict-imports` 中 `foundry -> console`、`runway -> console`、`connect -> console` 均为 0，Console / Workspace 已退出 dependency-cycle SCC；剩余 public-entry-level cycle 仍是 advisory，不声明全局低耦合完成。

Foundry Agent OS、multi-plane operating model、Temporal / StageRun transport、Runway current-control 和 Stage-route substrate 的当前读法统一回到 contracts、source、tests、read-model 与 [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)。`contracts/opl-framework/target-operating-architecture-contract.json#foundry_agent_os_standard` 定义 family-level target；`default_owner_route_policy` 进一步固定 MAG/RCA/OMA/OBF 的 default CLI、Skill/plugin、App/product-entry、status read model、workbench 和 conformance readback 走 `StageRun + current_owner_delta`，repo-local runner / private wrapper / generic owner surface 只能是 migration residue、deletion gate 或 diagnostic。`Console`、`Runway`、`Ledger`、`Pack` 只证明 refs-only transport、owner-action projection、provider repair / reconcile、false-authority flags 和 read-model availability。本文不维护历史 tranche、commit、closeout、provider tick 或 attempt 流水，也不把该结构默认路径写成 Live Evidence ready、domain ready、App release ready 或 production ready。

Foundry Agent landing acceptance：当前 canonical 定义由 `foundry-design-request.schema.json`、`foundry-agent-blueprint.schema.json`、`foundry-evidence-bundle.schema.json`、`foundry-evolution-proposal.schema.json`、FoundryRun 事件链、不可变 AgentVersion 与 ActivationPointer CAS 共同组成。完成声明必须绑定 exact baseline/candidate digest、冻结测试计划、独立 evaluator/reviewer evidence、风险分级、必要的 Owner receipt、canary 和可回滚 activation transaction；scaffold、generated interface、schema pass、suite pass、provider completion、Console projection 或 materialized candidate 均不能单独声明 Agent delivered、domain ready 或 production ready。

Standard Agent landing 的动态验收直接读取 `opl agents conformance --family-defaults --json`、`opl framework readiness --family-defaults --json`、`opl framework operating-maturity --family-defaults --json` 与 `opl foundry status|versions --json`。这些 direct readback 分别暴露结构 gate、open evidence tail、private residue owner decision、FoundryRun/version/Owner gate 和 false-ready boundary；它们不能把 schema、evidence refs、negative samples、focused tests 或 remote SHA 写成 standard-agent complete / domain ready / target-agent ready / production ready。

ordinary route 仍只有一条：fresh `current_owner_delta -> accepted owner answer shape -> domain/App/brand owner receipt、typed blocker、human gate、quality/export/review receipt、release verdict 或 route-back evidence -> next current_owner_delta`。`framework readiness`、`framework operating-maturity`、`agents conformance`、`brand-modules l5-status` 和 `agents default-callers` 是动态读面；`owner_evidence_intake` 与 live progress consumption 只能记录 observed refs 和 typed blocker refs，直到对应 domain / App / brand owner 关闭前都只能读作 `not_ready_claim`。

OPL 5-agent smooth progression structural smoke 当前读法：`opl agents conformance --family-defaults --json` 与 `opl foundry inspect mas|mag|rca|oma|obf --json` 只证明 MAS / MAG / RCA / OMA / OBF 以 `standard_domain_agent` 形态暴露 stage context、owner-answer shape 和 false-authority boundary。交付即推进的结构含义是：每次 stage attempt 都产出 domain artifact、raw/partial output、阴性/零结果或 no-output diagnostic；Codex CLI据此选择 advance、skip、repeat、reverse 或 route-back。Owner receipt / typed blocker / human gate只授权对应 claim或真实硬边界，缺失时关闭 ready claim而不关闭推进。OPL 只负责接收、运输和持久化 Codex 路由，不判断医学、基金、视觉、agent-building 或书稿内容是否 ready，不签 owner receipt，不创建 domain typed blocker。历史 backlog、retired residue、repair worklist、conformance和readiness只作为 diagnostic input，不是语义控制面。

Runtime / currentness 修复只处理 OPL provider transport、stage-attempt projection、stale-reuse identity 与 operator projection。Temporal closeout 保持 refs-only；`stage_progress_log` 投影可读 artifact、诊断、阴性结果或显式 missing。旧 sidecar task、SQLite row、selected dispatch residue、trace/span、read-model refresh 或 provider completion 只能作为诊断，不能生成 domain authority或阻止新的 Codex-selected stage。

MAS domain-progress refs 是 current-control / StageRun 的 domain progress 与 owner-answer 输入，只限 MAS 持有 authority 的产物 ref：`artifacts/controller/repair_execution_evidence/`、`repair_execution_receipts/`、`quality_repair_batch/`、`gate_clearing_batch/`、`gate_replay_requests/`、`artifacts/publication_eval/ai_reviewer_responses/`、`artifacts/reports/publishability_gate/`、`paper/draft.md`、`paper/build/review_manuscript.md`、`paper/evidence_ledger.json`、`paper/claim_evidence_map.json` 和 `paper/review/review_ledger.json`。这些 ref 可来自下一 payload 或同 lineage historical StageAttempt closeout，且支持 workspace-relative `artifacts/...` / `paper/...` 形态；`default_executor_execution`、`default_executor_dispatches` 和 `stage_attempt_closeouts` 仍只是 transport refs，不能声明论文 ready，也不能成为 admission authority。Repeated-lineage/no-progress 诊断保持 advisory-only；current-control 是否接收下一 route 只由 fresh currentness identity、StageRun/provider 状态和合法 owner answer 决定，不由 stop-loss counter 释放。

## 当前公开角色

`OPL` 当前公开认知固定为三层：`OPL Framework -> One Person Lab App -> Foundry Agents`。

| 层 | 当前职责 | 不拥有 |
| --- | --- | --- |
| `OPL Framework` | Codex-default activation、Temporal-backed provider、Fast Local Env 默认环境路径、后置 Local Docker / Devcontainer 与 Remote Sandbox provider binding、stage-attempt request/projection、stage attempt、receipt/projection、shared contracts/indexes、Foundry Kernel、generated/hosted surface、safe action shell 和跨仓治理。普通环境入口是 `opl env doctor|prepare|run`，advanced/operator 入口是 `opl runtime env ...`。 | domain truth、memory/artifact body、quality/export verdict、owner receipt authority、App release verdict、底层 agent VM/container sandbox implementation、E2B 默认依赖。 |
| `One Person Lab App` | 面向人的工作台，消费 framework/provider 状态和 domain-owned projection，展示任务、阶段、阻塞、source/artifact/memory refs、SLO、repair、workorder 和 owner-aware action。 | OPL runtime/provider implementation、domain truth、artifact authority、quality/export verdict、长跑任务外围驱动。 |
| `Foundry Agents` | MAS/MAG/RCA/OMA/OBF 及后续 domain agent，持有 domain pack、stage semantics、quality/export verdict、artifact authority、memory body / accept-reject decision、owner receipt、typed blocker 和 direct/generated skill path。 | Framework runtime、generic local queue/attempt loop、App release truth、跨 domain shared primitive。 |

普通用户 App 形态按 `Codex App wrapper` 读取：固定 `Codex CLI` executor，内置 MAS/MAG/RCA/OMA task entry。AionUI 原生多 backend、多 Agent 选择、非默认 executor adapter 和 shell implementation 细节只能进入显式 developer/operator diagnostic，不是普通用户 product truth。`one-person-lab-app` 是 GUI product truth、active-shell contract、validator 和 release/user-path evidence owner；当前 GUI 主线是通过 `shells/aionui` 消费 `opl-aion-shell` 的 OPL-branded AionUI shell。`opl-native-workbench` 是 App-owned foreground alternative GUI candidate；Hermes Desktop / `hermes-codex` 是 retained explicit reference candidate；`opl-agui-codex-shell` / `agui-codex` 只作为 AG-UI/CopilotKit archived technical proof 与显式 replay surface 保留，除非用户明确要求 AGUI，不再更新、完善或进入默认验证。这也是后续 GUI 工作分派规则：普通 GUI 缺口先落到 AionUI 主线，替代路线优先按 App registry 推进 Native Workbench，Hermes 只做显式 reference replay，AGUI 历史文件、`candidate` 命名或 replay manifest 不能被读成默认待办。

`Codex CLI` 是当前第一公民 executor。标准 OPL Agent 的默认长跑路径是 `opl_temporal_hosted_autonomous`：任务启动后进入 OPL/Temporal 托管的 stage attempt runtime，由 OPL provider scheduler cadence / stage-attempt projection / wakeup / resume-requery / retry-dead-letter / attempt ledger 持久在线推进；Codex App 只承担启动、观察、介入和投影入口。Temporal-backed provider 是 production online runtime 的必需 substrate；`local_sqlite` 只允许作为 retired-provider negative guard，SQLite sidecar 只承担 projection/readback index。`hermes_agent`、`claude_code` 与 `antigravity_cli` 同属显式非默认 executor adapter/backend，以 request/stage binding、receipt/audit/fail-closed 证明连接，不承诺行为、质量、工具语义或 resume 与 `Codex CLI` 等价。

`MDS` 不进入 OPL 顶层 agent 列表。它只作为 MAS 显式声明的 source provenance、historical fixture、explicit archive import、backend audit、upstream intake 或 parity oracle reference。

## 当前真实状态

OPL 已具备 framework 主干：

- domain descriptor / stage / action / memory discovery；
- Temporal provider code、service / worker lifecycle、stage-attempt projection、stage attempt ledger、progress/failure diagnostic、retry/dead-letter、human gate 和 wrong-target currentness guard；
- evidence worklist read model、provider proof / SLO projection、runtime snapshot、safe runtime action shell、App/operator drilldown read model 和 refs-only external evidence ledger；
- Runtime Environment Substrate 以 Fast Local Env 为默认，R 走 `renv.lock` refs + `R_LIBS_USER` managed library，Python 走 `uv.lock` / project refs + `UV_PROJECT_ENVIRONMENT` managed env；doctor 只读 host binary、language packages 和 system hints，不声明 runtime/domain/App ready；
- State Index Kernel、Stage Artifact / Workspace topology、workspace ensure/validate/doctor/adopt/upgrade/export-map/health/inspect/inventory surfaces；
- Foundry Kernel、Foundry Agent series contract、standard domain-agent scaffold / conformance、generated/hosted interface 和 default-caller / cleanup / no-resurrection guard；
- OPL-owned refs-only intake / projection for domain-dispatch、stage-production evidence、memory/artifact/lifecycle/no-regression receipt refs。

这些 surface 只证明 Framework 的 transport、ledger、projection、guard、recovery、admission 和 diagnostic 基础可用。它们不读取 memory/artifact body，不写 domain truth，不接受或拒绝 memory writeback，不授权 artifact/package/export readiness，不生成 owner receipt / typed blocker，也不声明 domain ready、App release ready 或 production ready。

Foundry Kernel、observability eval 和 mechanism improvement 继续是 refs-only control plane。OPL 只消费 refs，不写入 body、truth、artifact、owner receipt 或 quality verdict；domain truth、quality/export verdict、artifact authority、memory body 和 owner receipt 仍归 MAS/MAG/RCA。

当前状态摘要：

| 面 | 当前读法 |
| --- | --- |
| Framework runtime | `Codex CLI` first-class executor + Runway control-loop runtime L4 executable read surface + Temporal-backed production online substrate + SQLite projection/readback index；当前 Runway/provider 状态必须 live-read。Scheduler cadence、worker readiness、Runway readiness flags、recovery-repair selected action、attempt repair worklist、mutation guard 和 blocker ids 都是 runtime owner 派生读面，不在本文冻结样本。Provider readiness / repair worklist 只说明 OPL runtime substrate 与 owner 接力读面，不是 domain ready。 |
| App/operator | 默认 owner-delta-first；App 只启动、观察、介入和展示，不驱动外围长跑任务。MAS study currentness 先从 domain-owned `current_work_unit` refs-only projection 进入 `domain_current_work_unit_projection`，stale unbound dispatch/workstream 只作 full-detail provenance。 |
| Multi-plane operating model | `Console` 只消费 `current_owner_delta` 和 owner-action refs；`Runway` 只承载 provider attempt/currentness、quality budget、failure diagnostic 和 repair refs；`Ledger` 只保存 refs-only evidence。三者都不选择 semantic route。 |
| Foundry Agent structure | MAS/MAG/RCA/OMA 通过 contracts / descriptors / conformance 暴露标准 pack、stage、quality gate、authority function 和 direct/generated skill path。 |
| StageRun / owner answer | OPL 只运输 provider attempt、可选 transport identity 和 artifact/progress refs。任意可读 artifact、诊断、阴性结果、owner receipt、typed blocker、human gate 或 route-back evidence都可成为下一 stage 输入；owner receipt / review 只授权 quality/ready 声明。 |
| Ordinary progress spine | `current_owner_delta` 机器读面已暴露 `ordinary_progress_spine`、T0 `progress_delta_receipt`、`artifact_tier_policy` 和 passive `audit_sidecar_policy`；App ordinary cockpit 与默认读面同步消费这些字段。它只让普通 owner delta 更短、更可接力，不授权 domain ready、App release ready、artifact/memory mutation、physical delete 或 production ready。 |
| Workspace / State Index | OPL 维护 workspace topology、workspace inspection、resource inventory、workspace report、workspace fleet report、Project lifecycle runtime、delete safe gate、Stage Artifact Unit、stage outputs index/current pointer 和 refs-only SQLite sidecar；domain pack 只在 `contracts/stage_artifact_kernel_adoption.json#/opl_state_index_kernel_adoption` 声明 OPL-owned deferred sidecar 的消费边界，数据库布局与维护策略由 OPL state-index contract 持有，旧根合同仅作既有 agent 的过渡兼容；workspace governance v2/v3 把 canonical generated roots 固定到 `control/opl/projections` 与 `control/opl/reports`，根层 `workspace_*.json` 只作兼容 mirror；新 workspace 的 Project Unit 默认物理集合是 `projects/<project-id>`，MAS `studies` 与 RCA/MAG/OMA `deliverables` 只作为 display / legacy alias；完成状态继续由 stage folder、manifest validity、owner receipt / typed blocker、current pointer 和 lineage 推导。 |
| Pack OS | OPL Pack 已有通用 refs-only pack descriptor/install/registry/cache/distribution/lock/lifecycle/review receipt transport：descriptor intake、registry entry、content-addressed cache manifest、distribution bundle manifest、lock projection、resource hash/ref、artifact lifecycle refs、review receipt refs 和 provenance 可被 OPL 读取、校验和写入。 | 该 surface 不存 artifact body 到合同、不改写 domain artifact、不签 owner receipt、不生成 typed blocker、不把 review receipt refs 当 quality verdict，也不授权 publication/grant/visual/App ready 或 production ready。 |
| External evidence | OPL 可记录/验证 body-free refs-only receipts；`runtime brand-module-l5-evidence` 同样只记录品牌模块 L5 evidence refs。verified ledger 只证明 refs transport 与 preflight 可用，不关闭 domain verdict、L5 completion 或 production evidence。 |

Fresh readiness / maturity / conformance readout 必须 owner-delta-first：当前 owner、accepted answer shape、Brand L5、App release/user-path、provider long-soak、cleanup、memory/artifact/lifecycle 和 blocker tail 都从 live commands 读取。历史 2026-06-09 cohort、canary、release evidence、ledger receipt、cleanup counter、provider proof 和 branch closeout 只作 provenance；它们不授权 domain ready、App release-ready、Brand L5、artifact ready、memory accepted、package/export ready、physical delete authorization 或 production ready。

## 未闭合项

具体数量和 drilldown 以 live CLI/read-model 为准，本文只保留类别和完成口径。

| 类别 | 仍缺什么 | 完成口径 |
| --- | --- | --- |
| `App release / user path` | OPL maturity 已消费 App drilldown 的 same-cohort user-path refs，但当前 truth 必须从 live fast summary、full `.app_release_user_path_evidence` 和 maturity owner lane 三层读取。Fast counters、verified ledger receipt count、release / production claim flags、full-detail authority boundary、release-owner verdict handoff 和 owner work-order status 都是 high-drift projection，不在本文冻结；稳定边界是 refs-only evidence lane observed / owner evidence recorded 仍不授权 release-ready。App release evidence payload 现在 fail-closed 地拆成 same-cohort user-path refs、release-owner verdict refs、typed blocker refs三条互斥 path，并暴露 `release_owner_verdict_handoff`。仍缺 App release owner 的 release-ready / production-ready verdict。 | App release-owner receipt / install evidence / typed blocker 或 release verdict 关闭 release-ready；OPL Framework 只记录 refs，不替 App 声明 release-ready。 |
| `Domain owner-chain scaleout` | MAS paper、MAG grant、RCA visual、OMA target-agent stage 在 OPL-hosted path 下持续返回 owner receipt、typed blocker、human gate、quality/export/review receipt 或 no-regression evidence。 | Domain-owned receipt / typed blocker 关闭对应 stage / transition / owner-chain 缺口；OPL 只承载 transport、ledger 和 projection。 |
| `StageRun currentness` | OPL 观察 domain/stage/attempt/source/work-unit/dispatch/packet identity，用于判定旧 attempt 是否可复用。 | 缺字段只禁用复用并记录 advisory；identity 冲突阻止错误目标 mutation。两者都不阻止 Codex 新建或启动任意 declared stage。 |
| `Memory / artifact / lifecycle apply` | `operating-maturity` 已从 App drilldown 的 memory/artifact/lifecycle refs 与 reconcile counters 派生 `memory_artifact_lifecycle_open_count`；App drilldown 输出 `lifecycle_owner_work_order`、typed-blocker follow-through、accepted refs-only shapes 和 forbidden OPL claims。`opl runtime memory-artifact-lifecycle-evidence record|verify|list --json` 提供 refs-only intake ledger；App drilldown summary/full detail、`opl runtime memory-artifact-lifecycle --json` readback 和 maturity lifecycle lane 都消费同一 ledger projection，暴露 recorded/verified/pending counts、typed blocker refs、owner acceptance refs 与 false-authority flags。`opl workspace artifact-lifecycle --workspace <path> --project-id <id> --apply` 只会从项目内 domain-owned `handoff/review-repair-transport.json` 物化 `control/opl/artifact_lifecycle/review_repair_transport.json`，投影 current owner、accepted answer shape、route-back target、iteration limit、freshness blocker和 opaque repair refs。真实 memory retrieval/writeback、accepted/rejected receipt、artifact mutation receipt、package/export lifecycle receipt、review-repair domain decision、cleanup/restore/retention 对账仍属 domain owner。 | Domain-owned surface 产生真实 receipts、owner acceptance 或 typed blocker follow-through；OPL 只 intake / verify / project refs，count=0、verified refs-only ledger、owner acceptance ref、typed blocker ref、refs observed、review-repair transport passed 或 reconcile clean 都不代表 OPL 保存 memory body、修改 artifact body、解析 repair semantics、授权 physical delete 或声明 package/export ready。 |
| `Provider long-soak` | `operating-maturity` 已从 provider cadence window / capability projection 与 provider long-soak refs-only ledger 派生 provider lane readout。当前 truth 读取 `.framework_operating_maturity.provider_long_soak`、`.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.provider_long_soak` 与 App full `.app_operator_drilldown.provider_long_soak_evidence`；open count、ledger status / receipt count、owner work-order status、owner acceptance refs、capability checklist、capability status、observed ref counts 和 long evidence flag 都是 live projection，不在本文冻结。Fresh provider follow-through 已把当前 cadence/capability/recovery readout 记录成 verified refs-only long-soak / recovery / dead-letter refs；稳定 false-ready 字段仍固定为 `ready_claim_authorized=false`、`provider_completion_counts_as_production_ready=false`、`verified_refs_only_ledger_counts_as_production_ready=false`、`long_evidence_ready_counts_as_production_ready=false`、`capability_slo_satisfied_counts_as_production_ready=false`。Temporal service/worker、provider cadence/capability、domain owner-chain dispatch、retry/dead-letter 和 repair loop 仍需在更长窗口内持续满足并由 owner gate 消费。 | Long-soak refs、recovery refs、dead-letter refs、provider blocker refs、typed blocker refs 或 owner acceptance refs 可重复 record/verify；verified ledger、provider blocker、typed blocker、owner acceptance、capability checklist、long evidence ready、capability satisfied、owner-evidence work-order open count 和 counters 只说明 provider lane evidence 状态，不关闭 long-soak，不外推为 provider production ready、production ready 或 domain ready。 |
| `Runway control-loop runtime` | Runway 目标态按 L4/L4+ 结构强化读取：Temporal 只做 durable substrate，worker supervisor 只保 worker liveness，scheduler 只提供 cadence，Progress Reconciler 比较 desired/current 并输出唯一下一 safe action、owner/gate wait 或 OPL runtime blocker。`readiness`、`reconcile`、`handoff-gates`、`recovery-repair` 和 `control-loop status` 是已落地读面；Temporal 未配置、service down、worker not ready、worker source stale 或 scheduler cadence missing 时必须返回 provider repair owner。当前 Runway wrapper 必须读 `.opl_runway_readiness` 与 `.opl_runway_recovery_repair`；worker 与 attempt wrapper 分别读 `.family_runtime_worker` 和 `.family_runtime_stage_attempts`。Scheduler cadence、provider / live workflow readiness、repair blockers、worker mutation action 和 attempt repair worklist 均从 live readout 判断；若后续出现 running attempt、developer supervisor 缺失、Temporal unreachable 或 ledger unreadable，restart guard 仍必须返回 blocked。`family-runtime attempt list --json` 默认保持 `view_mode=compact_timeline`，包括 filtered `--status` / `--study` readout；需要 full filtered attempt body 时显式使用 `--full`；本文不冻结 active attempt count、status breakdown、compact timeline total 或 omitted total。 | 该能力是 L5 evidence 前置，不是 L5 完成。真实 L5 仍需长跑/恢复、跨 agent scaleout、release/install、owner acceptance 和其他未完成 owner evidence；当前 `operator_repair_loop` refs 也只能作为 L5 输入。handoff/gate/reconciler refs、provider repair、verified ledger、worker stale guard、attempt repair worklist、bounded attempt readout 和 no-open-blocker 不能伪造 domain truth、receipt、typed blocker、verdict 或 production long-soak closure。 |
| `Private platform retirement` | Default-caller active deletion worklist、surface retirement gate 与 App drilldown cleanup-retirement ready-domain worklist 是不同 live read surfaces，计数可能随 refs-only ledger 变化。`deletion_evidence_worklist_count` / `active_deletion_evidence_worklist_count` 只表示仍开放的 active tail；`surface_retirement_gate_count` / `closed_surface_retirement_gate_count` 表示全量 no-resurrection gate。closed gate 默认不暴露逐 surface 明细；只有 active worklist 仍开放时才展开 per-surface deletion detail。private residue cleanup lane 为空时也输出 `owner_decision_work_order.status=no_private_residue_classified_not_delete_ready`，`physical_delete_authorized=false`。`keep_as_authority_adapter_ref` 被观察到时只表示 OPL default-caller delete work 已有 owner decision closeout；当前应读 `owner_decision_result_shape`、`owner_decision_closeout_status`、`no_further_opl_default_caller_delete_work` 和 `next_opl_default_caller_delete_action` 判断 OPL 侧是否仍有 delete work。`no_further_opl_default_caller_delete_work=true` 可读作 no further OPL delete work；它仍不是 `default_caller_delete_ready` 或 physical delete authority。 | 只有 domain owner 给出 physical delete authorization 才能物理删除；empty cleanup lane、structural evidence、tombstone/provenance refs、App 顶层 cleanup projection、OPL work order、typed blocker 或 keep-as-authority-adapter 都不授权 OPL 删除；否则保持 authority adapter / tombstone / typed blocker 路径。 |
| `Brand module L5` | 当前品牌模块的 L5 evidence matrix、可执行 read/validate surface 和 refs-only runtime evidence ledger 已落地。`workspace/cross_agent_scaleout` 支持显示 domain owner-chain supporting refs 与 MAS/MAG/RCA/OMA target coverage，但这些 refs 不关闭 Brand L5。fresh `brand-modules l5-status` 的 `.brand_module_l5_status` 是 evidence-required / complete module counts、ledger receipt count、observed evidence classes、owner-route work orders 和 next evidence action 的唯一当前读面；`--module <module>` 才展开 evidence requirements、owner routes、observed refs / typed blocker groups 和 command examples。`framework operating-maturity` 的 Brand L5 lane 只把 per-requirement work order 与 module owner checklist live 投影出来，具体 `missing_owner_action_ids` / observed-not-L5 counts 每轮重跑读取。真实 L5 运营证据仍缺，当前稳定边界仍是 `evidence_required` 与 `ready_claim_authorized=false`。 | 各模块用真实用户路径、跨 agent scaleout、long-soak、release/install、owner acceptance 和运营闭环证明；L4/L3、contract validation、provider completion、verified ledger、contract refs-only evidence、owner-needed typed blocker refs、compact row 空 requirement 列表、top-level owner checklist 和 App projection 只能作为输入。 |
| `Workspace governance L5` | v3 已建立 profile binding、profile fingerprint、migration history、topology events、canonical generated projections/reports、root mirror 兼容、workspace report 用户检查面、workspace fleet report、workspace_norm 全量 projection drift gate、active/paused/archived/superseded/locked lifecycle、project delete safe gate 和 shared resource provenance refs-only inventory。 | 当前只声明 `L4_structural_baseline`：合同、初始化/ensure/upgrade/adopt 物化、validate/doctor drift gate、fleet report、lifecycle 操作和用户检查面可用。L5 仍需真实 App user path、跨 MAS/MAG/RCA/OMA scaleout、长跑迁移/恢复证据、release/install evidence 与 owner acceptance，不能由 workspace_norm pass、fleet report 或 generated projection currentness 单独关闭。 |

## Live 真相入口

当前状态、计数、receipt、attempt、workorder 和 owner-delta 必须从 live readout 读取。本仓内核查 currentness 时优先使用 repo-local `./bin/opl`；下面示例的 `rtk ./bin/opl ...` 表示用 RTK 包装本仓 CLI，不用全局 `opl` 版本判断本仓当前行为。

```bash
rtk ./bin/opl framework readiness --family-defaults --json
rtk ./bin/opl framework operating-maturity --family-defaults --json
rtk ./bin/opl stages readiness --family-defaults --json
rtk ./bin/opl runtime app-operator-drilldown --json
rtk ./bin/opl runtime app-operator-drilldown --detail full --json
rtk ./bin/opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json
rtk ./bin/opl family-runtime worker status --provider temporal --json
rtk ./bin/opl family-runtime scheduler status --provider temporal --json
rtk ./bin/opl family-runtime provider-slo tick --provider temporal --json
rtk ./bin/opl family-runtime attempt list --json
rtk ./bin/opl family-runtime attempt list --status queued --json
rtk ./bin/opl app state --profile fast --json
rtk ./bin/opl connect scientific search --provider openalex --query "diabetes mortality prediction" --limit 5 --json
rtk ./bin/opl brand-modules validate --json
rtk ./bin/opl brand-modules l5-status --json
rtk ./bin/opl runtime brand-module-l5-evidence list --json
rtk ./bin/opl runtime domain-owner-payload-summary list --json
rtk ./bin/opl runtime app-release-evidence list --json
rtk ./bin/opl runtime codex-app-runtime-evidence list --json
rtk ./bin/opl charter status --json
rtk ./bin/opl pack status --json
rtk ./bin/opl runway doctor --json
rtk ./bin/opl runway readiness --json
rtk ./bin/opl runway reconcile --json
rtk ./bin/opl runway handoff-gates --json
rtk ./bin/opl runway recovery-repair --json
rtk ./bin/opl runway control-loop status --json
rtk ./bin/opl agents modules validate --json
rtk ./bin/opl agents conformance --family-defaults --json
rtk ./bin/opl agents default-callers --family-defaults --json
rtk ./bin/opl index doctor --json
rtk ./bin/opl workspace interfaces --json
rtk ./bin/opl pack os inspect --descriptor <path> --json
rtk ./bin/opl pack os install --descriptor <path> --registry <path> --json
rtk ./bin/opl pack os registry --registry <path> --json
rtk ./bin/opl pack os cache --descriptor <path> --cache-root <dir> --json
rtk ./bin/opl pack os distribute --descriptor <path> --output <path> --json
rtk ./bin/opl pack os lock --descriptor <path> --json
rtk ./bin/opl pack os validate --descriptor <path> --json
rtk ./bin/opl workspace fleet report --json
rtk ./bin/opl workspace inspect --workspace <path> --json
rtk ./bin/opl workspace inventory --workspace <path> --json
rtk ./bin/opl workspace health --workspace <path> --json
rtk ./bin/opl workspace project lifecycle --workspace <path> --project-id <id> --status paused --dry-run --json
rtk ./bin/opl workspace project delete --workspace <path> --project-id <id> --dry-run --json
rtk ./bin/opl okf validate --bundle <okf_dir> --json
rtk ./bin/opl okf inspect --bundle <okf_dir> --json
rtk ./bin/opl okf project-pack --pack <pack_compiler_input.json> --output <okf_dir> --json
```

默认阅读顺序是 owner-delta-first：先从 `current_owner_delta` 看等待哪个 owner、需要什么 deliverable delta / receipt / typed blocker，以及该等待是否阻断 readiness。OPL provider / transport safe action 只有在没有 current owner delta 时才可成为默认下一步。raw refs-only counters、provider worker / redrive / scheduler route、evidence envelope、stage replay packet、typed blocker group、private residue inventory、历史 receipt 计数、attempt backlog 和 repair queue 都只作 drilldown / diagnostic，不进入普通用户默认 next action，也不改变研究、基金、视觉或 App 路线控制权。

Runtime / product / policy 入口的迁移读法同样服从 owner-delta-first：新增 App Console 页面、Foundry Kernel control-plane view、Atlas/Ledger telemetry、route reconciler、artifact reconciler 或 generated surface 时，先证明它是否能折叠成 `current_owner_delta`、owner answer、typed blocker、hard gate、route-back 或 audit/ref drilldown。不能折叠的内容只作为 diagnostic、history、cleanup 或 support 入口，不进入默认 next action，也不改变 active gap owner。

`family-runtime provider-worker supervisor` 现在提供 provider worker process 的本机监督面：macOS 上安装 LaunchAgent 后由 `KeepAlive` / `RunAtLoad` 托管 resident Temporal foreground worker，并把 `--family-runtime-root` 绑定到当前 OPL state root。这个 supervisor 对齐 Temporal 的部署分层：Temporal Service 持有 workflow history、task queue、retry 和 activity timeout；Worker process 的存活由 deployment / supervisor 层保证，云端目标是 Kubernetes / systemd / container supervisor，本机目标是 launchd。旧 `provider-slo watchdog` 的 5 分钟 `StartInterval` tick 安装面已退役，安装新 supervisor 时会清理旧 `ai.opl.family-runtime.provider-slo` LaunchAgent。`provider-slo tick` 继续作为显式 health-check / production proof / fallback repair receipt，不作为主调度器、不消费 domain work、不写 MAS/MAG/RCA truth、不生成 owner receipt 或 typed blocker，也不把 provider 恢复解释成 paper/grant/visual 进展。默认读面仍必须先看 current owner delta；supervisor 只负责让 OPL provider worker 不因进程退出而静默停止。

当 worker lifecycle 投影为 `worker_source_stale` 时，`provider-slo tick` / `provider repair` 只能在 `restart_guard.guard_status=ready` 时自动 stop/start worker。该 guard 需要同时满足：worker mutation guard 为 `allowed_explicit_developer_supervisor`，Temporal service reachable，stage attempt ledger 可读，且 `running` blocking active stage attempt 数为 0。任一 blocking 条件不满足时，worker repair receipt 必须返回 `repair_status=blocked` 与 `blocker_ids`，不得杀掉旧 worker；`queued`、`checkpointed` 和 `human_gate` 只进入 `diagnostic_stage_attempt_*` backlog 统计，不阻止加载新 worker source。`opl runway recovery-repair --json` 当前会只读投影同一 restart guard；当 guard blocked 时输出 `repair_blocked_by_worker_restart_guard` 和 `repair_can_mutate_worker=false`，因此 operator 能看见 repair plan 与 mutation guard 的区别。blocked / failed attempt backlog 不再停在自引用 recovery-repair 命令，而是进入 `.attempt_repair_queue`，并给出 `family-runtime attempt list --status blocked|failed --json` 这类只读查询入口。这个规则把源码更新后的 worker stale 从“运维提醒”收敛为 fail-closed supervised restart 策略，也把失败 attempt 处理收敛成 operator repair queue；它仍只修 OPL provider / attempt 接力读面，不生成 domain owner receipt、typed blocker、quality verdict 或 deliverable progress。

Runway control-loop runtime 的当前人读方向是：scheduler/cadence 只制造 provider readback 和 reconcile 机会，worker supervisor 只维持 worker process liveness，Temporal 只保存 durable execution history，Progress Reconciler 只被动对账 transport/currentness 并展示可执行的 OPL repair action。它不能生成、排名、选择、接受、拒绝或覆盖 Codex 的下一 stage。Temporal 未配置、service down、worker not ready、worker source stale 或 scheduler missing 只阻止对应 provider 执行；已有可读 artifact 仍可被下一 stage 消费。provider completed、worker healthy、gate passed、repair succeeded 或 no-open-blocker 也不能写成 domain ready、owner receipt、quality verdict、artifact ready、production ready 或 Runway L5 闭合。

## 当前默认入口

- 默认前门是 `opl`；`opl --help` / `opl help` 展示 OPL Framework 自有命令树，`opl exec` 负责一次性请求，`opl exec --help` 保留 Codex-compatible 执行器帮助边界，`opl resume` 负责续接会话。
- `opl install` 是当前一键安装入口，负责安装或复用 Codex CLI、Temporal-backed family runtime provider、MAS、MAG、RCA、OPL Meta Agent、推荐 skills 和 App 入口。
- `opl system` / `opl system initialize` / `opl system startup-maintenance` 管理 Codex CLI、provider profile/readiness、Connect module install/update、Connect skill sync、managed environment freshness、plugin cache freshness、reload prompt 和 local runtime state。
- `opl system dependency-doctor --profile bookforge-publication-proof --json` 与 `opl system dependency-maintenance --profile bookforge-publication-proof --json` 管理 Book Forge publication proof 所需的本机工具链诊断和显式维护 route。它们只证明 Pandoc/XeLaTeX/Poppler/TeX package discoverability 或维护计划，不写 Book Forge truth、不写 manuscript、不授权 publication proof / final export / owner acceptance；普通写作推进不因该 surface blocked 自动停止。
- `opl framework readiness --family-defaults --json` 是 family readiness 动态真相入口；它只输出 framework/operator 读面，不授权 domain ready、artifact authority 或 production ready。
- `opl framework operating-maturity --family-defaults --json` 是轻量 maturity gap aggregator，把 `current_owner_delta_bridge`、`owner_evidence_intake`、domain owner-chain scaleout、Brand module L5、App release/user-path、provider long-soak、private wrapper retirement 和 memory/artifact/lifecycle receipts 汇总为 refs-only 下一步读面；`owner_evidence_intake` 可读取 MAG production acceptance、RCA owner-chain live progress、OMA target-agent owner-chain evidence、`agents default-callers` private-platform refs 和 `runtime app-operator-drilldown` memory/artifact lifecycle refs，并只投影为 observed refs。它以 current owner delta 为默认 planning root，不跑全量 release/user-path proof，不替 App/domain owner 签 receipt 或 typed blocker，不声明 L5、App release ready、domain ready 或 production ready。
- 旧 `opl framework production-closeout` 已退役；当前 production-tail / owner-evidence / provider long-soak / App release / Brand L5 / cleanup / lifecycle 读面统一回到 `opl framework operating-maturity --family-defaults --json`，默认 readiness 回到 `opl framework readiness --family-defaults --json`。不得恢复 production-closeout 兼容别名、facade、测试保护面或旧 machine payload 字段。
- `opl stages readiness --family-defaults --json` 是 stage readiness family drilldown 入口；单仓诊断继续使用 `opl stages readiness --domain <domain> --json`。
- `opl runtime app-operator-drilldown --json` 与 `opl runtime app-operator-drilldown --detail full --json` 是 App/operator drilldown 入口。
- `opl family-runtime evidence-worklist --family-defaults --provider temporal --executor-kind codex_cli --detail full --json` 是 runtime safe-action evidence worklist、stage evidence workorder packet、stage replay missing receipt attention packet 与 domain-dispatch evidence workorder packet 入口；cache-derived attention 仍不授权 domain truth、owner receipt、artifact authority 或 production ready。`framework production-closeout` 与 `family-runtime production-closeout` 均已退役，不再是 active interface 或兼容 alias。
- Foundry Agent 的创建、接管和改进不再经过公开补丁执行命令。普通入口是 `opl agents run --domain oma --action engineer-agent`；OPL 内部以 `FoundryRun` 调用 OMA `design|diagnose`，由 Pack/Runway 完成确定性物化与隔离评测，再由 Version Registry、Owner gate、canary 和 ActivationPointer CAS 完成版本治理。operator 只使用 `opl foundry status|approve|reject|cancel|versions|rollback`；这些命令不暴露 repo、worktree、patch 或保护测试正文。
- `npm run capability-map:audit -- <repo...> --json` 是轻量跨仓 capability map 检查入口，用于在 live evidence 之前发现缺 capability id、能力类型、owner、canonical path、verification ref、forbidden surface 或 owner closeout boundary 的结构缺口。该脚本只读 contracts 与本地路径，不调度 runtime、不写 domain truth、不签 owner receipt、不创建 typed blocker。
- Standard capability map schema、scaffold conformance、profile capability-plan 与跨仓 audit 已共同支持 `capability_policy_profiles` + per-capability local profile ref；normalizer 只展开 authority/forbidden/verification/owner-closeout policy，保留 expanded shape compatibility，且 unresolved ref fail closed。该收敛减少重复合同文本，不改变 capability identity、source refs、target paths、improvement tokens 或 owner authority。
- 当前 self-evolution 非 live 闭环已经收敛为 capability-map-first：OMA 的 patch target 只来自目标仓 `contracts/capability_map.json` 或 target-owned explicit policy；`foundry_kernel_handoff` 只保留 suite entry、fixture/context 与外部学习 refs。MAS figure、RCA PPT visual feedback、MAS Scholar Skills professional pack 和 RCA PPT professional skills 都可被 `capability-map:audit` 结构化读回；这仍只是 dry-run / readback / contract evidence，不替代 live owner acceptance、domain ready、artifact ready 或 publication ready。

## 当前不能声明

- 不能声明 OPL 已全量生产可用。
- 不能声明 Temporal provider proof 等于 MAS paper closure、MAG grant readiness 或 RCA visual ready。
- 不能把 StageRun authorization、stage evidence workorder、domain-dispatch evidence workorder、refs-only receipt verified、provider/SLO satisfied、ledger closed 或 open worklist 为 0 写成 domain owner-chain、App 用户路径、release/dist、artifact authority、expected receipt instance、monitor freshness、long-soak evidence、domain ready、App release ready 或 production ready。
- 不能把 Runway safe action / repair、Console owner-action projection、Ledger verified refs、multi-plane health 或 ideal operating model foldback 写成 domain owner answer、domain typed blocker、quality/export/review verdict、artifact authority、App release verdict、Brand L5、physical delete authorization 或 ready declaration。
- 不能声明 private functional audit 分类完成就等于物理代码路径清零。
- 不能把 owner-delta summary、`next_forced_delta`、`goal_oracle_missing`、selected cohort 或 typed blocker verified 写成完成。
- 不能把 `agents legacy-cleanup apply` 的 dry-run / apply / verify ready 状态写成 OPL 已删除 domain repo 文件或 production evidence 已闭合。
- 不能为了兼容保留旧模块、旧接口、旧测试、旧 CLI alias、facade 或 wrapper；active caller 迁走后直接删除或进入 history/tombstone。

Agent Package directory 安全边界：canonical first-party identity、source 与 trust 只归 Framework Release Set；fresh 外部 registry/catalog 必须显式声明非 first-party `source`/`trust_tier`，canonical collision 或 first-party claim typed fail。persisted stale cache 的 collision/invalid claim 行在读取时丢弃，合法 external 行保留且不能隐藏 built-in seven。installed status read 失败时 directory 只暴露 `agent_package_repair`；`exposure_state=disabled` 时不得暴露 activate，恢复入口继续由 preferences action 持有。

## 参考入口

- [文档索引](./README.md)
- [项目概览](./project.md)
- [架构](./architecture.md)
- [硬约束](./invariants.md)
- [关键决策](./decisions.md)
- [OPL Family 当前状态与理想目标差距](./active/current-state-vs-ideal-gap.md)
- [OPL 与 Foundry Agents 理想目标态](./references/runtime-substrate/opl-family-agent-ideal-state.md)
