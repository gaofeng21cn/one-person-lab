# OPL 当前状态

## 当前公开角色

- `OPL` 当前公开认知固定为三层：`OPL Framework -> One Person Lab App -> Foundry Agents`。Framework 是开发与运行集成的 stage-led 智能体框架；App 是普通用户运行和使用这些智能体的桌面工作台；Foundry Agents 是 MAS/MAG/RCA 以及后续领域智能体。
- `OPL Framework` 是 one-person lab 的顶层完整智能体运行框架：它面向高价值知识工作的全自动交付，提供 `Codex-default session runtime`、显式 activation 层、跨仓 shared modules / contracts / indexes，以及 domain stage 的 durable orchestration / projection 支撑。
- 当前公开产品线保持三层：`Framework control plane -> App workbench -> Foundry/domain agents`。
- `OPL` 持有 Codex-default session/runtime、智能体注册表、工作空间 / 会话 / 进度 / 交付物接口面，以及机器可读合同。
- `OPL` 持有 stage descriptor、handoff envelope、lifecycle receipt、typed family queue、approval/wakeup/retry/dead-letter transport、trace/projection/parity 这类 domain-neutral 控制面；domain agent 持有 stage 内专家语义、prompt/skill、质量判断和 truth authority。大型任务应按接近人类专家实施的阶段推进，Agent executor 是阶段内最小执行单位，`Codex CLI` 是当前第一公民 executor。
- `OPL` 的目标是上收所有智能体运行外围能力：stage attempt ledger、typed queue、checkpoint/closeout/receipt、source fingerprint / idempotency、artifact index、file lifecycle、retention、restore proof、migration ledger、workspace lifecycle、human gate / resume token 和 operator projection。domain repo 只保留领域 stage 语义、知识/记忆合同、质量 gate、artifact locator contract 和最终 verdict authority；真实 artifact 内容属于 workspace / runtime artifact root。生产在线目标必须依赖 Temporal-backed provider 承担 durable workflow / activity / signal / query / history；active family runtime provider 只允许 `local_sqlite` 与 `temporal`，其中 local provider 只保留 dev/CI/offline diagnostic baseline；旧 Hermes provider / Gateway / readiness / proof-provider surface 只能作为历史 provenance、参考材料、诊断语料或负向 guard 出现，不拥有 OPL framework 边界或 domain truth。`hermes_agent` 另按显式非默认 executor adapter/backend 处理。
- `OPL` 现在把 `standard domain-agent skeleton` 作为目标形态：MAS/MAG/RCA 应按统一的 `agent/`、`contracts/`、`runtime/`、`docs/` 边界提供 stage 定义、提示词、Skill、knowledge、quality gate、sidecar、receipt schema、projection builder 和 artifact locator contract；业务内部不要求完全同构。当前对齐仍以 manifest adapter 为准：2026-05-14 fresh `./bin/opl agents list --json` 为 `aligned_count=3`、`missing_count=0`、`drift_detected_count=0`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`、`production_closure_gap_count=12`、`provider_temporal_residency_gap_status=closed_by_fresh_proven_proof`。MAS/MAG/RCA 三仓均已 descriptor-level aligned，且均已有 repo-source anchor evidence；provider residency gap 已由 fresh Temporal proof 关闭。破坏性物理目录重组仍需等 direct skill path、OPL-hosted path、restore/provenance proof、focused tests 和 no-forbidden-write proof 都稳定后执行。
- `OPL` 继续持有 family-level shared modules、shared contracts 与 shared indexes 的顶层语义与注册面。
- OPL framework 后续开发、跨仓 stage-led 对齐、执行语言/runtime dependency 选择和过时面退役的总入口是 `docs/references/runtime-substrate/opl-stage-led-agent-framework-roadmap.md`。Temporal 细化实施继续由 `docs/references/runtime-substrate/temporal-family-runtime-provider-plan.md` 承接；domain repo 的退役清理必须回指这个总入口和各自 status/project/invariants。
- OPL 距离完整生产级智能体框架的当前差距、功能 follow-through 和 live evidence gate 统一由 `docs/active/production-framework-closure-gap-matrix.md` 维护；2026-05-14 一次性 functional closure 计划已归档到 `docs/history/process/plans/2026-05-14-production-functional-closure-plan.md`。当前工程判断仍是 `65-75% framework/control-plane landed`、`25-35% production closure remaining`；其中 provider readiness、provider continuous proof receipt projection、latest production proof persistence、runtime tray provider proof operator item、owner receipt envelope、memory/lifecycle receipt refs、legacy no-active-default evidence、operator workbench、stage-attempt/workbench workspace/source intake projection、memory locator/index projection、artifact gallery projection、package/export lifecycle projection、route-decision graph projection、review-repair queue projection、quality/readiness projection、observability/SLO repair-command projection、operator action routing projection、native-helper execution envelope projection、domain projection ingestion projection、统一 domain-agent descriptor、release/dist descriptor CLI 同步、MAS/MAG/RCA provider-hosted task-bound attempt live path、MAS binding-derived hydrate、typed closeout memory-ref summary、MAS route-memory body-free inventory / grouping / review summary、manifest timeout fail-closed 和 closeout gate 已有可测 surface。2026-05-14 fresh 显式 Temporal service / worker / production proof 已返回 ready / `production_residency_proven`，且 closeout gate 可读到 latest proof receipt；operator 超时预算下 `framework production-closeout` 已返回 `functional_closure_ready_for_live_soak`、`typed_blocker_count=0`；不能写成 OPL 已全量生产可用：仍未闭合的是真实长时 domain owner receipt chain、domain memory body / writeback apply、破坏性物理目录迁移、legacy physical delete、App productization 和周期性 provider/operator SLO。
- `Family External Orchestration Learning Board` 已成为外部 agent / orchestration 学习的默认入口：`docs/references/convergence-governance/family-external-orchestration-learning-board-2026-04-30.md`。新 source 只允许归入 `adopt_family_contract`、`adopt_domain_template`、`watch_only`、`reject` 或 `saturated`，不能把学习结果只留在对话里。
- `OPL Runtime Manager` 已冻结为 provider-backed 产品控制面：它管理 family runtime provider 的 provision、profile、typed family queue、stage attempt ledger、domain dispatch、task registration、诊断、恢复入口、Rust native helper catalog 与高频状态索引，但不承担 domain scheduler/session/memory kernel。
- `Codex CLI` 是默认且第一公民的具体交互与执行宿主；Temporal-backed provider 是 OPL production online runtime 的必需 substrate，当前 provider code 已落地到 workflow/activity/signal/query、CLI start/query/signal、本机 service lifecycle、worker lifecycle contract、typed closeout ingestion、provider receipt 和 repo-native live residency proof。`opl family-runtime service start|status|stop --provider temporal` 管理本机 Temporal service state；`opl family-runtime residency proof --provider temporal --live` 会启动 Temporal test server 与真实 worker，跑通 completed attempt、human gate / user instruction / resume signal、worker restart 后 re-query、missing closeout blocked 和 domain-truth boundary proof。`opl family-runtime residency proof --provider temporal --production` 是本机 managed service / managed worker 验收入口：没有配置 Temporal 地址或 managed local service、服务不可达、worker 未 ready 或 worker transport probe 失败时 fail-closed 为必需依赖未就绪 blocker，并返回 typed platform blocker、operator repair action、runtime snapshot 与 blocked proof receipt；配置完成后用真实 service 跑 completed / blocked attempt、signal history、restart re-query 和 typed-closeout boundary，并把 latest proof 持久化到 OPL state 供后续 hydrate 使用。`hermes_agent` 与 `claude_code` 同属显式非默认 executor adapter/backend；它不作为兼容 runtime、engine、provider、默认 executor 或 readiness path，也不形成与 `Codex CLI` 的行为、质量、工具语义或 resume 等价承诺。
- 当前活跃实现是三个独立 `domain agent` / `Foundry Agent` 仓：`MAS`、`MAG`、`RCA`；其中 `MAS` 在 v2 alignment 下继续作为独立医学科研 domain agent，并以单一 MAS domain app skill 接入 `Codex` / `OPL`。IP、Award、Thesis 与 Review Foundry 均保持定义阶段。
- `OPL` 在 MAS v2 alignment 中只持有统一定义、shared module/contract/index 注册、模块发现、OPL-hosted runtime 外围和 MAS-owned projection 的消费面；`MAS` 继续持有医学 stage pack、controller truth、quality authority、publication gate 与研究交付真相。MAS 的 runtime-facing surface 只作为薄 adapter / owner receipt / projection builder 进入 OPL，不再被写成一套与 OPL 平行的通用运行框架。
- `MAS` 近期已完成 monolith / no-history physical absorb closeout；外部 `MDS` checkout 不再是 MAS 默认 study/status/progress/cockpit operation 的运行必需依赖，只保留 MAS 显式声明的 backend audit、source provenance、historical fixture、explicit archive import、upstream intake 和 parity oracle 引用。`MDS` 不进入 OPL 顶层 agent 列表，也不升级为单独 OPL-managed domain agent。

## 当前主线产品模型

当前主线公开模型统一为：

- `system`
- `engines`
- `modules`
- `agents`
- `workspaces`
- `sessions`
- `progress`
- `artifacts`

这组资源是 `opl` 与 One Person Lab App / OPL-branded AionUI GUI/WebUI 的共同产品真相。
其中 `agents` 资源已经开始消费各 domain 仓 repo-owned 的 `domain agent entry spec`，而不是只由 OPL 顶层静态描述。

## 当前默认入口

- 默认前门是 `opl`；`opl exec` 负责一次性请求，`opl resume` 负责续接会话。
- `opl install` 是当前最短一键安装入口：默认安装或复用 `Codex CLI`、Temporal-backed family runtime provider、`MAS`、`MAG`、`RCA`，同步短名 Codex skills，并以保守 managed 模式同步推荐 companion skills 和 `officecli` CLI 工具；MAS 声明的 MDS 相关 surface 只作为显式 companion diagnostic / oracle / intake 引用读取，不再作为 OPL 默认安装依赖。Temporal 与 Codex CLI 一样属于 production online OPL 的必需依赖，OPL 通过 `opl family-runtime install|repair`、`opl runtime manager action --apply` 或 provider-specific repair 路径触发安装/修复、检查 readiness 并报告状态；provider service lifecycle 仍由 Temporal/provider-specific 管理，不变成 OPL domain runtime 实现。旧 Hermes online-management / Gateway surface 不再作为 active provider、repair path、readiness path、provider proof surface 或 compat bridge；如出现只能按历史 provenance、诊断语料或负向 guard 读取。显式 `hermes_agent` executor adapter 不通过 install/readiness/Gateway 面启用，必须走独立 executor receipt / audit / fail-closed gate。若 OPL 品牌 GUI 已安装则尝试打开，macOS 上未安装时会自动下载并安装匹配当前平台的 release DMG 后再打开；历史 8787 Product API service 模块已退役。
- `opl system` / `opl system initialize` 现在把 `Codex CLI` 当作受管 runtime dependency：报告实际命中的 binary path、raw version、parsed version、最低版本策略、版本状态与必要诊断。同版本兼容 wrapper / alias 会归并到当前有效入口，不再作为普通用户诊断；低于最低版本或无法解析当前命中版本时不会报告为 ready。当前默认最低版本是 `0.125.0`，可用 `OPL_MIN_CODEX_CLI_VERSION` 覆盖。
- 这几个入口默认继承 `Codex` 语义；只有显式 runtime switch 或显式 domain activation 才进入不同语义。
- `opl skill sync` 负责把家族 domain app skill pack 同步到 Codex 环境，供默认 `opl` / `opl exec` / `opl resume` 直接使用；默认 sibling repo 发现已经按 workspace/worktree 布局自动解析，不再依赖 `OPL_FAMILY_WORKSPACE_ROOT`。MAS v2 对外只承认一个 MAS domain app skill，OPL 消费该 skill 的 repo-owned entry/projection truth，不新增 OPL-only MAS skill family。
- `opl module install --module <module_id>` 现在走完整闭环：clone 到 OPL-managed modules root，执行仓库 bootstrap，同步对应 skill pack，再跑仓库健康检查。
- `opl module exec --module <module_id> -- <domain_cli_args...>` 是当前自动化调用 domain CLI 的 OPL 管理入口：它从 OPL module registry 解析当前 checkout，并在该 checkout 内启动 repo-owned CLI，而不是依赖用户 `PATH` 上可能滞后的全局 tool。
- `opl framework locate` 是 OPL-compatible agent 查找外部 OPL Framework 运行依赖的稳定机器入口。解析顺序是 `OPL_FRAMEWORK_ROOT`、`OPL_CLI_BIN`、`OPL_BIN`、`PATH` 上的 `opl`、当前 CLI entry、sibling `one-person-lab` checkout；显式 env 配错时 fail-closed。这个入口定位 framework 环境，不负责自动发现 domain agent；`One Person Lab App` 仍是可选前端和投影消费者。
- `opl packages manifest` 现在暴露 OPL 中央 Packages 坐标雏形：WebUI Docker 镜像、native helper GHCR 包，以及 MAS/MAG/RCA 模块源码归档坐标；当前 `opl install`、App 首启协调、`opl system reconcile-modules` 与环境管理仍以 git checkout / sibling repo 更新到远端最新为正式路径。MAS 不恢复用户安装型 standalone GitHub Release 通道，MDS 也不作为独立 OPL module 坐标回流。
- `opl system initialize` 是当前一键配置安装的聚合面：同时暴露 workspace root、Codex CLI first-class executor、Temporal-backed family runtime provider readiness、domain modules、推荐 companion skills、OPL 品牌 GUI shell、local support service、developer supervisor 用户级配置与下一步动作；没有显式 workspace root 时默认使用用户 Home 目录。首启 readiness 分成 Core、Domain modules、Temporal family runtime provider 三层：Full OPL readiness 需要三层都 ready；Temporal 未配置、不可达或 worker 未 ready 是必需依赖 blocker；旧 Hermes provider/Gateway/readiness diagnostics 只按历史 provenance、诊断语料或负向 guard 报告。
- Fresh install 与 GUI 首启测试已有机器可读矩阵：`contracts/opl-framework/fresh-install-test-matrix.json` 冻结本机 clean-room、离线 blocker、干净 macOS VM 首启、首启 JSONL 日志和 GUI accessibility labels；本机验证入口是 `npm run test:fresh-install` / `./scripts/verify.sh fresh-install`，OPL Verify workflow 已纳入该 lane。真实 GUI 首启由 `gaofeng21cn/one-person-lab-app` 的 `shells/aionui` workflow / smoke harness 执行，目标 runner labels 为 `self-hosted`、`macOS`、`opl-gui-vm`。用户可见文案应把已配置 family runtime provider 未 ready 写成 Full online runtime degraded，不得把 Hermes gateway 单独写成未来唯一 readiness blocker。
- 测试入口已经收敛为机器可检查的 lane registry：`npm test` / `npm run test:fast` 是默认快速本地信号，`npm run test:smoke` 是秒级核心入口，`npm run test:regression` 承接宽回归，`npm run test:integration` 覆盖 ACP/session runtime、install/configure 与 retired Product API fail-closed，`npm run test:artifact`、`npm run test:fresh-install`、`./scripts/verify.sh native` 与 `./scripts/verify.sh structure` 保持独立语义。所有 active 测试文件必须通过 `node scripts/test-lanes.mjs assert-coverage` 归属到 lane；`docs/references/current-support/opl-test-lane-governance.md` 记录维护口径。
- `opl runtime manager` 是当前 Runtime Manager 的机器可读 projection：它展示 OPL 管理 Temporal-backed family runtime provider 的 owner split、非目标、v1 domain registration registry、family runtime queue、stage attempt provider surface、Rust native helper lifecycle、native helper target 与 state index target；Temporal-backed provider code 已落地，local provider 是 dev/CI/offline 诊断信号，旧 Hermes provider/Gateway/readiness/proof-provider surface 只作为历史 provenance、诊断语料或负向 guard 资产，真实 provider deployment / domain soak 仍是未完成证据。Rust helper 源码、doctor/repair/prebuild 脚本和 Cargo workspace 已进入 npm package surface；当 helper 可发现时，它会调用 native doctor / state indexer / artifact indexer 与 runtime watch，并把 `opl_runtime_manager_native_state_projection` 写入 `OPL_STATE_DIR/runtime-manager/native-state-index.json`，同时报告 TTL、history、failure、last-success、freshness、结构化 diff 与 history GC preserved/removed 状态。
- `opl family-runtime` 已能把 MAS sidecar 导出的 `paper_autonomy/repair-recheck`、`paper_autonomy/ai-reviewer-recheck`、`paper_autonomy/gate-replay`、`paper_autonomy/route-decision` 与 `paper_autonomy/guarded-apply` 入队、去重、派发，并在 task / dispatch JSON 中保留 `paper_autonomy` 投影：study id、next owner、callable surface、source refs、source fingerprint 与 idempotency key。MAS `paper_autonomy/guarded-apply` dispatch 会自动建立 task-bound provider-backed stage attempt，记录 workspace locator、provider kind、source fingerprint 和 domain sidecar closeout refs；MAS `mas_family_sidecar_dispatch_receipt` 中的 provider-hosted guarded-apply receipt / typed blocker 会被转换为 OPL typed closeout packet。`family-runtime intake/tick --hydrate` 现在能从 active workspace binding 派生 MAS sidecar export command，并自动携带 latest persisted Temporal production proof；env override 仍保留为显式 override。显式声明 provider-hosted / controlled 的 MAG/RCA sidecar task 也会建立 task-bound stage attempt；dispatch task JSON 同时保留 OPL envelope 和 domain product sidecar action envelope，MAG product sidecar receipt refs 与 RCA no-regression evidence refs 可进入 OPL typed closeout ledger。`opl family-runtime attempt create|list|inspect|start|query|signal` 现在把 domain stage 执行登记为 provider-backed stage attempt，记录 provider kind、workflow id、workspace locator、checkpoint/closeout/human gate/user instruction/resume refs、retry budget、task binding、provider run/activity event/heartbeat、route impact 与 provider receipt，并可对 Temporal workflow 执行 start/query/signal；缺少 Temporal 地址且没有 managed local service state 时 fail-closed。`opl family-runtime service start|status|stop --provider temporal` 写入/读取本机 service state，worker 与 production proof 可从该 state 读取 address；`opl family-runtime residency proof --provider temporal` 聚合当前配置与 ledger 证据，在 live server/worker 未就绪时明确返回 `production_residency_needs_live_evidence`；`--live` 启动 Temporal test server 和真实 worker，返回 `production_residency_code_path_proven` 级别的 repo-native 代码路径证明；`--production` 只使用已配置或本机托管的 Temporal service / managed worker，证明通过时返回 `production_residency_proven` 并持久化 latest proof，未配置、不可达、launcher 缺失、worker 未 ready 或 worker transport probe 失败时返回 typed blockers。attempt query/workbench 会把 typed closeout 缺失的 domain dispatch 视为 checkpointed，而不是 completed；typed closeout ledger 对同一 `closeout_id` 的相同 packet 重放保持幂等，对冲突 packet fail-closed；query/workbench 会投影 human gate ledger、user instruction ledger、resume ledger 与 dead-letter task ledger。该投影只服务 OPL Runtime Manager 和本地 inbox/event 可见性；OPL/provider 不写 MAS study truth、MAG grant truth、RCA visual truth、publication/fundability/visual quality、artifact gate 或 `current_package`。
- 当前完成边界：OPL 已落地 MAS paper autonomy task 的 family queue / projection / guarded dispatch bridge；MAS `paper_autonomy/guarded-apply` dispatch 已能自动建立 task-bound provider-backed stage attempt，并在 MAS sidecar 返回 dispatch receipt、typed blocker 或 owner receipt 时写入 OPL typed closeout ledger。DM002 早期 live dispatch 曾两次返回 `typed_blocker` / `blocked_no_mas_owner_apply_receipt`；MAS owner blocker projector 落地后，同一 task `frt_54187384a4fa403abda12ce5` 按 source fingerprint `a0cdbf341fae623d` requeue，OPL stage attempt `sat_df08b839ce33a33ee92974f2` ingest MAS dispatch receipt `artifacts/runtime/opl_family_sidecar/dispatch_receipts/e97b1c8cd25f3a019f90.json`，route impact 为 `mas_owner_apply_receipt_observed` / `mas_owner_receipt_present`，`writes_performed=false` / no-forbidden-write guard 成立；该 receipt 消费 MAS-owned `controller_decisions/latest.json` stable blocker，blocked reason 为 `medical_paper_readiness_missing`。MAG/RCA domain adapters 已在各自 repo main 上落地，并已通过显式 provider-hosted task 进入同一 OPL typed queue；MAG attempt `sat_3c210fbc86393af8d3a0e90c` 已 ingest product sidecar receipt refs，RCA attempt `sat_8623e55a341caf9672ed8ab2` 已 ingest no-regression evidence refs；standard skeleton、stage 和 domain-memory descriptor 当前均 resolved。Temporal provider code、本机 service lifecycle、worker lifecycle contract、typed closeout ingestion、Codex stage runner repo/test harness 与 stage attempt workbench 可见性已落地；local provider 只是 dev/CI/offline baseline，旧 Hermes provider/Gateway/readiness surface 只保留非 provider diagnostic/provenance/negative-guard 信号。OPL 侧已用 fixture E2E 覆盖 gateway stopped -> repair -> ready、跨仓 MAS/MAG/RCA notification / approval / retry / dead-letter、provider-hosted task-bound attempt bridge、typed closeout gate 和 closeout idempotency / conflict fail-closed；Codex runner 已具备 `dry_run`、`live_dry_run` 与 `codex_cli` process-supervision harness。2026-05-14 fresh 显式 Temporal service status 为 `running` / `server_reachable=true`，worker status 为 `ready` / `worker_ready=true`，`OPL_FAMILY_RUNTIME_PROVIDER=temporal ./bin/opl family-runtime status --json` 为 `full_online_ready=true` / `durable_online_ready=true`，`residency proof --provider temporal --production` 返回 `production_residency_proven` 并写入 latest proof。Aion Runtime Workbench 已展示 provider completion、domain ready verdict、human gate、dead letter、rejected writeback 五个 operator 状态轴，并把 human gate signal 限制为当前 attempt id。统一 Agent Executor Adapter 边界已在 OPL/MAS/MAG/RCA 中收紧：默认 concrete executor 仍是 `Codex CLI`；`hermes_agent` 与 `claude_code` 都是 canonical 显式非默认 executor adapter/backend，只走独立 receipt、audit 与 fail-closed gate，不作为旧 Hermes/Gateway 兼容接口，也不承诺行为、质量、tool semantics 或 resume 等价。`family_runtime_controlled_apply_contract` 已在 attempt query 中开放 MAG/RCA controlled apply projection，并固定 OPL 只接收 domain owner receipt、typed blocker 或 no-regression evidence refs，不写 domain truth、artifact 或 memory body。当前 production closure 优先 MAS 三篇真实 paper line；MAG/RCA live sidecar ingestion 已证明 OPL path 可用，但 grant/visual long soak 与 domain owner receipt 仍未完成。fresh MAS read-only closeout projection 已显示 DM002 -> `ai_reviewer_re_eval`、DM003 -> `artifact_delta`、Obesity -> `artifact_delta`，且 `writes_performed=false`；DM002 同时带 publication-route memory consumed ref 与 MAS-owned writeback receipt refs。MAS 2026-05-14 functional follow-through 已把 route-memory body-free inventory 扩展到 operator grouping / stale-deprecated review summary，并补齐 closed controller authorization 清理、artifact delta freshness、supervisor-only live quality repair owner routing、AI reviewer currentness guard 和 runtime-read manual finish guard；这些只增加 MAS owner/projection surface 的可见性和安全门槛，不改变 OPL 对 MAS memory body、study truth、publication verdict 或 artifact authority 的只读边界。剩余验收分两类：一类是真实生产 / 长时运行验收，包括 MAS 长时 paper activity soak、live provider-hosted guarded apply progress delta / human gate / stop-loss、真实 token/cost/progress 观测校准、Temporal 长时 SLO，以及后续 grant / visual line 的 controlled apply 到最终交付；另一类是 production closure 收尾，包括真实 workspace/runtime accepted/rejected receipt instance、三仓 memory body / writeback apply 泛化、repo-source 物理目录标准化，以及无 active caller 之后对旧 Hermes/Gateway/local-manager vocabulary 和 legacy manager 入口的物理删除或 history/tombstone 归档。
- `opl runtime snapshot` 是 OPL 品牌桌面托盘与 App Runtime Workbench 的轻量投影面：它把 active domain manifest / progress projection 归入 `attention_items`、`running_items`、`recent_items`，并可消费 MAS-owned `mas_opl_runtime_workbench_projection` 生成单篇 study drilldown / read-only workbench 数据。当前 snapshot 也暴露 `provider_continuous_proof`、stage-attempt operator items、operator action routing shell、native-helper execution envelope、domain projection ingestion 与 `managed_domain_provider_states.medautoscience`，用于 One Person Lab App / status 直接展示 Temporal proof receipt、stage attempt control/receipt/action-route refs、native helper index receipt metadata、domain-owned projection refs、MAS 提供的 managed Temporal state consistency 与 legacy retirement tombstone proof。stage-attempt item 可展示 controlled apply refs、artifact locator / restore proof、memory refs、writeback receipt refs 与 rejected writes；domain projection ingestion 只展示 manifest projection pointer、surface kind、owner 和 source refs。这个区块是只读 App/status read-model，保留 source refs 与 authority boundary，不写 MAS truth，不执行 domain action、domain projection builder 或 native helper mutation，不授权 quality verdict、submission readiness、artifact mutation、export verdict 或 publication readiness；剩余边界仍是真实 production provider-hosted live paper apply 与长时 domain soak。
- MAS progress、publication、quality、runtime control 与 `mas_opl_runtime_workbench_projection` 等投影在 OPL 中只作为 shared workbench projection 被读取和展示；这些投影不得被 OPL 文案升级为 `ready`、submission-ready、publication-ready、质量裁决、runtime authority 或 artifact authority 的最终 verdict。
- `Family Product Operator Projection` 已冻结为 OPL family 监督面：它消费 runtime attempt、domain quality projection 与 incident learning loop 合同，展示 `source_refs`、`freshness`、`owner_split`、`next_surface_ref` 与 `human_gate_reason`，但不接管 domain runtime truth 或 domain quality authority。
- Rust native helper 的生产化门禁已经进入仓库验证面：`native:doctor`、prebuild check、`npm pack --dry-run`、Rust tests/build、state cache 与 MAS/MAG family smoke 共同构成 native lane；CI 使用 fixture family smoke 验证 MAS/MAG 已声明的 `opl_stage_runtime_registration` 与 `native_helper_consumption.proof_surface` 投影，本地集成机继续可以对真实 MAS/MAG sibling workspace 做只读 indexing / registration / proof smoke。结构质量门禁分成两层：本地与 Verify workflow 的 `./scripts/verify.sh structure` 是 blocking gate，但其中 `sentrux gate .` baseline drift 只作为 advisory 并继续输出 OPL quality details；line budget 和显式 `sentrux check .` rules 仍是 blocking。单独的 Sentrux Advisory workflow 只发布非阻断结构信号和 OPL quality details sidecar。
- `Family Persistence / Lifecycle / Owner-Route` 已冻结为 OPL family 共享控制面：`contracts/family-orchestration/` 现在包含 persistence policy、lifecycle ledger 与 owner-route schema，TS helper 与 Python mirror 已可构建这些 surface；`family-product-entry-manifest-v2` 只暴露 discovery refs。MAS 作为完整参考 adapter 映射 SQLite sidecar / lifecycle ledger / owner-route，MAG 做既有 runtime-control / grant-progress 的轻 adapter，RCA 做 managed-run/session/review projection 的厚 adapter且 SQLite 继续 deferred。OPL 只消费这些 projection，不成为 domain runtime、memory store、scheduler、quality verdict 或 artifact authority。
- 2026-05-11 skeleton/lifecycle 校准：MAS 已验证的 SQLite 持久化、file lifecycle、restore proof、artifact index、retention 和 lifecycle 管理经验应抽象成 OPL framework primitives；MAS 私有 study truth、publication quality、evidence/review ledger 和 manuscript/package authority不得迁出。MAG/RCA 后续要复用 OPL lifecycle primitives，并继续通过 skeleton mapping 暴露现有 stage/action/projection surface；当前对齐以 manifest/adapter 为准，物理目录重组必须等 direct skill path、OPL-hosted path、restore/provenance proof 和 focused tests 都稳定后执行。
- `Family Runtime Supervision` 已进入 shared contract 面：`contracts/family-orchestration/family-runtime-supervision.schema.json` 冻结 adapter id、cadence、last_success / last_tick、lease freshness、SLO state、repair command、safe reconcile hint、domain-owned source refs 与 read-only authority boundary；`runtime-task-companions` 可构建该 surface，`family-product-entry-manifest-v2` 可发现它。OPL 只做 discovery / export / parity / queue dispatch / projection，在线 wakeup substrate 由已配置 family runtime provider 承担；OPL 不成为 domain scheduler、session store、memory owner、quality verdict owner 或 artifact authority。
- `Family Action Catalog` 已进入 shared contract 面：`contracts/family-orchestration/family-action-catalog.schema.json` 冻结可调用 action metadata，`family-product-entry-manifest-v2` 可携带 `family_action_catalog`，TS helper 与 Python mirror 可派生 CLI、MCP、Skill、product-entry、OpenAI 与 AI SDK descriptor；`opl actions list|inspect|export` 只做发现和导出，不执行 domain action，也不把 handler truth 移进 OPL。
- `Family Stage Control Plane` 已冻结为 family 级设计方向，并已有最小 shared descriptor / discovery surface：`contracts/family-orchestration/family-stage-control-plane.schema.json` 与只读 `opl stages list|inspect` 负责 stage descriptor、skill / prompt / evaluation refs、handoff、projection 与 parity 语义；`docs/references/convergence-governance/family-stage-control-plane-adoption-plan.md` 记录 MAS 以 stage 为控制面的经验如何上升到 OPL，RCA/MAG 如何按专家工作流吸收。domain repo 继续持有真实创作、审核、质量 verdict 和交付 authority。对 MAS 来说，第一阶段是 inventory 和映射，不是替换现有 stage 名称、数量或 route contract。
- `Family Transition Spec` 已接入 domain manifest / descriptor read model：`family-transition-runner` 仍持有通用 schema、guard 匹配、fail-closed、dead-letter、receipt/projection envelope 和 matrix runner；`domain manifests` / `agents descriptor` 可消费 domain 暴露的 actual `family_transition_spec` + `family_transition_matrix_cases` 并产出 matrix summary。若 MAS product-entry manifest 只挂 `family_transition_spec_descriptor`，OPL 投影为 `descriptor_only` / `refresh_required`，并指向 MAS `study-state-matrix` 的 spec/cases locator；OPL 不解释 publication quality、fundability、visual/export ready verdict，也不执行 domain action。
- `Family Domain Memory` 已进入 locator / receipt / migration-plan 合同面：`contracts/family-orchestration/family-domain-memory-ref.schema.json`、`family-domain-memory-writeback.schema.json` 与 stage `knowledge_refs` 让 OPL 可以发现、索引、投影和携带 domain-owned memory refs；`opl domain-memory list|inspect|migration-plan` 是只读索引入口。2026-05-15 fresh CLI 读模型显示 `resolved_memory_descriptor_count=3`、`missing_memory_descriptor_count=0`，并从 stage attempt closeout ledger 投影 `runtime_receipt_evidence.closeout_count=18`、`consumed_memory_ref_count=1`、`writeback_receipt_ref_count=2`、`rejected_write_count=15`、`opl_writes_memory_body=false`：MAS 的 `mas_publication_route_memory`、MAG 的 `mag_grant_strategy_memory`、RCA 的 `rca_visual_pattern_memory` 均已通过标准 `family_domain_memory_ref.v1` descriptor 被 OPL 解析；MAG accepted/rejected memory receipt refs 已可通过 `domain-memory inspect --domain mag` 钻取，MAS closeout refs 当前为 6 条且包含 DM002 owner stable blocker receipt。stage attempt query/workbench 已能显示 consumed memory refs、writeback receipt refs 与 rejected writes。MAS route-memory inventory 现在还会输出 body-free receipt refs 的 operator grouping 与 stale/deprecated review summary，供 OPL/App 继续做 ref-only grouping；MAS/MAG/RCA 的 memory 内容、写回接受/拒绝、route / fundability / visual quality 判断和 artifact authority继续归各自 domain 仓。当前状态是 `family_index_resolved_all_active_domains_descriptor_plus_runtime_receipt_ref_projection`，真实 retrieval、writeback apply、memory body migration 和跨 domain soak 仍需后续验证。
- `framework production-closeout` 现在同样汇总 stage attempt typed closeout 的 consumed refs、consumed memory refs、writeback receipt refs 与 rejected writes，并在 `memory_ref_summary` 中明确 `opl_writes_memory_body=false`。它还对 domain manifest command 使用 fail-closed timeout，从 admitted domain contracts 动态枚举 closeout breakdown，并读取 runtime event ledger 中的 Temporal production proof receipt 生成 `provider_continuous_proof`；latest production proof 会同时保存在 OPL state，供 MAS hydrate 自动携带。默认 closeout 预算已能覆盖当前真实 MAS manifest 并解析三仓 manifest；显式短预算或 stalled manifest 的 `command_timeout` 应读作 OPL manifest-command availability blocker，不是 MAS truth、质量或 owner apply 失败。当前 closeout 返回 `functional_closure_ready_for_live_soak`、`stage_attempt_evidence.ledger_attempt_count=18`、`closeout_packet_count=18`、`domain_memory.runtime_receipt_evidence.closeout_count=18`。
- `Unified Domain-Agent Descriptor` 已成为 OPL 统一总入口的 canonical CLI 事实：2026-05-14 fresh `./bin/opl agents descriptors --json` 可聚合 admitted domain 的 entry、standard skeleton、action catalog、stage control plane、family transition readiness、domain memory descriptor、skill catalog、runtime/session/progress/artifact refs、parity 和 authority boundary；`./bin/opl agents descriptor --domain mas --json` 可检查单个 domain；`node dist/cli.js agents descriptors --json` 已与 canonical 入口同步。维护者和 App 仍应把 `opl agents list|inspect`、`opl stages list|inspect`、`opl domain-memory list|inspect`、`opl actions list|inspect` 作为专题 drilldown。这个入口只做 refs/status/parity/authority boundary 聚合，不授权 OPL 读取或内嵌 memory 正文；MAS 论文路线经验库正文、维护、写回接受/拒绝和 publication-route 判断仍由 MAS Markdown-first memory 与 MAS owner surface 持有。release/dist 构建输出必须持续跟进 canonical `./bin/opl` 行为。
- 这个 stage-led 方向也明确了 OPL 与 domain agents 的产品关系：`MAS`、`MAG`、`RCA` 是运行在 OPL family framework 上的独立 domain agents，不是 OPL 内部模块；它们仍可被 Codex App 通过各自单一 app skill 直接调用，OPL 只是提供长期托管、唤醒、队列、投影、恢复和跨域可见性。
- 当前产品分层进一步固定为：开发者面向 `OPL Framework` 开发 OPL-compatible agent package / repo；普通用户面向 One Person Lab App 使用通用工作台和 Foundry Agents；领域仓不内嵌 OPL runtime，而是声明 framework version、descriptor、skill、stage、quality gate、artifact locator 和 projection，并在需要 framework 能力时先通过 `opl framework locate` 定位外部 OPL Framework 环境。OPL Framework 可安装、发现、托管和投影这些 Agent；App 是可选工作台，不是运行依赖定位或 domain truth owner。
- 2026-05-15 fresh assessment：stage-led / Temporal-backed 计划已经落到 `shared contracts + domain descriptors + local queue / attempt ledger + Temporal provider code + standard skeleton discovery / validation + unified domain-agent descriptor + runtime snapshot / Aion workbench projection + operator action routing shell + native-helper execution envelope + domain projection ingestion + Codex runner repo/test harness + Temporal live residency proof + provider continuous proof projection + latest proof persistence + MAS/MAG/RCA provider-hosted task-bound attempt live path + MAS binding-derived hydrate`，但没有完全落到生产闭环。实测 `./bin/opl agents list --json` 为 `aligned_count=3`、`missing_count=0`、`drift_detected_count=0`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`、`production_closure_gap_count=12`、`provider_temporal_residency_gap_status=closed_by_fresh_proven_proof`；`./bin/opl agents descriptors --json` 为 `descriptor_surfaces_resolved_count=3`、`memory_descriptor_resolved_count=3`、`stage_control_plane_resolved_count=3`、`action_catalog_resolved_count=3`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`、`production_closure_gap_count=12`、`provider_temporal_residency_gap_status=closed_by_fresh_proven_proof`；`node dist/cli.js agents descriptors --json` 已返回同样的 descriptor summary；`./bin/opl stages list --json` 为 `resolved_planes_count=3`、`stages_count=18`；`./bin/opl domain-memory list --json` 为 `resolved_memory_descriptor_count=3`、`missing_memory_descriptor_count=0`，并投影 `runtime_receipt_evidence.closeout_count=18`、`consumed_memory_ref_count=1`、`writeback_receipt_ref_count=2`、`rejected_write_count=15`；默认 `./bin/opl family-runtime status --json` 当前选中 `local_sqlite`，`full_online_ready=false`、`durable_online_ready=false`、queue `total=17` / `succeeded=17`，persisted stage attempt ledger 为 `total=18`、`completed=18`；这是 dev/offline baseline；显式 `OPL_FAMILY_RUNTIME_PROVIDER=temporal ./bin/opl family-runtime status --json` 返回 `full_online_ready=true` / `durable_online_ready=true`，service / worker 当前 ready，production proof 返回 `production_residency_proven`，且 `framework production-closeout` 可读到 `provider_continuous_proof.continuous_proof_status=all_observed_proofs_proven`。`family-runtime intake --domain medautoscience --source binding-hydrate-auto-proof --json` 可从 active workspace binding 派生 MAS sidecar export，并自动携带 latest persisted proof，重跑为 `exported_count=4` / `idempotent_noop_count=4`。`opl agents list/inspect/descriptors` 已暴露 `descriptor_readiness`、`physical_skeleton_layout_audit`、`physical_skeleton_evidence`、`provider_closure_evidence` 与 `production_closure_gaps`，因此 descriptor aligned、三仓 anchor observed、provider residency gap closed、production closure 依赖 live evidence 或 typed blocker 这几层可以被机器读模型直接区分。OPL 层当前可实测的 shared 面包括 `python/opl-harness-shared`、`contracts/family-orchestration/`、`contracts/opl-framework/`，以及 root `src/` 下 family runtime、Temporal provider、provider-hosted attempt bridge、Codex stage runner、stage control、action catalog、handoff、product-entry、domain catalog、runtime manager、native helper、skill sync、runtime tray 相关 TypeScript surface。MDS 保持较早 shared pin，符合 archive / diagnostic / upstream-intake 角色。普通 `Product Entry` / `opl resume` / `opl session resume` 路径已经收敛到 Codex-default executor；`opl session list/logs` 与 runtime status 中的旧 Hermes diagnostics 字段仅作为 diagnostics / provenance / fixture 可见性存在。`opl status runtime` 顶层 runtime substrate 应按 Temporal-backed production runtime 报告；旧 Hermes status、sessions 与 process usage 不代表默认 executor 或默认 runtime truth，local provider 也不代表 production online readiness。已新增 typed closeout required-for-completion gate、closeout idempotency / conflict fail-closed、human gate / user instruction / resume / dead-letter ledger 投影；runtime snapshot / Aion stage attempt workbench 已可展示 provider completion、domain ready verdict、controlled apply refs、artifact locator / restore proof、human gate、dead letter、memory refs、writeback receipt refs、rejected writes、明确 owner 的 action route refs、native helper index receipt metadata 和 manifest-owned domain projection refs，并可发送 human gate / resume / dead-letter repair provider-level signal。最新优先级是 OPL+MAS production paper autonomy：OPL queue 已能把 MAS guarded-apply task 绑定到 stage attempt，并已消费 DM002 owner stable blocker receipt；下一步用 DM002/DM003/Obesity 三篇真实 paper line 证明 OPL Temporal-backed attempt 能持续进入 live MAS sidecar、typed closeout、MAS owner receipt、progress delta、human gate 或 stop-loss。MAG/RCA live sidecar ingestion 已证明 OPL 可以接收 controlled sidecar receipt/no-regression refs，但 long controlled soak 与 domain owner receipt 仍后移。仍未完成的是真实 MAS 长时 paper activity soak，三仓 workspace/runtime memory body / writeback apply 的完整泛化，破坏性 repo-source 物理目录迁移、周期性长时 provider/operator SLO，以及旧默认面的 physical delete。后续文档和实现可以把“完整智能体运行框架、全自动交付”写成目标定位，但不得把当前状态表述成“全量生产可用”。
- 离理想生产级框架的主要差距不是概念或 descriptor，而是 production closure：长时 Codex/domain activity soak、MAS real paper guarded apply progress evidence、三仓 workspace/runtime memory apply receipt 泛化、MAG/RCA OPL-hosted controlled soak、human gate/resume 进入 domain owner chain 的运行证明、domain repo 物理目录标准化、Temporal provider 周期性长时 SLO，以及 active path 的旧 Hermes/Gateway/local-manager/MDS 物理残留清理。MAS 三篇 paper line 的 read-only proof 已把“OPL 能消费 MAS closeout refs”这一步补上；Temporal live proof 已把“OPL provider 代码路径能跑真实 Temporal server/worker attempt 并重启后 re-query”这一步补上；fresh production proof 已把“本机 managed Temporal service/worker 当前可达且 production residency proof 可通过”补上；provider continuous proof projection 与 latest proof persistence 已把“production proof receipt 可进入 runtime ledger、被 closeout gate 汇总并被后续 MAS hydrate 使用”补上；OPL provider-hosted task-bound attempt bridge 已把“MAS guarded apply task 以及显式 controlled MAG/RCA sidecar task 能进入 OPL stage attempt ledger 并接收 typed closeout refs”补上，当前 persisted ledger `total=18` / `completed=18`，其中 MAS 有 6 条 guarded-apply closeout refs 且最新为 DM002 owner stable blocker receipt observed，MAG 有 11 条 controlled / lifecycle / grant-stage closeout refs，RCA 有 1 条 visual no-regression closeout refs；还没有补上“OPL production Temporal provider 长时托管真实 MAS owner chain 并推动论文前进”。
- 当前旧面退役状态应按默认路径和物理残留分开判断：`MDS` 默认依赖、旧 Product API / `opl web`、project-level `.codex` / `.omx`、MAS standalone release 和 MDS 顶层 domain agent 已退出默认产品语义；旧 Hermes provider / Gateway / compatibility vocabulary 若仍出现在 domain docs、program records 或 command key 中，只能按历史来源、diagnostic、fixture、负向 guard 或待删除残留读取。它们不再拥有默认 authority。下一阶段清理应继续保证 direct skill path、domain sidecar、family queue 和 descriptor parity 可用，再删除或归档残留旧名，避免形成第二套污染源。
- 2026-05-11 retirement cleanup 已把可安全处理的 active-path residue 收口为两类：public help / command spec / Product Entry runtime 不再把旧 Hermes provider/Gateway cron、compatibility alias 或非默认 executor 展示为普通默认示例；active docs 只允许历史 provenance、diagnostic shell、fixture / test 或负向 guard 所需的旧命名存在。保留项必须显式说明 provenance/test/diagnostic/negative-guard 用途，不能重新取得默认 runtime、domain truth、readiness blocker 或 public integration authority。
- 2026-05-12 active-path residue scan 复跑 `tests/src/active-path-residue-scan.test.ts` 通过，覆盖 active docs、root help 与 operator closeout surface。当前没有新的可安全物理删除 active caller；保留的旧 Hermes provider/Gateway/frontdoor/local-manager/MDS/default-compat vocabulary 均需要继续落在 provenance、diagnostic、fixture、reference、negative guard 或 history 语境。物理删除继续以后续 no-active-caller proof、MAS paper guarded apply、direct-skill parity 和 domain fixture/provenance 需求为门槛。
- `MAS monolith / companion-retirement` 已上升为 OPL family 原则：domain 可以吸收外部 companion 的可保留能力，但吸收后默认只暴露 domain-owned capability surface；被降级的外部 companion 只能作为显式 audit、diagnostic、upstream intake 或 parity oracle 出现。未来类似吸收必须记录 source ref/hash、capability classification、license refs、domain owner、authority boundary、parity proof 和 no-history contributor audit。
- Rust native helper 的生产化门禁已经进入仓库验证面：`native:doctor`、prebuild check、`npm pack --dry-run`、Rust tests/build、state cache 与 MAS/MAG family smoke 共同构成 native lane；CI 使用 fixture family smoke 验证 MAS/MAG 已声明的 `opl_stage_runtime_registration` 与 `native_helper_consumption.proof_surface` 投影，本地集成机继续可以对真实 MAS/MAG sibling workspace 做只读 indexing / registration / proof smoke。结构质量门禁分成两层：本地与 Verify workflow 的 `./scripts/verify.sh structure` 是 blocking gate，但其中 `sentrux gate .` baseline drift 只作为 advisory 并继续输出 OPL quality details；line budget 和显式 `sentrux check .` rules 仍是 blocking。单独的 Sentrux Advisory workflow 只发布非阻断结构信号和 OPL quality details sidecar。
- 推荐 companion 能力当前包括 `superpowers`、`ui-ux-pro-max`、`officecli` skill 组和 `officecli` CLI binary；Office 类 skill 只有 skill payload 与 `officecli --version` 同时可用时才报告 ready。它们不改变 OPL runtime 语义，只作为 MAS / MAG / RCA 工作流的增强能力。
- 默认本地状态目录是 `~/Library/Application Support/OPL/state`；如需切换到其他本地状态根目录，使用 `OPL_STATE_DIR`。Developer Supervisor Mode 的 family 用户级配置文件是 `developer-supervisor.json`，可通过 `opl system developer-supervisor` 读取或设置；`enabled=auto` 使用 GitHub 登录做默认探测，`enabled=on/off` 分别手动开启或关闭。
- `Codex` 中显式调用 `OPL` 与其 domain agents 是并列的一等使用方式。
- Domain app 通过各自仓库提供的本地 CLI / 程序 / 脚本 / contract 与 skill pack 接入；`OPL` 负责统一同步与发现。
- GUI / Web 主线当前保持 `one-person-lab-app/shells/aionui -> ACP-compatible OPL session runtime`；App 产品仓持有当前 AionUI shell adapter，Framework 仓只保留 runtime truth、安装发现和消费合同。
- 当前 App 交付物是 `one-person-lab-app/shells/aionui` 构建的 One Person Lab 品牌桌面壳，它基于开源 AionUI codebase 做 OPL 裁剪与品牌化，并通过 ACP-compatible runtime surface 消费 OPL 的 Codex-default session/runtime truth；原版 AionUI app 不算 One Person Lab App，App 仓也不是 OPL runtime owner。OPL 一键安装负责打开已安装 App，macOS 上缺失时自动消费 one-person-lab GitHub Release 里的 OPL 品牌预编译 DMG；只有缺少匹配平台 / 架构 artifact 时才回退源码构建。
- App repo split 正在落地为默认维护拓扑：`one-person-lab` 保持 Framework repo；现有 GitHub 仓 `gaofeng21cn/opl-aion-shell` 已按计划迁移为 `gaofeng21cn/one-person-lab-app`，当前 AionUI-root 进入 App 仓 `shells/aionui/`；App 产品文档、打包、更新、Full first-install 包、页面状态测试、首启测试、截图和教程归 App 顶层；AionUI 只作为可替换 GUI adapter 存在。AionUI 2.0 或其他 GUI 基座应先在 `shells/*-next` 并行适配，验证通过后再切 active shell。
- `One Person Lab App` 预编译包指 Electron-builder 产出的 OPL 品牌 `.dmg` / `.exe` / `.deb` 分发文件及 `latest*.yml` updater metadata；当前这些 release artifact 由 `one-person-lab-app/shells/aionui` 构建，再通过 Framework 仓 `npm run gui:release` 上传到 `one-person-lab` GitHub Release。App 仓持有 App 包、Full 包、updater metadata 的构建/验证 source 与 App-side release contract；Framework repo 在兼容阶段继续保留 release discovery / upload companion。macOS arm64 另有 `One-Person-Lab-Full-<version>-mac-arm64.dmg` 首次安装资产，随包带 MAS/MAG/RCA、当前 family runtime provider payload、`officecli` CLI binary 和推荐 companion skill payload，但不得写入 `latest*.yml` 或成为 App 自动更新目标。
- App 内自动更新按 OPL 日期版本判断；GUI/AionUI 基线版本只作为关于页和维护诊断信息展示。
- 本地 8787 `Product API` / `opl web` 模块已退役；WebUI 路径由 OPL-branded AionUI shell 提供。

## 当前交互模式

| 模式 | 默认执行者 | 主要用途 | 状态 |
| --- | --- | --- | --- |
| 普通对话 | Codex | 讨论、解释、阅读、计划、轻量分析 | 默认 |
| 通用任务 | Codex | 本地文件工作、命令执行、验证、多步任务 | 默认 |
| 专用智能体 / domain agent | `MAS`、`MAG`、`RCA` | 医学科研、基金写作、视觉交付 | 活跃 |

## 当前 Foundry 产品线

| 产品家族 | 当前实现 | 当前覆盖范围 | 公开状态 |
| --- | --- | --- | --- |
| `Research Foundry` | `MAS / Med Auto Science` | 医学科研、证据整理、稿件交付 | 活跃 |
| `Grant Foundry` | `MAG / Med Auto Grant` | 基金方向判断、申请书写作、修订工作 | 活跃 |
| `Presentation Foundry` | `RCA / RedCube AI` | 汇报、讲课、幻灯片与视觉交付 | 活跃 |
| `IP Foundry` | `Med Auto Patent` planned | 专利申请、技术交底、权利要求、实施例整理 | 定义阶段 |
| `Award Foundry` | `Med Auto Award` planned | 科技进步奖、自然科学奖、成果奖和荣誉材料 | 定义阶段 |
| `Thesis Foundry` | Planned | 学位论文装配与答辩准备 | 定义阶段 |
| `Review Foundry` | Planned | 审稿、回复与修回 | 定义阶段 |

## 当前维护边界

- AI / 维护者核心工作集保持在 `project / architecture / invariants / decisions / status`。
- 默认公开文档保持在 `README*` 与 `docs/README*`。
- `contracts/` 只保留机器可读合同面。
- `docs/references/` 承接参考级配套文档；`docs/specs/` 只保留当前 active runtime / product-boundary 规格；`docs/history/` 承接历史归档与过程计划记录。
- 历史 `gateway / federation / routed-action` 语料已经退到 reference / provenance 层，不再作为默认实现依据。
- 旧本地 Product API / UI-adapter 公开语义已经退出当前主线，只保留在参考或历史层。
- 本地 Product API projection 已退役，避免把历史 adapter 面误导成当前产品主线。
- 自有完整长期常驻 runtime sidecar 不是当前 active work；当前通过 Runtime Manager 冻结可迁移边界，并把 Temporal-backed provider 作为 production required substrate 推进。只有 Temporal/provider abstraction 无法表达 OPL 必需的 task/wakeup/approval/audit/product isolation contract 时，才评估 OPL sidecar promotion。
- `hermes_agent` 属于当前 canonical executor backend set，并且只能作为显式非默认 executor adapter/backend 使用；Hermes provider/Gateway/readiness/compat 语义只保留为历史 provenance、参考材料、诊断语料或负向 guard。
- 各 domain 仓的 `harness / controller` 继续作为内部分层语言存在；对外公开主语优先写成独立 `domain agent` 与其 `agent entry / direct entry`。
- MAS v2 公开 wording 必须保持：`MAS = independent domain agent + single domain app skill + MAS-owned monolith boundary`，`OPL = unified definitions and projection/shared-contract consumer`，`MDS = optional MAS-declared backend audit / source provenance / historical fixture / explicit archive import / upstream intake / parity oracle companion`。不得写成新增 OPL runtime kernel、恢复 MAS standalone release、恢复 MDS 默认运行依赖，或由 OPL 对 MAS projection 给出 ready verdict。

## 参考入口

- `docs/references/README.md`
- `docs/history/README.md`
