# OPL Operating Record Catalog

Owner: `One Person Lab`
Purpose: `legacy_operating_record_catalog_tombstone`
State: `history_only`
Machine boundary: 本文只保存旧 operating record catalog 的人读 provenance。当前仓库没有发布 `operating-record-catalog.json` active contract；当前机器真相继续归 active contracts、source、CLI/API 行为、runtime ledger、provider receipts 与 domain-owned manifests / receipts。本文不得作为 active catalog contract、runtime manifest、truth surface、test oracle、compatibility interface、alias、facade 或 wrapper 保留依据。

## 当前读法

这份 catalog 曾索引旧 P5.M1 / P5.M2 operating record kinds，包括 routing audit、governance decision、publish readiness、cross-domain review、publish outcome、promotion candidate 与 promotion surface。它现在只作为旧 record vocabulary 的 provenance。

当前 OPL 不用该 catalog 发布 runtime manifest、schema owner、example owner 或 follow-on action owner。真正的 record schema、receipt shape、projection、runtime behavior、domain truth 和 App release truth 必须从 active contracts/source/tests/CLI/read-model、runtime ledger、provider receipts、domain repo 与 App repo 读取。

## 历史保留内容

本文保留的唯一有用信息是旧 operating record vocabulary 曾如何分组：

- governance / audit support records；
- publish / outcome support records；
- promotion / public-surface support records。

这些组只解释历史来源，不定义当前 catalog coverage、schema references、example references、stage boundary 或 action route。

## 退役面

以下历史面已退役：

- `operating-record-catalog.json` 作为 active machine contract；
- catalog-driven runtime manifest、schema source、example owner 或 stage lifecycle map；
- OPL-owned review/publish/promotion/public-channel truth；
- catalog 作为 current test oracle、compatibility interface 或 readiness gate；
- 为旧 record kinds 保留 alias、facade、wrapper 或 compatibility-only aggregate test。

## 当前 SSOT

- 当前 operating / projection truth：active contracts、source、tests、CLI/read-model、runtime ledger 与 provider receipts。
- domain truth：domain-owned manifests、receipts、typed blockers、quality/export verdicts 与 artifact authority。
- docs lifecycle truth：`docs/docs_portfolio_consolidation.md` 与 `docs/policies/docs-lifecycle-policy.md`。
- 历史目录入口：`docs/history/compatibility/gateway-federation/examples-corpora/README.md`。

## No-Resurrection Rule

不得从本文恢复 active catalog contract、derived matrix、example artifact set、route compatibility value、workflow entry、test oracle 或 readiness claim。需要历史语义时只引用本文件；需要当前行为时读取当前 SSOT 与 live machine surfaces。
