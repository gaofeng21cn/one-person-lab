# OPL Operating Example Corpus

Owner: `One Person Lab`
Purpose: `legacy_operating_example_corpus_tombstone`
State: `history_only`
Machine boundary: 本文只保存旧 gateway-derived operating examples 的人读 provenance。当前仓库不再发布这组 examples 的 active machine-readable artifacts；当前机器真相继续归 active contracts、source、CLI/API 行为、runtime ledger、provider receipts 与 domain-owned manifests / receipts。本文不得作为 active operating contract、workflow runtime、truth surface、test oracle、compatibility interface、alias、facade 或 wrapper 保留依据。

## 当前读法

这组 corpus 曾把 governance decision、cross-domain review index、publish readiness signal、publish outcome index、promotion candidate signal 与 promotion surface index 写成 operating-record walkthrough。它现在只作为旧 operating vocabulary 和 schema archaeology 的 provenance。

当前 review、publish、release、export、submission、promotion、public-channel posting 和 quality/export verdict 均归 domain-owned truth 或 App/release owner；OPL 只能消费 refs 和投影。本文不能授权 OPL 直接执行这些动作，也不能把历史 operating examples 恢复成 active workflow 或 readiness evidence。

## 历史保留内容

Former coverage 只保留为三类历史语义：

- governance / audit index：曾说明 OPL 如何记录顶层 routing / review support refs；
- publish / outcome index：曾说明 OPL 如何引用 domain-owned publish/release/export/submission outcomes；
- promotion surface index：曾说明 post-outcome public-surface reference 与 blocker 的旧索引形态。

这些语义只用于 provenance，不定义当前 record schema、runtime behavior 或 follow-on action。

## 退役面

以下历史面已退役：

- retired operating examples 作为 active machine artifacts；
- OPL-owned review、publish、promotion 或 public-channel truth；
- direct publish/release/export/submission/posting authorization；
- operating corpus 作为 current workflow、fixture owner、test oracle 或 compatibility interface；
- 为旧 route/action wording 保留 alias、facade、wrapper 或 compatibility-only aggregate test。

## 当前 SSOT

- 当前 operating truth：active contracts、source、tests、CLI/read-model、runtime ledger 与 provider receipts。
- domain truth：domain-owned receipts、typed blockers、quality/export verdicts、artifact authority 与 manifests。
- App/release truth：App repo contracts、tests、release evidence 与 workbench projection。
- docs lifecycle truth：`docs/docs_portfolio_consolidation.md` 与 `docs/policies/docs-lifecycle-policy.md`。
- 历史目录入口：`docs/history/compatibility/gateway-federation/examples-corpora/README.md`。

## No-Resurrection Rule

本文只能解释旧 operating example corpus。任何当前 action、receipt、quality/export verdict、release readiness 或 public surface posting 必须从当前 owner 和 live machine surfaces 判断；不得从本文恢复 active operating contract、compatibility route、workflow entry 或 readiness claim。
