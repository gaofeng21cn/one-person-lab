# OPL Gateway Example Corpus

Owner: `One Person Lab`
Purpose: `legacy_gateway_example_corpus_tombstone`
State: `history_only`
Machine boundary: 本文只保存旧 gateway example corpus 的人读 provenance。当前仓库不再发布这组 corpus 的 active JSON artifacts；当前机器真相继续归 active contracts、source、CLI/API 行为、runtime ledger、provider receipts 与 domain-owned manifests / receipts。本文不得作为 active examples contract、fixture owner、test oracle、workflow runtime、compatibility interface、alias、facade 或 wrapper 保留依据。

## 当前读法

这组 corpus 曾用 `research_ops` submission flow 与 `presentation_ops` publish/promotion flow 展示旧 G1/G3/P5 contract composition。它现在只作为 schema archaeology、migration review 和 negative-guard provenance 保留。

当前 OPL 行为必须从 stage-led runtime、domain-owned capability/action refs、contracts/source/tests/CLI/read-model 和 domain repo truth 读取。这里列出的 former artifacts 已退出 active artifact set，不能恢复为可执行样例、routing fixture、compatibility value 或 readiness evidence。

## 历史保留内容

Former coverage 只概括为两类：

- research submission flow：曾组合旧 routed-action decision、handoff payload、governance/audit record 与 publish/promotion record；
- presentation publish/promotion flow：曾展示 presentation surface 的同类 historical contract walkthrough。

这些名称只说明旧合同 walkthrough 曾经存在，不指向 active repo artifact path。

## 退役面

以下历史面已退役：

- retired JSON examples 作为 active repo artifacts；
- gateway corpus 作为 current routing examples、test oracle 或 fixture owner；
- direct harness execution、hidden fallback 或 best-effort handoff；
- 把 domain outcome truth 上收给 OPL；
- 通过 corpus 保留旧 route value、alias、facade、wrapper 或 compatibility-only aggregate test。

## 当前 SSOT

- 当前 runtime / routing truth：active contracts、source、tests、CLI/read-model、runtime ledger 与 provider receipts。
- domain outcome truth：对应 domain repo 的 manifests、receipts、typed blockers、quality/export verdicts 与 artifact authority。
- docs lifecycle truth：`docs/docs_portfolio_consolidation.md` 与 `docs/policies/docs-lifecycle-policy.md`。
- 历史目录入口：`docs/history/compatibility/gateway-federation/examples-corpora/README.md`。

## No-Resurrection Rule

需要追溯旧 examples 时，只把本文作为 provenance。需要新增或验证 example 时，必须先确定当前 owner：framework-neutral schema/contract 示例回 active contract/source/test owner，domain-specific 示例回对应 domain repo，历史 gateway/federation 示例继续留在 history/tombstone 语境。
