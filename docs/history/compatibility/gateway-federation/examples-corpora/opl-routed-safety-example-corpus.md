# OPL Routed-Safety Example Corpus

Owner: `One Person Lab`
Purpose: `legacy_routed_safety_example_corpus_tombstone`
State: `history_only`
Machine boundary: 本文只保存旧 routed-safety examples 的人读 provenance。当前仓库不再发布这组 examples 的 active machine-readable artifacts；当前机器真相继续归 active contracts、source、CLI/API 行为、runtime ledger、provider receipts 与 domain-owned manifests / receipts。本文不得作为 active routing behavior、fallback mechanism、test oracle、compatibility interface、alias、facade 或 wrapper 保留依据。

## 当前读法

这组 corpus 曾用 ambiguous task、unknown domain 与 refusal examples 展示旧 routed-action safety vocabulary。它现在只作为历史 safety wording 与 negative-guard provenance 保留。

当前 routing / refusal / blocker 行为必须从 active contracts/source/tests/CLI/read-model、runtime ledger、provider receipts 和 domain-owned refs 读取。本文不能恢复 hidden fallback、best-effort routing、handoff payload construction 或 old-route compatibility。

## 历史保留内容

Former coverage 只保留为三类历史语义：

- ambiguous task：旧设计中如何记录未决顶层语义；
- unknown domain：旧设计中如何记录无人 owning 的 candidate workstream；
- refusal：旧设计中如何记录绕过 owner boundary 的请求。

这些语义只用于 provenance，不定义当前 routing behavior、fallback behavior 或 route-back policy。

## 退役面

以下历史面已退役：

- retired routed-safety examples 作为 active machine artifacts；
- safety corpus 作为 active routing fixture、test oracle 或 compatibility interface；
- route unresolved 时构造 handoff payload；
- direct harness fallback、hidden best-effort route 或 old-route alias；
- 把 routing blocker、refusal 或 domain truth 上收给 OPL。

## 当前 SSOT

- 当前 routing truth：active contracts、source、tests、CLI/read-model、runtime ledger 与 provider receipts。
- domain boundary truth：domain-owned manifests、receipts、typed blockers、quality/export verdicts 与 artifact authority。
- docs lifecycle truth：`docs/docs_portfolio_consolidation.md` 与 `docs/policies/docs-lifecycle-policy.md`。
- 历史目录入口：`docs/history/compatibility/gateway-federation/examples-corpora/README.md`。

## No-Resurrection Rule

本文只能作为旧 routed-safety example provenance。不得从本文恢复 active routing behavior、fallback mechanism、compatibility value、workflow entry、fixture owner、test oracle 或 readiness claim。
