# OPL 生产级框架闭环差距矩阵

Owner: `One Person Lab`
Purpose: 记录 OPL 距离完整生产级智能体框架的当前差距、证据和下一跳闭环。
State: `active_support`
Machine boundary: 本文是人读 program / gap matrix。机器真相继续归 `contracts/`、source code、CLI/API behavior、runtime ledgers、provider receipts、domain-owned manifests 与真实 workspace / app evidence。
Date: `2026-05-14`

## 当前判断

如果把理想状态定义为“OPL 能生产级托管 MAS/MAG/RCA 的长时间 stage attempt，同时 direct skill path 等价、domain truth 不迁出、旧默认面退役干净”，当前约为 `65-75% framework/control-plane landed`，仍有 `25-35% production closure remaining`。

这个比例是工程判断，不是测试分数。已落地部分主要是 framework 骨架和控制面：shared contracts、domain descriptors、Temporal provider code、本机 service lifecycle、worker lifecycle contract、repo-native Temporal live proof、typed family queue、stage attempt ledger、provider-hosted task-bound attempt bridge、typed closeout memory-ref summary、provider continuous proof receipt projection、latest production proof 持久化、provider proof freshness blocker、runtime tray provider proof operator item、domain manifest timeout fail-closed、Codex runner repo/test harness、Agent Executor Adapter、Aion stage-attempt workbench projection，以及 MAS/MAG/RCA descriptor alignment。

未闭合部分集中在 production closure 和受证据门槛约束的 functional follow-through：真实长时 domain activity soak、MAS/MAG/RCA domain owner receipt chain、domain memory body / writeback apply、domain repo physical skeleton layout、domain transition spec 接入 OPL generic runner / matrix runner、Temporal provider 周期性长时 SLO，以及 active-path legacy residue 的最终物理删除。Temporal service/worker 当前 readiness、production proof、proof receipt ledger/projection、latest proof 持久化、active workspace binding 派生 MAS hydrate、三仓四条 task-bound stage attempt、MAG legacy tombstone evidence consumption 和 operator closeout `functional_closure_ready_for_live_soak` 已有 fresh 证据；release/dist descriptor CLI 口径已同步，后续作为持续验证项保留。

2026-05-14 补充：真实 paper/grant/visual 长时 soak 属于 production evidence gate；排除这类耗时运行后，核心功能性 surface 已推进到 `functional_closure_ready_for_live_soak`。当前 active follow-through owner 就是本矩阵；不能写成“全量生产可用”：仍待闭合的是真实 domain memory body / writeback apply、破坏性 physical skeleton path move、legacy physical delete、domain transition spec 接入、周期性 provider/operator SLO 校准和真实 domain owner chain。

## Closure Gap Matrix

| gap | 当前已落地 | 未闭合证据 | 下一跳完成门槛 |
| --- | --- | --- | --- |
| `production_temporal_residency` | Temporal provider code、`family-runtime service start|status|stop --provider temporal`、worker lifecycle contract、`residency proof --provider temporal --live` repo-native Temporal server + real worker proof、`--production` proof surface 与 runtime proof receipt event。2026-05-14 fresh service status 为 `running` / `server_reachable=true`，worker status 为 `ready` / `worker_ready=true`，显式 `OPL_FAMILY_RUNTIME_PROVIDER=temporal` view 为 `full_online_ready=true` / `durable_online_ready=true`，production proof 返回 `production_residency_proven` 并持久化到 `~/Library/Application Support/OPL/state/family-runtime/proofs/latest-temporal-production-proof.json`，`framework production-closeout` 读到 `provider_continuous_proof.continuous_proof_status=all_observed_proofs_proven` / `proof_slo_status=proof_fresh`，`runtime snapshot` 已把 provider proof 投到 operator attention/recent item；过期 proof 会 fail-closed 成 freshness typed blocker。 | 默认 `family-runtime status --json` 仍选中 `local_sqlite`，`full_online_ready=false`、`durable_online_ready=false`，只能代表 dev/offline baseline；persisted stage attempt ledger 已有 `total=4`、`completed=4`，但仍缺周期性长时 residency、operator repair loop、真实 domain attempt 压测和 MAS/MAG/RCA owner chain 证据。 | production proof 需按 operator cadence 持续可重复通过，并用长时 SLO 证明 service reachable、worker ready、workflow query/signal/history、typed closeout boundary、restart recovery 和真实 domain owner chain 不退化。 |
| `mas_paper_line_guarded_apply_soak` | MAS 三条真实 paper line 已有 OPL-ingestable closeout / owner ref surface；MAS 侧 provider guarded apply receipt 与 DM002 route-memory consumed/writeback receipt chain 已落地；OPL 禁止写 MAS truth 的边界可见。OPL queue dispatch 对 MAS `paper_autonomy/guarded-apply` 已能自动建立 task-bound provider-backed stage attempt，并 ingest MAS sidecar dispatch receipt / typed blocker refs。`family-runtime intake/tick --hydrate` 现在能从 active workspace binding 派生 MAS sidecar export，并自动携带 latest persisted Temporal proof；DM002 live dispatch 两次返回 `typed_blocker` / `blocked_no_mas_owner_apply_receipt`，receipt refs 为 `artifacts/runtime/opl_family_sidecar/dispatch_receipts/409abb7d61ccadced8e6.json`、`artifacts/runtime/opl_family_sidecar/dispatch_receipts/418103627a9c82fa3f1c.json`，OPL stage attempts `sat_d25ea828d7cd06051c859329`、`sat_53c82e269f809355cfc925fb` 已为 `accepted_typed_closeout`。 | 仍缺生产 Temporal attempt 长时托管真实 MAS owner chain，逐条 paper line 留下 live sidecar dispatch receipt、MAS owner apply receipt、artifact/gate/reviewer/route/human-gate/stop-loss evidence 或稳定 typed blocker。 | DM002、DM003、Obesity 各自产出 provider-hosted guarded apply receipt 或 typed blocker；OPL 只持 attempt/proof/ref，不写 `publication_eval`、`controller_decisions`、artifact gate、review ledger、memory body 或 final verdict。 |
| `domain_memory_apply_generalization` | `family-domain-memory-ref` / `family-domain-memory-writeback` descriptor 已三仓 resolved；attempt query/workbench 可投影 consumed memory refs、writeback receipt refs 与 rejected writes；production closeout gate 会从 typed closeout packet 汇总 consumed refs、consumed memory refs、writeback receipt refs、rejected writes，并暴露 `memory_ref_summary.opl_writes_memory_body=false`；MAS/MAG/RCA 均有 receipt locator/projection 或 evidence writer surface。 | 2026-05-14 fresh `domain-memory list --json` 的 OPL readiness 仍不是 OPL-applied retrieval/writeback/body success；真实 memory body migration / retrieval / writeback apply、更多 accepted/rejected receipt instances 和按 domain/stage 分组的 operator proof 仍归 domain runtime/workspace owner。 | 三仓都能用 domain-owned surface 产出真实 memory consumed/writeback receipt；OPL 只展示 locator、attempt/proof/ref 和 blocker，不写 memory body 或 accept/reject truth。 |
| `generic_state_machine_runner` | OPL 已有 `family-transition-runner` contract/source/test：domain-neutral transition schema、guard 匹配、fail-closed blocker/dead-letter intent、receipt/projection envelope 和 matrix runner 已落在 OPL；MAS-like fixture 已证明 `bundle_stage_ready`、`bundle_stage_blocked`、`publishability_gate_blocked` 相邻状态不会误转场。 | MAS/MAG/RCA 还没有把真实 publication gate / bundle-stage / AI reviewer、fundability / aims / package、visual route / review / export 转换声明成各自 domain transition spec；runner 尚未接入 provider attempt tick、domain sidecar dispatch、真实 owner receipt refs 和 long-soak matrix evidence。 | MAS/MAG/RCA 提供 domain transition table、guard、oracle fixture、typed blocker 和 owner action；OPL 只执行 spec、审计 matrix、投影 receipt/blocker/dead-letter/human gate，并接入 stage attempt bridge。OPL 不能解释 publication/fundability/visual/export ready verdict。 |
| `lifecycle_guarded_apply` | OPL lifecycle schema / locator / owner-route primitives 已落地，MAS/MAG/RCA 都暴露 lifecycle guarded apply proof 或 receipt requirement refs。 | cleanup/restore/retention 的 repo-level guarded refs 已存在，但真实 workspace artifact mutation 仍必须返回 domain receipt requirement 或 typed blocker；OPL 不能把 ref surface 写成 artifact mutation success。 | OPL-owned ledger/locator 可以 apply；domain-owned artifact mutation 必须有 domain receipt 或 typed blocker；三仓 adapter 只暴露 locator、receipt、blocker。 |
| `mag_controlled_soak` | OPL controlled apply contract projection 已开放；MAG manifest、standard skeleton、controlled memory apply proof 与 controlled soak typed blocker surface 已对齐到 domain receipt / no-regression evidence required。OPL queue 对显式 provider-hosted MAG guarded-run task 已有 live task-bound attempt `sat_3c210fbc86393af8d3a0e90c`，并已 ingest MAG product sidecar receipt refs。 | MAG 仍未产出真实 grant-stage domain owner receipt、grant quality verdict 或 no-regression evidence，不能声明 production soak success。 | MAG controlled attempt 返回 domain receipt、no-regression evidence 或 typed blocker；grant truth、fundability verdict 与 submission-ready authority 仍归 MAG。 |
| `rca_controlled_soak` | OPL controlled visual apply contract projection 已开放；RCA product-entry、sidecar、standard skeleton、controlled memory apply proof、runtime architecture alignment、controlled soak typed blocker surface 和 no-regression evidence writer 已对齐到 domain receipt / no-regression evidence required；PPT review helper line-budget baseline 已删除并拆出 geometry / summary / markdown report helper。OPL queue 对显式 provider-hosted RCA `emit_no_regression_evidence` task 已有 live task-bound attempt `sat_8623e55a341caf9672ed8ab2`，并已 ingest no-regression evidence ref。 | RCA 仍未产出真实 visual-stage domain owner receipt 或 long visual-stage no-regression evidence，不能声明 production soak success；真实 artifact-producing owner receipt 仍未跑出。 | RCA controlled visual attempt 返回 domain receipt、no-regression evidence 或 typed blocker；visual verdict、export gate 与 canonical artifact authority 仍归 RCA。 |
| `physical_skeleton_layout` | OPL agents read model 已能区分 `descriptor_readiness`、`physical_skeleton_layout_audit`、`physical_skeleton_evidence` 与 `production_closure_gaps`；MAS/MAG/RCA 当前 descriptor-level aligned。 | 2026-05-14 fresh `./bin/opl agents list --json` 显示 `physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`；MAS/MAG/RCA 均已有 repo-source anchor evidence。三仓破坏性目录迁移仍未执行，直接移动仍可能破坏 direct skill path 或 provenance refs。 | direct skill path、OPL-hosted path、restore/provenance proof、focused tests 与 no-forbidden-write proof 都稳定后，逐仓迁移 repo-source schema/adapter/builder/prompt/skill/knowledge refs；workspace/runtime artifacts 不迁入 repo skeleton。 |
| `unified_domain_agent_descriptor_cli` | `./bin/opl agents descriptors --json` / `./bin/opl agents descriptor --domain mas --json` 已成为 canonical 统一只读入口，并聚合 entry、skeleton、stage、action、memory、skill、runtime/session/progress/artifact refs、parity 和 authority boundary；`node dist/cli.js agents descriptors --json` 已验证同步；domain manifest command 已支持 timeout，production closeout gate 默认可解析当前三仓 manifest，并从 admitted domain contracts 动态枚举 closeout breakdown。显式短预算或真正 stalled manifest 仍 fail-closed 为 typed blocker。 | App/maintainer 仍应保留专题 read models 作为 drilldown，不能把 unified descriptor 写成 domain truth；release/dist 需要持续验证防漂移；manifest `command_timeout` 只能读作 manifest-command availability blocker。 | 保持 canonical CLI、release/dist、App consumption 与专题 drilldown 同步；只聚合 refs、authority boundary 和 readiness，不读取 memory body 或 domain truth；manifest timeout 必须 fail-closed 为 typed blocker。 |
| `executor_adapter_hygiene` | Executor registry / request / receipt 边界已落地；`codex_cli` 是默认 executor，`hermes_agent` 与 `claude_code` 是显式非默认 executor adapter；`hermes_agent` 需要 full agent loop、tool events、receipt、audit 和 fail-closed proof。 | 旧 Hermes provider/Gateway/readiness/compat 命名仍可能以 history、fixture、diagnostic 或 tombstone 形式存在；这些不能重新成为 provider、默认 executor、compat fallback 或 readiness blocker。 | 保持非默认 adapter 的独立 receipt gate；replacement proof + no-active-caller 证明成立后，旧模块、接口、alias 和测试直接删除或迁入 history/tombstone，不保留兼容接口。 |
| `legacy_active_path_retirement` | 默认语义已转为 Codex-default executor + Temporal-backed provider；active-path residue scan 与 domain tombstone/no-active evidence 已证明当前保留旧名只落在 diagnostic、fixture、provenance、history 或负向 guard 语境。OPL closeout 已能消费 MAG `physical_skeleton_follow_through` 中的 `physically_removed_or_history_tombstone_only` evidence。 | Hermes/Gateway/frontdoor/local-manager/MDS/default-compat vocabulary 的部分物理残留只保留真实 no-active-caller / provenance typed blocker；OPL 不把 no-active-default-caller 证据升级成 delete authority。 | replacement proof 与 full no-active-caller proof 同时通过后，删除 active residue 或迁入 history/tombstone；保留项必须有明确 provenance/test/diagnostic/negative-guard 语境，不能保留兼容接口。 |

## 统一口径

当前可以写成“OPL 已经具备完整生产级智能体框架的控制面骨架，并且 functional closeout gate 已 ready for live soak”，不能写成“OPL 已经全量生产可用”。准确边界是：framework contracts、runtime provider code、attempt ledger、MAS/MAG/RCA provider-hosted task-bound attempt live path、executor adapter、domain descriptors、统一 descriptor CLI、release/dist descriptor CLI 同步、receipt/ref surfaces、typed closeout memory-ref summary、provider continuous proof projection、runtime tray provider proof operator item、manifest timeout fail-closed、workbench projection、MAG legacy evidence consumption 和显式 Temporal production proof 已落地；真实 domain owner receipt chain、真实 memory body / writeback apply、physical skeleton path migration、legacy physical delete 和周期性长时 provider/operator SLO 仍是未完成闭环。

后续实现不需要再新建平行大计划。下一跳应直接围绕 `MAS paper-line guarded apply soak -> Temporal provider continuous proof/SLO -> domain transition spec ingestion on OPL transition runner -> domain memory/lifecycle apply generalization -> MAG/RCA controlled soak -> physical skeleton layout -> legacy physical retirement` 收口，每一步都必须留下 repo-native verification、domain owner receipt 或 typed blocker。
