# OPL 生产级框架闭环差距矩阵

Owner: `One Person Lab`
Purpose: 记录 OPL 距离完整生产级智能体框架的当前差距、证据门和下一跳闭环。
State: `active_plan`
Machine boundary: 本文是人读 gap matrix。机器真相继续归 `contracts/`、source code、CLI/API behavior、runtime ledgers、provider receipts、domain-owned manifests 与真实 workspace / App evidence。
Date: `2026-05-20`

## 当前判断

如果把理想状态定义为“OPL 能生产级托管 MAS/MAG/RCA 的长时间 stage attempt，同时 direct skill path 等价、domain truth 不迁出、旧默认面退役干净”，当前只能写成：framework/control-plane 与 closeout read model 已进入 live-soak-ready 基础面；MAS/MAG/RCA 已有可被 OPL 识别和消费的标准 Agent 迁移输入；三仓 stage proof-bundle admission 已达到 18/18 admitted；standard conformance gate 已落地，fresh family defaults 当前为 4/4 structural / physical pass，MAS、MAG、RCA 与 OPL Meta Agent 均通过；`opl agents readiness --family-defaults --json` 当前为 `passed_with_production_evidence_tail`，将 structural gate 与 production evidence tail ledger 分开呈现；`evidence_tail_classification` 已从抽象 tail 字符串扩展为可审计结构，并能读取 domain repo 的 `contracts/production_acceptance/*production-acceptance.json`，将 production evidence tail 报告为 open、domain-owned receipt closed 或 domain-owned typed blocker。MAS 5 个结构 follow-through 已关闭，并已通过 `runtime_transport_handoff_projection` 把 runner / worker lease / supervisor / SQLite sidecar 的 generic owner 机器化归 OPL；MAG repo-side handler/ref-only/authority boundary 与 active legacy residue 命名 gate 已闭合，且已有 1 个 `owner_receipt_typed_blocker_ref_roundtrip` refs-only external evidence typed-blocker receipt 被 OPL 记录并验证；RCA repo-side generated/handler boundary、README-only required path cleanup、8 项结构差距和 legacy physical cleanup 已按 OPL consumer 口径闭合；RCA 已有 1 个 production acceptance refs 被 OPL 记录并验证为 refs-only evidence gate receipt。MAS/MAG/RCA 三个 OPL legacy cleanup plan 已全部 dry-run ready，且 OPL refs-only lifecycle ledger 已 apply/verify 到 cleanup receipts。fresh functional privatization read model 当前为 `total_module_count=50`、`default_watchlist_count=0`、`active_private_generic_residue_count=0`、`semantic_equivalence_review_count=0`、`blocker_count=0`。三仓仍缺 production default caller / App release evidence / owner-chain / live-soak evidence；MAS 仍缺 physical delete/archive/tombstone closeout；MAG 仍缺 6 个 external evidence request receipt、production consumption evidence、App consumption 和 live owner-chain scaleout；RCA 仍缺更完整的 production evidence tail / naming hygiene follow-through。production usable closure 只能由对应 domain-owned receipt 或 typed blocker 判定，OPL conformance/readiness 不持有 domain ready。

已经成立的是 OPL framework 骨架和读模型：Temporal provider、OPL-owned provider scheduler cadence/tick surface、typed queue、stage attempt ledger、typed closeout、safe runtime action shell、lifecycle refs-only index、external evidence refs-only receipt ledger、App/operator drilldown read model、refs-only domain dispatch evidence、refs-only stage production evidence、provider proof/readiness projection、functional runtime harness、generic substrate projection、transition runner/read model、domain pack compiler active-caller proof、Agent Lab 和 private functional audit。

2026-05-19 的系统评估把下一跳从“继续加 surface”改成“关闭语义污染点”。当前 main 已把 provider/runtime readiness 多真相、generated surface drift/block、App/operator drilldown 默认过重、`family-runtime` CLI parser 语义堆叠、stage launch guarantee 含糊、legacy vocabulary active leakage，以及专家判断被 readiness / scorecard / contract completeness 机械替代的风险落成机器守门面：runtime single truth、generated surface gate、App drilldown summary-first、family-runtime parser split、stage scope / `guarantee_mode` projection、文档 vocabulary hygiene 和 contract-floor policy guard。后续 MAS/MAG/RCA production evidence 必须建立在这些单真相 surface 之上；这些守门面不证明 MAS/MAG/RCA production ready，也不改变 Temporal production substrate 与 domain truth/quality authority 的分工。

2026-05-18 的 provider evidence 曾把 provider cadence 从“命令面存在”推进到“本机真实 receipt 存在”：Temporal schedule `opl-family-runtime-provider-scheduler` 已创建，`scheduler trigger` 已返回 triggered，`scheduler tick --provider temporal --force` 已生成新的 `production_residency_proven` SLO execution receipt。该证据作为 SLO / residency history 读取；当前 ready/unconfigured 状态必须由统一 lifecycle-aware provider payload 判断。`family-runtime status.periodic_execution` 与 `runtime app-operator-drilldown.periodic_execution_refs` 已把 scheduler cadence、provider SLO receipt 和 domain-daemon replacement policy 投成机器读面。`local_sqlite` 只有显式选择时作为 dev/CI/offline diagnostic baseline，不能替代 production provider 或 domain daemon。本机 `launchctl list` 与 `~/Library/LaunchAgents` fresh check 未发现 MAS / MedAutoScience supervision scheduler label 或 plist；MAS 旧 local scheduler 只能作为 cleanup diagnostic / tombstone 处理。

Fresh runtime evidence（2026-05-20）补上了 App/operator 对 domain evidence 与 cleanup plan 的消费面：`opl agents evidence apply` 可记录/验证 OPL-owned refs-only external evidence receipt 与 evidence gate receipt；`runtime app-operator-drilldown` 现在会投影 domain 声明的 external evidence request、remaining evidence gate、OPL replacement expectation、replacement coverage、已记录/已验证 receipt、verified gate receipt，以及 physical skeleton follow-through gate 生成的 legacy cleanup executable plan、apply/verify 命令和 no-domain-delete authority；顶层 summary 同步暴露 external request receipt、evidence gate request / receipt 和 provider cadence window counters。`domain_dispatch_evidence` 已新增为 App/operator refs-only owner-chain dispatch projection：按 domain 汇总 stage attempt 的 owner receipt refs、typed blocker refs、no-regression evidence refs、memory writeback receipt refs 和 domain-ready 非授权状态，并明确 `provider_completion_is_domain_ready=false`；`stage_production_evidence` 已新增为 App/operator refs-only stage production caller projection：按 domain/stage 汇总 selected executor binding、production caller attempt、expected receipt refs、source/artifact/workspace scope refs、runtime event refs、monitor freshness refs 和 observed evidence refs，并明确 `projection_can_authorize_domain_ready=false`。App/operator 现在会把 admitted-but-missing production caller stage 投成 `stage_production_attempt_request` safe action route；当前 live summary 为 `stage_production_attempt_request_route_count=16`，full detail 可见结构化 `opl_cli_args`，`runtime action execute` 可据此调用 `family-runtime attempt create --provider temporal --executor-kind codex_cli --require-stage-admission` 创建 OPL-owned stage attempt request。两个 projection 都默认 summary-first，完整 attempts/stages/routes 只在 `--detail full` 展开。当前 live summary 为 7 个 total external evidence request、6 个 open external evidence request、1 个 verified MAG external evidence receipt request、1 个 external verified receipt、4 个 total evidence gate、3 个 remaining/open evidence gate request、1 个 verified evidence gate request、1 个 verified evidence gate receipt、10 个 remaining bridge modules；stage production evidence live summary 为 3 个 domain、18 个 stage、2 个 observed stage、16 个 missing production caller stage、18 个 missing expected receipt stage、16 个 missing executor binding stage、18 个 missing monitor freshness stage。MAG 的 verified external receipt 对应 `owner_receipt_typed_blocker_ref_roundtrip`，domain receipt ref 是用户级 runtime-state 下的 typed-blocker receipt；它只证明 MAG owner receipt / typed blocker ref roundtrip 可被 OPL refs-only ledger 消费，不授权 OPL 写 domain truth、artifact、memory body 或 verdict，也不关闭 production/default caller、App workbench、direct/hosted parity、continuous guard 或 Temporal long-soak 证据门。stage production attempt route 只创建 OPL attempt request，不执行 domain action、不写 domain truth、不生成 owner receipt，也不关闭 expected receipt / monitor freshness 缺口。AionUI runtime 页面已把 external evidence、evidence gate receipt、legacy cleanup 和 provider cadence window summary-first counters 放入 `AppOperatorDrilldown` 默认卡片。verified gate receipt 只关闭对应 refs-only gate 的 App 投影缺口，不授权 OPL 写 domain truth、artifact、memory body 或 verdict。同日 `opl system startup-maintenance` 也补上了 App-managed environment startup maintenance 的 OPL 机器入口：clean MAS/MAG/RCA managed modules 可受控 install/update、repo bootstrap、health check、domain skill sync、plugin cache freshness 和 reload prompt；dirty/ahead/diverged/no-upstream/env override/sibling workspace/invalid checkout fail closed 到 `manual_required`，且不写 domain truth、memory body、artifact body、quality/export verdict 或 domain daemon。AionUI first-launch / app-version maintenance 源码路径已改为自动调用 `system startup-maintenance`，并在 Full runtime 下跳过 git-backed maintenance；剩余证据门是 release artifact、截图、reload prompt 用户路径和长时 operator evidence。

Fresh legacy cleanup evidence（2026-05-19）进一步把 physical cleanup gate 从“可投影计划”推进到“三仓 dry-run apply ready + OPL ledger apply/verify ready”：`opl agents legacy-cleanup apply --domain mas|mag|rca --mode dry-run` 均为 `plan_status=ready` / `lifecycle_apply.status=dry_run_ready`，App/operator 汇总为 3 ready、0 blocked、3 apply-ready；随后 `--mode apply` / `--mode verify` 已在 OPL refs-only lifecycle ledger 写入并读回 MAS 空计划 closure receipt、MAG batch + 3 action receipts、RCA 空计划 closure receipt。该证据关闭 OPL cleanup ledger blocker，可证明 OPL refs-only ledger 能安全消费 cleanup plan；domain repo 文件删除、production default caller、App release/user path 和 owner-chain long soak 仍需单独 receipt。

仍未闭合的是 MAS/MAG/RCA production consumption、App 真实用户证据和真实 owner-chain：

- MAS 已关闭 `functional_structure_gap_count=0` 的结构 follow-through；MAS/MAG/RCA 的 effect-boundary stage admission 当前均为 admitted；standard conformance family defaults 当前为 4/4 structural pass，readiness 当前为 `passed_with_production_evidence_tail`；RCA README-only required pack path blocker 已关闭；MAG/RCA repo-side generated/handler target consumption 已闭合，但外部默认 caller、App 消费和 live owner-chain evidence 仍需 scaleout；
- One Person Lab App root contract 与 AionUI runtime 页面已消费 OPL App/operator drilldown read model，并已在默认卡片显示 evidence/gate/cleanup/provider-cadence summary；full detail 通过显式按钮 lazy-load；剩余是截图/发布包证据和长时 operator evidence；
- lifecycle / artifact / memory locator 与三仓结构收敛、真实 workspace receipt long-soak 仍需持续对账；
- Temporal provider SLO / repair cadence 已有真实 schedule install/trigger/tick receipt，并已新增 `cadence_window` 机器投影；2026-05-20 fresh live window 已达到 `window_cadence_satisfied`，App/operator summary 显示 7 expected、observed >= 359、0 missing、0 blocked repair，`production_evidence_tail_open_item_count=0`；`provider_capability_slo` 现在从最近一条带 capability receipt 的 provider SLO execution receipt 投影 restart re-query、signal history、typed closeout required、missing closeout blocked、retry/dead-letter boundary 和 domain truth boundary preservation，2026-05-20 fresh live summary 为 `capability_slo_satisfied`、9/9 checks proven、failed check 0，且后续 skipped cadence receipt 不覆盖上一条 capability proof；App/operator 已有 `domain_dispatch_evidence` 汇总 owner receipt / typed blocker / no-regression / memory writeback refs，并已有 `stage_production_evidence` 显示 stage-level production caller / executor / expected receipt / monitor freshness 缺口，但当前还缺真实 domain owner-chain dispatch scaleout 与长时 operator evidence；
- MAS 机器合同中的 active generic residue / classification gate 已关闭，runtime runner、worker lease、supervisor 和 SQLite lifecycle writer 已由 handoff 投影收成 OPL generic runtime 的 domain bridge / refs-only sidecar / diagnostic；fresh functional privatization summary 也显示 default watchlist、active private generic residue、semantic-equivalence review 和 blocker 均为 0。严格物理纯净模板目标下，MAS 仍需对不再需要的 active-path residue 做 no-active-caller、OPL parity、domain receipt parity 后的删除、archive 或 tombstone，workbench/status/product projection 还需要 production/App caller evidence；MAG legacy runtime cleanup gate 已 dry-run ready，并已有 1 个 verified refs-only typed-blocker external receipt，仍剩 6 个 external evidence request receipt、production/App caller evidence 和 live owner-chain；RCA 已闭合 legacy physical cleanup，并已有 1 个 verified refs-only evidence gate receipt，仍剩 naming hygiene 与更完整的 production evidence tail；
- MAS/MAG/RCA 的真实 owner receipt chain、memory body/writeback apply、artifact mutation receipt 和 controlled long soak 仍需 scaleout。

过程性 proof、receipt 事件、具体 task id 和阶段 closeout 摘要不放在本矩阵，统一归档到 [OPL family 文档过程归档 2026-05](../history/process/plans/2026-05-18-opl-family-doc-process-history.md)。

## Closure Gap Matrix

| gap | 当前已落地 | 未闭合内容 | 下一跳完成门槛 |
| --- | --- | --- | --- |
| `semantic_hygiene_guard_surface_closeout` | 原语义污染点已在 main 落成机器守门面：provider readiness 共享 lifecycle-aware payload；MAG transition oracle 收成 pack compiler evidence gate；App drilldown 默认 summary-first；`family-runtime` parser 拆分为 scoped modules；stage control plane 投影 source/artifact/workspace scope refs、runtime assumptions、monitor refs 与 `guarantee_mode`；主文档限定 legacy vocabulary 到 history/tombstone/provenance。 | 剩余不是框架结构缺口，而是 App release/user path evidence、MAS/MAG/RCA production evidence gate，以及后续新增 surface 的回归守卫。 | readiness、generated surfaces、App projection、CLI grammar、stage launch guarantee、legacy vocabulary 均有唯一 owner payload；派生 surface 不自行推断 truth；相关 focused tests 和 CLI smoke 可重复通过。 |
| `expert_judgment_floor_policy` | `opl stages readiness` 与 `opl agents readiness` 已输出 AI 原生专家判断优先、contract 只保下限、机械信号不得替代质量裁决的 policy / authority boundary；核心文档已同步写清 readiness、scorecard、checklist、schema/contract completeness、descriptor ready、provider proof 和 generated surface proof 只能 advisory。 | 后续 production evidence lane 仍要补真实 AI stage / domain quality gate / owner receipt / typed blocker / route-back receipt；当前 policy 只阻止 OPL 误报 domain ready，不关闭任何 domain production evidence。 | 任何 OPL machine surface 都不得把结构通过、合同完整、scorecard 通过或 provider 完成写成 domain ready、artifact ready、quality/export/fundability/visual verdict 或 production ready；专家判断只能由独立 AI stage、domain-owned gate、owner receipt、typed blocker 或 route-back receipt 关闭。 |
| `production_temporal_residency` | Temporal 是 production required provider；`local_sqlite` 已降为 dev/CI/offline diagnostic baseline；provider proof/status/readiness 可读；本机 Temporal schedule 已安装并可 trigger/tick，fresh SLO receipt 为 `production_residency_proven`。`provider_continuous_proof.cadence_window` 现在把最近窗口的 expected / observed / missing / blocked repair receipt counters、窗口状态和 authority boundary 投给 runtime snapshot、tray item 和 App drilldown summary。2026-05-20 fresh live window 已达到 `window_cadence_satisfied`：7 expected、observed >= 359、0 missing、0 blocked repair；历史 blocked proof / execution receipt 保留审计计数，但后续 proven proof 与完整 cadence coverage 已把 provider long-window framework evidence 收口。`provider_capability_slo` 已把 restart re-query、signal history、typed closeout required、missing closeout blocked、retry/dead-letter boundary 和 domain truth boundary preservation 做成 runtime snapshot / App summary 可见的 OPL provider capability projection；fresh live capability 为 `capability_slo_satisfied`、9/9 checks proven、failed check 0。 | 真实 domain attempt / owner-chain dispatch 压测与长时 operator evidence 仍未闭合；`cadence_window` 和 `provider_capability_slo` 是 provider/SLO 机器审计面，不是 domain live soak 完成证明。 | 保持 Temporal production proof cadence 与 provider capability SLO 持续 satisfied；domain owner-chain dispatch 仍单独返回 receipt / typed blocker。 |
| `stage_launch_admission_gate` | `family-stage-admission` read model 已接入 `family-runtime attempt create`；`--require-stage-admission` 已在 attempt start 与 provider-hosted tick 路径只阻断启动安全、越权、effect-boundary/runtime-guard 缺关键 runtime event 记录、组合不满足、hard human gate 或 executor binding 缺失。`opl stages readiness --domain <domain>` 是默认 operator/App 聚合入口，汇总 admission、proof-bundle、assumptions、cohort-loop、runtime-budget 与 replay certification 的现有 drilldown refs，不新增 domain verdict。旧 capacity-budget / domain-validity 独立面已经折叠为 warning / recommendation / drilldown ref；`needs_contracts`、assumption、cohort-loop、runtime-budget、domain-owner review 和 replay evidence 缺口默认作为 advisory 可见，不单独阻断 executor。`opl stages proof-bundle --domain <domain>` 仍输出 admission、runtime-event、idempotency 与 expected receipt obligation bundle；`family-stage-control-plane` 投影 source/artifact/workspace scope refs 和 `guarantee_mode`；`opl stages registry` 机器化 stage pack hash、migration policy 和 active attempt binding；`stage_production_evidence` 已把 selected executor binding、production caller attempt、expected receipt refs、scope refs、runtime event refs、monitor refs 和 observed evidence refs 投给 App/operator；missing caller admitted stage 已生成 `stage_production_attempt_request` safe action route，并可由 `runtime action execute` 创建 Temporal/codex-cli/required-admission stage attempt request；MAS/MAG/RCA 当前 18 个 stage 均为 `admitted`。未声明旧 stage 只作为 legacy/diagnostic attempt metadata，不能当作标准 OPL Agent production launch evidence。 | 真实 consumed refs / expected receipt refs、scope freshness、monitor freshness 和 cohort-loop live consumption 的 production caller scaleout 仍需进入 queue/provider/App 可见证据；当前 gate 已阻断明确启动安全 blocker，并已机器化 stage pack obligation projection、assumption lifecycle、hash migration、replay certification、cohort-loop refs closure、stage production evidence projection 和 attempt-create request route，但还缺真实 production caller 链路的持续证明；descriptor/read model/provider proof 或创建 request 不能替代 domain ready 或 production evidence complete。 | 每个 production stage attempt 在启动前都有 stage pack admission、executor binding、idempotency、consumed refs、expected receipt refs、source/artifact/workspace scope refs、monitor refs、stage pack hash binding、replay event/receipt refs、cohort-loop refs closure 和 owner boundary proof；硬启动安全缺口产出 typed blocker / human gate / route-back，轻量增强缺口保持 warning / recommendation。 |
| `generated_surface_production_consumption` | `domain-pack-compiler` 已能投影 CLI/MCP/product-entry/sidecar/status/workbench/harness handoff metadata 与 active-caller proof；MAS/MAG/RCA generated interface descriptor 均可读；MAS/MAG/RCA repo-side generated/handler target consumption 已闭合；pack compiler 已把 generated artifact drift 与 transition oracle / evidence gate 区分开；MAS runtime transport handoff projection 已把 generic runtime owner 归 OPL。App/operator 当前显示 7 个 total external evidence request、6 个 open external evidence request、1 个 MAG verified typed-blocker external receipt、3 个 remaining/open evidence gate request、1 个 verified evidence gate request、1 个 verified evidence gate receipt、10 个 evidence-gated bridge modules。 | MAG/RCA 还缺外部默认 caller / App / live owner-chain evidence；MAG 的 verified typed-blocker roundtrip 不等于 generated/hosted caller default 化；MAS 还缺真实 provider / App / owner receipt evidence，不是 domain ready；MAS physical delete/archive 还需 no-active-caller 与 parity gate。 | MAG/RCA 用 release/dist/default caller evidence、App evidence 和 live owner receipt chain 证明生产消费；MAS 用 paper-line provider/app evidence 证明迁移后目标边界持续成立，并在 parity 成立后清理不再需要的 active-path residue；必须等待 production evidence 的项写成 explicit evidence gate，不写成 generated drift。 |
| `app_operator_drilldown` | runtime snapshot、stage attempt、substrate projection、transition evidence、lifecycle refs、safe action routing 和 production closeout readiness 已有 CLI/runtime read model；One Person Lab App root contract 已绑定 `runtime_tray_snapshot.app_operator_drilldown`，AionUI runtime 页面已接入 `AppOperatorDrilldown` 组件；`app_execution_bridge` 已把可提交 route 指向 `opl runtime action execute`，并声明 domain route 只 queue/approval、不直接执行 domain action，OPL CLI route 可创建 stage attempt request；`domain_evidence_request_refs` 与 `domain_legacy_cleanup_plan_refs` 已把 domain request/gate/replacement coverage、external evidence receipt ledger、verified gate receipt 状态和 legacy cleanup apply/verify plan 投给 App/operator；`domain_dispatch_evidence` 与 `stage_production_evidence` 已分别按 domain 和 stage 投影 owner-chain dispatch refs、executor binding、production caller、expected receipt、scope、runtime event 和 monitor freshness 缺口；`stage_production_attempt_request` 已把 missing caller stage 接到 OPL attempt-create route；默认 payload 已改为 summary-first，顶层 summary 暴露 evidence/gate/cleanup/provider-cadence/domain-dispatch/stage-production counters 和 attempt request route counter，AionUI 默认卡片已消费这些 counters，并能通过显式按钮 lazy-load `--detail full` refs/routes/domain dispatch attempts/stages。 | 截图/发布包证据、长时 operator evidence、release artifact 证据和三仓真实 owner-chain dispatch scaleout 仍需闭合；domain dispatch evidence、stage production evidence 与 stage production attempt request 是 refs/request projection，不是 domain ready。 | App/workbench 能按 owner 展示 provider、attempt、source/artifact/memory refs、blocker、repair、evidence request、verified/recorded receipt、cleanup plan、domain dispatch evidence、stage production evidence 和 action routing，并可把 missing caller stage 提交为 OPL attempt-create request；App 默认页消费 summary payload，drilldown 再取完整 refs/routes/attempt/stage evidence；发布包和截图证据证明该路径可用。 |
| `app_operator_drilldown_summary_first` | App/operator drilldown 默认面已改成 summary-first；完整 refs/routes/domain dispatch attempts 和 stage production evidence 通过显式 `--detail full` 获取；AionUI runtime 页面已展示 external evidence、evidence gate receipt、legacy cleanup 和 provider cadence window summary counters，并可按需加载 full detail。 | 截图/发布包和长时 operator evidence 仍需闭合。 | 默认 `opl runtime app-operator-drilldown` 返回 summary-first payload；完整 refs/routes/attempt/stage evidence 只能通过显式 full/detail 查询；App 默认页不需要拉取巨量数组，并能直接审计 evidence/gate/cleanup/provider-cadence/domain-dispatch/stage-production counters。 |
| `family_runtime_cli_parser_boundary` | `family-runtime` parser 已拆成 provider、service-worker、scheduler、attempt、queue、lifecycle 等 scoped modules，public `parseFamilyRuntimeCommand` 入口保持稳定。 | 后续新增子命令仍需遵守 scoped module 边界，不能回堆主入口。 | parser 拆成 scoped modules，public API 不变，focused CLI tests 与 line-budget 通过。 |
| `app_managed_environment_startup_maintenance` | `opl system startup-maintenance` 已提供 App 可调用 action；默认维护 MAS/MAG/RCA 与 OPL Meta Agent clean OPL-managed ecosystem modules，执行 install/update、repo bootstrap、health check、domain skill sync、plugin cache freshness 投影和 restart/reload prompt；dirty/ahead/diverged/no-upstream/env override/sibling workspace/invalid checkout 进入 `manual_required`；authority boundary 明确禁止写 domain truth、memory body、artifact body、quality/export verdict 或 domain daemon。AionUI first-launch / app-version maintenance 源码路径已自动调用该 action；`opl modules`、`opl module update` 和 `opl skill sync` 仍可作为手动入口。OPL Meta Agent 是 managed ecosystem module / standard agent builder，不是 MAS/MAG/RCA 之外的新 domain truth owner。 | release artifact / screenshot evidence、provider state 联动、reload prompt 真实用户路径和长时 operator evidence 仍未闭合。 | App 启动维护默认检查 managed environment 并调用 OPL startup maintenance；clean managed checkout 可受控更新并同步投影；manual_required 状态可见且不会静默覆盖 developer checkout 或 managed runtime；发布包和截图证明该路径可用。 |
| `standard_agent_conformance_support` | `opl agents conformance` 已提供 standard domain agent structural / physical conformance report，覆盖 scaffold、`agent/` pack root、README-only path guard、generated surface owner、generated interface readiness、private generic-owner guard、physical morphology policy 和 active path scan；`opl agents readiness --family-defaults` 已作为默认聚合入口，汇总 conformance、pack compiler、generated interfaces、semantic hygiene 与 production evidence tail ledger；这些 surface 已纳入 CLI/help/test lane 与 OPL framework contract 文档。fresh family-defaults 当前为 4/4 structural pass，readiness 为 `passed_with_production_evidence_tail`；报告的 `evidence_tail_classification.tail_items` 现在输出 `status`、`repo_path`、`domain_owner`、`evidence_ref`、`doc_ref`、`next_verification_command` 与 `authority_boundary`，并能读取 domain-owned `contracts/production_acceptance/*production-acceptance.json`。 | 真实新 Agent 消费、App 展示 conformance/readiness status、production caller scaleout 和 owner-chain evidence 仍未闭合；structural conformance passed 或 tail ledger closed 不等于 domain ready、artifact authority 或 production ready；domain repo 未提供 production acceptance evidence 时仍作为 open tail 单独报告。 | 新 Agent 默认开发路径同时通过 scaffold validation、conformance/readiness report、pack compiler、private surface policy 与 focused tests；任何 conformance failure 都以 blocker 显示，不能被 descriptor ready 掩盖；production usable closure 只按 domain-owned production acceptance receipt 或 typed blocker 报告，OPL 不声明 domain ready。 |
| `developer_mode_agent_lab_repair_route` | `opl system developer-supervisor` 已有系统级配置入口；`developer_mode` projection 已能检测 GitHub identity、repo permission，并计算 direct repo fix / fork PR / mixed / observe-only 路由；`opl system`、`opl system initialize` 和 App settings surface 已暴露同一个 `developer_supervisor` action；Agent Lab workbench 已输出 refs-only repair route read model。 | 真实 repo 问题上的 owner direct-fix 与 non-owner fork/PR closeout 证据尚未形成；这属于 live exercise/evidence gap，不是配置或 projection 缺口。 | Developer Mode 开启后，任务默认可触发 Agent Lab 外围巡检；有 repo 权限时走受控 worktree/branch 修复提交和 owner-visible evidence；无 repo 权限时走 fork / PR；全程不改变 managed runtime truth，不写 domain truth。 |
| `mas_paper_line_guarded_apply_soak` | MAS 三条真实 paper line 已有 OPL-ingestable refs/read-only evidence；OPL 禁止写 MAS truth 的边界可见。 | 多条真实 paper line 的 provider-hosted apply、owner receipt、progress delta、AI reviewer/gate/artifact movement、human gate 或 stable typed blocker 仍需 scaleout。 | MAS 每条主线都产出 owner receipt、progress delta 或 typed blocker；OPL 只持 attempt/proof/ref，不写 `publication_eval`、`controller_decisions`、artifact gate、review ledger、memory body 或 final verdict。 |
| `mag_controlled_soak` | MAG 已有 grant transition oracle、receipt reconciliation proof、refs-only handoff surface 和 owner receipt contract。 | 真实 OPL-hosted grant-stage attempt、持续 owner receipt / typed blocker / no-regression evidence 和 long soak 未闭合。 | MAG controlled transition / stage attempt 在真实 workspace 中持续返回 owner receipt、typed blocker 或 no-regression evidence；fundability/export authority 仍归 MAG。 |
| `rca_controlled_soak` | RCA 已有 visual transition spec/evaluator、hosted-attempt receipt shape、workspace receipt inventory 和 no-regression refs-only projection。 | 真实 artifact-producing owner receipt、visual memory body reuse、workspace receipt scaleout 和 long visual-stage no-regression evidence 未闭合。 | RCA controlled visual / transition attempt 接到真实 provider evidence，并返回 domain receipt、no-regression evidence 或 typed blocker；visual verdict/export gate 仍归 RCA。 |
| `domain_memory_apply_generalization` | MAS/MAG/RCA 都有 memory descriptor、body-free refs、writeback proposal/receipt ref 和 forbidden-write boundary。 | 真实 memory body migration/retrieval/writeback apply、accepted/rejected receipt scaleout 和 operator proof 仍归 domain workspace owner；descriptor/read model 不等于 apply 已完成。 | 三仓都能用 domain-owned surface 产出真实 memory consumed/writeback receipt；OPL 只展示 locator、attempt/proof/ref 和 blocker。 |
| `lifecycle_guarded_apply` | OPL lifecycle schema / locator / refs-only index、guarded apply 和 restore/cleanup proof refs 已存在，domain 仓暴露 guarded apply proof 或 receipt requirement refs。 | cleanup/restore/retention 与真实 workspace artifact mutation receipt 的持续对账还未闭合；replacement proof 不等于 lifecycle reconciliation 已完成。 | OPL-owned ledger/locator 只写 framework refs；domain-owned artifact mutation 必须返回 domain receipt 或 typed blocker。 |
| `generic_state_machine_runner` | OPL 已有 domain-neutral transition schema、runner、matrix runner、receipt/projection envelope 和 refs-only transition bridge projection。 | MAS/MAG/RCA 真实 sidecar dispatch 后的 accepted owner receipt、typed blocker/no-regression evidence 和 long-soak matrix evidence 仍需 scaleout。 | Domain 提供 transition table、guard、oracle fixture、typed blocker 和 owner receipt；OPL 只执行 spec、审计 matrix、hydrate provider task 和投影 refs。 |
| `physical_skeleton_layout` | OPL read model 能区分 descriptor readiness、skeleton audit、repo-source anchor evidence 和 production closure gaps；MAS/MAG/RCA/OPL Meta Agent 都已把 canonical `agent/` pack、contracts 和 required semantic refs 写入单仓文档。 | 三仓破坏性目录迁移未执行；直接移动仍可能破坏 direct skill path、provenance refs 或 workspace boundary。 | direct/hosted parity、restore/provenance proof、focused tests 与 no-forbidden-write proof 稳定后，逐仓迁移 repo-source schema/adapter/builder/prompt/skill/knowledge refs；workspace artifacts 不迁入 repo skeleton。 |
| `physical_source_morphology_standardization` | 2026-05-19 文档调研已把物理源码形态列成独立结构治理口径：MAS 的 supervisor/runtime/scheduler/workbench/sidecar/product cockpit，MAG 的 domain_runtime/product-entry/sidecar/runtime registration/lifecycle/autonomy controller，RCA 的 MCP/session/runtimeWatch/sidecar/operator evidence/gateway naming，OPL Meta Agent 的 scripts/authority-function 边界，均已写入对应 ideal/gap/status 文档。外部成熟经验只作为分层依据：agent declaration、tools/authority、runtime orchestration、state persistence、workbench/evidence gate 分离。 | 这不是 production evidence tail，也不能被 descriptor ready、generated interface ready、functional audit closed、legacy cleanup ledger apply/verify 或 `functional_structure_gap_count=0` 吞掉。MAS/MAG/RCA 仍有 active source 命名或包边界容易被读成 repo-owned generic runtime；OPL Meta Agent 基本归位，但 scripts 仍需持续防止长成 meta-runtime/promotion engine。 | 逐仓按 active caller、OPL generated/provider parity、domain receipt parity、no-forbidden-write、focused tests、provenance/tombstone 和 no-compatibility-alias gate 做 rename、split、archive、delete 或 tombstone；关闭前只能写成 physical morphology cleanup tail，不能写成标准源码模板完全完成。 |
| `legacy_active_path_retirement` | 默认语义已转为 Codex-default executor + Temporal-backed provider；旧名只允许在 diagnostic、fixture、provenance、history 或 negative guard 语境中出现；physical skeleton gate 已能生成 `agents legacy-cleanup apply` 可消费的 plan，App drilldown 已能显示 ready/blocked plan、apply/verify 命令和 no-delete authority。MAS/MAG/RCA 三个 dry-run apply plan 当前均 ready；OPL refs-only lifecycle ledger 已 apply 并 verify 到三仓 cleanup receipts；MAG 3 个 lifecycle action 均 safe。 | Hermes/Gateway/frontdoor/local-manager/MDS/default-compat 仍可能有新发现物理残留或文档残留；domain repo active 文件删除必须由对应 domain owner 执行并返回 receipt，不能由 OPL 直接删。 | replacement proof 与 no-active-caller proof 同时通过后，删除 active residue 或迁入 history/tombstone；OPL 写自身 cleanup ledger / tombstone ref，domain repo 物理删除由 domain owner receipt 证明，不保留兼容接口；OPL ledger apply/verify 只作为安全消费 gate。 |
| `executor_adapter_hygiene` | `codex_cli` 是默认 executor；`hermes_agent`、Claude Code 等是显式非默认 adapter。 | 非默认 adapter 的 receipt gate、tool-event proof、timeout、closeout 和 fail-closed 仍需按 route 验证；旧 Hermes provider/readiness/Gateway 词汇不得回流。 | 非默认 adapter 只证明连接和回执，不承诺 domain quality；旧 provider/Gateway/compat 面清理到 history/tombstone。 |

## 统一口径

可以写成：

OPL 已具备完整生产级智能体框架的控制面骨架，并已进入 live-soak-ready 基础面；Temporal 是 production required provider；MAS/MAG/RCA 的 descriptor、stage/action/memory、transition refs、owner receipt refs 和 authority boundary 已能被 OPL 只读消费。这个状态是继续收敛到标准 OPL Agent 的基础，不是三仓功能/结构 gap 清零。

不能写成：

- OPL 已全量生产可用。
- MAS paper closure、MAG grant-stage soak、RCA visual-stage soak 已完成。
- 不能把 MAS/MAG/RCA generated surface repo-side closure 写成外部发布默认 caller、App 真实用户路径或 live owner-chain evidence 已全部闭合。
- App/workbench 的真实用户路径、截图/发布包证据和长时 operator evidence 已完整闭合。
- 不能把 Developer Mode repair route read model 写成真实 repo direct-fix / fork-PR closeout 证据已完成。
- private functional audit 分类完成等于代码路径物理清零。
- provider completion 等于 domain ready、quality ready、fundability ready、visual ready 或 export ready。
- 不能把 MAS descriptor ready、read model、provider proof、replacement proof、generated bundle ready、`runtime_transport_handoff_projection` 或机器合同 gap count 为 0 单独写成 MAS 物理纯净模板已完成；MAS generic runtime owner 已机器化归 OPL，但 physical delete/archive、App/workbench production caller 和 live paper evidence 仍单独验收。

## 一步到位并行守门面

这里记录的是已落地守门面和仍需 production evidence 的分界，不是阶段计划。语义治理类 lane 可由并行 worktree/subagent 独立推进；完成后必须吸收回 main、清理临时 worktree/branch，并在 main 上完成统一验证。任一 production evidence lane 只能在满足 owner boundary、repo-native verification、domain owner receipt、no-regression evidence 或 typed blocker 后写成完成；没有 fresh evidence 不写完成。

| lane | 当前状态 | 可并行完成门槛 |
| --- | --- | --- |
| `semantic_hygiene_regression_guard` | 语义污染点的机器守门面已落地：provider readiness single payload、generated drift/evidence gate、summary-first App drilldown、family-runtime parser split、stage scope/guarantee projection、legacy vocabulary hygiene 和 expert judgment floor policy。 | 后续新增 surface 只能通过回归测试扩展；任何 readiness、generated surface、App route、stage guarantee、legacy cleanup 或 scorecard/readiness 读法都必须引用唯一 owner payload，且不能声明 domain quality。 |
| `stage_launch_scope_and_executor_scaleout` | stage admission / proof-bundle / `guarantee_mode` projection 已落地；`stage_production_evidence` 已把 selected executor binding、production caller attempt、expected receipt refs、source/artifact/workspace scope refs、runtime event refs、monitor freshness refs 和 observed evidence refs 投给 App/operator；missing caller stage 已生成 `stage_production_attempt_request` OPL safe action route。 | selected executor binding、consumed refs、expected receipt refs、source/artifact/workspace scope refs 与 monitor freshness 在真实 production caller 链路中持续出现，并由 domain owner receipt / typed blocker 关闭缺口；attempt-create request 不替代真实 evidence。 |
| `stage_cohort_loop_refs_support` | cohort-loop refs projection 已落地。 | 在真实 stage pack 中持续消费 cohort query、trigger、monitor、metric 和 dashboard refs，并保持 refs-only / non-authority boundary。 |
| `external_generated_surface_production_evidence` for MAS/MAG/RCA | repo-side generated/handler target consumption 已闭合；OPL refs-only evidence receipt ledger 与 App drilldown 状态消费已落地。 | 补真实外部 default caller、App、release/dist、live owner-chain 和 direct/hosted parity 证据。 |
| `MAS physical thinning closeout` | runtime handoff projection 已落地。 | runner / worker lease / supervisor / workbench projection / SQLite lifecycle writer 完成 no-active-caller、parity、delete/archive/tombstone。 |
| `App/operator drilldown GUI evidence` | CLI/runtime summary-first 与 full detail read model 已落地；App/operator drilldown 已消费 evidence/gate/cleanup/provider-cadence/domain-dispatch/stage-production summary counters，并支持 full-detail lazy load。 | App GUI 截图/发布包和长时 operator evidence 可重复证明。 |
| `App managed-environment startup maintenance GUI/release evidence` | `opl system startup-maintenance` 机器入口已落地；AionUI first-launch / app-version maintenance 源码路径已自动调用。 | release artifact、截图、reload prompt 用户路径、provider state 联动和 fail-closed manual_required 长时证据成立。 |
| `OPL Meta Agent managed module consumption` | `oplmetaagent` 已进入 managed module、package distribution、turnkey install、startup maintenance、update management 和 default skill sync 路径。 | 用真实 managed install/update/skill sync 与 App reload evidence 证明该模块可被普通用户路径消费，同时保持 MAS/MAG/RCA truth ownership 不变。 |
| `standard_agent_conformance_consumption` | conformance/readiness report 已作为 CLI/read-model support surface 落地；fresh family-defaults 当前为 4/4 structural / physical pass，readiness 当前为 `passed_with_production_evidence_tail`；RCA README-only required path blocker 已关闭；production evidence tail 已改为 domain-owned receipt / typed blocker / open tail 结构化报告。 | 用真实新 Agent 或 OPL Meta Agent 生成的 pack 反复消费该报告，并把失败项作为 blocker 而不是 ready 文案；structural pass 和 tail closed 仍不能写成 production/domain/artifact readiness。 |
| `Developer Mode direct-fix / fork-PR live closeout evidence` | developer supervisor config、permission projection 和 route read model 已落地。 | 用真实 repo 问题证明 owner direct-fix 与 non-owner fork/PR closeout，且不写 managed runtime truth 或 domain truth。 |
| `MAS paper-line guarded apply scaleout` | MAS paper refs 与 guarded boundary 可被 OPL ingest。 | 多条真实 paper line 产出 progress delta、AI reviewer update、artifact delta、human gate、stop-loss 或 stable typed blocker。 |
| `Temporal provider continuous proof / SLO cadence long-window evidence` | schedule install/trigger/tick 与 SLO receipt 已有当前 proof；runtime snapshot / App drilldown 已暴露 `cadence_window`，可机器区分 window satisfied、incomplete、repair observed 或 no evidence。2026-05-20 fresh live window 已达到 `window_cadence_satisfied`，且 App/operator production evidence tail open item 当前为 0。`provider_capability_slo` 已暴露 restart re-query、signal history、typed closeout、missing-closeout block、retry/dead-letter boundary 和 domain truth boundary 的当前 capability proof；`domain_dispatch_evidence` 可见 owner-chain refs 是否出现；`stage_production_evidence` 可见 stage-level production caller 缺口。 | 保持 cadence/capability 窗口持续 satisfied，并补 domain owner-chain dispatch 不退化证据；refs projection 不替代 live soak。 |
| `transition owner receipt / no-regression evidence on OPL bridge` | transition spec、runner、receipt/projection envelope、refs-only bridge 和 App/operator `domain_dispatch_evidence` projection 已有基础。 | MAS/MAG/RCA sidecar dispatch 仍需在真实 workspace 中持续返回 accepted owner receipt、typed blocker 或 no-regression evidence，并形成 long-soak / no-forbidden-write 证据。 |
| `domain memory/lifecycle apply generalization` | locator/index/proof refs 已有基础。 | 三仓真实 workspace 中 accepted/rejected memory writeback、cleanup/restore/retention 与 artifact mutation receipt 成立。 |
| `MAG/RCA controlled soak` | MAG transition oracle 与 RCA visual transition evidence surface 已有基础。 | 真实 OPL-hosted grant/visual stage attempt 形成 owner receipt、typed blocker、artifact-producing receipt 或 repeated no-regression evidence。 |
| `RCA naming contract hygiene tail` | generated/hosted shell 与 legacy cleanup 已闭合，剩 naming tail。 | 历史 `managed` 命名完成 compatibility-free contract / tombstone 清理。 |
| `physical skeleton layout` | OPL read model 能区分 descriptor readiness、skeleton audit 与 production closure gaps。 | direct/hosted parity、restore/provenance proof、focused tests 与 no-forbidden-write proof 稳定后逐仓迁移 repo-source skeleton；workspace artifacts 不迁入源码树。 |
| `legacy physical retirement` | 旧 vocabulary 已限定到 diagnostic、fixture、provenance、history 或 negative guard；三仓 OPL cleanup dry-run apply 已 ready，且 OPL refs-only cleanup ledger 已 apply/verify 到 receipt。 | replacement proof 与 no-active-caller proof 同时通过后，domain owner 删除 active residue 或迁入 history/tombstone；OPL 只写 refs-only cleanup ledger，不保留兼容接口。 |

主线验证命令必须在 main 吸收后执行并记录结果：

- `rtk ./scripts/verify.sh`
- `rtk npm run test:fast`
- `rtk npm run test:meta`
- `rtk npm run test:artifact`
- `rtk opl agents interfaces --domain mas --json`
- `rtk opl agents interfaces --domain mag --json`
- `rtk opl agents interfaces --domain rca --json`
- `rtk opl stages proof-bundle --domain mas --json`
- `rtk opl stages proof-bundle --domain mag --json`
- `rtk opl stages proof-bundle --domain rca --json`
- `rtk opl family-runtime status --json`
- `rtk opl runtime app-operator-drilldown --json`
- `rtk opl runtime app-operator-drilldown --detail full --json`
- `rtk git status --short`
- `rtk git worktree list`
