# OPL 生产级框架闭环差距矩阵

Owner: `One Person Lab`
Purpose: 记录 OPL 距离完整生产级智能体框架的当前差距、证据和下一跳闭环。
State: `active_support`
Machine boundary: 本文是人读 program / gap matrix。机器真相继续归 `contracts/`、source code、CLI/API behavior、runtime ledgers、provider receipts、domain-owned manifests 与真实 workspace / app evidence。
Date: `2026-05-15`

## 当前判断

如果把理想状态定义为“OPL 能生产级托管 MAS/MAG/RCA 的长时间 stage attempt，同时 direct skill path 等价、domain truth 不迁出、旧默认面退役干净”，当前约为 `65-75% framework/control-plane landed`，仍有 `25-35% production closure remaining`。

这个比例是工程判断，不是测试分数。已落地部分主要是 framework 骨架和控制面：shared contracts、domain descriptors、Temporal provider code、本机 service lifecycle、worker lifecycle contract、repo-native Temporal live proof、typed family queue、stage attempt ledger、provider-hosted task-bound attempt bridge、typed closeout memory-ref summary、provider continuous proof receipt projection、latest production proof 持久化、provider proof freshness blocker、runtime tray provider proof operator item、domain manifest timeout fail-closed、MAG transition oracle ingestion / matrix smoke、RCA visual transition spec ingestion / matrix smoke、transition matrix result provider bridge、transition bridge evidence operator projection、Codex runner repo/test harness、Agent Executor Adapter、Aion stage-attempt workbench projection，以及 MAS/MAG/RCA descriptor alignment。

未闭合部分集中在 production closure 和受证据门槛约束的 functional follow-through：真实长时 domain activity soak、MAS/MAG/RCA domain owner receipt chain、domain memory body / writeback apply、domain repo physical skeleton layout、MAG/RCA transition owner receipt 对账、Temporal provider 长时 SLO，以及 active-path legacy residue 的最终物理删除。Temporal service/worker 当前 readiness、production proof、proof receipt ledger/projection、provider SLO cadence tick、latest proof 持久化、active workspace binding 派生 MAS hydrate、source-fingerprint requeue、三仓 task-bound stage attempt、MAS `study-state-matrix` transition descriptor locator、MAG transition oracle ingestion smoke、RCA visual transition spec ingestion smoke、transition matrix result hydrate into provider-hosted task、transition bridge owner/no-regression/blocker refs workbench projection、MAG legacy tombstone evidence consumption 和 operator closeout `functional_closure_ready_for_live_soak` 已有 fresh 证据；release/dist descriptor CLI 口径已同步，后续作为持续验证项保留。

2026-05-14 补充：真实 paper/grant/visual 长时 soak 属于 production evidence gate；排除这类耗时运行后，核心功能性 surface 已推进到 `functional_closure_ready_for_live_soak`。当前 active follow-through owner 就是本矩阵；不能写成“全量生产可用”：仍待闭合的是真实 domain memory body / writeback apply、破坏性 physical skeleton path move、legacy physical delete、MAS/MAG/RCA transition owner receipt / no-regression evidence 对账、长时 provider/operator SLO 证据和真实 domain owner chain。

2026-05-15 补充：MAS live provider-hosted guarded-apply 路径已从“旧 succeeded task 被 dedupe 吞掉”推进到 source-fingerprint aware rehydrate，并进一步由 MAS owner blocker 投影闭合到 owner receipt observed。MAS sidecar export 产生 `source_fingerprint=a0cdbf341fae623d`，source ref 指向 `studies/002-early-residual-risk/artifacts/controller_decisions/latest.json`，content sha256 为 `afacdd922c4db4a6c92556754f1a6baea196162ec567d16b8e11f054f18a9db3`；`family-runtime tick --hydrate` 对同一 task `frt_54187384a4fa403abda12ce5` 生成 stage attempt `sat_df08b839ce33a33ee92974f2` / workflow `wf_ed74c076d08db7c23570f605`，provider run completed，并 ingest MAS source-keyed dispatch receipt `artifacts/runtime/opl_family_sidecar/dispatch_receipts/e97b1c8cd25f3a019f90.json`。route impact 为 `applied` / `mas_owner_apply_receipt_observed`，`provider_attempt_state=mas_owner_receipt_present`，`writes_performed=false`，`forbidden_write_guard_result=fail_closed_no_forbidden_writes`。这证明 OPL framework 的 provider-backed task/attempt/receipt loop 已能跟随 domain owner evidence 刷新并消费 MAS owner stable blocker receipt，但不证明 MAS paper closure。

2026-05-15 provider SLO 补充：`opl family-runtime provider-slo tick --provider temporal` 已成为显式 supervised cadence executor。它会先读取 `provider_continuous_proof` 投影；当 proof 缺失、阻塞或过期时执行 `residency proof --provider temporal --production` 并写入 `temporal_provider_slo_execution_receipt`；当 cadence 仍 fresh 时写入 `skipped / cadence_current` receipt，`--force` 可强制重跑。runtime snapshot / closeout 的 `operator_slo_repair_loop.execution_receipts` 现在会汇总 receipt history 的 `executed_count`、`skipped_count`、`blocked_count` 和 `proven_count`，让 operator 能区分“周期性证明已按 cadence 执行/跳过/阻塞”的历史形态。2026-05-15 live tick 返回 `skipped` / `cadence_current`，写入 receipt `evt_ef27fb14-d949-4651-9fc4-03640fd1899b`，`continuous_proof_status=all_observed_proofs_proven`，`slo_execution_receipt_event_count` 从 4 增至 5，authority boundary 均为 false。该入口只证明 Temporal provider proof cadence execution，不写 domain truth、不授权 domain ready / quality / artifact verdict，也不关闭长时 SLO 窗口。

2026-05-15 provider SLO 恢复语义补充：`provider_continuous_proof` 现在区分 `all_observed_proofs_proven` 与 `latest_proof_proven`。如果历史 ledger 中保留过 blocked proof，但最新 Temporal production proof 的 `closeout_status=production_residency_proven` 且 receipt status 为 `proven`，runtime snapshot、provider closure evidence、production closeout typed blocker 和 `production_evidence_readiness` 都按最新 fresh proof 清除当前 provider SLO blocker；历史 blocked receipt 仍保留为审计证据。该恢复语义只关闭“当前 provider proof 是否可用”的 blocker，不关闭真实 domain owner chain、long soak、memory body/writeback apply 或 artifact authority。

2026-05-15 MAS transition locator 补充：OPL descriptor-only MAS fixture 已对齐 MAS 仓当前 `family_transition_spec_descriptor`。完整 `family_transition_spec` 与 `family_transition_matrix_cases` 的 owner surface 是 MAS `study-state-matrix` 的 `domain_transition_table`；sidecar export 与 product-entry manifest 只暴露 `family_transition_spec_descriptor` locator，不再声明 sidecar 内有完整 spec。OPL 在 descriptor-only 情况下继续投影 `refresh_required=true`，并指向 `study_state_matrix_spec` / `study_state_matrix_cases`；这关闭的是 locator / ingestion baseline，不关闭真实 transition owner receipt、paper progress 或 long-soak evidence。

2026-05-15 transition workbench 补充：runtime stage-attempt workbench 现在会把 provider-hosted transition attempt locator 中的 `transition_bridge.evidence` 投影成 `opl_transition_bridge_evidence_projection`，并同步出现在 workbench 顶层、单条 attempt projection、stage-attempt operator item 和 App drilldown action route 中；workbench filter metadata 也暴露 `has_transition_bridge`、`has_transition_owner_receipt_refs`、`has_transition_no_regression_evidence_refs`、`has_transition_typed_blockers` 四个筛选键。该 projection 只暴露 receipt refs、owner receipt refs、no-regression evidence refs、typed blocker refs/count、observed booleans 和 `refs_only_no_domain_verdict_authority` 边界；`can_execute_domain_action`、`can_write_domain_truth`、`can_authorize_domain_verdict` 均为 false。它关闭的是 operator/workbench 可见性和筛选缺口，不关闭真实 transition owner receipt scaleout、domain ready verdict 或 production soak。

2026-05-15 production closeout 补充：`framework production-closeout` 现在也消费同一 `transition_bridge.evidence` projection，并在 `stage_attempt_evidence.transition_bridge_evidence_summary`、`generic_projection_summary.transition_bridge_evidence`、`generic_projections.projections.transition_bridge_evidence` 和每个 domain breakdown 的 `transition_bridge_*_refs` / `transition_bridge_typed_blocker_count` 中 refs-only 暴露 owner receipt、no-regression evidence 与 typed blocker。该 closeout surface 与 workbench 共享 `can_execute_domain_action=false`、`can_write_domain_truth=false` 边界；它关闭的是 production closeout gate 的 transition evidence 可审计缺口，不关闭 domain transition ready verdict、owner-chain soak 或真实 artifact/memory apply。

2026-05-15 evidence readiness 补充：`framework production-closeout` 现在新增 `production_evidence_readiness` 聚合面。该 surface 把 provider readiness、Temporal proof/SLO receipt history、stage attempt ledger、controlled apply、lifecycle guarded apply、memory refs、transition bridge refs 和每个 domain 的 owner-chain ref / typed blocker 覆盖度汇总到一个 refs-only readiness summary；它会显式列出 `pending_gates`，并固定 `live_soak_gate.claims_live_soak_complete=false`。这个聚合面用于 operator / App 判断哪些证据已经进入 closeout read model、哪些仍需外部 live-soak evidence，不授权 OPL 执行 domain action、写 domain truth、写 memory body、判断 domain ready 或替代 MAS/MAG/RCA 的质量 / artifact / export verdict。

2026-05-16 稳定性投影补充：`cybernetics-agent` 的可取之处已按 OPL 边界落成三类 refs-only surface。第一，`control_loop_summary` 进入 stage attempt workbench、stage-attempt operator item 和 production closeout，用 `trigger -> decision -> action route -> receipt refs -> retry/dead-letter/human-gate/blocker` 摘要帮助 operator 看清控制回路。第二，`usage_projection` / `resource_pressure` 进入 attempt query、stage attempt ledger payload、workbench group summary、tray item 和 production evidence readiness，只汇总已观测 token/cost/API/duration/cadence 与 retry budget pressure，不估算 unknown，不触发 executor auto-degradation。第三，`opl runtime observability-export [--format json|openmetrics]` 将 provider proof counts、stage attempt counts、human gate/dead-letter/blocker、memory writeback receipt/rejection 和 provider SLO receipt history 导出为只读 JSON/OpenMetrics。以上能力提高的是稳定性观测、预算压力识别和运维导出，不关闭任何 production live-soak gate，也不改变 domain owner chain、memory body apply、artifact authority 或 quality verdict 的完成门槛。

2026-05-16 non-core pattern 边界补充：`generic fallback`、字符串规则 retry、通用 event bus 和通用 runtime adapter 均已固定为外部稳定性学习的负向边界。OPL 可以学习它们的分类、指标、SLO / alert 语言、budget projection 和 dashboard 组织；不能把它们写成核心运行成功机制。fallback 只能是 degraded attempt 或 alternative route proposal，且必须携带 blocker / evidence gap / owner receipt ref；retry 只能是 typed SLO policy，parse failure fail-closed；event bus 只能是 read-only projection，不替代 ledger / Temporal / typed closeout / domain receipt；executor adapter 必须有独立 capability、receipt、tool-event、timeout、closeout 和 fail-closed gate。这个补充关闭的是“外部 wrapper 机制是否能照搬”的治理歧义，不关闭 live-soak、owner-chain、quality verdict 或 artifact authority。

2026-05-15 MAS receipt-consumption 补充：MAS 已把 `completion_receipt_consumption` 纳入 `study-state-matrix` domain transition row、family transition spec receipt 和 matrix case context/expected oracle。DM002 package closure / completed turn closeout 会投成 `completion_receipt_consumed` 并禁止 redrive consumed work unit；DM003 newer default executor execution receipt 会 supersede stale blocked closeout，并要求 honor newer owner execution receipt；AI reviewer publication eval、human-gate resume receipt、delivered-package metadata parking、无需 human confirmation 的 stop-loss owner receipt 和 publication-route memory writeback receipt 也已进入 repo-level matrix coverage。memory writeback case 只对账 MAS-owned router/writeback receipt refs，`body_included=false`、`quality_authorized=false`、`submission_authorized=false`、`can_accept_or_reject_writeback=false`。本轮 MAS runtime consumer guard 也已把 `memory_writeback_receipt_consumed` 纳入 terminal/handoff decision set，确保 writeback receipt 只进入 refs-only inspect/handoff，不触发旧 work unit redrive 或 generic runner resume。该补充关闭的是 MAS-owned transition oracle 对 completed/newer execution receipt、terminal stop receipt、stale delivered-package metadata parking 和 memory writeback receipt 的消费缺口；OPL 只消费 spec/context，不把它解释为 publication closure、quality verdict、memory body apply 或 provider-hosted owner-chain soak 完成。

## Closure Gap Matrix

| gap | 当前已落地 | 未闭合证据 | 下一跳完成门槛 |
| --- | --- | --- | --- |
| `production_temporal_residency` | Temporal provider code、`family-runtime service start|status|stop --provider temporal`、worker lifecycle contract、`residency proof --provider temporal --live` repo-native Temporal server + real worker proof、`--production` proof surface 与 runtime proof receipt event。2026-05-15 fresh service status 仍为 `running` / `server_reachable=true`，worker status 为 `ready` / `worker_ready=true`，production proof 返回 `production_residency_proven` 并持久化到 `~/Library/Application Support/OPL/state/family-runtime/proofs/latest-temporal-production-proof.json`；刷新 proof 产生 workflow `wf-temporal-production-complete-1778815688773_c5c536f2e596d8`，并能驱动 MAS binding-derived hydrate 产生新的 provider evidence fingerprint。`family-runtime provider-slo tick --provider temporal` 已把 operator cadence action 从 read-model 变成可审计 supervised execution：due 时重跑 production proof，fresh 时写 skipped receipt，`--force` 时强制重跑；operator SLO read model 会汇总执行历史中的 executed/skipped/blocked/proven counts；latest observed skipped / cadence-current receipt 为 `evt_ef27fb14-d949-4651-9fc4-03640fd1899b`，event count 4 -> 5；过期 proof 会 fail-closed 成 freshness typed blocker。 | 默认 `family-runtime status --json` 仍可代表 dev/offline baseline；Temporal production proof 与 supervised cadence tick 已能重复刷新，receipt history 也已可审计，但仍缺真实长时 residency 时间窗、真实 domain attempt 压测和 MAS/MAG/RCA owner chain 的持续 SLO 证据。 | production proof 需按 operator cadence 持续可重复通过，并用长时 SLO 证明 service reachable、worker ready、workflow query/signal/history、typed closeout boundary、restart recovery 和真实 domain owner chain 不退化。 |
| `mas_paper_line_guarded_apply_soak` | MAS 三条真实 paper line 已有 OPL-ingestable closeout / owner ref surface；MAS 侧 provider guarded apply receipt、DM002 route-memory consumed/writeback receipt chain 和 DM002 owner stable blocker receipt 已落地；OPL 禁止写 MAS truth 的边界可见。2026-05-15 已验证 `family-runtime tick --hydrate` 从 active workspace binding 派生 MAS sidecar export，按 `source_fingerprint` 识别同一 dedupe key 的新 owner evidence 并 requeue。最新 DM002 run：task `frt_54187384a4fa403abda12ce5`、source fingerprint `a0cdbf341fae623d`、attempt `sat_df08b839ce33a33ee92974f2`、workflow `wf_ed74c076d08db7c23570f605`、MAS receipt `artifacts/runtime/opl_family_sidecar/dispatch_receipts/e97b1c8cd25f3a019f90.json`，closeout status `accepted_typed_closeout`，route impact `mas_owner_apply_receipt_observed`。 | 最新 live result 是 MAS owner `stable_blocker` receipt：provider attempt ready，MAS owner receipt present，`writes_performed=false`，`forbidden_write_guard_result=fail_closed_no_forbidden_writes`；domain blocker reason 是 `medical_paper_readiness_missing`。仍缺逐条 paper line 的 artifact/gate/reviewer/route/human-gate/stop-loss progress evidence，或稳定 blocker scaleout；stable blocker 不能写成 paper closure、quality verdict 或 submission readiness。 | DM002、DM003、Obesity 各自产出 provider-hosted guarded apply owner receipt、progress delta、human gate、stop-loss 或 stable blocker；OPL 只持 attempt/proof/ref，不写 `publication_eval`、`controller_decisions`、artifact gate、review ledger、memory body 或 final verdict。 |
| `domain_memory_apply_generalization` | `family-domain-memory-ref` / `family-domain-memory-writeback` descriptor 已三仓 resolved；attempt query/workbench 可投影 consumed memory refs、writeback receipt refs 与 rejected writes；production closeout gate 会从 typed closeout packet 汇总 consumed refs、consumed memory refs、writeback receipt refs、rejected writes，并暴露 `memory_ref_summary.opl_writes_memory_body=false`；MAS/MAG/RCA 均有 receipt locator/projection 或 evidence writer surface。 | 2026-05-14 fresh `domain-memory list --json` 的 OPL readiness 仍不是 OPL-applied retrieval/writeback/body success；真实 memory body migration / retrieval / writeback apply、更多 accepted/rejected receipt instances 和按 domain/stage 分组的 operator proof 仍归 domain runtime/workspace owner。 | 三仓都能用 domain-owned surface 产出真实 memory consumed/writeback receipt；OPL 只展示 locator、attempt/proof/ref 和 blocker，不写 memory body 或 accept/reject truth。 |
| `generic_state_machine_runner` | OPL 已有 `family-transition-runner` contract/source/test：domain-neutral transition schema、guard 匹配、fail-closed blocker/dead-letter intent、receipt/projection envelope 和 matrix runner 已落在 OPL；机器合同还明确 OPL runner 只执行 domain-declared spec、记录 receipt/owner refs/typed blocker refs/human gate refs/dead-letter intent refs，不解释 MAS publication verdict、`publication_eval/latest.json`、`controller_decisions/latest.json`、`current_package`、domain artifact authority 或 domain quality authority。MAS-like fixture 已证明 `bundle_stage_ready`、`bundle_stage_blocked`、`publishability_gate_blocked` 相邻状态不会误转场，并新增测试锁定 MAS publication/artifact refs 只能作为 refs/projection 携带。`domain manifests` / `agents descriptor` 现在还能消费 domain manifest 暴露的 `family_transition_spec` 与 `family_transition_matrix_cases`，执行 matrix summary；如果 MAS 只提供 `family_transition_spec_descriptor`，则投影为 `descriptor_only` / `refresh_required` 并指向 `study-state-matrix` 的 spec 与 cases locator；OPL 测试已锁定 sidecar export / product-entry manifest 只挂 descriptor，不把 descriptor-only sidecar 当完整 spec。MAS `study-state-matrix` 已把 `completion_receipt_consumption` 写入 transition row、family spec receipt 和 matrix case context/expected oracle，用于消费 completed turn closeout、newer default executor execution receipt、AI reviewer receipt、human-gate resume receipt、delivered-package metadata parking、stop-loss owner receipt 和 publication-route memory writeback receipt；MAS runtime consumer guard 已把 `memory_writeback_receipt_consumed` 纳入 terminal/handoff decision 集合，阻断旧 redrive。OPL 已有 `family-transition-oracle-ingestion`，可把 MAG-owned `mag_grant_transition_oracle` transition table / oracle fixtures 适配成通用 `family_transition_spec` / matrix cases，并在 unified descriptor 中投影 matrix oracle smoke；`family-transition-visual-ingestion` 可把 RCA-owned `visual_transition_spec` transition table / guard refs / oracle fixture contract 适配成通用 spec / matrix cases，并在 `domain manifests` / `agents descriptor` 中投影 matrix ingestion；family runtime intake 现在还能把 sidecar/export 返回的 `family_transition_matrix_result` 自动派生成 provider-hosted `family_transition/domain_tick` task，并在 task-bound stage attempt locator、runtime stage-attempt workbench、operator item 和 production closeout gate 中 refs-only 投影 owner receipt refs、no-regression evidence refs 和 typed blocker refs，同时对显式未知 domain fail-closed。 | MAS product-entry manifest 当前默认只挂 descriptor，完整 spec/cases 来自 MAS `study-state-matrix`；MAS completion receipt consumption 已覆盖多类 repo-level receipt，memory writeback receipt 已有 accepted/rejected matrix case 和 runtime consumer guard，但真实 paper-line owner-chain、更多真实 memory writeback receipt 和更多 package closure 变体仍需规模化；MAG oracle 与 RCA visual spec 已能被 OPL read model / matrix runner 消费，matrix result 已能进入 provider-hosted transition task；MAG 已有 repo-local receipt reconciliation proof、receipt reconciliation inventory 和 grouped CLI surface；RCA hosted-attempt reconciliation 已从测试 helper 上升为 repo-owned product-sidecar helper，并补强 provider-attempt ref 绑定和 forbidden payload guard；OPL transition bridge 已能对 owner/no-regression evidence 做 refs-only 对账投影并进入 operator workbench 和 production closeout breakdown；尚未闭合的是真实 domain sidecar dispatch 后的 accepted owner receipt、typed blocker / no-regression evidence 持续返回和 long-soak matrix evidence。 | MAS/MAG/RCA 提供 domain transition table、guard、oracle fixture、typed blocker、owner action、completion receipt consumption context 和 owner receipt；OPL 只执行 spec、审计 matrix、hydrate provider task、投影 receipt/blocker/dead-letter/human gate、owner receipt refs 与 no-regression evidence refs，并接入 stage attempt bridge / operator workbench / closeout gate。OPL 不能解释 publication/fundability/visual/export ready verdict。 |
| `lifecycle_guarded_apply` | OPL lifecycle schema / locator / owner-route primitives 已落地，MAS/MAG/RCA 都暴露 lifecycle guarded apply proof 或 receipt requirement refs。 | cleanup/restore/retention 的 repo-level guarded refs 已存在，但真实 workspace artifact mutation 仍必须返回 domain receipt requirement 或 typed blocker；OPL 不能把 ref surface 写成 artifact mutation success。 | OPL-owned ledger/locator 可以 apply；domain-owned artifact mutation 必须有 domain receipt 或 typed blocker；三仓 adapter 只暴露 locator、receipt、blocker。 |
| `mag_controlled_soak` | OPL controlled apply contract projection 已开放；MAG manifest、standard skeleton、controlled memory apply proof 与 controlled soak typed blocker surface 已对齐到 domain receipt / no-regression evidence required。OPL queue 对显式 provider-hosted MAG guarded-run task 已有 live task-bound attempt `sat_3c210fbc86393af8d3a0e90c`，并已 ingest MAG product sidecar receipt refs。OPL descriptor / transition ingestion 现在能消费 MAG `grant_transition_oracle` 并跑通 matrix oracle smoke；family runtime intake 可把 MAG-style `family_transition_matrix_result` hydrate 成 provider-hosted transition task。MAG repo-local `controlled-soak-receipt-reconciliation-proof` 能把 sidecar closeout result、typed blocker / no-regression evidence 和外部 OPL ledger ref 对账成 probe payload，固定 `claims_production_long_run_soak_complete=false`；`controlled-soak-receipt-reconciliation-inventory` 还能用同一规则汇总多条 owner receipt evidence 的 shape/status/blocker/evidence refs，并已通过 `medautogrant product receipt-reconciliation-proof` / `receipt-reconciliation-inventory` grouped CLI 暴露为维护者可调用 surface。 | MAG 已有 receipt reconciliation proof / inventory / API / CLI，但仍未产出真实长时 grant-stage domain owner receipt、grant quality verdict 或持续 no-regression evidence，不能声明 production soak success；MAG transition bridge 已具备 transport，真实 owner receipt / live 对账尚未形成 soak 窗口。 | MAG controlled transition / stage attempt 返回 domain receipt、no-regression evidence 或 typed blocker，并在真实 OPL-hosted grant-stage line 中持续对账；grant truth、fundability verdict 与 submission-ready authority 仍归 MAG；OPL 只把 oracle table / matrix result 接入 generic runner / provider bridge。 |
| `rca_controlled_soak` | OPL controlled visual apply contract projection 已开放；RCA product-entry、sidecar、standard skeleton、controlled memory apply proof、runtime architecture alignment、controlled soak typed blocker surface 和 no-regression evidence writer 已对齐到 domain receipt / no-regression evidence required；PPT review helper line-budget baseline 已删除并拆出 geometry / summary / markdown report helper。OPL queue 对显式 provider-hosted RCA `emit_no_regression_evidence` task 已有 live task-bound attempt `sat_8623e55a341caf9672ed8ab2`，并已 ingest no-regression evidence ref。OPL descriptor / transition ingestion 已能消费 RCA `visual_transition_spec` 并跑通 matrix ingestion；family runtime intake 可把 RCA-compatible `family_transition_matrix_result` hydrate 成 provider-hosted transition task。RCA repo-local hosted-attempt receipt reconciliation 已从测试 helper 上升为 `product-sidecar` repo-owned helper，模拟 OPL provider attempt bridge 消费 `visual_transition_spec` 后只对账 `domain_owner_receipt_ref`、`typed_blocker` 或 `no_regression_evidence_ref`；本轮补强 transition receipt 必须显式引用 provider attempt ref，并递归禁止 visual-ready/exportable/handoffable claim、artifact blob、memory body 或 review/export verdict payload。 | RCA 已有 receipt 对账形状 helper和focused fixture，但仍未产出真实 visual-stage domain owner receipt 或 long visual-stage no-regression evidence，不能声明 production soak success；真实 artifact-producing owner receipt 仍未跑出。transition bridge 已具备 transport，真实 RCA owner receipt / visual no-regression 对账尚未形成 soak 窗口。 | RCA controlled visual / transition attempt 返回 domain receipt、no-regression evidence 或 typed blocker，并把 repo-owned reconciliation helper 接到真实 provider attempt 证据；visual verdict、export gate 与 canonical artifact authority 仍归 RCA。 |
| `physical_skeleton_layout` | OPL agents read model 已能区分 `descriptor_readiness`、`physical_skeleton_layout_audit`、`physical_skeleton_evidence` 与 `production_closure_gaps`；MAS/MAG/RCA 当前 descriptor-level aligned。 | 2026-05-14 fresh `./bin/opl agents list --json` 显示 `physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`；MAS/MAG/RCA 均已有 repo-source anchor evidence。三仓破坏性目录迁移仍未执行，直接移动仍可能破坏 direct skill path 或 provenance refs。 | direct skill path、OPL-hosted path、restore/provenance proof、focused tests 与 no-forbidden-write proof 都稳定后，逐仓迁移 repo-source schema/adapter/builder/prompt/skill/knowledge refs；workspace/runtime artifacts 不迁入 repo skeleton。 |
| `unified_domain_agent_descriptor_cli` | `./bin/opl agents descriptors --json` / `./bin/opl agents descriptor --domain mas --json` 已成为 canonical 统一只读入口，并聚合 entry、skeleton、stage、action、memory、skill、runtime/session/progress/artifact refs、parity 和 authority boundary；`node dist/cli.js agents descriptors --json` 已验证同步；domain manifest command 已支持 timeout，production closeout gate 默认可解析当前三仓 manifest，并从 admitted domain contracts 动态枚举 closeout breakdown。`production_evidence_readiness` 现在把 provider SLO、stage attempt、owner-chain refs、transition refs、memory/lifecycle refs、control-loop summary、usage/resource pressure 和 pending evidence gates 汇成 refs-only readiness summary。显式短预算或真正 stalled manifest 仍 fail-closed 为 typed blocker。 | App/maintainer 仍应保留专题 read models 作为 drilldown，不能把 unified descriptor、evidence readiness、control-loop summary、usage projection 或 observability export 写成 domain truth；release/dist 需要持续验证防漂移；manifest `command_timeout` 只能读作 manifest-command availability blocker；`live_soak_gate.claims_live_soak_complete=false` 仍是硬边界。 | 保持 canonical CLI、release/dist、App consumption 与专题 drilldown 同步；只聚合 refs、authority boundary、readiness、usage pressure 和 pending gates，不读取 memory body 或 domain truth；manifest timeout 必须 fail-closed 为 typed blocker。 |
| `executor_adapter_hygiene` | Executor registry / request / receipt 边界已落地；`codex_cli` 是默认 executor，`hermes_agent` 与 `claude_code` 是显式非默认 executor adapter；`hermes_agent` 需要 full agent loop、tool events、receipt、audit 和 fail-closed proof。 | 旧 Hermes provider/Gateway/readiness/compat 命名仍可能以 history、fixture、diagnostic 或 tombstone 形式存在；这些不能重新成为 provider、默认 executor、compat fallback 或 readiness blocker。 | 保持非默认 adapter 的独立 receipt gate；replacement proof + no-active-caller 证明成立后，旧模块、接口、alias 和测试直接删除或迁入 history/tombstone，不保留兼容接口。 |
| `legacy_active_path_retirement` | 默认语义已转为 Codex-default executor + Temporal-backed provider；active-path residue scan 与 domain tombstone/no-active evidence 已证明当前保留旧名只落在 diagnostic、fixture、provenance、history 或负向 guard 语境。OPL closeout 已能消费 MAG `physical_skeleton_follow_through` 中的 `physically_removed_or_history_tombstone_only` evidence。 | Hermes/Gateway/frontdoor/local-manager/MDS/default-compat vocabulary 的部分物理残留只保留真实 no-active-caller / provenance typed blocker；OPL 不把 no-active-default-caller 证据升级成 delete authority。 | replacement proof 与 full no-active-caller proof 同时通过后，删除 active residue 或迁入 history/tombstone；保留项必须有明确 provenance/test/diagnostic/negative-guard 语境，不能保留兼容接口。 |

## 统一口径

当前可以写成“OPL 已经具备完整生产级智能体框架的控制面骨架，并且 functional closeout gate 已 ready for live soak；MAS guarded-apply provider loop 已能按 source fingerprint 重跑并留下 provider attempt + MAS owner stable blocker receipt；OPL transition runner 机器合同已明确只执行/记录 domain-declared transition refs，不解释 MAS publication/artifact authority；MAS transition descriptor 已对齐 `study-state-matrix` owner locator，且 MAS-owned transition oracle 已能消费 package closure / newer executor execution / AI reviewer / human-gate resume / delivered-package metadata / stop-loss / publication-route memory writeback receipt，runtime consumer guard 也已阻断 `memory_writeback_receipt_consumed` redrive；MAG transition oracle 已能被 OPL descriptor / matrix runner 消费，MAG 侧 receipt reconciliation proof 和 inventory 已有 product-entry API 与 grouped CLI surface；RCA visual transition hosted-attempt reconciliation 已成为 repo-owned helper，并锁定 refs-only 对账、provider attempt ref 绑定和 forbidden payload guard；transition bridge 已能在 attempt locator、runtime workbench、operator item 和 production closeout gate 中 refs-only 投影 owner receipt / no-regression evidence / typed blocker refs；`production_evidence_readiness` 已把 provider SLO、stage attempt、owner-chain refs、transition refs、memory/lifecycle refs 和 pending live-soak gates 聚合到一个 refs-only readiness summary”，不能写成“OPL 已经全量生产可用”或“MAS paper closure / MAG grant-stage soak / RCA visual-stage soak 已完成”。准确边界是：framework contracts、runtime provider code、attempt ledger、MAS/MAG/RCA provider-hosted task-bound attempt live path、executor adapter、domain descriptors、统一 descriptor CLI、release/dist descriptor CLI 同步、receipt/ref surfaces、typed closeout memory-ref summary、provider continuous proof projection、provider SLO cadence tick、runtime tray provider proof operator item、manifest timeout fail-closed、workbench projection、production closeout transition evidence breakdown、production evidence readiness pending-gate summary、MAS `study-state-matrix` transition descriptor locator / completion and memory writeback receipt consumption / runtime consumer guard、transition bridge refs-only owner/no-regression/blocker evidence projection、MAG transition oracle ingestion smoke、MAG receipt reconciliation proof/inventory/API/CLI、RCA hosted-attempt reconciliation helper/fixture、MAG legacy evidence consumption 和显式 Temporal production proof 已落地；真实 progress delta / human gate / artifact gate movement、真实 transition owner receipt / no-regression evidence 长时返回、真实 memory body / writeback apply、physical skeleton path migration、legacy physical delete 和长时 provider/operator SLO 仍是未完成闭环。

后续实现不需要再新建平行大计划。下一跳应直接围绕 `MAS paper-line guarded apply soak -> Temporal provider continuous proof/SLO -> transition owner receipt / no-regression evidence on OPL transition bridge -> domain memory/lifecycle apply generalization -> MAG/RCA controlled soak -> physical skeleton layout -> legacy physical retirement` 收口，每一步都必须留下 repo-native verification、domain owner receipt 或 typed blocker。
