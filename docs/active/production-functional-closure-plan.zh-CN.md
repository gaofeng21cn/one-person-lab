# OPL / Foundry Agents 生产功能闭环一步到位计划

Owner: `One Person Lab`
Purpose: 记录排除真实论文、基金、视觉交付长时 soak 这类耗时验收后，OPL 成为完整生产级智能体框架所需的功能性闭环、已落地证据、剩余 production evidence gate，以及 MAS/MAG/RCA 的同一收口口径。
State: `functional_surfaces_landed_evidence_gates_remaining`
Machine boundary: 本文是人读 program plan。机器真相继续归 `contracts/`、source code、CLI/API behavior、runtime ledgers、provider receipts、domain-owned manifests、workspace/runtime artifacts 与真实 app evidence。
Date: `2026-05-14`

## 当前真实状态

当前不是“只剩跑真实论文”。OPL 已具备完整生产级智能体框架的控制面骨架；2026-05-14 本轮已经把主要 functional closure surface 收口到机器可读 contracts、refs、focused tests、typed blockers 和跨仓 status/docs。canonical `./bin/opl agents descriptors --json` 统一 domain-agent descriptor CLI 已可用，`node dist/cli.js agents descriptors --json` 也已验证同步；显式 Temporal service / worker / production proof 已在本机 managed provider 上通过，proof receipt 已进入 runtime event ledger，并持久化为 latest production proof，由 `framework production-closeout` 汇总为 `provider_continuous_proof` 与 `runtime_ledger.provider_continuous_proof`，同时由 `runtime snapshot` 投影成 provider proof operator item；MAS `paper_autonomy/guarded-apply` queue dispatch 已能自动建立 task-bound provider-backed stage attempt，并在 MAS sidecar 返回 dispatch receipt / typed blocker 时写入 OPL typed closeout ledger；`family-runtime intake/tick --hydrate` 现在能从 active workspace binding 派生 MAS sidecar export command，并自动携带 latest persisted Temporal production proof；stage-attempt workbench 现在还会派生 `opl:stage-attempt:<id>` operator item，展示 controlled apply refs、artifact locator / restore proof、memory refs、writeback receipt refs、rejected writes 和明确 owner 的 action route refs；runtime snapshot 也已有 refs-only native-helper execution envelope，读取现有 native index / helper receipt metadata，但不触发 helper 执行；MAG/RCA 显式 provider-hosted / controlled sidecar task 也已有 live task-bound attempt 与 typed closeout 证据。使用 operator 超时预算重跑 `framework production-closeout` 时，当前 closeout 已返回 `functional_closure_ready_for_live_soak`、`typed_blocker_count=0`。但原计划里仍有几项 live evidence / follow-through 不能写成已完成：破坏性物理目录迁移、真实 memory body / writeback apply、legacy 最终物理删除、长时 provider/operator SLO 和真实 domain owner chain。

已落地的部分：

- OPL Framework identity、Codex CLI first-class executor、Agent executor 最小执行单位、Temporal-backed production provider 口径已经写入核心文档、invariants 和 decisions。
- Shared contracts、family action catalog、stage control plane、domain memory descriptor、standard domain-agent skeleton、runtime supervision、persistence / lifecycle / owner-route schema 已落地。
- MAS/MAG/RCA 均已 descriptor-level aligned；OPL 能解析三仓 stage plane、domain memory descriptor 和 artifact locator boundary。
- Temporal provider code、workflow/activity/signal/query、stage attempt ledger、typed queue、typed closeout gate、Codex runner repo/test harness、production proof receipt ledger、latest proof persistence、runtime tray provider proof operator item、stage-attempt operator item、operator action routing shell、native-helper execution envelope、Aion stage-attempt workbench projection 已落地。
- Provider-hosted task-bound attempt bridge 已落地：OPL queue dispatch 对 MAS `paper_autonomy/guarded-apply` 自动创建 task-bound provider-backed stage attempt，保留 workspace locator、source fingerprint、provider kind 和 domain sidecar closeout refs；live DM002 dispatch 已两次把 MAS `typed_blocker` receipt ingest 为 OPL typed closeout，其中最新一次经 active workspace binding hydrate 和 latest persisted Temporal proof 自动触发；显式 provider-hosted / controlled 的 MAG/RCA sidecar task 也已进入 persisted stage attempt ledger，并将 MAG receipt refs / RCA no-regression evidence ref 作为 OPL typed closeout refs；不写 domain truth。
- OPL controlled apply contract 已开放给 MAG/RCA；MAG/RCA 当前 live evidence 已从 OPL contract gap 推进为 task-bound sidecar receipt / no-regression evidence ref ingestion，剩余缺口是 domain owner receipt、grant/visual quality verdict 或 long soak 未产出。
- OPL production functional closeout gate 已落地，能够只读聚合 admitted domains、descriptor alignment、provider readiness、provider continuous proof receipt、provider proof freshness、memory/lifecycle receipt coverage、legacy residue state 和 typed blockers；stage attempt typed closeout 中的 consumed refs、consumed memory refs、writeback receipt refs 和 rejected writes 会进入 `memory_ref_summary`，provider proof 会同时进入顶层 `provider_continuous_proof` 与 `runtime_ledger.provider_continuous_proof`，latest production proof 还会持久化到 OPL state 供 MAS hydrate 使用，stale proof 会形成 `temporal_provider_proof_freshness_not_current` typed blocker。MAG 的 `physical_skeleton_follow_through.legacy_active_path_policy` 现在也被 closeout 识别为 legacy active-path tombstone/no-active evidence。operator 超时预算下 fresh closeout 为 `functional_closure_ready_for_live_soak`、`typed_blocker_count=0`，但这些仍是 ref-only / receipt-only projection，不是 OPL memory body apply 或 domain ready verdict。
- Domain manifest command 读取已加 timeout fail-closed：默认 closeout 预算已调到能覆盖当前真实 MAS manifest，并可解析三仓 manifest；显式短预算或真正 stalled manifest 仍会标成 `domain_manifest_timeout`，并给出 `OPL_DOMAIN_MANIFEST_COMMAND_TIMEOUT_MS=10000 opl framework production-closeout` 修复命令。timeout 应读成 OPL manifest-command availability blocker，不是 MAS truth / quality / owner apply failure。
- OPL agents read model 能区分 descriptor readiness、physical skeleton layout audit、physical skeleton evidence 与 production closure gaps；2026-05-14 fresh `./bin/opl agents list --json` 为 `aligned_count=3`、`missing_count=0`、`drift_detected_count=0`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`、`production_closure_gap_count=12`、`provider_temporal_residency_gap_status=closed_by_fresh_proven_proof`。这表示三仓 descriptor 已 aligned，MAS/MAG/RCA 均已有 repo-source anchor evidence，provider residency gap 已由 fresh Temporal proof 关闭；不表示 OPL 或三仓已完成破坏性物理目录迁移、domain memory apply 或 live domain owner chain。
- MAS 已有 provider proof ingestion、Stage Review workspace locator proof、publication-route memory Markdown-first library、DM002 route-memory receipt refs、body-free route-memory receipt inventory、operator grouping、stale/deprecated review summary、provider guarded apply receipt surface、domain memory/lifecycle/physical skeleton projections，以及拆分后的 provider-ready / progress portal / guarded-apply task surfaces。2026-05-14 MAS runtime owner surface 还补齐了 closed controller work-unit authorization 清理、runtime turn closeout artifact delta freshness、supervisor-only live quality repair owner routing、AI reviewer currentness guard 和 runtime-read manual finish guard；这些都是 MAS-owned projection / owner-surface follow-through，不是 OPL-held study truth 或 publication verdict。
- MAG/RCA 已在各自 main 上落地 functional closure proofs：domain owner receipt envelope、domain memory accepted/rejected receipt refs、lifecycle guarded apply refs、controlled soak typed blocker 和文档/status 对齐；MAG/RCA 均已有 repo-source anchor evidence。

仍未闭合的部分分两类：

- `production evidence gates`：真实长时 Temporal SLO、真实 MAS paper-line provider-hosted guarded apply、真实 MAG/RCA production controlled soak。这类需要 live workspace、owner gate、时间和运行成本，不在本文中伪装成短期功能开发。当前显式 Temporal provider readiness / production proof 已通过，proof receipt 已进入 closeout read model 并持久化为 latest proof；剩余不是 service/worker 是否可达，也不是 OPL functional closeout blocker，而是持续 residency 的周期性证据与 domain owner chain。
- `functional follow-through gates`：physical skeleton 的破坏性目录搬迁、legacy residue 的最终物理删除、真实 workspace/runtime memory body migration，以及长时 provider/operator SLO 校准。release/dist CLI descriptor 入口、RCA repo-source anchor evidence 和 MAG legacy tombstone evidence consumption 已有 fresh proof；后续只作为持续验证项保留。RCA review helper 的 line-budget baseline removal 与 geometry/summary/report 拆分已完成；后续 screenshot capture 可自然拆分，但不再是本轮 blocker。

## 距理想状态还有多远

工程判断保持：`65-75% framework/control-plane landed`，`25-35% production closure remaining`。如果只看“排除真实论文/真实交付长时 soak 后，可由工程实现闭合的功能性缺口”，当前 closeout 已进入 `functional_closure_ready_for_live_soak`；剩余约 `5-10%` follow-through 主要是破坏性物理目录迁移、真实 memory body / writeback apply、legacy 物理删除和 provider SLO 校准这类必须等 no-active-caller、path parity、owner receipt、provenance 或长时运行证据的收尾。

| 理想目标 | 当前距离 |
| --- | --- |
| OPL 成为完整生产级智能体框架 | 控制面、contract、attempt、provider code、workbench、descriptor discovery、统一 descriptor CLI、release/dist descriptor CLI 同步、controlled apply projection、production closeout gate、typed closeout memory-ref summary、manifest timeout fail-closed、provider continuous proof projection、provider proof operator item、operator action routing shell、native-helper execution envelope、MAS/MAG/RCA sidecar typed closeout live ingestion、显式 Temporal production proof 和 operator closeout `functional_closure_ready_for_live_soak` 已落地；剩余距离主要是周期性长时 operator SLO 和真实 domain attempt evidence。 |
| 各 repo 完成迁移、分层 | MAS/MAG/RCA 已完成 manifest/descriptor/receipt 层迁移和 functional closure proof；后续物理迁移必须先通过 path compatibility、direct skill / OPL-hosted path parity、restore/provenance proof 和 no-forbidden-write proof。 |
| domain repo 目录结构标准化 | OPL 已定义 `agent/ contracts/ runtime/ docs/` skeleton；fresh OPL read model 显示 `physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`，MAS/MAG/RCA 均已有 repo-source anchor evidence。完整物理重组仍是 follow-through，workspace artifacts、memory body 和 receipt instances 不进 repo source。 |
| domain memory 泛化 | 三仓 memory descriptor、receipt locator/projection 和 OPL ref-only projection 已落地；真实 memory body migration / retrieval / writeback apply 仍归 domain runtime/workspace evidence gate。 |
| lifecycle / artifact primitive 泛化 | cleanup/restore/retention guarded apply refs 已跨仓对齐；OPL 只 apply OPL-owned metadata，domain artifact mutation 继续要求 domain receipt 或 typed blocker。 |
| legacy residue 退役 | 默认 caller 已离开 Hermes/Gateway/frontdoor/local-manager/MDS default；当前以 no-active-default-caller / tombstone proof 管理，最终物理删除仍需 no-active-caller 与 provenance 保留证据同时成立。 |

## 一步到位并行计划与落地状态

这些 lane 已按并行 worktree / 分支推进并吸收回各自 `main`。真实长时 soak 继续作为 typed blocker 或 later evidence，不阻塞功能性闭环。

| lane | repo owner | 当前状态 | 证据 / 下一跳 |
| --- | --- | --- | --- |
| `provider-readiness-operator-closure` | OPL | `production_proof_ledger_projection_observed` | Managed provider state、service/worker/proof projection、latest proof persistence、runtime tray provider proof item、proof freshness blocker 与 production closeout gate 已落地；2026-05-14 fresh explicit Temporal service/worker ready，production proof 返回 `production_residency_proven`，并被 closeout gate 读成 `provider_continuous_proof.continuous_proof_status=all_observed_proofs_proven` / `proof_slo_status=proof_fresh`；真实长时 SLO 仍需周期性 live evidence。 |
| `provider-hosted-task-bound-attempt-bridge` | OPL | `landed_with_live_four_attempt_evidence` | MAS `paper_autonomy/guarded-apply` task dispatch 已自动建立 task-bound provider-backed stage attempt，并可 ingest MAS sidecar dispatch receipt / typed blocker refs；MAS hydrate 可从 active workspace binding 派生 sidecar export，并自动携带 latest persisted Temporal proof；显式 provider-hosted / controlled 的 MAG/RCA sidecar task 已有 live path，可把 MAG product sidecar receipt refs / RCA no-regression evidence ref ingest 为 OPL typed closeout refs。当前 stage attempt ledger `total=4`、`completed=4`：MAS attempts `sat_d25ea828d7cd06051c859329`、`sat_53c82e269f809355cfc925fb` 均返回 `blocked_no_mas_owner_apply_receipt`，MAG attempt `sat_3c210fbc86393af8d3a0e90c` 返回 sidecar guarded-action refs，RCA attempt `sat_8623e55a341caf9672ed8ab2` 返回 no-regression evidence refs；真实 MAS owner apply receipt、MAG grant-stage owner receipt、RCA long visual-stage soak 仍未产出。 |
| `owner-receipt-contract-generalization` | OPL + MAS/MAG/RCA | `landed` | 三仓使用 `domain_receipt | typed_blocker | no_regression_evidence` 口径；OPL attempt/closeout gate 保存 refs，不保存 domain truth。 |
| `domain-memory-apply-generalization` | MAS/MAG/RCA | `descriptor_and_receipt_projection_landed` | MAS/MAG/RCA 都暴露 descriptor、receipt locator/projection 或 typed blocker；MAS route-memory inventory 已能给出 body-free receipt refs、operator grouping 和 stale/deprecated review summary；OPL production closeout gate 已能汇总 stage closeout consumed/writeback/rejected memory refs，并明确 `opl_writes_memory_body=false`；真实 memory body、retrieval apply 和 writeback truth 仍归 domain owner。 |
| `lifecycle-guarded-apply-generalization` | OPL + MAS/MAG/RCA | `landed_as_guarded_refs` | cleanup/restore/retention 已形成 OPL-owned metadata apply 与 domain-owned artifact mutation receipt requirement。 |
| `physical-skeleton-follow-through` | MAS/MAG/RCA | `anchor_evidence_observed` | MAS/MAG/RCA 均已有 repo-source anchor evidence；三仓 descriptor aligned，破坏性 path move 后续按 parity/provenance/no-forbidden-write 证据推进。 |
| `legacy-active-path-final-retirement` | OPL + domains | `no_active_caller_evidence_observed_delete_blocked` | 默认旧面已失去 authority；OPL closeout 已能消费三仓 no-active-caller/tombstone evidence，包括 MAG physical skeleton follow-through 中的 legacy active-path residue。最终 delete 仍需 full no-active-caller 与 fixture/provenance 保留证据同时成立。 |
| `operator-workbench-drilldown` | OPL App / OPL | `landed` | Workbench/closeout gate 能展示 provider proof operator item、stage-attempt operator item、provider completion、controlled apply refs、workspace/source intake refs、memory locator/index refs、artifact locator / restore proof、package/export lifecycle refs、domain owner receipt refs、human gate、dead-letter、route/decision refs、review/repair refs、quality/readiness refs、SLO/repair-command refs、memory refs、rejected writeback 和 typed blockers；真实 domain owner receipt / artifact mutation receipt 仍由 domain live evidence 闭合。 |
| `artifact-gallery-handoff-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_artifact_gallery_projection`，把 closeout refs、artifact refs、writeback receipt refs、owner receipt/no-regression refs 和 lifecycle locator 汇成通用 gallery / handoff shell；OPL 明确不读取 artifact body、不变更 artifact、不授权 export verdict。真实 artifact mutation/export result ingestion 与 App productization 仍需后续 live evidence。 |
| `workspace-source-intake-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_workspace_source_intake_projection`，把 workspace root、runtime root、profile ref、source fingerprint、source/material refs、missing-material attention refs 和 checkpoint refs 汇成通用 source intake shell；OPL 明确不选择 domain profile、不授权 source readiness、不写 domain truth。真实 source readiness verdict 与 missing material resolution 仍由 domain owner 闭合。 |
| `memory-locator-index-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_memory_locator_index_projection`，把 consumed memory refs、writeback receipt refs 和 rejected write refs 汇成通用 body-free memory index；OPL 明确不读取 memory body、不接受或拒绝 writeback、不写 domain memory body。真实 memory retrieval/writeback/body migration 仍由 domain owner 闭合。 |
| `package-export-lifecycle-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_package_export_lifecycle_projection`，把 package refs、export refs、gap-report refs、handoff refs、external submission status ref 与 artifact refs 汇成通用 package/export lifecycle shell；OPL 明确不授权 package readiness、不授权 export verdict、不变更 artifact。真实 package/export result ingestion 与 App productization 仍需后续 live evidence。 |
| `route-decision-graph-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_route_decision_graph_projection`，用 domain-owned `route_impact`、next owner、closeout refs、memory refs 和 artifact refs 构成通用 graph renderer 输入；OPL 明确不推断 route decision、不授权 quality verdict、不写 domain truth。真实 domain route projection ingestion 与 App productization 仍需后续 live evidence。 |
| `review-repair-queue-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_review_repair_queue_projection`，把 human gate、dead-letter、rejected write、controlled-apply blocker 和 lifecycle blocker refs 统一成 repair target transport；OPL 明确不决定 repair、不授权 review verdict、不写 domain truth。真实 domain repair/review result ingestion 与 App productization 仍需后续 live evidence。 |
| `quality-readiness-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_quality_readiness_projection`，把 domain ready verdict refs、quality refs、readiness refs 和 evidence refs 汇成通用 scorecard/drilldown shell；OPL 明确不授权 quality verdict、不授权 submission/package readiness、不写 domain truth。真实 publication/fundability/visual readiness verdict 仍由 domain owner 闭合。 |
| `observability-slo-read-model-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_observability_slo_projection`，把 provider readiness、SLO refs、breached SLO ids 和 repair command refs 汇成通用 observability / repair-command projection；OPL 明确不执行 repair command、不授权 SLO verdict、不写 domain truth。周期性 provider/operator SLO 和真实 domain repair result 仍需 live evidence。 |
| `operator-action-routing-shell` | OPL App / OPL | `landed_as_generic_read_model_shell` | Stage-attempt workbench 与 operator item 已输出 `opl_operator_action_routing_projection`，把 attempt query、App drilldown、domain-owner handoff 和 domain sidecar repair command 汇成明确 owner 的 action route envelope；OPL 明确不执行 domain action、不发 provider signal、不调用 direct skill、不写 domain truth。真实 App action productization 与 domain action receipt 仍需后续 live evidence。 |
| `native-helper-execution-envelope` | OPL / OPL App | `landed_as_refs_only_execution_envelope` | Runtime snapshot 已输出 `opl_native_helper_execution_envelope_projection`，从现有 native index / last-success / failure metadata 读取 helper id、request id、status、version/proof surface 与 operator-safe route；OPL 明确不在 snapshot 中执行 helper、不变更 domain artifact、不授权 domain gate。真实 domain helper implementation 与 helper-driven artifact mutation 仍由 domain owner receipt 或 typed blocker 闭合。 |
| `cross-repo-production-closeout-gate` | OPL + all domains | `functional_closure_ready_for_live_soak` | OPL `production functional closeout gate` 聚合 admitted domains 当前状态，汇总 typed closeout memory refs 和 Temporal proof receipt，在 `runtime_ledger` 中保留 provider proof 钻取摘要；默认预算可解析当前三仓 manifest，显式短预算下 stalled manifest 仍 fail-closed。当前 `stage_attempt_evidence.ledger_attempt_count=4`、`typed_blocker_count=0`；真实 live owner-chain evidence 仍由后续 soak 闭合。 |

## 吸收顺序

本轮吸收顺序已经完成：OPL provider/closeout gate 先落地，MAG/RCA functional closure proofs 已吸收到各自 `main`，MAS functional closure 和结构拆分在 MAS `main` 收口，最后由 OPL 文档记录当前统一口径。后续不再需要新建平行大计划，直接按 typed blocker 的 owner surface 推进真实 evidence gate。

## 非目标

- 不把真实论文质量、基金 fundability、视觉质量或 artifact authority 迁到 OPL。
- 不把真实 paper/grant/visual 长时 soak 写成已完成。
- 不把 MAS `append-block` 主 stage 独立 skill surface 纳入本计划。
- 不移动 workspace/runtime artifacts、receipt instances、memory body 或真实输出包到 repo-source skeleton。
- 不把 Hermes/Gateway/local provider 重新写成 production required substrate。

## 验收口径

每条 lane 的有效结果只有三类：

- repo/source/contract/CLI/app surface 通过 focused verification；
- domain owner receipt / no-regression evidence 证明可用；
- 来自 provider readiness、owner guard、authorization gate、live gate 或 domain contract gap 的 typed blocker。

`typed blocker` 是真实完成结果，前提是它来自机器可读 owner surface，并附带 source refs、owner、repair/next action 和 forbidden-write proof。

## 当前剩余项

剩余项只应写成 evidence gate 或 follow-through，不应写成“还缺基础功能”：

- `temporal_provider_slo_and_continuous_proof`：默认 fresh family runtime 仍选中 `local_sqlite`，`full_online_ready=false`、`durable_online_ready=false`，只能代表 dev/offline baseline；显式 `OPL_FAMILY_RUNTIME_PROVIDER=temporal` view 已显示本机 managed Temporal service + worker ready，`residency proof --provider temporal --production` 已返回 `production_residency_proven`，且 proof receipt 已进入 runtime ledger / closeout projection / runtime tray operator item，并持久化为 latest proof；stale proof 会被 closeout gate 标成 typed blocker。剩余是周期性持续 proof、长时 residency、operator repair loop 和真实 domain owner chain 证据。
- `mas_live_paper_owner_chain`：OPL queue 到 task-bound stage attempt 的桥接已落地；DM002 live dispatch 已两次产出 `blocked_no_mas_owner_apply_receipt` typed blocker，且 `writes_performed=false` / no-forbidden-write proof 成立；最新路径已证明 OPL 可从 active workspace binding hydrate MAS sidecar export 并自动带上 latest persisted proof。DM002、DM003、Obesity 仍需真实 provider-hosted guarded apply 进入 MAS owner apply receipt chain，产出 artifact/gate/reviewer/route/human-gate/stop-loss evidence 或稳定 owner typed blocker。
- `mag_rca_live_controlled_soak`：OPL 已能把显式 provider-hosted / controlled MAG/RCA sidecar task 绑定到 stage attempt，并接收 MAG receipt refs / RCA no-regression evidence refs；MAG/RCA 仍需真实 grant/visual stage attempt 的 domain owner receipt、quality verdict 或 long-run no-regression evidence。
- `domain_memory_body_apply`：三仓 descriptor/receipt projection 已存在，MAS body-free inventory 已补齐 operator grouping 与 stale/deprecated review summary，production closeout gate 已能汇总 typed closeout memory refs / writeback receipt refs / rejected writes，但 OPL fresh read model 的 retrieval/writeback/body migration 仍不是 OPL-applied success；真实 body、accept/reject 和 writeback apply 继续由 domain workspace/runtime owner 闭合。
- `unified_domain_agent_descriptor_cli`：canonical `./bin/opl agents descriptors --json` / `./bin/opl agents descriptor --domain mas --json` 已可用，release/dist `node dist/cli.js agents descriptors --json` 已同步；后续是保持 release/dist 构建输出、App consumption 和专题 drilldown 口径同步。
- `physical_path_migration`：MAS/MAG/RCA physical skeleton anchor evidence 已存在；完整物理 `agent/ contracts/ runtime/ docs/` 目录迁移仍需 direct skill path、OPL-hosted path、restore/provenance proof 和 no-forbidden-write proof。
- `legacy_physical_delete`：旧 Hermes/Gateway/frontdoor/local-manager/MDS/default-compat residue 的物理删除只保留真实 no-active-caller / provenance typed blocker；历史、fixture、provenance 引用继续保留语境标签。
