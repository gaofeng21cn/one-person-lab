# OPL 当前开发线路

Owner: `One Person Lab`
Purpose: `current_execution_map`
State: `active_support`
Machine boundary: 本文是人读执行地图。机器真相继续归 `contracts/`、source code、CLI/API behavior、runtime ledgers、provider receipts、domain-owned manifests 和真实 workspace / App evidence。
Date: `2026-05-28`

## 当前结论

OPL 当前开发继续按 Codex-default、provider-backed、framework-first 执行：先守住 OPL 作为完整智能体开发/运行框架，再让 MAS/MAG/RCA 作为标准 OPL Agents 消费 framework surface，并用真实 domain owner receipt / App evidence / long soak 验收目标结构。

当前 OPL 已有 Temporal provider、stage attempt ledger、typed queue、typed closeout、evidence worklist read model、domain descriptor aggregation、functional runtime harness、generic substrate projection、Agent Lab、pack compiler handoff、external evidence refs-only receipt ledger、App/operator drilldown read model、App runtime 页面消费路径、managed clean runner、`opl system startup-maintenance` App 启动维护机器入口、`opl_managed_module_install_update_ledger`、`opl_oma_app_live_path_ledger`、`opl_oma_production_consumption_ledger`、`opl_app_release_user_path_evidence_ledger`、`opl_codex_app_runtime_evidence_ledger`、`opl_standard_agent_template_consumption_ledger` 和 `opl_developer_mode_closeout_ledger`。provider scheduler、legacy cleanup、external evidence request、evidence gate、stage evidence accounting、Codex App runtime evidence、standard-agent template consumption replay evidence 和 memory/artifact/lifecycle receipt refs 都按 refs-only ledger / typed blocker / requirement 口径读取；具体 workorder、open safe action 和 receipt 数量只从 `opl framework readiness --family-defaults --json`、App drilldown 与 `family-runtime evidence-worklist` 生成读面读取。

当前 standard-agent structural conformance 由 `opl agents conformance --family-defaults --json` 读为 4 个 repo 通过、0 个 repo blocked，`structural_conformance_status=passed`，并继续把 `production_evidence_tail_count=4` 作为单独证据尾项报告。该通过项只证明 MAS/MAG/RCA/OMA 的 descriptor / skeleton / policy / morphology gate 当前可读，不授权 domain ready、artifact authority、physical delete 或 production ready。

`framework readiness` 默认总入口本轮恢复为 bounded read-model：同一 `opl framework readiness --family-defaults --json` 聚合只构建一份 5s bounded domain manifest catalog，并复用到 pack compiler、stage readiness 和 full App/operator snapshot；`runtime app-operator-drilldown --detail full --json` 只展开 full refs/routes，不再把 live manifest command timeout 放宽到 120s。若 live domain manifest 超时，读面会使用 projection cache 并暴露 cache-used、failed project 和 timeout 诊断；该 fallback 只保证 operator visibility / fail-closed availability，不授权 domain ready、production ready、artifact authority、owner receipt 或 App release ready。

`family-runtime evidence-worklist`、`framework readiness` 和 App/operator drilldown 的 open worklist、payload-required / payload-free、domain-dispatch workorder、evidence envelope、operator attention 和 ready-claim 字段都是动态读数，只从 fresh CLI/read-model 读取，不在本文冻结具体数值。`open_worklist_item_count` 为 0 时只表示当前没有 OPL 可执行 safe-action route；大于 0 时只表示 OPL refs-only accounting 暴露了可提交 route，payload 仍必须由 domain/App/live owner 提供真实 owner receipt、owner-chain、no-regression refs 或 typed blocker。`domain_ready_authorized=false`、`production_ready_authorized=false`、`zero_open_worklist_is_completion_claim=false`、`zero_open_worklist_is_domain_ready=false`、`zero_open_worklist_is_production_ready=false` 和 App release / production ready claim false flags 是该读面的固定完成口径：worklist/read-model 状态不能写成 completion、domain ready、App release ready 或 production ready。MAS legacy cleanup 的上一轮 `missing_no_active_caller_evidence` blocked plan 已由当前 `standard_agent_source_shape_landed` / `standard_agent_purity_cutover_guard` 证据关闭为 OPL cleanup ledger ready；这只关闭 refs-only cleanup ledger gate，不授权 domain repo 物理删除、domain ready、artifact authority 或 production ready。已关闭、blocked 或 superseded 的 MAS/MAG/RCA/OMA refs-only tail 仍作为 attention / evidence envelope 审计，不构成 paper、grant、visual、App 或 production closeout。MAG standalone external evidence receipt 与 RCA workspace receipt scaleout receipt 让 App/operator summary 可读 memory writeback、package lifecycle、cleanup/restore/retention lifecycle、artifact mutation 和 restore proof 等 verified external receipt 计数；RCA standalone no-regression receipt 现在也会进入 App/operator external receipt 与 evidence-worklist closed refs-only item，但只计入 `domain_external_verified_no_regression_ref_count`，不并入 memory/artifact/lifecycle 分项计数。RCA 当前 6-workspace snapshot `rca.workspace_receipt_scaleout.2026-05-28.6-workspaces` 已以 `opl://external-evidence/redcube_ai/rca-workspace-receipt-scaleout-20260528-6-workspaces` 验证为 OPL standalone external evidence，消费 6 条 artifact-producing owner receipt refs、12 条 accepted/rejected visual memory writeback receipt refs、18 条 cleanup/restore/retention lifecycle receipt refs 与 6 条 restore proof refs；旧 `20260527-5-workspaces` receipt 只保留为累计 provenance，RCA export 继续保持 `workspace_receipt_scaleout_claimed=false` 和 `declares_production_soak_complete=false`。RCA repeated visual-stage no-regression refs 已以 `opl://external-evidence/redcube_ai/rca-cross-family-repeated-no-regression-20260528-4-refs` 验证并投影，消费 `rca-no-regression:visual-stage:2026-05-27-opl-family-cross-family-repeat-a`、`rca-no-regression:visual-stage:2026-05-27-opl-family-cross-family-repeat-b`、`rca-no-regression:visual-stage:2026-05-28-opl-family-ppt-deck-window2` 与 `rca-no-regression:visual-stage:2026-05-28-opl-family-xiaohongshu-window2`，但不声明 production-like repeated route soak complete、visual ready 或 exportable。MAG `package_and_submit_ready` stage production evidence 也已通过 `opl://external-evidence/medautogrant/mag-package-stage-production-evidence-20260526` 作为 verified stage receipt 被 App/operator 消费；MAG full owner-payload response 的 7 条 recommended typed-blocker path 也已进入 OPL `domain-owner-payload-summary` ledger 并全部 verified，其中 `submission_ready_export_gate` 仍是 human approval required，其余 stage expected receipt 仍是 source/runtime-live-evidence pending。上述 receipts 只消费 domain-owned body-free refs；framework readiness 仍把未绑定真实 human approval、domain-declared request/gate 或 visual long-soak 的 evidence envelope 保持为 operator attention，而不是 domain/App/live closeout verdict。provider worker lifecycle repair 仍可能作为独立 OPL-owned maintenance action 暴露，不改变 domain/App/live owner evidence 边界。

Framework readiness 默认 summary 会把 App/operator 的 `domain_dispatch_attention_*` 折叠为 `domain_dispatch_attention_count`，用于提示 owner-chain dispatch 仍需真实 receipt / typed blocker / no-regression / long-soak scaleout，且不授权 domain ready 或 production ready。App/operator drilldown 也已消费 OMA patch-loop closeout refs；默认 summary 仅保留 OMA App section、scaleout target、patch-loop 总量和 OMA production-consumption follow-through gate count，full detail 才展开 OMA real-target scaleout 的 owner receipt / typed blocker、Agent Lab result、no-forbidden-write、cleanup closeout target counters，以及 `managed_install_update_refs`、`app_live_path_refs`、`owner_receipt_or_typed_blocker_scaleout_refs`、`long_soak_refs` 四类 production-consumption gate。

当前 OMA target owner receipt / typed blocker scaleout refs 已被机器读面消费为 observed，不再作为 open gate 重复投给 operator；managed install/update gate 现在会消费同一个 OPL module inspection，在 OMA 处于 env override、sibling workspace、dirty、ahead、diverged、no-upstream、unknown 或 invalid checkout 且没有 clean managed-root receipt 时，向 App/default caller 投影 manual-required blocker 和 `opl modules` review action；App/live operator 可用 `opl runtime oma-app-live-path record --payload <json>|--payload-file <path>` 写入 body-free `opl_oma_app_live_path_receipt`，关闭 `app_live_path_refs` gate。`opl runtime oma-production-consumption record --payload <json>|--payload-file <path>`、`verify` 和 `list` 现在可记录、验证或读取 OMA production-consumption 的 long-soak refs、typed blocker refs 和 operator evidence refs，其中只有 verified 真实 `long_soak_refs` 能关闭 `long_soak_refs` gate，recorded 但未 verify 的 receipt 只投影为 pending verify，typed blocker 和 operator evidence 只投影为阻塞或审计信号。Codex App runtime evidence 使用同样的 refs-only intake 口径：`opl runtime codex-app-runtime-evidence record --payload <json>|--payload-file <path>` 可记录真实 Temporal-hosted long-soak refs、provider state linkage refs、operator evidence refs 或 typed blocker refs，`verify [--receipt-ref <ref>]` 推进 verify follow-through，`list` 只读本地 ledger；同一 surface 的 `opl runtime codex-app-runtime-evidence long-soak start|event|finish` 只创建本地 observation workorder、追加受控 Temporal/provider/operator events，并在通过观察窗口后物化 manifest 与 record payload，仍需后续 `record` / `verify` 进入 ledger；当前本机已验证 1 条真实 `temporal_hosted_long_soak_refs` receipt，fresh App/operator drilldown 读为 `codex_app_runtime_evidence_open_gate_count=0`、`codex_app_runtime_evidence_verified_ledger_receipt_ref_count=1`、`codex_app_production_long_soak_claimed=false`、`codex_app_production_evidence_gate_remains_open=false`。App/default caller 对应 safe-action id 是 `codex_app_runtime_evidence:codex_app_runtime_role:record` / `verify`，且 success payload 必须包含 `temporal_hosted_long_soak_refs`，provider state linkage 与 operator evidence refs 只作为支撑审计 refs，support-only payload 会 fail closed。该 ledger 和 observation surface 都不启动长时任务、不创建 owner receipt、不生成 typed blocker、不写 domain truth，也不把 verified refs 写成 production-ready verdict。

App release/user-path evidence 现在也可通过 App/default caller 的 `runtime action execute` record/verify route 或 direct `opl runtime app-release-evidence record --payload <json>|--payload-file <path>` 写入 body-free ledger receipt，消费真实 release package、screenshot、reload prompt user path、provider state linkage、long operator evidence 或 typed blocker refs；该 read model 要求同一 release/user-path cohort，且只把 verified ledger receipts 计入 observed refs。多个候选 cohort 并存时，只有最新候选 cohort 自身完整覆盖五类 gate 才会被选择；如果更新候选只有部分 refs，旧完整 cohort 不会遮蔽新 cohort 缺口。当前本机已验证 `26.5.19` package/provider refs、同 cohort真实已安装 App 窗口截图 receipt、startup-maintenance / first-run reload-check receipt和同 cohort long operator evidence；2026-05-28 draft candidate `26.5.28-draft.20260527235839` 已有 GitHub Actions remote verification、标准/Full clean VM smoke、one-shot installer smoke 和 Docker WebUI smoke refs 可作为更新 cohort evidence。first-run `already-prepared` 日志只证明 reload-check / GUI preparation user path 已检查，不证明实际 reload 发生。上述 route / ledger 只关闭对应 refs-observed gate 或暴露 blocker，不生成 release artifact、不声明 App release ready、domain ready 或 family production ready。

Developer Mode closeout 现在可通过 `opl runtime developer-mode-closeout record|verify|list` 写入 body-free live repair closeout refs，并由 Agent Lab 只消费 verified receipt；2026-05-23 已记录并验证 `one-person-lab` direct-fix ledger receipt，Agent Lab 读为 `verified_direct_fix_closeout_refs_observed`，同时保持 full closeout incomplete，因为 fork/PR owner acceptance 还没有 verified live ledger receipt。`opl app state --profile fast|full` 现在把同一 Agent Lab live closeout read model 收窄投影到 `developer_mode.live_closeout_evidence`，只暴露 ledger status、verified / pending counter、fixture owner-acceptance open counter、owner acceptance policy 和 refs-only authority boundary，不包含 full drill 列表；App/operator drilldown 默认 attention payload 和 `framework readiness` 同步投影 `developer_mode_live_closeout_evidence`，把 missing direct-fix / fork-PR live ledger route、recorded verify-pending receipt 和 required external owner acceptance refs 作为 refs-only operator guidance 暴露，full detail 才展开 Agent Lab live closeout evidence drills 与 ledger refs。recorded receipt 保持 verify-pending，且 ledger intake、App state summary、App/operator projection 和 framework readiness 都不生成 owner receipt、不写 domain truth、不写 managed runtime truth、不声明 Developer Mode global closeout。真实 closure 仍必须来自 Developer Mode operator 或 external repo owner 的真实 direct-fix / fork-PR closeout refs；fixture drill、verified direct-fix-only receipt 或 App/framework 可见 projection 都不能写成 domain ready、owner receipt、managed runtime truth、release ready 或 production ready。

dated proof、receipt 事件、具体命令输出和阶段 closeout 摘要归档到 [OPL family 文档过程归档 2026-05](../history/process/plans/2026-05-18-opl-family-doc-process-history.md)。本文只保留当前顺序和 owner 边界。

当前清理口径按 strict standard-agent source purity 读取：OPL 自身持有 `family-runtime*`、Temporal provider、App/operator drilldown、standard conformance、generated surface 和 legacy cleanup guard 等 framework owner surface；这些是目标 runtime/control plane。MAS/MAG/RCA/OMA 中凡仍有或未来重新出现 active caller 的 hand-written product/status/workbench/sidecar、runtime projection、executor adapter shell、script materializer、compat alias、facade、re-export wrapper 或 compatibility-only 聚合测试，只能作为迁移输入，不再写成标准智能体长期组成。历史 `gateway` / `frontdoor` / `Hermes-first` / `managed runtime` / compatibility 相关文件只允许留在 `docs/history/**`、外部 provenance、negative guard 或 focused no-resurrection test 中；不保留 repo-local tombstone code path。若某个旧模块、旧接口、旧测试、旧文档入口已经没有 active caller，且 replacement proof / no-active-caller / owner receipt 或 history/provenance proof 成立，执行动作是删除 active source；不新增兼容 alias、facade、re-export wrapper 或 compatibility-only 聚合测试。

当前 JS/TS source hygiene 由 fallow 辅助读取。OPL 的 `.fallowrc.json` 只登记真实 package export、CLI/runtime/product/system 入口和动态 CLI case/module 入口，不把 `opl-aion-shell` 或历史 facade 当作本仓 active surface；`npm run hygiene:fallow -- --format json --summary` 是人工检查入口。当前 production fallow 已关闭 unused-file、unlisted-dependency 和 unresolved-import 问题；`src/management/domain-manifest-catalog.ts` 纯转发 facade 已删除，调用方直接读取 canonical domain-manifest builder/types，Python program companion 测试 fixture 也不再使用旧 `gateway/runtime truth` step id。剩余 public-export、duplicate helper 与 import-cycle tail 只按 owner surface 收薄、shared helper 上收或 runtime cycle 拆分处理。`family-runtime` 本轮已把 queue dispatch 从 `src/family-runtime.ts` 拆到 `family-runtime-task-dispatch.ts`，并通过 `family-runtime-scheduler-tick-runner.ts` 让 CLI tick 与 Temporal scheduler activity 共享同一 dispatch runner；该拆分降低入口文件耦合，但剩余 Temporal provider / activities / scheduler / provider-hosted attempt cycle 仍是后续结构 hygiene gate，不写成 production evidence。

## 当前顺序

1. `opl_framework_foundation`
   保持 provider-backed stage runtime、typed queue、attempt ledger、signal/query、retry/dead-letter、human gate、receipt/projection、shared lifecycle/index primitive、safe action shell 和 generated surface 基础。

2. `production_evidence_scaleout`
   保持 `runtime app-operator-drilldown`、`runtime action execute` 和 `family-runtime evidence-worklist` 的 refs-only worklist / typed blocker / workorder 能力，但当前重心转到真实 production evidence、conformance regression guard、domain owner-chain、memory/artifact/lifecycle receipt、direct/hosted parity 和 long-soak refs。Domain-dispatch evidence workorder 已显式投影 success refs path 与 typed blocker path，并由 payload preflight 阻断空模板、占位 ref、身份冲突和 generic evidence-only payload；success refs path 只接受 `domain_receipt_refs`、`owner_chain_refs` 或 `no_regression_refs`，`evidence_refs` 只作为 supplemental context，不单独关闭 domain-dispatch owner-chain。这些 payload path、preflight gate 与 required return shapes 也已进入 App `evidence_next_steps`、`owner_handoff_packet` 和 `framework readiness.next_safe_actions` 默认接力面。Owner payload group、owner handoff owner entry 和 framework owner handoff review 现在携带 `opl_owner_handoff_payload_workorder`，把 owner 级 success refs path、typed blocker path、payload template、empty-template block 和 false authority flags 一起投给默认 caller；该 workorder 不创建 owner receipt、typed blocker、owner-chain ref、no-regression ref、domain ready 或 production ready。Framework readiness 与 App/operator drilldown 同步暴露 `operator_payload_required_attention_*`、`operator_payload_free_attention_*` 和 `domain_blocked_attention_*` 子计数，`family-runtime evidence-worklist` 也同步暴露 open safe action 的 payload-required / payload-free 子计数和语义字段；worklist split 表示全量 open safe-action route，framework readiness split 只表示默认 operator-attention tail，只有两边 route 集合同一时才一一相等。payload-required route 可以由 safe-action shell 记录真实 owner refs 或 typed blocker，但不能被误读成 OPL 自己能生成 owner payload、关闭 domain ready，或把 provider scheduler / install / verify 这类 payload-free route 归入 owner evidence payload-required attention；只有 domain-owned blocker、route-back blocker 或 blocked refs-only envelope 时，framework readiness 读为 `framework_control_plane_available_with_blocked_refs_only_attention`，next safe action 只给 refs-only review，不给可提交 workorder。Group-level `domain_dispatch_evidence_group_workorder` 会给出 bounded record action ids、`opl runtime action execute` record command refs、空 payload template、payload hints 和 empty-template policy，便于 Codex/default caller 先判定回填路径并拿到最小可执行命令，再按需读取 full detail。App release/user path 也已有专门 `app_release_user_path_evidence` 接力面、refs-only evidence ledger、`runtime action execute` record/verify safe action route 和 `opl_app_release_user_path_evidence_payload_workorder`，按 release package、screenshot、reload prompt user path、provider state linkage 和 long operator evidence 五类 required refs 暴露 gate，并把真实 App/live/release refs path 与 typed blocker path 写成默认机器字段；该面带 cohort guard，只有同一 release/user-path cohort 的 refs 才会合并计算 gate，并且多 cohort 时只在最新候选 cohort 自身完整时选择它，避免把旧 VM first-run evidence 与新 release package 拼成虚假 closeout，也避免旧完整 cohort 遮蔽更新 candidate 的缺口。OMA production-consumption 也进入同一默认接力面：App/default caller 可通过 `oma_production_consumption:opl-meta-agent:record` safe-action route 提交真实 long-soak、typed blocker 或 operator evidence refs payload；当前 OMA 四类 production-consumption gate 已由 verified refs 读成 observed，历史 typed blocker 只保留 provenance。OPL 只提供 transport、preflight、ledger、local observation event intake、manifest materialization 和 projection；domain/App/live refs 才能关闭生产可用性。`family-runtime production-closeout` 旧 alias 已退役，不再作为 active interface 或兼容面保留。

   当前 OMA App live-path ledger 的 operator CLI intake 是 `opl runtime oma-app-live-path record --payload <json>|--payload-file <path>`，本地只读检查是 `opl runtime oma-app-live-path list`。这两条命令只记录或读取 OMA App live path、App surface 和 operator evidence refs；它们不生成 App artifact、不执行截图、不证明 long-soak，也不关闭 production-ready verdict。当前 OMA production-consumption ledger 的 operator CLI intake 是 `opl runtime oma-production-consumption record --payload <json>|--payload-file <path>`，follow-through 是 `opl runtime oma-production-consumption verify [--receipt-ref <ref>]`，本地只读检查是 `opl runtime oma-production-consumption list`；同一 surface 还有 `opl runtime oma-production-consumption long-soak start --minimum-duration-minutes <n> --evidence-dir <path>`、`opl runtime oma-production-consumption long-soak event --workorder-file <path> --event-kind <kind> [--observed-at <iso>] [--evidence-ref <ref>]` 和 `opl runtime oma-production-consumption long-soak finish --workorder-file <path> [--finished-at <iso>]`。App/default caller 对应的 safe-action id 是 `oma_production_consumption:opl-meta-agent:record`。这些命令和 safe-action route 只记录、验证、读取或本地物化 OMA long-soak refs、typed blocker refs 和 operator evidence refs；它们不启动长时运行、不生成 owner receipt、不写 OMA domain truth、不声明 global production ready，且 recorded 但未 verify 的 receipt 不参与 `long_soak_refs` gate closure。long-soak `start` 只写本地观察 workorder 和 operator log 文件；`event` 只追加 body-free required event，不写 ledger、不生成 `long_soak_ref`；`finish` 只有在最小时长和必要 operator events 满足后才物化 `long_soak_refs` record payload 文件，仍需再走 `record` / `verify` 才能进入 refs-only ledger。typed blocker refs 只说明 production-consumption closure 被 owner 显式阻塞，operator evidence refs 只作为审计/监控引用，不会单独关闭 `long_soak_refs`。Codex App runtime evidence 的 operator CLI intake 是 `opl runtime codex-app-runtime-evidence record --payload <json>|--payload-file <path>`，follow-through 是 `opl runtime codex-app-runtime-evidence verify [--receipt-ref <ref>]`，本地只读检查是 `opl runtime codex-app-runtime-evidence list`；同一 surface 还有 `opl runtime codex-app-runtime-evidence long-soak start --minimum-duration-minutes <n> --evidence-dir <path>`、`opl runtime codex-app-runtime-evidence long-soak event --workorder-file <path> --event-kind <kind> [--observed-at <iso>] [--evidence-ref <ref>]` 和 `opl runtime codex-app-runtime-evidence long-soak finish --workorder-file <path> [--finished-at <iso>]`。App/default caller 对应 safe-action id 是 `codex_app_runtime_evidence:codex_app_runtime_role:record` / `verify`。这些命令只记录、验证、读取或本地物化真实 Temporal-hosted long-soak refs、provider state linkage refs、operator evidence refs 或 provider-owner typed blocker refs；long-soak `start` 不写 ledger，`event` 只追加 body-free required event，`finish` 只物化 manifest、hash-bound refs 和可提交 record payload，仍需再走 `record` / `verify` 才能进入 refs-only ledger。recorded 但未 verify 的 receipt 只投影为 verify-pending，typed blocker refs 只保持 blocked，App/default caller success path 必须包含真实 `temporal_hosted_long_soak_refs`；provider state linkage 与 operator evidence refs 只作为支撑审计 refs，单独 verified 不关闭 `codex_app_runtime_role.production_evidence_followthrough` gate，不创建 owner receipt、不关闭 long-soak 语义、不声明 production ready。当前 App release/user-path ledger 的 operator CLI intake 是 `opl runtime app-release-evidence record --payload <json>|--payload-file <path>`，本地只读检查是 `opl runtime app-release-evidence list`；同一 surface 现在还有 `opl runtime app-release-evidence long-operator start --cohort <version> --minimum-duration-minutes <n> --evidence-dir <path>`、`opl runtime app-release-evidence long-operator event --workorder-file <path> --event-kind <kind> [--evidence-ref <ref>]` 和 `opl runtime app-release-evidence long-operator finish --workorder-file <path> [--finished-at <iso>]`。long-operator `start` 只写本地观察 workorder 和 operator log 文件，不写 ledger、不关闭 evidence；`event` 只追加 body-free required event；`finish` 只有在最小时长和必要 operator events 满足后才物化 `long_operator_evidence_refs` record payload 文件，仍需再走 `record` / `verify` 才能进入 refs-only ledger。`record` 和 `list` 只记录或读取 release package、screenshot、reload prompt user path、provider state linkage、long operator evidence 或 typed blocker refs；recorded 但未 verify 的 receipt 会继续投影为 verify follow-through，不会被默认读成闭环完成。当前 verified receipt `opl://app-release-user-path-evidence/26.5.19-package-provider`、同 cohort screenshot receipt、reload-check receipt 和 long operator receipt 让 `26.5.19` cohort 的五类 App release/user-path gate observed；verified typed blocker receipt 保留为 historical provenance，active `typed_blocker_ref_count=0`。当前 Developer Mode closeout ledger 的 operator CLI intake 是 `opl runtime developer-mode-closeout record --payload <json>`，本地 follow-through 是 `verify` 与 `list`；它只记录或验证 route eligibility、patrol observation、diff、verification、no-forbidden-write、commit 或 fork/PR、external owner acceptance refs，并让 Agent Lab 与 App state summary 消费 verified live receipt，不写 owner receipt、不写 managed runtime truth，也不关闭 production-ready verdict。App state summary 只让 default caller 看到 closeout ledger 的 bounded 状态和计数，不生成 App artifact、不证明长时用户路径，也不关闭 release-ready / production-ready verdict。

   Domain-dispatch evidence 已从多轮逐条 route closeout 收敛为当前语义：MAS guarded-apply / owner-route / aftercare / default-executor 与逐 paper-line `paper_line_owner_chain_results`、MAG grant-stage / lifecycle / legacy route-back、RCA visual-stage typed blocker payload 都能按真实 owner refs、typed blocker path 或显式 route-back blocker 被 OPL safe-action shell 记录、验证或投影，并由 payload preflight 防止跨 attempt / stale payload 误闭合。MAS per-line result 进入 OPL 时只作为 refs-only owner receipt / owner-chain support / no-forbidden-write proof / stable typed blocker payload，不携带 body，不授权 artifact mutation，也不关闭 paper 或 publication readiness；若 payload 显式携带 body 或 readiness/artifact authority claim，preflight 会 fail closed 而不是静默吸收。Preflight 会继续接收并计数 supplemental `evidence_refs`，但 evidence-only payload 必须 fail closed，不能被写成 success closeout；worklist 只有看到 owner receipt、owner-chain、no-regression 或 typed blocker refs 才能关闭对应 domain-dispatch record action。OPL normalizer 现在能消费 MAS/MAG/RCA/OMA current standard-agent evidence；fresh App/operator drilldown 读为 `domain_legacy_cleanup_plan_count=3`、`domain_legacy_cleanup_ready_plan_count=3`、`domain_legacy_cleanup_blocked_plan_count=0`，且 domain physical delete can execute 仍为 false。Legacy cleanup plan ready 只表示 OPL cleanup ledger/read-model 可关闭 source-purity gate；它不会被写成 domain physical delete authorization、domain typed blocker、domain ready 或 production ready。Fresh `family-runtime evidence-worklist` 和 `framework readiness` 读面只从 CLI 动态读取；open worklist、closed refs-only、domain-dispatch workorder、stage receipt freshness、open evidence safe action、payload-required/free evidence safe action、evidence envelope 和 operator attention 计数不在本文固化。当前这些读面只说明 OPL refs-only accounting / identity preflight / ledger transport / route-back blocker classification 可用，不声明 MAS guarded apply、reviewer refresh、MAG grant stage、RCA visual stage、App release/user path、domain ready、visual ready、artifact authority、long-soak 或 production ready。当当前状态只有 blocked refs-only attention、没有 operator-actionable safe action 时，`review_blocked_refs_only_attention` 会携带 full readiness / App drilldown / evidence-worklist drilldown 命令、top owner payload group 摘要和 detail section 指针，方便 operator 直接定位 domain-owned typed blocker 或 owner payload tail；该 action 仍保持 refs-only review，不允许提交 payload、执行 domain action、创建 owner receipt / typed blocker、关闭 domain ready 或声明 production ready。

   具体 stage attempt id、receipt ref、source fingerprint、record / verify 命令输出和 tranche-by-tranche 数字只从 OPL external evidence ledger、runtime read model、提交历史和 [OPL family 文档过程归档 2026-05](../history/process/plans/2026-05-18-opl-family-doc-process-history.md) 读取，不再复制到 active 支撑文档。Active 文档只保留当前 owner、当前差距和下一步：App release/user-path、Codex App runtime evidence 与 OMA production-consumption 的当前 gate 已有 verified refs，后续重点转到 MAS/MAG/RCA/App/live operator 的真实 owner receipt、typed blocker closeout、no-regression、expected receipt、monitor freshness、memory/artifact/lifecycle receipt 和后续 long-soak scaleout；OPL 不从 workorder 模板自造 owner receipt、typed blocker、owner-chain、no-regression、release-ready 或 production-ready 证据。

3. `strict_standard_agent_source_purity`
   先让 OPL generated/hosted CLI、MCP、Skill/product-entry、sidecar、status、session、workbench 和 harness 成为 MAS/MAG/RCA/OMA 的生产默认 caller，再删除 domain repo 手写 default caller、wrapper、runtime projection、diagnostic cleanup shell、compat facade 和 compatibility-only tests。Domain repo 最终只留 domain pack、machine-readable contract、authority function、domain handler target、domain-specific implementation 与必要 native helper。

4. `domain_private_residue_retirement`
   把 framework-generic 能力上收到 OPL，把 domain truth 留在 domain。满足 replacement parity、no-active-caller、domain receipt parity、provenance/history 和 no-forbidden-write 证据后，旧模块、接口、alias、facade、wrapper、旧测试入口和 compatibility tests 直接删除；测试改为锁定当前 machine-readable contract、generated surface、domain owner receipt 或 no-resurrection guard。

5. `opl_app_runtime_workbench`
   将 provider readiness、stage attempt、route graph、review/repair queue、source refs、artifact refs、memory refs、quality/readiness、SLO、workorder packet、owner-aware action routing 和 refs-only graph/timeline/research lens 做成人可用工作台。App 从 `runtime_visualization_projection` 读取 stage/route/receipt/blocker/safe-action graph、timeline event、`runtime_workbench` 纵向工作台模型和 MAS `paper_route_lens_refs`，默认展示全局摘要、行动队列、domain lane 动态地图、单任务 drilldown 与论文路线 lens refs；summary 允许 10 秒轻轮询兜底，full detail 按需加载。该面只做展示与 drilldown，不读取论文正文、memory body 或 artifact body，不声明 publication ready、domain ready 或 production ready。

6. `domain_soak_and_acceptance`
   MAS 完成真实 paper-line provider apply 证据；MAG/RCA 分别完成 controlled grant / visual stage attempt、owner receipt / no-regression evidence、expected receipt instance、monitor freshness 和 long SLO。

7. `new_domain_admission`
   新 domain 只按 OPL scaffold、descriptor、stage/action/memory/artifact locator、authority function ABI、stage evidence workorder policy 和 docs taxonomy 接入；旧 gateway/frontdoor/local-runtime 路线已退役且不得恢复。OMA / New Agent 已有 structural consumption proof；`opl agents scaffold --consumption-evidence` 可让 `award-foundry`、`thesis-foundry`、`review-foundry` 三个临时样本穿过 generate、scaffold validation、standard conformance、agent readiness 和 App/operator projection，三样本均按 ephemeral policy 清理临时目录。App/operator full drilldown 现在给 `standard_agent_template_consumption_refs` 暴露 replayable `evidence_contract`，明确复跑命令、期望输出 root、cohort sample success path、消费 surface 和 forbidden claim fields；`opl runtime standard-agent-template-consumption record|verify|list` 可把 replay evidence ref/fingerprint、cohort ref/fingerprint、sample refs 与 consumed surface refs 写入 body-free ledger，并由 App/operator summary 投影 recorded / verified / pending verify receipt 计数。该 contract 和 ledger 只让 operator 可复验 scaffold consumption 形状并保留 durable receipt，不隐式生成临时仓、不授权 domain ready、artifact authority、quality/export 或 production ready。production-consumption follow-through 现在按 managed install/update、App live path、owner receipt / typed blocker scaleout 和 long-soak 四个 gate 进入 App/framework 默认读面，当前四类 gate 均有 observed refs，OMA production-consumption ready；后续重点是更多真实 target patch/rerun/owner receipt 样本，而不是把 OMA ready 外推成目标 domain ready、family production ready 或默认 promotion。

## 内容线路

| 线路 | 当前 owner | 当前要做 |
| --- | --- | --- |
| `provider_runtime` | OPL Runtime Manager / Temporal provider | 固定 Temporal production provider，保持 cadence / capability SLO satisfied；继续补真实 domain owner-chain dispatch 和长时 operator evidence。 |
| `stage_evidence_accounting` | OPL production closeout / App operator shell | Stage production evidence workorder 继续是 refs-only route、payload preflight、typed blocker 和 domain/stage packet 守门面；同 stage 的 domain-owner payload summary 现在会作为 candidate refs 投影到 record route 和 workorder，帮助 operator 定位可提交 success / typed-blocker payload 与剩余缺口，但不自动关闭 route、不生成 owner receipt、不声明 domain ready 或 production ready。 |
| `generated_surface` | OPL pack compiler / generated surface | 从 domain descriptor/stage/action/memory/transition/receipt metadata 派生 entry/status/sidecar/workbench/harness，并迁移生产 caller。 |
| `conformance_physical_morphology` | OPL agents conformance | 保持 conformance 主入口为薄聚合器；physical morphology policy、active residue scan 和 provenance/tombstone allowance 只能在 scoped module 中演进，并由 line-budget / modularization tests 防回堆。 |
| `domain_private_residue` | OPL functional audit + domain repos | 按 OPL replacement、generated surface、refs-only adapter、minimal authority function、tombstone 分类收薄或删除；MAS 旧 runtime / runner / worker lease / lifecycle writer 已按 no-alias 退役，当前只守 no-resurrection 和仍有 caller 的 domain-ref projection / owner-route / workbench 删除门。 |
| `lifecycle_memory_artifact` | OPL primitive + domain owner receipt | OPL 只持 locator/index/ledger/ref transport；external evidence ledger 可 intake / verify / project memory writeback、artifact mutation、package lifecycle、lifecycle、restore proof 和 no-regression refs；MAG `product receipt-readiness` 已以 `opl://external-evidence/med-autogrant/mag-package-lifecycle-receipt-scaleout-20260526` 进入 App/operator observed 计数，RCA 6-workspace refs-only memory/lifecycle/workspace receipt proof 已以 `opl://external-evidence/redcube_ai/rca-workspace-receipt-scaleout-20260528-6-workspaces` 进入 OPL external-evidence ledger，RCA four-ref no-regression receipt 已以 `opl://external-evidence/redcube_ai/rca-cross-family-repeated-no-regression-20260528-4-refs` verified 并投影到 App/operator 与 evidence-worklist。domain 持 body、mutation authority、accept/reject 和 final verdict；observed refs 不授权 package/export/visual readiness、physical delete、domain ready、Temporal visual-stage long soak 或 production ready。 |
| `app_workbench` | One Person Lab App / OPL product surface | 消费 App/operator drilldown、`runtime_visualization_projection` graph/timeline/research lens、safe action routes、cleanup plan、stage evidence accounting、OMA patch-loop closeout refs、OPL Meta Agent refs-only workbench sections、OMA production-consumption follow-through gates 和 App release/user-path evidence gates；full detail 提供 OMA evidence-after-contract counters，默认 next steps 暴露 release package / screenshot / reload prompt / provider state / long operator evidence 的 return-shape guidance，并把 App release/user-path evidence record/verify route 放入 safe action shell；App release gate 按单一 cohort 计算，当前 `26.5.19` package/provider refs、真实已安装 App 窗口截图 receipt、startup-maintenance / first-run reload-check receipt 与同 cohort long operator evidence 均已 verified，historical typed blocker 不再 active。 |
| `legacy_cleanup` | OPL gate + domain repo owner | replacement proof 和 no-active-caller proof 后直接删除或 tombstone；OPL 可写 cleanup ledger / tombstone refs，domain repo 文件删除需要 domain owner receipt。 |

## 当前直接退役优先级

| surface family | 当前实际状态 | 执行动作 |
| --- | --- | --- |
| OPL 历史 gateway/frontdoor/federation docs | 已在 `docs/history/compatibility/**` 或 `docs/history/frontdoor-legacy/**` 承担 provenance。 | 不恢复 active reference；若有 active doc / README / policy 继续指向旧路线，改为当前 OPL runtime / App / Foundry Agent 边界。 |
| `family-runtime production-closeout` 等旧 CLI alias | 已被当前 readiness / evidence-worklist / App drilldown route 替代；`opl framework production-closeout` 只作为 framework-level 汇总命令保留。 | 不保留 compatibility alias；仍被测试或文档引用时迁到当前命令或 negative guard。 |
| domain repo 手写 product/status/workbench/sidecar wrappers | MAS/MAG/RCA 仍有 active retained adapters。 | 只按 domain handler / refs-only adapter / diagnostic / migration input 读取；OPL generated/default caller parity 成立后删除旧 wrapper 和兼容测试。 |
| stale compatibility tests | OPL 当前只需要 no-resurrection guard、contract behavior test 或 migration proof。 | 删除只保护旧路径的测试；保留的测试必须断言当前 contract、no-active-caller、fail-closed、retired alias rejection 或 tombstone semantics。 |

## 合并与退役规则

| 内容类型 | 长期归属 |
| --- | --- |
| stage attempt、provider runtime、queue、signal/query、retry/dead-letter、approval transport | OPL Framework / Runtime Manager |
| expected receipt / monitor freshness route、payload preflight、workorder packet、typed blocker、refs-only evidence ledger | OPL Framework / App operator shell |
| lifecycle ledger、artifact locator/index、retention、restore proof、migration ledger、workspace lifecycle metadata | OPL Framework primitive |
| MAS study truth、publication gate、evidence/review ledger、manuscript/package authority | MAS |
| MAG grant strategy、fundability / proposal quality、specific aims authority | MAG |
| RCA visual direction、creative artifact generation、review/export gate | RCA |
| old gateway/frontdoor/Hermes-first/local-manager default wording | replacement proof 与 no-active-caller scan 通过后删除或进入 history/tombstone |
| external framework learning | references only，除非明确提升为 contracts/source/active owner docs |

## 完成信号

| 线路 | 完成信号 |
| --- | --- |
| `opl_framework_foundation` | OPL provider/framework 能稳定承载 stage attempt、queue/wakeup、retry/dead-letter、approval/human gate、receipt/projection 和 shared lifecycle/index primitive。 |
| `production_evidence_scaleout` | OPL closeout/workorder accounting 可 fail-closed 投影并当前无 open safe action；真实 production closure 由 App release/user path、domain owner-chain、memory/artifact/lifecycle receipt、direct/hosted parity、no-regression 或 long-soak refs 关闭。 |
| `generated_surface_production_consumption` | MAS/MAG/RCA 生产默认 caller 使用 OPL generated/hosted surfaces；domain repo 只保留 domain handler、authority function、refs-only adapter 或 diagnostic cleanup。 |
| `domain_private_residue_retirement` | 旧默认依赖、legacy compat、重复 UI、过时 manager surface 完成分类、替代和退役；无 active caller 的旧模块、接口、测试、alias、facade 和 wrapper 已删除或 tombstone，不保留兼容入口。 |
| `opl_app_runtime_workbench` | App/workbench 能按 owner drill down provider、stage attempt、route graph、timeline、MAS paper route lens refs、domain refs、memory/artifact/source refs、workorder、SLO、repair 和 safe actions，并有截图/发布包/长时 evidence。 |
| `domain_soak_and_acceptance` | MAS/MAG/RCA 在迁移后目标形态下各自产出真实 progress delta、quality gate movement、human gate、stop-loss、domain owner receipt、no-regression evidence 或 typed blocker。 |

## 文档落点

- 当前差距、执行顺序和 baton：`docs/active/`。
- 目标态和支撑参考：`docs/references/`。
- runtime/provider/executor/control plane 支撑：`docs/runtime/` 和 `docs/references/runtime-substrate/`。
- App/workbench/product surface：`docs/product/`。
- 旧路线、完成计划、dated proof、receipt 流水和 process archive：`docs/history/`。

如果内容仍决定“接下来按什么顺序做、什么算完成”，放当前 owner doc；如果只是来龙去脉或过程证据，放 history/provenance。
