# OPL Family 当前状态与理想目标差距

Owner: `One Person Lab`
Purpose: 对照 OPL / Foundry Agents 理想目标态，记录 OPL、MAS、MAG、RCA、One Person Lab App 与 MDS 当前实际状态、差距和需要完善的部分。
State: `active_support`
Machine boundary: 本文是人读 gap / completion map。机器真相继续归 `contracts/`、源码、CLI/API 行为、runtime ledger、provider receipt、domain-owned manifest、真实 workspace / App 证据。
Date: `2026-05-15`

## 结论

当前 family 已经完成“目标形态的控制面骨架”：`OPL Framework -> One Person Lab App -> Foundry Agents` 三层产品认知成立，MAS/MAG/RCA 都能被 OPL 识别为 descriptor-level aligned 的 standard domain agent，三仓 stage plane 和 domain memory descriptor 也都能被 OPL 只读解析。

MAS、MAG 与 RCA 理想目标态进一步校准了 family 边界：通用 runtime、queue、workspace/source intake shell、memory locator、artifact/package lifecycle、restore/retention、projection、workbench shell、route/decision visualization、review/repair transport、native-helper execution envelope 和 observability primitive 应尽量由 OPL Framework / One Person Lab App 提供；MAS/MAG/RCA 这类 Foundry Agents 应收窄成 `Domain Knowledge / Authority Pack`，持有领域知识、路线/策略、质量 verdict、artifact/export authority、memory body/writeback decision、owner receipt 和薄 sidecar/projection adapter。这个校准不改变当前 truth owner，只改变“后续缺口应该补在哪里”的判断。

回答原计划完成度时应分开两层：核心功能 surface / contract / receipt-ref / typed-blocker 机制已经落地；canonical `./bin/opl agents descriptors --json` 统一 descriptor CLI 已可用，`node dist/cli.js agents descriptors --json` 也已验证同步；显式 Temporal service / worker / production proof 已在本机 managed provider 上返回 `production_residency_proven`，proof receipt 已写入 runtime event ledger 并持久化到 OPL state，随后可被 `framework production-closeout` 投影为 `provider_continuous_proof`；OPL family queue 对 MAS `paper_autonomy/guarded-apply` 任务已能自动建立 task-bound provider-backed stage attempt，并接收 MAS sidecar typed closeout refs；`family-runtime intake/tick --hydrate` 现在能从 active workspace binding 派生 MAS sidecar export command，并自动携带最近一次 persisted Temporal production proof；显式声明 provider-hosted / controlled 的 MAG/RCA sidecar task 也已有 live 证据进入 task-bound attempt，并把 MAG product sidecar receipt / RCA no-regression evidence ref ingest 为 OPL typed closeout；domain sidecar 导出的 `family_transition_matrix_result` 现在也可被 OPL intake 自动派生成 provider-hosted `family_transition/domain_tick` queue task，随后进入 task-bound stage attempt / transition bridge。使用 operator 超时预算重跑 production closeout 时，当前状态已推进到 `functional_closure_ready_for_live_soak`、`typed_blocker_count=0`。原计划里仍需要 evidence gate 的部分是破坏性物理目录迁移、真实 memory body / writeback apply、legacy 最终物理删除、长时 provider/operator SLO 和真实 domain owner chain；它们不再表现为 OPL functional closeout blocker，但也不能写成真实 paper/grant/visual soak 已完成。

离理想情况的主要差距不是概念、命名或 descriptor，而是 production closure：

- OPL 默认 fresh runtime view 仍是 `local_sqlite` provider ready，`full_online_ready=false`、`durable_online_ready=false`；这是 dev/offline baseline，不代表 production online readiness。
- 显式 Temporal view 已显示本机 managed service + worker ready，`OPL_FAMILY_RUNTIME_PROVIDER=temporal` 时 `full_online_ready=true`、`durable_online_ready=true`，且 `residency proof --provider temporal --production` 返回 `production_residency_proven`；这证明 provider readiness / residency proof，不等于真实 MAS/MAG/RCA domain owner chain 已闭合。
- `family-runtime residency proof --provider temporal --production` 现在会写入 `temporal_residency_proof` runtime event，并持久化到 `~/Library/Application Support/OPL/state/family-runtime/proofs/latest-temporal-production-proof.json`；`framework production-closeout` 会读取最近 proof receipt 并输出 `provider_continuous_proof`，同时在 `runtime_ledger.provider_continuous_proof` 保留可钻取摘要；`runtime snapshot` 会把 provider proof 作为 operator lane item 投到 attention / recent。当前 live proof projection 为 `all_observed_proofs_proven`，并带有 `latest_event_age_seconds`、`proof_freshness_status`、`proof_slo_status`；provider SLO execution receipt 与 repair-loop projection 现在还携带稳定 `temporal-provider-production-proof-cadence` action envelope，供 App / operator / automation 只读路由下一次 supervised proof。2026-05-15 live `provider-slo tick --provider temporal` 返回 `skipped` / `cadence_current`，写入 receipt `evt_ef27fb14-d949-4651-9fc4-03640fd1899b`，`slo_execution_receipt_event_count` 从 4 增至 5。它关闭的是 provider proof ledger / operator visibility / cadence action projection surface 和 supervised cadence receipt evidence，不关闭自动长时调度或 domain owner chain。
- MAS/MAG/RCA 都是 `descriptor_aligned`；当前 OPL 读模型显示三仓均已有 repo-source anchor evidence，三仓 legacy tombstone / no-active evidence 已被 closeout 消费。剩余 production closure 主要是 live owner-chain 与物理 follow-through，不是 descriptor 或 tombstone 声明缺口。
- 三仓 memory descriptor 均已 resolved；production closeout gate 和 `domain-memory` read model 可从 typed closeout packet / stage attempt closeout ledger 汇总 consumed refs、consumed memory refs、writeback receipt refs 和 rejected writes，并显式保留 `opl_writes_memory_body=false`。当前 fresh domain-memory read model 已能把 MAG accepted / rejected memory receipt refs、MAS rejected writes 与 RCA closeout refs 按 domain 投影出来；retrieval apply、writeback apply 和 memory body migration 仍不是 OPL-applied success。
- Runtime snapshot 已有 stage-attempt workbench、stage-attempt operator item、attention queue、provider continuous proof operator item、operator action routing shell、native-helper execution envelope 和 domain projection ingestion 投影；stage attempt item 可只读展示 provider completion、domain ready verdict、controlled apply contract、artifact locator / restore proof、lifecycle restore / domain receipt refs、memory refs、writeback receipt refs、rejected writes、route-impact no-regression evidence refs、transition bridge owner receipt / no-regression evidence / typed blocker refs 和明确 owner 的 action route refs；workbench filter metadata 还显式暴露 `has_transition_bridge`、`has_transition_owner_receipt_refs`、`has_transition_no_regression_evidence_refs`、`has_transition_typed_blockers` 四个 transition evidence filter flags；production closeout gate 也会在 `stage_attempt_evidence.transition_bridge_evidence_summary`、generic projections 和每个 domain breakdown 中 refs-only 汇总 transition bridge owner receipt / no-regression / blocker refs；action routing 已能把 OPL CLI、App drilldown、transition bridge evidence drilldown、lifecycle primitives drilldown、provider signal、domain sidecar 与 domain direct skill ref/command 都投影成 `route_only_no_execution` 路由。domain projection ingestion 只汇总 manifest-owned projection refs / surface kind / owner / source refs，不执行 domain projection builder、不读取 domain truth body、不授权 ready / quality / export verdict。当前 fresh snapshot 里 `stage_attempt_workbench.summary.total=18`、`by_status.completed=18`，覆盖 `medautogrant=11`、`medautoscience=6`、`redcube=1`；MAS 最新 DM002 attempt 已观测到 owner stable blocker receipt，真实 long-running provider-hosted domain activity 仍未形成连续 owner-chain 证据。
- One Person Lab App 目前是 OPL-branded AionUI shell / workbench fork，消费 OPL runtime truth；它还不是 domain truth owner，也不是 OPL runtime owner。
- MDS 当前只是 MAS 声明的 archive/reference/diagnostic/upstream-intake surface，不是 active Foundry Agent，不需要补成 OPL 同级 domain agent。

本文对照 [OPL 与 Foundry Agents 理想目标态](../references/runtime-substrate/opl-family-agent-ideal-state.md)。MAS/MAG/RCA 的单仓理想态、仓内差距和实施计划回到各自仓库维护；OPL 只维护 family-level framework、shared primitive、App/workbench 和 domain admission 的完善顺序。当前状态和比例判断仍以 [OPL 当前状态](../status.md)、[OPL 架构](../architecture.md)、[OPL Stage-Led Agent Framework Roadmap](../references/runtime-substrate/opl-stage-led-agent-framework-roadmap.md) 与 [OPL 生产级框架闭环差距矩阵](./production-framework-closure-gap-matrix.md) 为准。

## Fresh Evidence 2026-05-15

本次文档使用以下 fresh checks：

| surface | fresh result | 读法 |
| --- | --- | --- |
| `git status --short --branch` | OPL transition lanes 已吸收到本地 `main`，fresh check 为 `main...origin/main [ahead 3]`；工作区可能存在另行维护的 App split 文档改动，发布/提交口径以当前 `git status` 为准；MAG/RCA/MAS 证据来自各自 active manifest / runtime surface | 当前 gap 文档基于 fresh read model 和 runtime ledger；不把未提交文档当作已发布机器真相。 |
| `./bin/opl agents list --json` | `aligned_count=3`、`missing_count=0`、`drift_detected_count=0`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`、`production_closure_gap_count=12`、`provider_temporal_residency_gap_status=closed_by_fresh_proven_proof` | 三仓 descriptor 已对齐；MAS/MAG/RCA 均已有 repo-source anchor evidence；provider residency gap 已由 fresh Temporal proof 关闭；剩余 production closure 仍未闭合。 |
| `./bin/opl agents descriptors --json` | `descriptor_surfaces_resolved_count=3`、`memory_descriptor_resolved_count=3`、`stage_control_plane_resolved_count=3`、`action_catalog_resolved_count=3`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`、`production_closure_gap_count=12`、`provider_temporal_residency_gap_status=closed_by_fresh_proven_proof` | 统一 domain-agent descriptor 总入口已可用；它聚合 refs、readiness、parity、authority boundary 和 provider closure evidence，不承载 domain truth 或 memory body。 |
| `node dist/cli.js agents descriptors --json` | `descriptor_surfaces_resolved_count=3`、`memory_descriptor_resolved_count=3`、`stage_control_plane_resolved_count=3`、`action_catalog_resolved_count=3`、`physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0` | release/dist CLI 已与 canonical `./bin/opl` descriptor 入口同步；后续作为持续验证项保留。 |
| `./bin/opl stages list --json` | `resolved_planes_count=3`、`stages_count=18`；MAS/MAG/RCA 各 6 个 stage | Stage control plane 已成为 OPL 可读 surface。 |
| `./bin/opl domain-memory list --json` | `resolved_memory_descriptor_count=3`、`missing_memory_descriptor_count=0`；`runtime_receipt_evidence.closeout_count=18`、`consumed_memory_ref_count=1`、`writeback_receipt_ref_count=2`、`rejected_write_count=15`、`opl_writes_memory_body=false`；MAS closeout refs 当前为 6 条，包含 `artifacts/runtime/opl_family_sidecar/dispatch_receipts/e97b1c8cd25f3a019f90.json` | Memory locator/descriptor 已对齐；OPL 现在只读投影 stage attempt closeout ledger 中的 memory receipt refs。MAG 已有 accepted/rejected memory receipt refs 进入 OPL read model；MAS 当前主要是 rejected writes 与 owner stable blocker receipt refs；RCA 有 closeout refs。真实 retrieval/writeback/body migration 仍由 domain owner 后续闭合。 |
| `./bin/opl domain-memory inspect --domain mag --json` | `runtime_receipt_evidence.status=runtime_closeout_refs_observed`、`closeout_count=11`、`consumed_memory_ref_count=1`、`writeback_receipt_ref_count=2`、`rejected_write_count=5`、`opl_writes_memory_body=false` | MAG grant strategy memory 的 accepted/rejected receipt refs 已可从 OPL read model 钻取；OPL 仍只投影 refs，不写 memory body，不接受/拒绝 writeback，也不 apply memory writeback。 |
| `./bin/opl family-runtime status --json` | 默认 `configured_provider=local_sqlite`、`provider_ready=true`、`full_online_ready=false`、`durable_online_ready=false`；同一 runtime ledger 中 queue `total=17` / `succeeded=17`，stage attempts `total=18` / `completed=18` | 默认本机 provider 是 dev/offline baseline，不是 production online readiness；attempt ledger 是持久 OPL state，可被 workbench/closeout/domain-memory read model 消费。 |
| `./bin/opl family-runtime service status --provider temporal --json` | `service_status=running`、`address=127.0.0.1:7233`、`server_reachable=true`、`managed_service_pid=71440` | 本机 managed Temporal service 当前可达；这是显式 Temporal provider 证据。 |
| `./bin/opl family-runtime worker status --provider temporal --json` | `readiness_status=ready`、`worker_ready=true`、`server_reachable=true`、`managed_worker_pid=73456`、`task_queue=opl-stage-attempts` | 本机 managed Temporal worker 当前 ready；worker 仍只拥有 transport/lifecycle authority，不拥有 domain truth。 |
| `OPL_FAMILY_RUNTIME_PROVIDER=temporal ./bin/opl family-runtime status --json` | `configured_provider=temporal`、`selected_provider=temporal`、`provider_ready=true`、`full_online_ready=true`、`durable_online_ready=true`、`adapter_mode=managed_temporal_provider_ready` | 显式 production provider view ready；默认 `local_sqlite` view 与显式 Temporal view 必须分开读。 |
| `./bin/opl family-runtime residency proof --provider temporal --production --json` | `closeout_status=production_residency_proven`，checks 全 true：service reachable、worker ready、completed attempt、restart re-query、signal history、typed closeout required、missing closeout blocks completion、retry/dead-letter boundary、domain truth boundary；runtime event ledger 写入 `temporal_residency_proof` receipt，并返回 `persisted_proof_ref=/Users/gaofeng/Library/Application Support/OPL/state/family-runtime/proofs/latest-temporal-production-proof.json` | Temporal production proof 已通过、已持久化，并可被后续 closeout gate 与 MAS hydrate 读取；剩余 production closure 是真实 domain owner chain、长时 SLO、memory/lifecycle apply 和 artifact mutation receipt。 |
| `node --test tests/src/cli/cases/family-runtime.test.ts tests/src/cli/cases/family-runtime-provider-hosted-attempts.test.ts tests/src/cli/cases/family-runtime-stage-attempts.test.ts` | focused suite passed | OPL queue dispatch 对 MAS `paper_autonomy/guarded-apply` 已有 task-bound provider-backed stage attempt 证据；显式声明 provider-hosted / controlled 的 MAG/RCA sidecar task 也会建立 task-bound attempt，并把 MAS typed blocker、MAG product sidecar receipt、RCA no-regression evidence ref 进入 OPL typed closeout ledger；测试不证明真实 MAS owner apply receipt、MAG grant-stage owner receipt 或 RCA visual-stage soak 已闭合。 |
| `node --test tests/src/cli/cases/runtime-tray-stage-attempt-workbench.test.ts tests/src/cli/cases/workspace-domain.production-closeout.test.ts` | focused suites passed；runtime-tray suite 现在覆盖 route-impact no-regression evidence、transition bridge owner receipt / no-regression evidence / typed blocker refs-only projection、lifecycle guarded-apply restore/domain receipt refs、domain direct skill route、transition bridge evidence 与 lifecycle primitives App drilldown route 进入 operator workbench refs；production-closeout fixture 覆盖 attempt-ledger lifecycle restore/domain receipt refs、transition bridge evidence summary / generic projection / domain breakdown、MAG `physical_skeleton_follow_through.legacy_active_path_policy` tombstone evidence | Runtime snapshot / production closeout 已能把 provider proof、stage attempt operator item、typed closeout memory refs、controlled apply refs、route-impact no-regression evidence refs、transition bridge owner/no-regression/blocker refs、lifecycle guarded-apply restore/domain receipt refs、domain direct skill route refs、workspace/source intake refs、memory locator refs、artifact locator / restore proof、transition bridge / lifecycle primitives drilldown、package/export lifecycle refs、route/review/quality/SLO refs、provider proof freshness 和 MAG legacy active-path tombstone evidence 投影给 App / operator / closeout gate；这些仍是 ref-only read model，不是 domain ready verdict、source readiness、memory body apply、package/export verdict、direct skill execution 或 artifact mutation receipt。 |
| `./bin/opl family-runtime intake --domain medautoscience --source binding-hydrate-auto-proof --json` | MAS export command 从 active workspace binding 派生：`command_source=workspace_binding`、`command_cwd=/Users/gaofeng/workspace/med-autoscience`、profile 为 `/Users/gaofeng/workspace/Yang/NF-PitNET/ops/medautoscience/profiles/nfpitnet.workspace.toml`，并自动带上 latest persisted proof；`exported_count=4`，当前重跑为 `idempotent_noop_count=4` | MAS hydrate 已脱离手工 env / `/tmp` proof；OPL 只读取 binding locator 和 proof ref，不写 MAS truth。 |
| MAS guarded-apply live dispatch receipts | DM002 早期 live dispatch 曾返回 `typed_blocker` / `blocked_no_mas_owner_apply_receipt`；MAS owner blocker projector 落地后，同一 task `frt_54187384a4fa403abda12ce5` 按 source fingerprint `a0cdbf341fae623d` requeue，OPL stage attempt `sat_df08b839ce33a33ee92974f2` ingest `artifacts/runtime/opl_family_sidecar/dispatch_receipts/e97b1c8cd25f3a019f90.json`，route impact 为 `mas_owner_apply_receipt_observed` / `mas_owner_receipt_present`，`writes_performed=false`，`forbidden_write_guard_result=fail_closed_no_forbidden_writes` | OPL/MAS live bridge 已能跟随 domain owner evidence 刷新并消费 MAS-owned `controller_decisions/latest.json` stable blocker receipt；这是 owner receipt observed，不是论文推进完成、publication quality verdict 或 submission readiness。 |
| MAG / RCA live sidecar attempts | MAG attempt `sat_3c210fbc86393af8d3a0e90c` / task `frt_4c4945a0295e8e2c0c6dba6a` 已完成，closeout refs 指向 MAG sidecar receipt；RCA attempt `sat_8623e55a341caf9672ed8ab2` / task `frt_0a2e1877774cecc94f7f2b0e` 已完成，closeout refs 指向 RCA no-regression evidence file | 两条都是 OPL task-bound typed closeout 证据；MAG 仍不是 grant quality / owner apply success，RCA 仍不是 long visual soak 或 artifact-producing owner receipt。 |
| `./bin/opl framework production-closeout --json` | `status=functional_closure_ready_for_live_soak`、`stage_attempt_evidence.ledger_attempt_count=18`、`closeout_packet_count=18`、`domain_memory.runtime_receipt_evidence.closeout_count=18`、`provider_continuous_proof.continuous_proof_status=all_observed_proofs_proven` | 默认 closeout 预算已能解析当前真实三仓 manifest；排除真实 long soak 后，OPL functional closeout gate 已进入 live-soak-ready 状态；下一阶段是 live soak / owner-chain / memory body / artifact lifecycle evidence gate。 |
| `OPL_DOMAIN_MANIFEST_COMMAND_TIMEOUT_MS=100 ./bin/opl framework production-closeout --json` with stalled manifest fixture | `medautoscience=command_timeout`，typed blocker 为 `domain_manifest_timeout` 且给出 `OPL_DOMAIN_MANIFEST_COMMAND_TIMEOUT_MS=10000 opl framework production-closeout` | 显式短预算或真正 stalled manifest 仍 fail-closed；MAS timeout 不是 MAS 论文 truth、质量或 owner apply 失败。 |
| `./bin/opl runtime snapshot --json` | `running_items=0`、`attention_items=21`、`recent_items=1`、`runtime_health.summary=0 个运行中，18 个处理中项目，0 个后台恢复项，3 个用户处理项，1 个近期项目。`、`stage_attempt_workbench.total=18`、`by_status.completed=18`、`by_domain={medautogrant:11, medautoscience:6, redcube:1}`、`attention_count=11`、`provider_continuous_proof.latest_closeout_status=production_residency_proven`；workbench 顶层实际输出 `opl_workspace_source_intake_projection`、`opl_memory_locator_index_projection`、`opl_artifact_gallery_projection`、`opl_package_export_lifecycle_projection`、`opl_route_decision_graph_projection`、`opl_review_repair_queue_projection`、`opl_quality_readiness_projection`、`opl_observability_slo_projection`、`opl_operator_action_routing_projection`；snapshot 顶层实际输出 `opl_native_helper_execution_envelope_projection` 与 `opl_domain_projection_ingestion_projection` | Workbench 已展示 18 条 completed stage attempt 与 provider proof，其中 MAS 有 6 条 guarded-apply closeout refs，最新 DM002 attempt 指向 MAS owner stable blocker receipt；MAG 有多条 controlled / lifecycle / grant-stage closeout refs，RCA 有 visual no-regression closeout refs。generic projections 都是 locator/ref/receipt/handoff/action-route shell，native helper envelope 只读已有 native index / helper receipt metadata，domain projection ingestion 只汇总 normalized domain manifest 中的 projection refs / surface kind / source refs / owner metadata，provider proof item 只代表 provider residency，stage attempt item 只代表 attempt/control/receipt/action route refs，不代表 domain ready；OPL 没有新增本地 daemon，也没有替 domain 执行 action、domain projection builder 或 helper mutation。 |
| `npm run build` | exit 0；`tsc -p tsconfig.build.json` passed | release/dist descriptor CLI 验证前已重新构建。 |
| `npm run typecheck` | exit 0；`tsc --noEmit` passed | TypeScript 类型面通过。 |
| `node --test --experimental-strip-types tests/src/stale-compat-retirement-guard.test.ts` | exit 0；`9` tests passed、`0` failed | Guard 明确防止 operating-governance 参考重新声称退役 surface matrix JSON 是 active contract，防止 examples/domain-admission 参考把 `domain_gateway` 保留成 active route，防止 examples-corpora 重新链接已退役且当前不存在的 `examples/opl-framework/*.json` artifacts，防止 domain-admission 重新发布不存在的 `candidate-domain-backlog.json` 合同，也防止人读文档把 canonical `hermes_agent` executor adapter 写成 unsupported / retired、排除出 canonical backend，或把 Hermes-Agent 整体写成只剩 migration/proof/historical context；同时正向断言核心 executor contract 与人读文档必须保留 `canonical_executor_backends = [codex_cli, hermes_agent, claude_code]` 和非默认非等价边界；本轮还新增 active machine surface guard，防止旧 Hermes provider / Gateway compatibility alias 字段回流，并新增 convergence-governance snapshot guard，防止 2026-04 `Gateway / Federation` 快照重新被读成当前 truth。 |
| `npm run test:fast` | exit 0；`190` tests passed、`0` failed | Fast lane 覆盖 family runtime、provider-hosted attempts、production closeout、runtime snapshot/workbench、route-impact no-regression evidence operator projection、lifecycle guarded-apply receipt refs operator projection、lifecycle primitives App drilldown route、domain direct skill route operator projection、attempt-ledger lifecycle refs closeout projection、descriptor CLI、domain-memory runtime receipt projection、native smoke、quality details、repo hygiene、family structure advisory report alignment、stale compatibility retirement guard、active machine surface no-compat-alias guard、examples/domain-admission/convergence-governance retirement guard 和 executor adapter guard。 |
| `./scripts/verify.sh structure` | exit 0；line budget / explicit rules passed；Sentrux baseline drift 以 advisory 输出，`new complex functions=0`、`worsened complex functions=0`、rules findings `0` | 结构 gate 的阻断项通过；Sentrux quality drift / god-file drift 继续作为后续治理输入，不把它误写成功能阻断。 |

## 总体差距矩阵

| 维度 | 理想情况 | 当前实际 | 差距 | 要完善的部分 |
| --- | --- | --- | --- | --- |
| OPL Framework | 完整生产级智能体开发与运行框架，支撑长期在线、stage attempt、状态、记忆、文件生命周期、恢复和审计 | 控制面骨架、shared contracts、Temporal provider code、local queue/attempt ledger、native helper、runtime snapshot、stage/domain-memory discovery、统一 domain-agent descriptor、owner receipt/ref surface、closeout gate、MAS/MAG/RCA task-bound stage attempt ledger、typed closeout memory-ref summary、attempt-ledger lifecycle refs closeout summary、domain-memory runtime receipt read model、manifest command timeout fail-closed、provider continuous proof ledger/projection、release/dist descriptor CLI 同步、显式 Temporal production proof 和 `functional_closure_ready_for_live_soak` closeout 已落地 | 真实 domain owner receipt chain、长时 Codex/domain activity soak、memory/lifecycle apply 和 legacy physical delete 未闭合 | 保持 production Temporal proof 持续验证，优先完成 MAS 真实 paper-line owner chain、owner receipt envelope live proof，并把 `./bin/opl` / release CLI 输出一致性纳入持续验证 |
| OPL generic primitives | OPL 提供跨 domain 可复用的 runtime transport、queue、human gate transport、workspace/source intake shell、memory locator/index、artifact/package lifecycle、restore/retention、artifact gallery、review/repair transport、native-helper execution envelope、domain projection ingestion、workbench shell、route/decision graph renderer、quality/readiness projection shell 和 observability transport | 部分已落地为 provider/attempt ledger、runtime snapshot、stage-attempt workbench、workspace/source intake projection、memory locator/index projection、artifact gallery projection、package/export lifecycle projection、route-decision graph projection、review-repair queue projection、quality/readiness projection、observability/SLO repair-command projection、operator action routing projection、native helper execution envelope projection、domain projection ingestion projection、domain-memory descriptor、artifact locator refs、closeout gate、native helper state cache 与 App workbench projection | 这些能力仍偏 read-model / receipt-ref / typed-blocker / action-route / projection-ref 层；还缺跨三仓真实 live apply、memory body/writeback transport 泛化、artifact/package lifecycle apply、App productization 和长时 operator SLO；当前 generic read-model shells 不替代 domain owner receipt、quality verdict、source readiness、memory accept/reject、native helper domain mutation、domain projection builder execution 或 export verdict | 把剩余 MAS/MAG/RCA generic-layer 候选继续上收到 OPL：App productization、operator SLO 和 live apply receipt；domain 只回传 authority refs、domain projection refs、route/review/quality/export refs、receipt 和 typed blocker |
| Stage-led 模型 | 每个 domain stage 有输入、prompt、skill、knowledge、tool、quality gate、handoff 与 closeout | 三仓 18 个 stage plane 可读，descriptor ready；当前 OPL ledger 有 18 条 completed task-bound stage attempt 和 accepted typed closeout，覆盖 MAG/MAS/RCA | Stage 仍主要证明 OPL attempt/closeout/ref projection；真实 provider-hosted live domain activity 和连续 owner chain 证据不足 | 用 MAS 真实 paper line、MAG grant controlled attempt、RCA visual controlled attempt 跑出 owner receipt / typed blocker / no-regression evidence |
| Agent executor | `Codex CLI` 为默认最小执行器，非默认 executor 必须由当前 canonical registry 显式允许并可审计 | 默认 Codex 口径已收口；`hermes_agent` 与 `claude_code` 保留为显式非默认 executor adapter；Hermes/Gateway 旧 provider、readiness、Gateway cron、compat alias 已退出 active/default path | 非默认 executor 不承诺行为等价；`hermes_agent` 必须证明 full agent loop、tool events 和独立 receipt，且不得 silent fallback 到 Codex；真实长时 Codex runner production soak 未完成 | 继续保留 non-equivalence notice、receipt/audit/fail-closed gate；优先证明 Codex CLI long-running activity、heartbeat、typed closeout 和 domain receipt，同时把 Hermes/Gateway 旧 provider/compat 面继续按 no-active-caller 清理 |
| Domain skeleton | MAS/MAG/RCA 使用统一 `agent/ contracts/ runtime/ docs/` repo-source 边界，runtime artifacts 不进开发仓 | 三仓 manifest/descriptor 声明 aligned；artifact locator surface declared；MAS/MAG/RCA 均已有 repo-source anchor evidence | 当前是 `physical_skeleton_evidence_observed_count=3`、`physical_skeleton_audit_pending_count=0`；破坏性物理目录重组仍未完成 | 下一步做 path compatibility audit、direct skill parity、OPL-hosted parity、restore/provenance proof、no-forbidden artifact proof，并逐仓物理迁移 |
| Domain memory | OPL 只持 locator / refs / receipts；domain 持有正文、接受/拒绝、route/quality truth | 三仓 memory descriptor resolved；OPL `domain-memory` read model 已从 stage attempt closeout ledger 投影 `closeout_count=18`、`consumed_memory_ref_count=1`、`writeback_receipt_ref_count=2`、`rejected_write_count=15`；MAS 有 publication-route memory workspace apply closure、body-free receipt inventory、operator grouping、stale/deprecated review summary 和 owner stable blocker receipt ref；MAG accepted/rejected receipt refs 已可钻取；RCA 有 closeout refs | Fresh OPL read model 中 retrieval/writeback/body migration 仍不是 OPL-applied success；真实 memory body apply 和跨 domain soak 仍不足 | 三仓产出真实 consumed/writeback accepted/rejected receipt；OPL/App 做 ref-only grouping；memory body 始终留在 workspace/runtime owner |
| File lifecycle | OPL 持 workspace/artifact locator、retention、cleanup、restore proof、migration ledger；产物在 workspace/runtime root | OPL lifecycle schema / locator、guarded apply proof、artifact gallery lifecycle restore/domain receipt refs 和 domain proof surface 已存在；MAG/RCA/MAS 都声明 guarded apply proof 或 locator | 跨三仓真实 workspace cleanup/restore/retention guarded apply 仍缺 live artifact mutation evidence | 用 domain-owned receipt 或 typed blocker 证明 artifact mutation；OPL 只写 framework-owned ledger/locator，App/gallery 只展示 refs |
| App / Workbench | 用户可以看见 Agent、workspace、stage、progress、route/decision map、review/repair queue、artifact gallery、human gate、attention queue，并能按 owner 路由 action | OPL App 是 OPL fork of AionUI；runtime snapshot 有 attention queue、stage-attempt workbench、stage-attempt operator item、provider continuous proof operator item、operator action routing shell 和 native-helper execution envelope；operator action routing 已覆盖 OPL CLI、App surface、provider signal、domain sidecar 与 domain direct skill route；Aion workbench 已有五轴 visibility；MAS 已有 domain-owned route map / decision trail projection 可供 OPL 消费，RCA 理想态要求展示 visual review / repair / artifact handoff refs | 当前 fresh snapshot 无 active domain attempt；App 仍需真实生产运行状态下的 domain receipt / artifact mutation receipt drilldown、route graph productization、review/repair queue、artifact gallery/handoff drilldown 与 UI polish | 对接真实 domain receipt；按 domain/stage/route/review/memory/artifact refs 分组；App 提供通用 route graph、review queue、artifact gallery 和 action routing shell，但不推断 domain route、quality 或 export verdict，也不直接执行 direct skill |
| Legacy retirement | Hermes-first/Gateway/frontdoor/local-manager/MDS-default 退出 active/default path；`hermes_agent` 保留为 canonical 显式非默认 executor adapter/backend | 默认语义已退役，三仓 legacy tombstone / no-active evidence 已能被 OPL closeout 消费；operating-governance / examples-corpora / docs portfolio 已不再把退役 surface matrix JSON 或 `domain_gateway` literal 写成 active compatibility contract；operating-record catalog 已不再指向不存在的 `operating-record-catalog.json` 机器合同；examples-corpora 已不再把不存在的 `examples/opl-framework/*.json` 作为当前可点击 machine artifacts；candidate-domain backlog 已不再指向不存在的 `candidate-domain-backlog.json` 合同，当前候选可见性回到 `task-topology.json` / `workstreams.json`；convergence-governance 的 2026-04 `Gateway / Federation` 快照已显式降级为 historical snapshot，不再作为 current topology 或 compatibility surface；stale-compat guard 防止该类口径回流 | 物理残留仍需 full no-active-caller、replacement parity 和 provenance 保留证据后清理；历史语料仍需继续按 inbound link / provenance 审计逐项迁移或归档 | 逐项删除或迁入 history/tombstone；保留项必须标明 diagnostic / fixture / provenance / negative guard，不能作为兼容接口；同时保持 `hermes_agent` executor adapter 的 canonical / explicit / fail-closed 口径 |

## OPL Framework 当前差距

### 已经成立

- 产品认知固定为 `OPL Framework -> One Person Lab App -> Foundry Agents`。
- `OPL` 已持有 family-level shared contracts、action catalog、stage control plane、runtime supervision、persistence/lifecycle/owner-route、domain memory ref/writeback 和 standard domain-agent skeleton contract。
- Runtime Manager 是 provider-backed 产品控制面，不是 domain runtime kernel。
- Native helper 已可用，本机 runtime manager 读模型显示 helper 来自 state cache，native state index freshness 为 fresh。
- `runtime snapshot` 已能投影 attention items、stage-attempt workbench、stage-attempt operator item、provider continuous proof operator item、native-helper execution envelope、daemon policy 和 non-goals；stage-attempt operator item 只展示 attempt/control/receipt/action route refs，fresh snapshot 显示 `local_daemon_added=false`。
- `stage_attempt_workbench` 已新增一组 OPL-owned generic read-model shells：`opl_workspace_source_intake_projection`、`opl_memory_locator_index_projection`、`opl_artifact_gallery_projection`、`opl_package_export_lifecycle_projection`、`opl_route_decision_graph_projection`、`opl_review_repair_queue_projection`、`opl_quality_readiness_projection`、`opl_observability_slo_projection`、`opl_operator_action_routing_projection` 与 `opl_transition_bridge_evidence_projection`。`framework production-closeout` 现在也把同一个 transition bridge evidence projection 纳入 `stage_attempt_evidence` summary、generic projections 和 domain breakdown。`runtime snapshot` 顶层还新增 `opl_domain_projection_ingestion_projection`，只从 normalized domain manifest 读取 domain-owned projection ref、surface kind、owner 和 source refs。OPL 只把 stage attempt、workspace locator、source fingerprint、domain-owned route/projection refs、next owner、closeout/memory/artifact/package/export refs、transition bridge owner/no-regression/blocker refs、human gate、dead-letter、rejected write、quality/readiness refs、SLO refs、repair command refs、blocker refs 与明确 owner 的 action route refs 投影成通用 workbench / snapshot / closeout shell；source readiness、memory body、artifact content、package readiness、export verdict、route choice、repair decision、review verdict、quality verdict、SLO verdict、transition ready verdict、domain projection builder execution、domain action execution 和 truth 仍由 domain projection / owner 持有。

### 主要差距

- 默认 `family-runtime status` 当前选中 `local_sqlite`，只能代表 dev/CI/offline ledger ready；显式 Temporal provider view 已显示本机 managed service / worker ready，并且 production proof 已返回 `production_residency_proven`。proof receipt 现在进入 runtime event ledger，production closeout 与 runtime snapshot 都能读出 `provider_continuous_proof`，并把它投影成 operator 可发现项；stale proof 会成为 `temporal_provider_proof_freshness_not_current` typed blocker。这关闭的是 provider readiness/proof 可见性缺口，不关闭真实 domain owner chain。
- OPL queue 已能为 MAS `paper_autonomy/guarded-apply` 自动建立 task-bound provider-backed stage attempt，并在 MAS sidecar 返回 typed blocker 或 owner stable blocker receipt 时写入 typed closeout ledger；显式声明 provider-hosted / controlled 的 MAG/RCA sidecar task 也已具备 task-bound attempt 与 typed closeout live proof；当前 persisted stage attempt ledger 为 `total=18`、`completed=18`，其中 MAG 11、MAS 6、RCA 1。这证明 OPL task-bound attempt / closeout refs 可跨三仓运转，并已消费 DM002 owner stable blocker receipt，但仍不能证明真实长时间 online owner-chain execution 或论文推进完成。
- OPL 已新增 domain-neutral family transition runner 机器面：`contracts/opl-framework/family-transition-runner-contract.json`、`src/family-transition-runner.ts`、`src/family-transition-oracle-ingestion.ts`、`src/family-transition-visual-ingestion.ts`、`src/family-runtime-domain-intake.ts`、`src/family-runtime-provider-hosted-attempts.ts`、`tests/src/family-transition-runner.test.ts`、`tests/src/cli/cases/family-runtime-transition-bridge.test.ts` 和 package export `./family-transition-runner`。它把 transition schema、guard 匹配、fail-closed、dead-letter、receipt/projection envelope、matrix runner、matrix-result hydrate 和 provider-hosted transition attempt bridge 放在 OPL；MAS/MAG/RCA 继续持有各自 domain transition table、guard 定义、oracle fixtures、质量 verdict、artifact authority 和 owner receipt。当前测试使用 MAS-like fixture 锁定 `bundle_stage_ready`、`bundle_stage_blocked`、`publishability_gate_blocked` 相邻状态不会误转场；`domain manifests` / `agents descriptor` 还会读取实际 `family_transition_spec` + `family_transition_matrix_cases` 并执行 matrix summary，或在 descriptor-only 时显示 `refresh_required` 和 MAS `study-state-matrix` spec/cases locator；测试已锁定 sidecar export / product-entry manifest 只挂 descriptor，不把 descriptor-only sidecar 当完整 spec。MAG-like `mag_grant_transition_oracle` fixture 证明 OPL 能把 MAG-owned transition table / oracle fixtures 适配成通用 matrix cases；RCA-like `visual_transition_spec` fixture 证明 OPL 能把 RCA-owned visual transition table / guard refs / oracle fixture contract 适配成通用 matrix cases；family runtime focused test 证明 `family_transition_matrix_result` 可被 OPL intake 派生成 `family_transition/domain_tick` task，task-bound stage attempt locator 和 runtime workbench 会 refs-only 投影 owner receipt refs、no-regression evidence refs 和 typed blocker refs，并在显式未知 domain 时 fail-closed。以上证明通用 runner / ingestion / transport bridge / operator projection 边界，不证明 MAS 三篇论文已经被推进，也不证明 MAG grant-stage 或 RCA visual-stage production soak 已完成。
- `./bin/opl agents descriptors` 已成为可调用的统一只读入口；`node dist/cli.js agents descriptors --json` 已验证同步；后续是继续把 release/dist 构建输出、App consumption 和专题入口 drilldown 放进持续验证。
- Production closure 的 OPL functional gate 当前默认返回 `functional_closure_ready_for_live_soak`、`typed_blocker_count=0`；显式短预算或真正 stalled manifest 仍会 fail-closed 成 `domain_manifest_timeout`。剩余缺口是 live evidence：真实 provider-hosted guarded apply owner receipt chain、domain memory body / writeback apply、workspace artifact lifecycle apply 和旧面物理删除。当前 live run 证明了 typed blocker / receipt-ref / no-regression evidence 路径，不证明 owner apply receipt 或 domain quality 成功。
- MAS/MAG/RCA 理想目标态暴露出一批应由 OPL 上收的 generic primitives：workspace/source intake shell、memory locator/index、artifact/package lifecycle、restore/retention、artifact gallery/handoff shell、route/decision graph renderer、review/repair transport、native helper catalog/envelope、domain projection ingestion、workbench shell、attention queue、quality/readiness projection、transition bridge evidence projection、observability/SLO 和 repair command projection。OPL 当前已经把这些候选中的大部分落成 stage attempt / attempt query / workbench / runtime snapshot / production closeout 的 generic read-model shell：workspace/source intake、memory locator/index、artifact gallery / handoff、package/export lifecycle、route/decision graph、review/repair queue、quality/readiness、transition bridge evidence、observability/SLO repair command、operator action routing、native helper execution envelope、domain projection ingestion 和 provider SLO repair-loop drilldown。剩余不是再让 MAS/MAG/RCA 各自重复实现通用壳，而是补 App productization、周期性 operator SLO 执行证据、真实 live apply receipt、domain owner receipt 和 artifact/memory lifecycle evidence。
- OPL 已有跨 domain 共用的 transition runner 机器面，并已能 ingest MAG `grant_transition_oracle` 到通用 spec / matrix runner、ingest RCA `visual_transition_spec` 到通用 spec / matrix runner，还能把 sidecar/export 返回的 `family_transition_matrix_result` hydrate 成 provider-hosted `family_transition/domain_tick` task，并在 provider-hosted transition attempt locator 中 refs-only 对账 owner receipt、typed blocker 与 no-regression evidence。MAS `study-state-matrix` 现在还会把 package closure / completed turn closeout 和 newer default executor execution receipt 投入 `completion_receipt_consumption`，并把该 context 带入 MAS-owned family transition spec / matrix cases，防止已完成或更新 owner execution 被旧 route redrive 覆盖；这仍是 MAS-owned transition truth，不是 OPL 解释 publication verdict。MAG/RCA 剩余缺口从“不能进入 OPL provider bridge”收窄为“真实 owner receipt、typed blocker、no-regression evidence 与 long-soak 规模化返回不足”。当前分工是 OPL 提供 transition schema、matrix runner、fail-closed/dead-letter/human-gate receipt envelope、oracle/spec ingestion、queue/task hydrate、provider attempt bridge 和 projection transport，domain repo 提供 transition table、guard、oracle fixture、typed blocker、owner action 和 owner receipt；不能让 OPL 自己推断 domain ready verdict。
- 当前 generic read-model shell 已覆盖“怎么看”，但“怎么被 App 使用”和“怎么被周期性执行”仍是 OPL 层缺口。App 还需要把 route graph、review queue、artifact gallery、package/export lifecycle、memory refs、quality/readiness、SLO 和 action routing 做成人用 drilldown；provider proof 还需要自动/受监督 cadence 与真实 repair execution receipt；lifecycle 还需要 OPL-owned locator/retention/restore ledger 与 domain-owned artifact mutation receipt 的持续对账。

### 需要完善

1. Provider SLO：Temporal service/worker/proof 已纳入 runtime ledger、closeout projection、freshness blocker、`operator_slo_repair_loop` drilldown、supervised production-proof execution receipt 和稳定 cadence action envelope；supervised cadence execution 已有 `skipped` / `cadence_current` receipt `evt_ef27fb14-d949-4651-9fc4-03640fd1899b`。继续补自动化长期调度、restart/re-query、query/signal/history、真实 repair execution receipt 和 domain owner chain 的长时稳定性证据。
2. Stage execution：让 provider-backed attempt 能真实启动 Codex/domain activity，留下 heartbeat、checkpoint、typed closeout、owner receipt 或 typed blocker。
3. Unified read model：保持 `./bin/opl agents descriptors` 作为 domain-agent descriptor 总入口，并让 release/dist 构建、App consumption 和专题 drilldown 持续同步。
4. Generic primitives：已落地的 read-model shell、family transition runner、generic `family_transition_spec` manifest ingestion、MAS `study-state-matrix` descriptor locator、MAG transition oracle ingestion、RCA visual transition spec ingestion、transition matrix result provider bridge 和 `transition_bridge_evidence` operator / closeout projection 继续由 OPL/App 产品化、domain spec 和真实 receipt 驱动；domain agent 只提供 authority refs、projection refs、route nodes/edges、transition table、guard/oracle fixtures、review/quality/export refs、receipt 和 typed blocker。缺口集中在真实 live apply、artifact/memory lifecycle evidence、operator SLO、App drilldown、transition owner receipt / no-regression evidence 与 long soak，而不是重新设计一套通用壳。
5. Transition tables：保持 OPL 只运行 domain-declared transition table；MAS descriptor-only 情况已经对齐为刷新 `study-state-matrix` 后读取 `domain_transition_table.family_transition_spec` 与 `domain_transition_table.family_transition_matrix_cases`，sidecar/export 和 product-entry manifest 只提供 descriptor locator；MAS 已把 `completion_receipt_consumption` 纳入 transition row、family spec receipt 和 matrix case context/expected oracle；MAG `grant_transition_oracle`、RCA `visual_transition_spec` 和 generic `family_transition_spec` / matrix cases 已可被 OPL descriptor / matrix runner 只读消费；sidecar/export 返回的 `family_transition_matrix_result` 已可进入 OPL provider-hosted transition bridge，并在 attempt locator、runtime workbench 与 operator item 中 refs-only 投影 owner receipt / typed blocker / no-regression evidence；MAS/MAG/RCA 后续还需用真实 provider-hosted transition attempt 规模化返回 owner receipt、typed blocker 或 no-regression evidence，不再把分散状态判断散落在 controller、priority、authorization 和人读文档里。
6. Closeout gate：保持 owner receipt / typed blocker / no-regression evidence 跨仓生产闭环门禁；当前 operator closeout 已无功能性 typed blocker，后续仍需真实 domain owner receipt / memory apply evidence。
7. Legacy cleanup：`physical_skeleton_follow_through_gate` 已把 full no-active-caller、replacement parity、provenance proof、history/tombstone 和 no-retained-legacy-entry 条件机器化；下一步由受控清理逐项删除 active-path residue 或迁入 history/tombstone，OPL gate 本身不执行删除。

## MAS 当前差距

### 已经成立

- MAS 是活跃 `Research Foundry` / 医学研究 Foundry Agent，也是 OPL-compatible package。
- MAS 已完成 MDS default dependency 退役；MDS 只保留 backend audit、source provenance、historical fixture、explicit archive import、upstream intake 或 parity oracle reference。
- MAS product-entry manifest 已暴露 product positioning、family action catalog、family stage control plane、domain memory descriptor、provider runtime residency read model、guarded soak read model、legacy residue audit 和 standard domain-agent skeleton descriptor。
- Stage-Led Autonomy 已是 repo surface：stage knowledge packet、stage memory closeout packet、memory write router receipt、stage recall index、typed closeout routing、Progress/Portal visibility 和 route materialization guard 已落地。
- MAS 真实三篇 paper line 已有 read-only OPL-ingestable proof：DM002 -> `ai_reviewer_re_eval`，DM003 / Obesity -> `artifact_delta`，且 `writes_performed=false`。
- Publication-route memory 已从 descriptor/seed library 推进到 MAS-owned body-free inventory、OPL/Aion receipt inventory、按 workspace / stage / route family / status 的 operator grouping 和 stale/deprecated review summary；OPL 仍只消费 refs、freshness、分组和 review metadata。
- MAS runtime owner surface 已补齐 closed controller work-unit authorization 清理、runtime turn closeout artifact delta freshness、supervisor-only live quality repair owner routing、AI reviewer currentness guard 和 runtime-read manual finish guard，当前属于 repo-level owner/projection follow-through。
- MAS `study-state-matrix` 已把 `completion_receipt_consumption` 纳入 domain transition row、family transition spec receipt 和 matrix case oracle：DM002 package closure / completed turn closeout 会投成 `completion_receipt_consumed`，DM003 newer default executor execution receipt 会 supersede stale blocked closeout；这关闭的是旧 work unit redrive / stale closeout priority 的 MAS-owned matrix 缺口，不是 publication closure。

### 主要差距

- MAS 仍不能声明 OPL production-hosted paper automation 已闭合。
- MAS 理想形态已经把自身收窄为医学研究 `Domain Knowledge / Authority Pack`：医学 stage pack、研究路线知识、publication-route memory policy、AI reviewer / quality rubric、artifact authority contract、owner receipt schema 和 domain projection builder。与此相对，generic runtime、queue、memory locator、artifact lifecycle、restore/retention、workbench shell、route visualization 和 observability primitive 应尽量由 OPL 提供；当前 OPL 已有通用 read-model shell，MAS 仍保留 route decision、quality verdict、artifact mutation permission、memory body/writeback decision 和 owner receipt。
- OPL 侧 task-bound guarded-apply stage attempt bridge 已有 focused test 证据；DM002 live guarded-apply dispatch 已从 `blocked_no_mas_owner_apply_receipt` 推进到 `mas_owner_apply_receipt_observed`，并进入 OPL typed closeout ledger；当前 owner receipt 是 `medical_paper_readiness_missing` 的 stable blocker，不是 paper closure。真实 provider-hosted live guarded apply soak 尚未跑出连续 progress delta / human gate / stop-loss chain。
- Human gate / resume 进入 MAS owner route 的运行证明仍不足。
- Owner receipt、AI reviewer receipt、human gate receipt、memory writeback receipt 和更多 package closure 变体仍需继续纳入 MAS transition matrix 并返回真实 provider-hosted owner evidence。
- Publication-route memory 还需要更多真实 paper-line accepted/rejected writeback receipts；当前 grouping/review summary 已落地，但不替代真实 memory body/writeback apply。
- Legacy compatibility residue 虽有 tombstone/no-active-default-caller proof，物理删除仍需逐项执行。

### 需要完善

1. 用 DM002、DM003、Obesity 继续跑 live provider-hosted guarded apply；DM002 当前最新有效结果是 MAS owner `stable_blocker` receipt，blocked reason 为 `medical_paper_readiness_missing`；DM002/DM003 的 completion receipt consumption 已进入 matrix oracle，下一步仍需 artifact delta、AI reviewer update、route decision、human gate、stop-loss 或可推进论文的 MAS owner apply receipt。
2. 每条真实 paper line 留下 attempt query、typed closeout、MAS owner receipt、progress delta / blocker 和 no-forbidden-write proof，并把 owner receipt、AI reviewer receipt、human gate receipt、memory writeback receipt 和 package closure 变体继续纳入同一 transition oracle。
3. 扩展 publication-route memory 的真实 accepted/rejected receipts，保持 memory body 和 accept/reject authority 在 MAS workspace/runtime root；OPL/App 只展示 MAS 输出的 body-free refs、分组、freshness 和 review summary。
4. 把 stage review locator proof 与 Portal/Workbench read-only 展示接到 production provider-hosted live apply。
5. 将 MAS-specific 的通用能力需求逐项压回 OPL/App 通用壳：memory locator/index、artifact lifecycle、route graph shell、attention queue、workbench navigation、observability/SLO 和 repair command projection 已有 read-model 基础；下一步用真实 paper-line receipt 驱动这些壳，而不是让 MAS 继续维护第二套通用工作台。
6. 在 replacement proof 和 no-active-reference proof 后，物理清理旧 compatibility residue。

## MAG 当前差距

### 已经成立

- MAG 是活跃 `Grant Foundry` / medical grant domain agent，也是 OPL-compatible package。
- 单一 MAG app skill、CLI、MedAutoGrantDomainEntry、product-entry/projection commands 与 schema-backed contract 是当前默认 capability surface。
- MAG 已暴露 6-stage grant control plane、family action catalog、runtime_control、runtime_continuity、product sidecar export/dispatch、OPL stage runtime registration 和 standard domain-agent skeleton。
- MAG 已完成 owner receipt contract generalization、controlled domain-memory accepted/rejected runtime receipt evidence path、lifecycle cleanup/restore/retention guarded apply proof、physical skeleton minimum anchors 和 no-forbidden-write projection。
- MAG 已在 manifest 中暴露 `grant_transition_oracle`，OPL 现在能通过 descriptor 读模型只读消费该 surface，并把 MAG-owned transition table / oracle fixtures 适配到通用 family transition matrix runner；fundability、authoring quality、package readiness 和 export verdict 仍归 MAG。

### 主要差距

- OPL 控制面已把显式 provider-hosted MAG guarded-run task 写成 MAG product sidecar action envelope，并把 MAG sidecar receipt refs ingest 为 task-bound controlled stage attempt 的 typed closeout；这是 live OPL 接入证据，但不是 grant-stage owner receipt、grant quality verdict 或 no-regression evidence。
- OPL 已能 ingest MAG `grant_transition_oracle` 并完成 matrix oracle smoke；`family-runtime intake` 现在也能把 sidecar/export 中的 `family_transition_matrix_result` 派生成 provider-hosted `family_transition/domain_tick` task。这关闭的是“OPL runner 不消费 MAG spec / matrix result 不能进入 provider attempt bridge”的 transport 缺口，不关闭真实 grant-stage owner receipt、fundability verdict、submission-ready package verdict 或 long soak。
- MAG 理想目标态进一步说明哪些 grant-facing 能力不应长期由 MAG 重复维护通用外围：pre-authoring workspace/source intake shell、explicit wakeup / human-gate transport、grant strategy memory locator/writeback transport、submission-ready package locator/gap-report projection、route/decision drilldown、quality/readiness projection shell、artifact lifecycle/restore/retention 和 operator observability 应由 OPL Framework / App 提供通用壳。OPL 当前已有 refs-only shell；MAG 保留 funding-call 解释、fundability、specific aims、authoring quality、grant memory body/accept-reject、package readiness 和 export authority。
- MAG repo 内 receipt evidence writer 已可把 accepted/rejected decision 写成 runtime receipt instance；真实 OPL-hosted grant-stage attempt 下的 receipt / no-regression evidence 仍未产出。
- Grant strategy memory 的 migration/readiness 是 descriptor 或 proof contract 层，真实 memory body / writeback apply 泛化未闭合。
- Submission-ready package 当前是 MAG-owned export gate；OPL/App 已有 package/export lifecycle read-model shell，后续缺的是用真实 MAG owner receipt / gap report / handoff refs 产品化展示，而不是让 OPL 推断 export readiness。
- 更大范围 source path 迁移和 legacy active-path 删除仍需 path compatibility 与 no-active-caller proof。

### 需要完善

1. 用已可 ingest / hydrate 的 MAG transition oracle 与 matrix result 跑真实 provider-hosted transition attempt，并让 controlled grant-stage attempt 经 MAG sidecar / direct entry 产出 domain receipt、typed blocker 或 no-regression evidence。
2. 用真实 OPL-hosted grant-stage attempt 产出 workspace/runtime accepted/rejected receipt 或 no-regression evidence。
3. 对 cleanup/restore/retention guarded apply 做真实 workspace 级 receipt proof。
4. 把 MAG-specific 的通用外围需求接到 OPL 已有 read-model shell：workspace/source intake、explicit wakeup / human-gate transport、grant strategy memory locator/writeback transport、package/export lifecycle、route/decision graph、quality/readiness projection、attention queue 和 observability/SLO；MAG 保留 fundability、authoring quality、route decision、package readiness、export verdict 和 owner receipt。
5. 在 direct skill path 和 OPL-hosted path parity 稳定后，推进 physical skeleton 物理迁移。
6. 清理旧 Hermes/Gateway/local-manager 命名和 legacy manager 入口，保留项迁入 diagnostic/provenance/history 或负向 guard。

## RCA 当前差距

### 已经成立

- RCA / RedCube AI 是活跃 `Presentation Foundry` / visual-deliverable Foundry Agent，也是 OPL-compatible package。
- Direct route 已 landed；OPL-hosted route 是 contract/projection landed，但 production provider soak pending。
- `ppt_deck` 与 `xiaohongshu` image-first 是当前默认视觉路线，HTML/native PPTX 是显式可选路线。
- RCA 已暴露 family action catalog、stage control projection、route equivalence、product sidecar export/dispatch、OPL runtime manager registration、standard skeleton、artifact locator contract、domain memory descriptor、controlled visual stage attempt、controlled memory apply proof、domain owner receipt contract、lifecycle guarded apply proof 和 physical skeleton follow-through。
- RCA 已拆分 `review.py` 的 geometry audit、markdown report 与 summary projection helper，并删除既有 line-budget baseline；主 helper 回到默认预算内。

### 主要差距

- RCA 还不能声明 Temporal-backed production execution 或 OPL-hosted controlled visual stage soak 已完成。
- OPL 控制面已把显式 provider-hosted RCA `emit_no_regression_evidence` task 写成 product sidecar action envelope，并把 RCA no-regression evidence ref ingest 为 task-bound visual stage attempt 的 typed closeout；这是 live no-regression evidence ref ingestion 证据，但不是 visual-stage long soak 或 artifact-producing owner receipt。
- RCA 理想目标态显示出一批不应长期留在 RCA 内重复实现的通用外围：source/workspace intake envelope、artifact gallery/handoff shell、route/decision graph renderer、review/repair transport、native helper catalog/execution envelope、screenshot/export proof locator、operator observability 和 workbench drilldown 应由 OPL Framework / App 提供通用壳。OPL 当前已有 refs-only shell；RCA 保留 communication strategy、visual direction、review verdict、export verdict、artifact authority、visual memory body/accept-reject 和 artifact-producing owner receipt。
- 真实 reusable visual lesson body 写入、真实 artifact-producing owner receipt 和 OPL-hosted visual-stage no-regression evidence 仍未跑出。
- Review helper 后续只剩 screenshot capture 主体可继续按自然边界拆分；geometry audit、markdown report、summary projection 已不是 blocker。

### 需要完善

1. 跑真实 OPL-hosted controlled visual stage attempt，让 RCA 返回 domain receipt、artifact-producing owner receipt、typed blocker 或 no-regression evidence。
2. 把 visual pattern memory 从 descriptor/proof 推进到真实 runtime receipt refs 和 reusable lesson body writeback，同时继续禁止 repo 保存 memory body 或 artifact blob。
3. 对 lifecycle cleanup/restore/retention 做真实 visual workspace guarded apply proof。
4. 将 RCA-specific 的通用外围需求接到 OPL 已有 read-model shell：source/workspace intake envelope、artifact gallery/handoff shell、review/repair transport、route/decision graph、native helper execution envelope、screenshot/export proof locator、attention queue 和 observability/SLO；RCA 保留 visual direction、route decision、review/export verdict、artifact mutation permission 和 owner receipt。
5. 继续按自然边界拆分 screenshot capture 主体，但不得恢复 line-budget baseline。
6. 在 no-active-caller proof 后，继续清理旧 Hermes/Gateway/local-manager history residue。

## One Person Lab App 当前差距

### 已经成立

- 当前 App 维护拓扑已迁移为 clean App repo + 独立 shell repo：`one-person-lab-app` 持有 App 产品层，`opl-aion-shell` 持有 upstream-backed AionUI shell adapter。
- App repo split 已完成为本地默认拓扑：独立 `one-person-lab-app` 产品仓持有 App 顶层文档、合同、脚本和 release/test lifecycle；本机 checkout 路径已同步为 `/Users/gaofeng/workspace/one-person-lab-app`；当前 AionUI fork 作为 `/Users/gaofeng/workspace/opl-aion-shell` 下的 upstream-backed shell repo 维护，并通过 App repo 的 `shells/aionui` external checkout 被消费。AionUI 2.0 或其他 GUI 基座可在 shell repo 或 App external checkout 合同下并行适配，验证通过后再切 active shell。
- App 作为 OPL-branded GUI shell，消费 Codex-default session/runtime truth、OPL runtime snapshot、stage-attempt workbench / operator item、provider continuous proof operator item 和 domain-owned projection。
- OPL 主仓状态已明确 App 预编译包由 App repo release wrapper 调用 `opl-aion-shell` 构建；Framework 只负责打开已安装 App 和发现 App repo release DMG，不上传或构建 App release artifact。
- App / workbench 已有 provider completion、domain ready verdict、human gate、dead letter、rejected writeback 等 operator 状态轴的实现方向。

### 主要差距

- App 不是 runtime owner，也不是 domain truth owner；它不能替代 OPL Framework 或 MAS/MAG/RCA。
- 当前 fresh runtime snapshot 没有 running domain attempt，但 `stage_attempt_workbench.total=18` 且 18 条 attempt 均 completed，覆盖 `medautogrant=11`、`medautoscience=6`、`redcube=1`，其中 MAS 最新 guarded-apply attempt 已指向 owner stable blocker receipt；provider proof receipt drilldown 已进入 snapshot 的 attention / recent lane item，stage-attempt operator item 已能投影 domain/stage/blocker/memory/artifact locator refs 与 action route refs，native helper execution envelope 已能投影现有 native index / helper receipt metadata。App repo split、contracts / unit / DOM / release mock / fresh-install matrix 级验证已完成；2026-05-15 已用 `one-person-lab-app/shells/aionui` 26.5.15 arm64 build 替换 `/Applications/One Person Lab.app` 并跑通真实 GUI 启动 smoke，smoke artifact `artifacts/opl-installed-smoke-20260515-154821` 记录 `status=passed`、`app_path=/Applications/One Person Lab.app`、label `opl-guid-entry`。App 仍缺真实生产运行状态下的 domain progress owner receipt、artifact mutation receipt、route/decision map drilldown、review/repair queue、artifact gallery/handoff drilldown 与长时运行 drilldown 证据。
- App 仓不再作为 fork overlay；upstream AionUI intake 与 OPL-specific overlay 边界由 `opl-aion-shell` 持有。
- App 顶层承接产品、发布验证和测试生命周期；AionUI 规则收缩到 `shells/aionui/`。Framework 只保留 App release discovery/consumer surface 和 App/workbench 可消费机器接口，不拥有 App 发布流程或 GUI source fork。
- 如果界面展示 provider completion、domain ready verdict、quality verdict 或 artifact authority，需要持续防止文案越权。

### 需要完善

1. 用真实 provider/domain receipts 驱动 App drilldown，而不是只展示 fixture / empty workbench。
2. 继续把 stage-attempt / provider proof drilldown 扩展到真实 domain receipt：按 domain、stage、route、review、blocker、memory refs、artifact locator 和 lifecycle refs 分组，让普通用户看见“谁负责、卡在哪里、为什么转向、哪里需要回修、能做什么”。
3. 所有 action button 必须路由到明确 owner：OPL CLI / provider signal / domain sidecar / direct skill。
4. 继续维护 OPL fork boundary：upstream AionUI 变化只通过显式 intake 分支吸收，OPL overlay 保持薄而清晰。
5. App 文案和状态轴持续避免把 provider completion 写成 domain ready verdict。

## MDS 当前差距

### 已经成立

- MDS 当前定位是 MAS/MDS 分层后的 frozen source archive、historical fixture、explicit legacy diagnostic target 与 upstream intake reference。
- MDS 不再是 MAS 默认 runtime backend、default diagnostic dependency、WebUI dependency 或独立 product entry。
- MDS 不作为 OPL active domain agent、默认安装依赖或 stage adapter。
- MDS 仍可作为 source provenance、historical fixture、explicit archive import、backend audit 或 parity oracle reference 被 MAS 显式读取。

### 主要差距

- MDS 不需要补成理想 Foundry Agent；它与理想目标态的差距不是“缺少 OPL skeleton”，而是“历史兼容面还需要继续收缩”。
- MDS daemon、WebUI、quest layout、connector docs、`ds` launcher、runtime home、fork-local runner 和兼容 namespace 仍作为 archive/diagnostic/fixture surface 存在。
- 物理删除必须等待 MAS source provenance、behavior fixture、explicit restore/import 或 upstream intake 替代面闭合。

### 需要完善

1. 保持 MDS archive/reference/diagnostic/upstream-intake 定位，不把它列为 OPL Foundry Agent。
2. 对每个旧兼容面标明 provenance、diagnostic、fixture 或 upstream-intake 用途；不保留 active compatibility interface。
3. 等 MAS 已有 replacement proof 后，再逐项删除或迁入 history/tombstone。
4. 保持 MDS 较早 shared pin 只作为 legacy diagnostic / archive reference，不作为 active OPL adapter 要求。

## 优先完善顺序

当前不建议再新建平行总计划。下一步应按下面顺序收口：

1. `MAS paper-line provider-hosted guarded apply`
   OPL task-bound attempt bridge 已落地；DM002 live dispatch 已从 owner receipt missing 推进到 MAS owner `stable_blocker` receipt observed，经 active workspace binding hydrate 与 latest persisted Temporal proof 自动触发。下一步以三篇真实 paper line 为主验收，继续争取 progress delta、human gate、stop-loss 或稳定 blocker scaleout。
2. `Temporal provider SLO and continuous proof`
   保持显式 Temporal service / worker / production proof 可重复通过，并把 query/signal/history、restart/re-query 和 operator repair 纳入长时 SLO 证据。
3. `MAS/MAG/RCA live owner receipt`
   以 MAS 三篇真实 paper line 为主线，继续争取 progress delta、human gate、stop-loss 或稳定 blocker scaleout；MAG/RCA 在 controlled grant / visual stage attempt 中产出 domain receipt、no-regression evidence 或 typed blocker。
4. `Unified descriptor and closeout read model`
   保持 `agents descriptors` 聚合入口稳定；MAS/MAG/RCA sidecar 返回的 owner receipt、typed blocker、no-regression evidence refs 都应进入统一 OPL closeout gate，typed closeout memory refs 只做 ref-only 汇总，domain ready verdict 仍由 domain owner 给出。
5. `Domain memory and lifecycle apply`
   三仓都产出真实 consumed/writeback accepted/rejected receipt、memory body apply evidence 和 cleanup/restore/retention guarded apply receipt。
6. `OPL App operator drilldown`
   用真实 receipts 做 domain/stage/route/review/memory/artifact drilldown，明确 action owner；route graph、review queue 和 artifact gallery 由 App/OPL 提供通用壳，route choice、review verdict 和 rationale 由 domain projection 提供。
7. `Physical skeleton follow-through`
   在 direct/hosted parity、restore/provenance proof、focused tests 和 no-forbidden-write proof 都成立后，逐仓迁移 repo-source skeleton。
8. `Legacy physical retirement`
   对 Hermes/Gateway/frontdoor/local-manager/MDS-default residue 做 no-active-caller proof，再删除或归档。
9. `New Foundry Agent admission`
   Patent/Award/Thesis/Review 只按 standard skeleton、stage descriptor、memory locator、artifact locator、quality gate 和 authority boundary 接入，不复制旧路线。

## 当前不能写成

- 不能写成 OPL 已经全量生产可用。
- 不能写成 `local_sqlite ready` 等于 production online ready。
- 不能写成 MAS/MAG/RCA descriptor aligned 等于 physical skeleton layout 已完成。
- 不能写成 provider completion 等于 domain ready / publication-ready / fundability-ready / visual-ready。
- 不能写成 OPL 持有 MAS study truth、MAG grant truth、RCA visual truth 或 MDS archive truth。
- 不能写成 MDS 是新的 OPL Foundry Agent。
- 不能写成 One Person Lab App 是 OPL runtime owner 或 domain truth owner。
- 不能写成 `hermes_agent` 已退役为不可选 executor；准确说法是 Hermes/Gateway 旧 provider、readiness、Gateway cron 和 compat alias 退役，`hermes_agent` 仍是显式非默认 executor adapter，必须独立 receipt / audit / fail-closed。
