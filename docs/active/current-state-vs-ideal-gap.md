# OPL Family 当前状态与理想目标差距

Owner: `One Person Lab`
Purpose: `family_ideal_state_gap_plan`
State: `active_plan`
Machine boundary: 本文是人读 gap / completion map。机器真相继续归 `contracts/`、源码、CLI/API 行为、runtime ledger、provider receipt、domain-owned manifest、真实 workspace 与 App evidence。
Last reviewed: `2026-06-14`

## 读法

本文是 OPL family 当前目标、功能/结构差距、测试/证据差距和 active-goal baton 的唯一 active owner。它只保留当前判断、owner boundary、gap 分类、完成口径和验证入口。

本文不冻结 receipt id、workorder 数字、attempt id、branch/worktree、provider tick、safe-action record/verify 流水或某轮 closeout 细节。需要当前事实时读取 live 机器面；需要历史过程时读取 `docs/history/**`、runtime ledger、提交历史或对应 repo-local history ledger。

North-star 目标态回到 [OPL 与 Foundry Agents 理想目标态](../references/runtime-substrate/opl-family-agent-ideal-state.md)。系统级理想评估、成熟工程经验映射和品牌一致性目标回到 [OPL Family 理想系统评估](../references/runtime-substrate/opl-family-ideal-system-assessment.md)。顶层 operating model 重设计回到 [OPL Family Ideal Operating Model Redesign](./opl-family-ideal-operating-model-redesign.md)，其中 2026-06-10 的 `multi-plane operating system` refresh 只定义 MAS/OPL 理想平面拆分、外部成熟模式吸收分类和审计标准；2026-06-12 的 `运行流畅度 / 输出质量 / 品牌感` refresh 只定义理想体验合同、MAS 旗舰 journey、品牌体验 profile 和 OPL 基座优化口径；它们都不维护第二 active backlog。普通推进主干、审计旁路、artifact tiering、readiness JIT、MDS / DeepScientist smoothness learning 与 P0-P5 完整治理规划回到 [Ordinary Progress Spine 与 Audit Sidecar 完整规划](./ordinary-progress-spine-and-audit-sidecar-plan.md)。公开角色、runtime 边界和默认入口回到 `project/status/architecture/invariants/decisions`。文档生命周期规则回到 [文档组合治理](../docs_portfolio_consolidation.md) 和 [文档生命周期政策](../policies/docs-lifecycle-policy.md)。

当前 ideal top-level redesign 的 active owner 口径是：`TASTE.md` 固化长期维护 taste，`docs/decisions.md` 记录仍有效决策，`contracts/opl-framework/surface-budget-policy.json` 冻结机器预算，`contracts/opl-framework/target-operating-architecture-contract.json` 镜像目标态 compiler policy，本文维护当前 owner、gap、gate、next action 和完成口径。支撑文档只能提供目标态、评估标准或证据解释，不能维护第二 active backlog、第二 ordinary route 或第二 truth。

近期 foldback 的统一读法是：support 文档和 contracts 只保留目标态、验收标准、机器锚点和 false-authority guard；本文只维护当前 owner、gap、gate、next action 和完成口径。`opl-family-ideal-operating-model-redesign.md` 的 `multi-plane operating system`、`运行流畅度 / 输出质量 / 品牌感` 和 OPL 基座优化是 north-star / audit standard；`contracts/opl-framework/target-operating-architecture-contract.json#experience_operating_model`、`#one_shot_plan_landing_model` 和 `#foundry_agent_os_standard` 是机器锚点；`framework operating-maturity`、`agents conformance`、`brand-modules l5-status` 和 App operator read model 是动态读面。近期 read-model 重构后必须读取 wrapper 后的 payload：`framework operating-maturity` 的当前 truth 在 `.framework_operating_maturity`，App operator drilldown 在 `.app_operator_drilldown`，Brand L5 在 `.brand_module_l5_status`，attempt list 在 `.family_runtime_stage_attempts`。Brand L5 独立 readout 的 runtime ledger 计数在 `.brand_module_l5_status.evidence_ledger`，`framework operating-maturity` 内的 Brand L5 聚合在 `.framework_operating_maturity.brand_module_l5`。这些 surface 只能说明 OPL-controlled transport/read-model/contract/test 已落地或 owner-evidence work order 可接力，不能关闭 MAS/MAG/RCA/OMA domain ready、MAS paper done、App release ready、Brand L5、quality/export verdict、physical delete 或 production ready。

2026-06-14 任务目标更新：原 durable 目标“持续推进 OPL family ideal operating model 在所有相关 repo 中落地：逐项关闭 OPL/MAS/MAG/RCA/OMA/App/brand owner evidence、runtime、App release、Brand L5、wrapper retirement、domain live progression 缺口；每轮 fresh 读取机器真相，能安全并行则隔离 worktree/subagent，完成后吸收、验证、清理，并同步 docs/contracts/tests/ledger”仍然有效。需要更新的是 active 解释：当前不是继续扩大 OPL 基座、support docs 或 read-model 投影，而是把全部可执行工作收敛到 `W7-owner-evidence-and-production-tail`。每轮先 fresh 读取 `.framework_readiness`、`.framework_operating_maturity.current_owner_delta_bridge`、`.framework_operating_maturity.unresolved_owner_gates.gates`、`.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane`、`.app_operator_drilldown` 和 `.brand_module_l5_status`，再按 owner repo 处理真实 closing refs、current pointer closeout、route-back 或 typed blocker。`open_count=0`、App 顶层投影、verified refs-only ledger、source-boundary cleanup、docs foldback、contract pass、conformance pass 和 test pass 都只能作为 supporting evidence；它们不关闭全局目标。本轮 readback 证明 current owner delta identity 与 payload ledger 都会漂移；即使 live identity 已有 verified refs-only payload summary，只要 readiness current pointer 仍未记录 owner answer，就还必须由对应 owner 折成 current pointer 可消费的 owner answer、route-back 或 updated currentness evidence，不能重复消费 stale refs-only summary，也不能把已验证 summary 本身写成 ready。

2026-06-14 workspace currentness foldback：相关 active/support 文档已按近期 read-model wrapper、重构事实和真实 workspace binding 刷新。RedCube 与 MAG active bindings 已回到 main workspace，并包含 `direct_entry.command` / `manifest_command`；`framework readiness.summary.domain_manifest_*` samples 曾在 clean 与 stale/not-configured 之间漂移。当前 owner-map 支撑面仍必读 `framework operating-maturity`、App/operator drilldown、Brand L5 与 default-callers delete gate；workspace currentness 必须 live-read `opl workspace projects --json` 和 `opl framework readiness --family-defaults --json`。这次更新只把 workspace currentness、计划读法和 owner map 对齐，不构成任何 item 的 `done` 证据。

Standard Agent landing acceptance 当前机器入口是 `contracts/opl-framework/standard-agent-landing-acceptance-contract.json` 和 `contracts/opl-framework/standard-agent-landing-evidence-status.json`。它把 MAS DM002/DM003 暴露的失败模式提升为 family-level 验收门：真实 `agent/` pack semantics、generated surface production consumption、private-platform residue owner decision、`current_owner_delta` single ordinary route、stage-route arbiter / stop-loss、OMA target-agent work-order guard、cross-agent negative conformance。该合同状态是 `acceptance_definition_landed` / `family_evidence_tail_open_not_complete`；ledger 逐项记录七个 gate 的 `satisfied`、`evidence_required` 或 `satisfied_or_owner_typed_blocker`，并明确 generated-surface production consumption、OMA target-agent samples、cross-agent negative conformance scaleout、long-soak / real-user-path owner evidence 仍未闭合。`descriptor_ready`、`generated_interface_ready`、`standard_pack_conformance_passed`、`suite_pass`、`functional_structure_gap_count_zero` / `classification_zero`、`provider_completed`、`verified_refs_only_ledger`、`App_projection_ready` 和 `contract_landed` 都不能声明标准 Agent 已彻底完成。OMA 构建或接管 target agent 时，缺 source morphology、target owner route、generated surface consumption、private residue decision、no-forbidden-write proof 或 owner answer shape，`opl work-order execute` 必须返回 typed blocker / developer work order，而不是启动执行、签 target owner receipt 或生成半标准 agent。

MAS 的 family-level Foundry Agent OS 目标态按 `OPL Agent OS + Domain Declarative Pack + Domain Minimal Authority Kernel + Domain Capability Registry` 读取。MAS flagship experience mapping 只定义 `Evidence Map -> Analysis Pack -> Manuscript Draft -> Reviewer Letter -> Revision Packet -> Publication Handoff` 的理想体验与迁移边界；私有 scheduler / runner / workbench / status shell 只能作为迁移输入。Capability Registry 归入 `Atlas + Pack + Stagecraft` 的 registry / ABI / use-policy，不新增第 11 个品牌模块，也不生成 domain authority。原先独立的 OPL / MAS supervisory acceptance 目标只作为 Foundry Agent OS plan 的验收细目存在；当前执行顺序和 owner gate 仍回本文。

## 目标态

OPL family 的目标形态固定为 `OPL Framework -> One Person Lab App -> Foundry Agents`。

- `OPL Framework` 持有 Codex-default activation、Temporal-backed provider、typed queue、stage attempt、receipt/projection、generated/hosted surfaces、Agent Lab、safe action shell 和 App/operator read model。
- `One Person Lab App` 是人用工作台，只消费 framework/provider 状态和 domain-owned projection；普通用户形态是 `Codex App wrapper`，固定 `Codex CLI` executor，内置 MAS/MAG/RCA/OMA task entry。
- `MAS`、`MAG`、`RCA` 是当前 domain Foundry Agents，持有各自 domain truth、quality/export verdict、artifact authority、memory body、owner receipt、typed blocker 和 direct app skill path。
- `OPL Meta Agent` 是 Agent Foundry / new-agent builder/tester module，不持有 MAS/MAG/RCA 的 domain truth。

标准 Foundry Agent 的目标源码形态是：

```text
Declarative Domain Pack
  + OPL generated/hosted surfaces
  + standard authority functions
```

`Codex CLI` 是当前第一公民 executor。Temporal-backed provider 是 production online runtime 的必需 substrate；`local_sqlite` 只允许作为 dev/CI/offline diagnostic baseline。`hermes_agent`、`claude_code`、`antigravity_cli` 等只能作为显式非默认 executor adapter/backend。

## 当前统一执行目标

2026-06-04 以后，本页把 MVP friction 线、purpose-first cross-repo audit 线和 Co-Scientist-inspired cognitive kernel 线折成同一个 active owner 口径：

```text
current_owner_delta outer loop
  -> stage attempt with Cognitive Computation Kernel
  -> Stage Transition Authority
  -> stage artifact unit / owner receipt / typed blocker
  -> passive evidence vault and diagnostic drilldown
```

一句话目标：OPL 是 `Cognitive Computation Kernel + Agent Runtime Platform`；默认读面只问当前 owner 欠什么，Stage 内部让 Codex executor 在 tool affordance / authority / quality gate 边界内完成开放式专家工作，Stage Transition Authority 把多来源 transition intent / owner answer / typed blocker / human gate / provider observation 裁决成唯一 current pointer、terminal state 和 `current_owner_delta`，domain repo 只保留领域 truth、artifact authority、quality verdict、owner receipt 和 typed blocker。

2026-06-10 以后，目标操作模型进一步收敛为 multi-plane operating model：

```text
current_owner_delta single ordinary route
  -> Stage Artifact Unit
  -> domain authority decision
  -> next current_owner_delta

support planes:
  Pack / Stagecraft / Runway / Vault / Console / Foundry Lab / Connect
```

该模型的 active 口径是：`Console` 只展示 `current_owner_delta`、owner action、artifact/blocker refs 和 safe actions；`Runway` 只保证 durable execution、provider observation、repair action 和 OPL runtime blocker；`Vault` 只记录 refs-only evidence、lineage、telemetry 和 audit packet。三者都不能生成 domain owner answer、domain typed blocker、quality/export/review verdict、ready declaration、physical delete authorization 或 App release verdict。

2026-06-05 以后，后续优化统一按 `目的反推必要性，MVP 检查阻碍性` 审计：

- `目的反推必要性`: 只有能推动 owner delta、保护 authority boundary、支撑 durable execution / replay / audit、或减少 App/operator 决策成本的 surface，才有资格进入默认面。
- `MVP 检查阻碍性`: 凡是让普通尝试先陷入 receipt-only、read-model reconcile、platform repair、stale-route redrive、diagnostic proof、evidence accounting、route variant 或 wrapper lineage 的 surface，都应下沉为 audit/detail、显式 lane，或进入 retire gate。
- `统一判断`: 更符合预期不是“更多证据、更全 worklist、更厚合同”，而是默认路径更短、当前 owner 更明确、accepted answer shape 更清楚、artifact / receipt / typed blocker 更可接力、domain verdict 更稳定地回到 domain owner。

2026-06-08 以后，workspace topology v2 暴露出的后续风险按 `抓大放小` 扩展为全 family 审计原则：

- `抓大`: 硬守 owner、authority、stage lifecycle、workspace topology、selected executor、single ordinary route、launch / execution / closeout admission、accepted owner answer shape、App release verdict、physical delete authority 和 no-second-truth。缺这些会走歪、越权、误闭合或不可审计，必须 fail closed。
- `放小`: prompt / skill / tool / knowledge / rubric refs 完整性、path alias、generated projection mirror、workspace fleet/detail drift、worklist raw counter、diagnostic proof、route variant、receipt accounting、wrapper lineage、L5 evidence matrix item、provider ops detail 和 release cohort diagnostic，默认不能抢占 ordinary path。它们只在造成错误启动、越权、不可恢复、不可审计、无法 closeout、owner answer shape 不合法或不可逆 mutation 时升级为 hard blocker。
- `默认判断`: 新增 surface / gate 先问它是否保护大边界，再问它是否让普通 owner delta 更短。如果答案只是不确定性更少、报告更全或证明更漂亮，默认下沉为 advisory / audit / diagnostic / cleanup / production evidence lane。

当前机器边界已经由 `current-owner-delta.schema.json`、`surface-budget-policy.json`、`target-operating-architecture-contract.json`、App `ordinary_cockpit`、default read surface policy 和 focused drift guards 持有。`Ordinary Progress Spine + Audit Sidecar` 的 current read 是：ordinary path 只从 `current_owner_delta -> stage goal -> concrete delta -> ProgressDeltaReceipt / OwnerReceipt / TypedBlocker -> next current_owner_delta` 推进；lineage、trace、restore、readiness inventory、long-soak、cleanup、release cohort、L5 evidence、raw worklist、raw evidence 和 full replay 默认进入 passive sidecar，不能生成默认 next action。

`ProgressDeltaReceipt` 是 T0 轻量接力记录，只能说明 produced refs、consumed refs、changed surface、next owner 和 next required delta；它不能授权 stage complete、publication/package ready、artifact/memory mutation、physical delete、domain ready、App release ready 或 production ready。重复无交付尝试由 progress-first anti-spin gate 分类为 receipt-only、read-model reconcile-only、stale-route redrive-only、platform-repair-only 或 no-deliverable-delta；恢复 ordinary path 需要 fresh owner delta、稳定 typed blocker、human decision 或 provider hard-gate clearance。

当前三条线的分工固定为：

- `outer_owner_delta`: `current_owner_delta` 是 App/CLI/operator 默认 root；worklist、replay、typed-blocker group、provider trace 和 private residue 只能作为 audit / full-detail。
- `inner_cognitive_kernel`: `tool_affordance_boundary` 是 available affordance catalog，不是 workflow script；工具目录不能规定顺序、认知策略、stage goal 或 forbidden write。
- `stage_transition_authority`: Stage current pointer、StageRun terminal state 和 `current_owner_delta` 只能从 OPL append-only authority event log 派生；MAS/MAG/RCA/OMA、human gate、provider 和 App 只能提交 transition intent、owner answer、typed blocker、human gate decision 或 provider observation。
- `domain_adoption`: MAS/MAG/RCA/OMA 只声明 domain pack、stage strategy refs、tool/knowledge/quality gate refs 和最小 authority functions；真实 ready 仍由 domain owner receipt / typed blocker / human gate 关闭。

当前 live owner gate 只从 fresh CLI/read-model 读取，不从本文继承历史 counter、attempt id、dry-run 结论或 commit 流水。本轮 fresh source-of-truth 读法如下：

| live surface | 当前读法 | 本页结论 |
| --- | --- | --- |
| `opl framework readiness --family-defaults --json` | 当前 truth 读取 `.framework_readiness`。稳定 readout 是 `.framework_readiness.status=framework_control_plane_available_with_open_production_tail`、`.framework_readiness.summary.owner_delta_handoff_status=needs_domain_or_app_live_owner_payload`、current owner delta hard gate open、`.framework_readiness.current_owner_delta.latest_owner_answer_ref=null` / `.framework_readiness.current_owner_delta.hard_gate.domain_ready_authorized=false`。`.attention_first_payload.summary` counters、workspace currentness arrays、`task_or_study_ref`、`lineage_ref` 和 `source_fingerprint` 都是高漂移 live values，必须重跑命令读取，不在本文冻结。 | Readiness current pointer 尚未记录当前 identity 的 owner answer；默认下一步不能再回 OPL 基座补 projection，也不能复用旧 identity ledger，而应回当前 live owner 或 route-back 指向的 OPL currentness/stage-packet owner，产出能被 current pointer 消费的 owner answer、quality gate receipt、typed blocker、route-back 或 updated currentness evidence。Audit sidecar、blocked refs-only residue、raw worklist、provider trace、replay packet、cleanup route、旧 owner payload summary、已验证但未进入 readiness current pointer 的 payload summary、running 未闭合 attempt 和 private residue都不能生成 ready claim。 |
| `opl agents conformance --family-defaults --json` | family structural contract 为 `passed_count=4`、`blocked_count=0`；ordinary path guard passed；live domain progress 仍是 `owner_evidence_recorded_not_ready_claim`。 | StageRun profile、controlled canary、scaffold/validate gate 和 conformance pass 只证明标准接入和 evidence shape，不声明 MAS/MAG/RCA/OMA domain ready、artifact ready、quality/export ready 或 production ready。 |
| `opl agents default-callers --family-defaults --json` | `blocked_count=0`，`deletion_evidence_worklist_count=32`，`default_caller_delete_ready=false`，`physical_delete_authorized=false`，`physical_delete_authorization_status=not_authorized_by_opl_projection`。 | No-active-caller / replacement / tombstone refs 只是 cleanup lane 输入。没有 domain owner physical delete authorization、keep decision 或 typed blocker ref 时，OPL 不删除 wrapper、facade、alias、helper、workflow、test 或 package surface。 |
| `opl framework operating-maturity --family-defaults --json` | 当前 truth 读取 `.framework_operating_maturity`。稳定 readout 是 `.framework_operating_maturity.status=evidence_required`、`.framework_operating_maturity.summary.ready_claim_authorized=false`、`.framework_operating_maturity.summary.current_owner_delta_owner_answer_missing=true` / `current_owner_delta_owner_answer_still_required=true`。`.framework_operating_maturity.current_owner_delta_bridge` 必须跟随 live readiness identity；current owner、stage、task/study ref、lineage、source fingerprint、verified payload summary 和 stale payload summary 都不在本文冻结。若 live readout 显示 refs-only payload summary 已验证，例如 `owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim` 或 `observed_owner_answer_ref_shapes` 非空，只要 `readiness_current_pointer_owner_answer_ref` 仍为 `null`，owner answer 仍缺。W7 owner-route work orders 的机器路径是 `.framework_operating_maturity.foundry_agent_os_production_evidence_gate.owner_route_work_orders_by_lane.<lane>`；数组 `owner_route_work_orders[]` 是兼容/人工扫描面。App release/user-path、Brand L5、provider long-soak、cleanup-retirement、memory/artifact/lifecycle 等投影的 open counts、observed refs、worklist counts、verified receipt counts 都是 live counters，不在本文冻结。 | Evidence lane 和 owner gate inventory 只能定位下一 owner、accepted ref shape、observed ref shape、closing source、verify path 和 false-authority boundary；不能授权 domain ready、App release ready、Brand L5、physical delete 或 production ready，也不能让 OPL 生成 owner receipt、typed blocker 或 quality/export/review verdict。 |

当前 owner answer handoff 未闭合：fresh readiness 仍必须显示 `latest_owner_answer_ref=null` 才能保持这一判断；current identity、source fingerprint、verified payload summary 和 stale owner payload summary 列表必须 live-read，不从本文继承。后续不能重复消费旧 `domain-owner-payload-summary record|verify` receipt 产生进展；必须由当前 live owner 或 blocker route-back 指向的 OPL currentness/stage-packet owner，把 live current identity 对应的 owner receipt、quality gate receipt、human gate、route-back evidence、updated currentness evidence 或 domain-owned typed blocker 折成 readiness current pointer 能消费的 owner answer。旧 publication-handoff typed blocker、controller decision、study-level blocker、stale provider identity、running 未闭合 attempt、audit counter，或未进入 readiness current pointer 的 refs-only summary，只能作为 history/audit/currentness 诊断；它们不能借位声明当前 owner stage 已完成，也不能被 OPL 文档合成为 paper ready、publication ready、domain ready 或 production ready。

2026-06-14 MAS owner-route dry-run 更新：当前 live owner gate 仍指向 MAS / `med-autoscience`，但 MAS controller dry-run 证明这不是 OPL 可自行 apply 的缺口。DM-CVD study `003-dpcc-primary-care-phenotype-treatment-gap` 的 `domain-health-diagnostic --request-opl-stage-attempts --dry-run` 返回 `decision=blocked`、`reason=quest_waiting_for_user`；当前 managed execution envelope 是 `state_kind=executable_owner_action`，当前 executable owner action 是 `gate_clearing_batch` / `run_gate_clearing_batch` / `publication_gate_replay`，work-unit fingerprint 为 `sha256:2c4793a4e41859fd21a0bc088459c85f298bacb7d06eea811b44beae568fbf9f`，required delta 是 `publication_gate_replay_delta_or_typed_blocker`，`running_provider_attempt=false`，`provider_admission_candidates=[]`，`runtime_recovery_allowed=false`，worker liveness 为 `not_live`，supervisor state 为 `stale`。publication gate / medical publication surface 仍 blocked，report 中 `bundle_tasks_downstream_only=true`，因此 bundle/build/proofing 仍是下游工作，不能当作当前 owner delta。`provider_admission_current_control_state` 仍能读到 `sat_e1063d97901cc3d70424fc5c.closeout.json#domain_blocker`，但它只是 MAS domain-owned typed blocker evidence / current-control residue，不能由 OPL 写成 readiness current pointer owner answer。`--request-opl-owner-route-reconcile --dry-run` 被 CLI 拒绝，提示该选项必须配 `--apply`；因此后续只在 MAS/runtime owner 明确授权 developer-supervisor exact lane、且 dry-run 证实 study id、work-unit fingerprint、owner route、dispatch identity 和写边界完全匹配时，才能考虑 MAS controller apply。

Workspace Protocol 的当前 active 口径只保留结构边界：`Workspace Group -> Project Unit -> Stage Artifact Unit -> Owner Receipt / Typed Blocker`，默认 physical project root 是 `projects/<project-id>`，canonical generated roots 是 `control/opl/projections` 与 `control/opl/reports`，project lifecycle 是 `active`、`paused`、`archived`、`superseded`、`locked`，project delete 是 refs-only safe-delete gate。`workspace ensure/init/adopt/upgrade/validate/doctor/fleet report/project lifecycle/interfaces` 证明 L4 structural baseline 和 operator inspection 可用；它们不声明 live StageRun progress、owner receipt、typed blocker、quality/export verdict、artifact ready、domain ready、L5 或 production ready。

## 统一审计标准

后续 closeout、设计评审和跨仓优化都按 [OPL Foundry Agent Target Operating Architecture](./opl-foundry-agent-target-operating-architecture.md) 的 `Audit Standard` 判断，并在本页只保留执行口径：

| audit lane | 本页完成口径 |
| --- | --- |
| `default_path` | 普通 App/CLI/operator summary 从 `current_owner_delta` 派生，不把 raw worklist、replay、provider trace、route variant 或 private residue 当 next-action root。 |
| `progress_truth` | progress 只由 artifact unit、owner answer / receipt / typed blocker、current pointer 和 domain/human gate 关闭；OPL evidence transport 只能做支撑。 |
| `mvp_friction` | 默认尝试能直接进入 stage artifact / owner answer 路径；重复 receipt-only、platform-repair-only、read-model-reconcile-only 或 stale-route redrive 时触发 stop-loss 或 typed blocker。 |
| `authority_boundary` | OPL/App/OMA/Agent Lab 不持有 MAS/MAG/RCA domain truth、quality/export/review verdict、artifact authority、owner answer、typed blocker 或 memory accept/reject。 |
| `surface_budget` | 新 surface 先按 default / diagnostic / audit / cleanup / production lane 分类；没有 default 资格的内容不进入普通入口。 |
| `golden_path` | MAS/MAG/RCA/OMA 每个 agent 只有一个 ordinary route；variants/proof/diagnostic/cleanup/long-soak 必须显式。 |
| `wrapper_retirement` | retained wrapper / adapter / script 只能保留到 no-active-caller、owner receipt / typed blocker、no-forbidden-write、tombstone/provenance gate 关闭。 |
| `app_cockpit` | App 默认展示 purpose、task/stage、next owner、accepted answer shape、artifact/blocker 和 release/user-path facts；full drilldown 只做诊断。 |
| `stage_native_kernel` | StageRun Kernel 默认面只回答 current owner delta、缺什么 role/receipt/blocker 和下一 owner；launch/closeout blockers 与 advisory/audit/drilldown 分层，避免把新 Kernel 变成更厚 admission、worklist 或 read-model 补偿链。Execution authorization、attempt lease 和 closeout receipt binding 是 launch-hard / closeout-binding gate；缺失时生成 OPL-owned execution authorization blocker，owner=`one-person-lab`，并明确它不是 domain typed blocker、不会改 domain truth、不会替 domain owner 签 receipt。 |
| `grip_big_release_small` | 大边界 fail closed，小细节默认下沉。任何 surface 如果只提高解释、审计、计数、展示或静态完整性，但不能直接防止走歪、越权、误闭合、不可恢复、不可审计或不可逆 mutation，就不能成为 ordinary default blocker。 |

本页的 gap 只能标成三类：

- `meets_target`: 默认路径、owner delta 和真实进度 owner 都更清楚。
- `needs_demotion`: surface 有审计或诊断价值，但不应出现在 ordinary path。
- `needs_retirement`: surface 已被 OPL generated/hosted surface、App contract 或 domain authority function 替代，应走删除或 tombstone。

不得用 `tests passed`、`conformance passed`、`open_worklist=0`、`verified ledger`、`doctor clean` 或 `docs updated` 单独关闭这些 gap。

## 当前完成进度

| area | 当前完成口径 | 仍未关闭 |
| --- | --- | --- |
| `framework_default_path` | `current_owner_delta`、Ordinary Progress Spine、Audit Sidecar、ProgressDeltaReceipt、StageRun cockpit、workspace L4 projection、standard agent conformance 和 operating-maturity read model 已进入同源机器面。 | 这些只证明 framework control plane 和 operator read surface 可用；不能声明 domain ready、App release ready、Brand L5 或 production ready。 |
| `app_operator_drilldown_source_boundary` | `maintenance_slice_landed`。App operator drilldown 的 core refs 逻辑已从主入口抽到 `runtime-tray-app-operator-drilldown-parts/core-refs.ts`；入口保持稳定，memory/artifact/lifecycle、cleanup-retirement、provider long-soak、owner-delta 和 detail view 仍从 `.app_operator_drilldown` 统一读取。fresh structure advisory 不再把 `runtime-tray-app-operator-drilldown.ts` 归入 source-shape advisory。 | 这是维护性结构落地，只降低 App/operator read-model 的局部维护压力；不改变 `.app_operator_drilldown` 的 authority boundary，不关闭 domain-owned lifecycle receipt、physical delete、App release verdict、Brand L5 或 production evidence。 |
| `multi_plane_operating_model` | `hygiene_only_supporting_active_gap`。理想模型已按 ordinary progress、durable Runway、Stage Artifact、authority decision、Vault evidence telemetry、Console cockpit、Foundry Lab improvement 等平面分层；本文承接 active baton，redesign 文档只作为 north-star / 验收标准。 | 后续任何 plane 的落地都必须回到 owner receipt、typed blocker、human gate、review/export receipt、safe action 或 audit refs。plane health、provider observation、telemetry、work-order projection 或 docs foldback 不能声明 domain ready、App release ready、Brand L5 或 production ready。 |
| `opl_base_optimization` | OPL 基座优化口径固定为 generated/hosted surfaces、durable Runway、Stage Artifact Unit、passive Vault、Console owner-action producer、Foundry Lab work-order loop 和 human/domain owner decision gate。 | 新增基座 primitive 必须先说明 owner、default lane、hard blocker 升级条件、false-authority flags 和 active consumer；不能把 domain repo 私有 scheduler、queue、dashboard、status shell 或 generic wrapper 继续写成长期架构。 |
| `pack_os_transport` | `contracts/opl-framework/pack-os-contract.json`、`src/pack-os.ts`、`brand-module-surfaces.json#modules.pack` 和 `opl pack os inspect|install|registry|cache|distribute|lock|validate --json` 已把 OPL Pack OS 落为通用 refs-only pack descriptor / install registry / content-addressed cache / distribution bundle / lock / lifecycle / review receipt transport surface。 | 该完成口径关闭 capability pack descriptor intake、install/registry、present-local-file cache、refs-only distribution、lock projection、artifact lifecycle refs 和 review receipt refs transport 缺口；不能声明 domain truth、artifact body authority、owner receipt authority、quality/export/review verdict、publication ready、App release ready、Brand L5 或 production ready。 |
| `foundry_agent_os_family_target` | `meets_target_for_w0_w1_w3_w4_w5_w6_structural_landing` / `needs_w7_live_production_evidence_and_owner_acceptance`。`target-operating-architecture-contract.json#foundry_agent_os_standard` 已把 MAS 方案抽象成 family-level target；`target-operating-architecture-contract.json#flagship_experience_mapping` 已把 MAS Research Foundry flagship journey、标准 agent shape、私有平台残留迁移输入和 false-ready claims 冻结为机器合同；`opl agents conformance` 已暴露 `foundry_agent_os_conformance` 及 MAS-only flagship mapping；`opl agents interfaces` 已暴露 MAS/MAG/RCA/OMA `generated_direct_parity`；Capability Registry resolver ABI 已落到 `capability-registry-resolver.schema.json` / `src/capability-registry-resolver.ts` 并消费 domain pack external-learning refs；MAS/MAG/RCA/OMA 已有 domain kernel manifest；App contract 已固定 `current_owner_delta` ordinary cockpit；`framework operating-maturity` 已暴露 `owner_evidence_intake`、`foundry_agent_os_production_evidence_gate.owner_route_work_orders`、domain owner evidence routes、private-platform retirement work order，以及 human gate / quality-export / reviewer / long-soak ref-shape intake。External-learning 后续优化已折回 `W3/W4/W7`，不再作为 MAS 私有 selector 或第二 active backlog。 | 当前关闭的是 W0/W1/W3/W4/W5/W6 structural / functional landing、MAS flagship experience mapping 的 OPL 侧 contract/read-model 映射，以及 W7 refs-only intake / owner-route work-order 投影字段 / per-domain ref-shape projection / non-closing guard。真实 production evidence 仍要由 owner receipt、typed blocker、human gate、reviewer/quality/export receipt、long-soak、release/install、private-platform owner decision 或 owner acceptance refs 关闭；external-learning refs、flagship mapping 和 `owner_evidence_intake` observed refs 只有被 domain/App/brand owner 消费并产出 owner receipt / typed blocker / reviewer receipt / human gate / release verdict 后才能计入真实进度。 |
| `domain_owner_progression` | MAS/MAG/RCA/OMA 的标准接入、generated/hosted surface、controlled evidence shape、owner-answer accepted shape、false-authority boundary 和 `foundry-agent-os-domain-kernel-manifest.json` 已能被 OPL 读取。2026-06-11 新增标准 `contracts/live_stage_run_progress_evidence.json` consumption：OPL 只接受 `surface_kind=domain_live_stage_run_progress_evidence`、标准 `status`、refs-only fields 和 false-authority flags；不满足标准时 fail-closed。fresh worktree conformance 已读到四个 domain 的 domain-owned refs，`live_stage_run_progress_evidence_open_domain_count=0`，每个 domain 仍是 `owner_typed_blocker_recorded_not_ready_claim`。 | 这只关闭“OPL 无法消费 domain-owned live progress evidence contract”的缺口。真实 MAS paper、MAG grant、RCA visual 和 OMA target-agent progress 仍需 domain-owned owner receipt、quality/export/review receipt、typed blocker、human gate、route-back evidence 或 no-regression ref；`open_domain_count=0` 不能声明 domain ready、production ready 或 L5。 |
| `cleanup_and_retirement` | Wrapper / facade / alias / helper 删除门已收敛到 cleanup lane，default-caller / physical-delete read model 只投影 refs-only owner-decision shape。 | 没有 domain owner physical delete authorization、keep decision 或 typed blocker ref 时，OPL 不物理删除 retained surface，也不保留 compatibility facade。 |
| `docs_lifecycle` | 本页持有唯一 active owner；core docs、active support、reference、history 和 docs portfolio 的职责已经分层。 | active docs 仍需持续防止 dated proof、receipt 流水、branch/worktree closeout 和 long checklist 回流；当前 coverage 与未覆盖项继续进入 active-goal prompt。 |

## Active Planning Gap Register

本节是所有 active/support 规划的单一缺口归口。support 文档里的 P0-P5、W0-W7、audit lane、rollout phase、closure matrix row 和 external-practice checklist 只提供标准或证据解释；它们不维护第二 backlog。

Fresh readout discipline:

| command | fields to read | how this plan may use it |
| --- | --- | --- |
| `opl framework readiness --family-defaults --json` | 当前 truth 读取 `.framework_readiness`。稳定 readout 是 `.framework_readiness.status=framework_control_plane_available_with_open_production_tail`、`.framework_readiness.summary.owner_delta_handoff_status=needs_domain_or_app_live_owner_payload`、current owner delta hard gate open、`.framework_readiness.current_owner_delta.latest_owner_answer_ref=null` / `.framework_readiness.current_owner_delta.hard_gate.domain_ready_authorized=false`。`.attention_first_payload.summary` counters、workspace currentness arrays、`task_or_study_ref`、`lineage_ref` 和 `source_fingerprint` 都是高漂移 live values，必须重跑命令读取，不在本文冻结。 | Readiness current pointer 尚未记录当前 identity 的 owner answer；默认下一步不能再回 OPL 基座补 projection，也不能复用旧 identity ledger，而应回当前 live owner 或 route-back 指向的 OPL currentness/stage-packet owner，产出能被 current pointer 消费的 owner answer、quality gate receipt、typed blocker、route-back 或 updated currentness evidence。Audit sidecar、blocked refs-only residue、raw worklist、provider trace、replay packet、cleanup route、旧 owner payload summary、已验证但未进入 readiness current pointer 的 payload summary、running 未闭合 attempt 和 private residue都不能生成 ready claim。 |
| `opl framework operating-maturity --family-defaults --json` | 当前 truth 读取 `.framework_operating_maturity`。稳定 readout 是 `.framework_operating_maturity.status=evidence_required`、`.framework_operating_maturity.summary.ready_claim_authorized=false`、`.framework_operating_maturity.summary.current_owner_delta_owner_answer_missing=true` / `current_owner_delta_owner_answer_still_required=true`。`.framework_operating_maturity.current_owner_delta_bridge` 必须跟随 live readiness identity；current owner、stage、task/study ref、lineage、source fingerprint、verified payload summary 和 stale payload summary 都不在本文冻结。若 live readout 显示 refs-only payload summary 已验证，例如 `owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim` 或 `observed_owner_answer_ref_shapes` 非空，只要 `readiness_current_pointer_owner_answer_ref` 仍为 `null`，owner answer 仍缺。App release/user-path、Brand L5、provider long-soak、cleanup-retirement、memory/artifact/lifecycle 等投影的 open counts、observed refs、worklist counts、verified receipt counts 都是 live counters，不在本文冻结。 | Evidence lane 和 owner gate inventory 只能定位下一 owner、accepted ref shape、observed ref shape、closing source、verify path 和 false-authority boundary；不能授权 domain ready、App release ready、Brand L5、physical delete 或 production ready，也不能让 OPL 生成 owner receipt、typed blocker 或 quality/export/review verdict。 |
| `opl agents conformance --family-defaults --json` | structural pass/block state, live progress evidence status, per-domain observed refs and false-authority flags. | Proves standard interface shape and refs-only consumption. It cannot close live domain progress. |
| `opl brand-modules l5-status --json` | `.brand_module_l5_status` 下的 L5 status、evidence-required modules、owner-route work orders、accepted ref shapes 与 `.evidence_ledger` refs-only ledger counters。 | Drives Brand L5 owner-evidence work; work orders and verified refs-only ledger entries do not claim L5. |
| `opl agents default-callers --family-defaults --json` | deletion evidence worklist, owner decision status and `physical_delete_authorized`. | Drives explicit cleanup lane only. Observed prerequisites do not authorize physical delete. |

2026-06-14 readback kept the same active conclusion: framework read models are available; `framework readiness` lives under `.framework_readiness`, and the summary-level payload is attention-first rather than the old top-level owner-delta shape. `.framework_readiness.current_owner_delta` remains the only ordinary owner root, but its current owner、`task_or_study_ref`、`lineage_ref`、`source_fingerprint`、payload summary refs and ledger counters are live values and must be reread before acting. `framework operating-maturity.current_owner_delta_bridge` may observe and verify refs-only payload summaries for the live identity, but when it still reports `readiness_current_pointer_owner_answer_ref=null`、`owner_answer_missing=true` and `owner_answer_still_required=true`, the remaining work is current pointer closure / route-back / updated currentness evidence, not another OPL payload-summary recording. Conformance is structurally passing but live domain progress remains `owner_evidence_recorded_not_ready_claim`; Brand L5, App release/user path, cleanup-retirement, memory/artifact/lifecycle and provider long-soak remain evidence lanes with live counters and `.framework_operating_maturity.summary.ready_claim_authorized=false`. Fresh structure foldback also confirms source-boundary maintenance slices can close without closing owner-evidence, release, cleanup, memory/artifact, Brand L5 or production gates. Future edits must rerun the commands above instead of copying these counters.

| active gap | 已落地 | 仍未落地 | owner / closing ref | source of truth |
| --- | --- | --- | --- | --- |
| `current_owner_delta_owner_answer` | OPL 已把 default read root 收敛到 `current_owner_delta`，并把 provider admission / closeout binding / currentness identity 缺口回收到 owner-delta gate。Fresh readiness 暴露的 current owner、`task_or_study_ref`、`lineage_ref`、`source_fingerprint`、payload summary refs 和 stale refs 必须 live-read；refs-only payload summary 记录/验证只证明 OPL ledger 观察到了某个 owner-shaped ref。MAS controller dry-run 已给出同一 owner lane 的 domain truth：DM-CVD 003 当前 managed envelope 是 `executable_owner_action`，由 `gate_clearing_batch` 持有 `publication_gate_replay`，required delta 是 `publication_gate_replay_delta_or_typed_blocker`，worker not live / supervisor stale，provider admission candidates 为空，runtime recovery 不允许。 | 当前 gap 仍未闭合：readiness current pointer 仍缺 owner answer，`framework operating-maturity` 仍在 `.framework_operating_maturity.summary` 显示 `current_owner_delta_owner_answer_missing=true` / `current_owner_delta_owner_answer_still_required=true`，并在 `.framework_operating_maturity.current_owner_delta_bridge` 显示 `owner_payload_summary_closure_state=verified_owner_payload_summary_observed_not_current_pointer_claim`、`observed_owner_answer_ref_shapes=["typed_blocker_ref"]`、`readiness_current_pointer_owner_answer_ref=null`。下一步不能重复消费 refs-only owner payload summary，也不能把 MAS `sat_e1063d97901cc3d70424fc5c.closeout.json#domain_blocker` 借位写成 current pointer closeout；需要 MAS owner / runtime owner / route-back owner 给出 current pointer 可消费的 owner receipt、quality gate receipt、typed blocker、human gate 或 updated currentness evidence。旧 attempt、旧 owner payload summary、未进入 readiness current pointer 的 verified ledger、provider completion、conformance pass、MAS dry-run output 和 docs foldback仍是 non-closing input。 | 当前 live owner，或 typed blocker 指回的 OPL currentness / stage-packet owner；当前 owner gate 是 `owner-gate:current_owner_delta_owner_answer`，accepted refs 是 `domain_owner_receipt_ref` / `quality_gate_receipt_ref` / `typed_blocker_ref`。OPL 只能记录/验证 refs-only summary、修正 currentness/stage-packet source truth 和投影 closeout binding，不能签 domain owner receipt、生成 domain typed blocker、授权 domain ready、执行 MAS owner-route reconcile apply 或声明 production ready。 | `opl framework readiness --family-defaults --json`、`opl framework operating-maturity --family-defaults --json`、`opl runtime domain-owner-payload-summary list --json`、MAS `domain-health-diagnostic --request-opl-stage-attempts --dry-run`、owner repo truth、currentness/stage-packet source truth。 |
| `domain_owner_chain_scaleout` | MAS/MAG/RCA/OMA 都有标准 `contracts/live_stage_run_progress_evidence.json`，OPL conformance 已能 refs-only 消费，`open_domain_count=0`。`agents conformance` 和 `framework operating-maturity` 现在把每个 domain 的 `owner_repo` / `next_owner_repo`、`closing_ref_source`、`typed_blocker_source`、`verification_commands`、`forbidden_opl_claims`、`non_closing_inputs`、`stop_loss` 和 `ready_claim_authorized=false` 上卷到 owner route。 | 这只是 `owner_typed_blocker_recorded_not_ready_claim` 或 `owner_evidence_recorded_not_ready_claim` 的可接力读面；仍缺真实 MAS paper、MAG grant、RCA visual、OMA target-agent 在 OPL-hosted path 下持续返回可关闭 stage/transition 的 owner evidence。 | MAS/MAG/RCA/OMA domain owners；owner receipt、typed blocker、human gate、quality/export/review receipt、long-soak 或 no-regression ref。 | `opl agents conformance --family-defaults --json`、`opl framework operating-maturity --family-defaults --json` 与各 domain repo live evidence contracts。 |
| `brand_module_l5_operating_maturity` | L5 evidence matrix、refs-only ledger、模块级 `l5-status` 和 `framework operating-maturity` 聚合已落地。当前 contract 按 13 个 evidence class 为每个模块定义 owner route、accepted ref shapes、command examples、false-completion guard 和 non-closing inputs；all-module `l5-status` 默认使用 compact rows，fresh readout 为 `evidence_required_module_count=10`、`l5_complete_module_count=0`、每个模块 `open_requirement_count=12`，ledger `.verified_receipt_count=3`：Runway `long_soak_recovery`、Console `release_install_evidence`、Connect `release_install_evidence`。Console/Connect 的 release/install evidence 均绑定 App-owned `v26.6.12` release-owner receipt 与 install-evidence ref，并在 work order 中显示为 `owner_evidence_recorded_not_l5_claim`。需要 requirement-level owner route 时使用 `opl brand-modules l5-status --module <module_id> --json`；全模块 compact row 的空 `requirements=[]` 不是完成信号。`framework operating-maturity` 的 owner gate 只聚合到 lane-level `brand_module_l5_operating_maturity`，保留 accepted refs、closing source、typed blocker source、forbidden claims 和 stop-loss，便于 owner 消费。 | 十个品牌模块仍全部 `evidence_required`，`l5_complete_module_count=0`；verified refs-only ledger、release-owner receipt ref、install evidence ref、owner-needed typed blocker refs、compact row、contract validation 和 App projection 只是可接力输入，不是 L5 closure；仍缺真实用户路径、跨 agent scaleout、长跑/恢复、operator repair loop、owner acceptance 和真实 owner acceptance / release / long-soak closure。 | Brand module owners；per-requirement owner evidence ref、owner acceptance ref 或 typed blocker ref。 | `opl brand-modules l5-status --json`、`opl brand-modules l5-status --module <module_id> --json`、`opl runtime brand-module-l5-evidence list --json`、`contracts/opl-framework/brand-module-l5-operating-evidence.json`。 |
| `provider_long_soak` | Runway / provider projection、provider cadence/capability readout、false-authority guard 和 public `opl runtime provider-long-soak-evidence <record|verify|list>` command group 已落地；public ledger payload 现在支持 `capability_requirement_ids`，真实 OPL state 已记录并验证 per-capability refs-only blocker receipt。 | Fresh `.framework_operating_maturity.summary` 显示 `provider_long_soak_open_count=1`，并在 owner gate 中把 provider long-soak 读为 `owner_evidence_required`：已观察到 provider blocker / typed blocker refs，但 `long_evidence_ready=false`、`capability_status=capability_slo_blocked`。本轮 readout 将 capability SLO 细化为 `capability_checklist` / `capability_missing_requirement_ids` / `capability_evidence_observed_requirement_ids` / `capability_next_evidence_action` 并回投到 `owner_route_work_orders[lane=provider_long_soak]`；真实 readout 中 `restart_requery_ready`、`signal_history_ready`、`typed_closeout_required_ready`、`missing_closeout_block_ready`、`retry_dead_letter_boundary_ready` 已从 missing ids 移到 `capability_evidence_observed_requirement_ids`，`domain_truth_boundary_preserved` 已观测，因此 `capability_missing_requirement_ids=[]` / `capability_open_requirement_count=0` / `missing_owner_action_ids=[]`。这些 refs 和 checklist 只能解释或 park 当前 evidence tail，不能声明 provider production ready，也不能关闭 MAS/domain owner answer。后续若要升级为真实 long-soak closure，仍需可重复长窗口 provider cadence/capability、domain owner-chain dispatch、retry/dead-letter、recovery、dead-letter 或 owner acceptance evidence。 | OPL runtime owner；`long_soak_ref`、`recovery_ref`、`dead_letter_ref`、`provider_blocker_ref`、`typed_blocker_ref`、`owner_acceptance_ref` 或带 `capability_requirement_ids` 的 blocker/evidence payload。 | `opl runtime provider-long-soak-evidence record --payload ...`、`opl runtime provider-long-soak-evidence verify --receipt-ref <receipt_ref>`、`opl runtime provider-long-soak-evidence list --json`、`opl runtime app-operator-drilldown --json`、`opl framework operating-maturity --family-defaults --json`。 |
| `runway_worker_currentness` | Runway read surface、worker status、provider repair guard 和 stale-worker false-authority policy 已落地；`worker_source_stale` 会 fail-closed 投影为 OPL runtime repair blocker，而不是静默使用旧 worker 源码。restart guard 暴露 active attempt status readout，让 operator 可区分 queued 与 refs-only checkpointed residue。近期 runtime 重构还把 scheduler cadence 收到 `family-runtime-temporal-provider-parts/scheduler-cadence.ts`，并把 worker readiness status resolver 变成私有 helper；外部只应消费 `buildTemporalWorkerReadiness` / lifecycle read model / CLI JSON，而不是导入内部 status resolver。 | 当前 fresh readout 仍显示 `opl runway readiness --json` 为 `blocked_provider_not_ready`，`opl family-runtime worker status --provider temporal --json` 为 `readiness_status=worker_source_stale`；`opl family-runtime scheduler status --provider temporal --json` 被同一 provider blocker 阻断为 `blocked_provider_not_ready`。provider SLO repair 仍受 active attempts guard 阻断。具体 active attempt count、status breakdown、scheduler provider payload 和 source hash 都是高漂移 live values，必须从 live CLI/read-model 读取，不在本文冻结。该 guard 不能绕过，旧 worker 不能为了加载新源码被杀掉；这只留下 OPL runtime owner 的 currentness blocker，不产生 domain progress。 | OPL runtime owner；等待 active attempts 降为 0 后由 supervised repair 重启 worker，或由 runtime owner 给出可验证 typed blocker / maintenance decision。 | `opl runway readiness --json`、`opl family-runtime worker status --provider temporal --json`、`opl family-runtime scheduler status --provider temporal --json`、`opl family-runtime provider-slo tick --provider temporal --json`。 |
| `attempt_list_audit_readout` | filtered / compact attempt timeline 已有稳定读面和数组 shape；它是当前审计 active attempts、worker restart guard 和 currentness blocker 的关键辅助面。无过滤 `opl family-runtime attempt list --json` 是 bounded / audit-safe 默认读面，focused 回归测试防止 full heavy attempt bodies 回流。 | 当前 fresh live readout 在 `.family_runtime_stage_attempts`：无过滤 attempt list 返回 `view_mode=compact_timeline` 且只返回 compact limit 内的 items；filtered status view 仍为 `view_mode=full`。`summary.total`、`compact_timeline_omitted_total` 和 per-status counters 是 runtime ledger 派生计数，后续必须重跑命令读取；该能力只保证 readout 可审计和不会因 ledger 体量爆 `Invalid string length`，不减少 active attempts，也不解除 worker restart guard。 | OPL runtime owner；bounded default readout 已落地，后续仍需 owner 处理 worker currentness、active attempts 和 provider long-soak evidence。 | `opl family-runtime attempt list --json`、`opl family-runtime attempt list --status queued --json`、`tests/src/cli/cases/family-runtime-stage-attempt-monitoring.test.ts`。 |
| `app_release_user_path` | App release/user-path evidence 已进入 maturity readout，false-positive guard 已落地；`framework operating-maturity` 现在直接暴露 same-cohort / release-owner-verdict / typed-blocker 三条互斥 refs-only path 和 `release_owner_verdict_handoff`。当前 ledger 有 10 条 verified refs-only receipts，并已观察 App-owned `release_owner_receipt_ref://one-person-lab-app/release-owner/v26.6.12/receipt-20260612-owner-verdict`、`install_evidence_ref://one-person-lab-app/release-owner/26.6.12-owner-verdict/owner-review-handoff` 和历史 release-owner typed blocker refs。Maturity 顶层把 release-owner verdict handoff 归为 `release_owner_receipt_recorded_not_release_ready_claim`，底层 same-cohort user path 为 `open_gate_count=0`、`typed_blocker_ref_count=0`、`production_user_path_ready=true`。 | Fresh readout 仍是 `release_ready_authorized=false`；App owner receipt 和 same-cohort user-path refs 只关闭 release/user-path evidence lane，不授权 App release ready / production ready。混合 success refs / release-owner refs / typed blocker refs 会 fail closed；新 cohort 未完整时继续 false。 | One Person Lab App release owner；`release_evidence_ref`、`install_evidence_ref`、`operator_evidence_ref`、`release_owner_receipt_ref` 或 `typed_blocker_ref`。 | App repo release artifacts/contracts + `opl runtime app-release-evidence record --payload ...`、`opl runtime app-release-evidence verify --receipt-ref <receipt_ref>`、`opl runtime app-release-evidence list --json`、`opl runtime app-operator-drilldown --json`、`opl framework operating-maturity --family-defaults --json`。 |
| `memory_artifact_lifecycle_apply` | `runtime app-operator-drilldown` 已在 `.app_operator_drilldown.memory_artifact_lifecycle` 顶层输出 memory/artifact/lifecycle evidence，并被 `operating-maturity` 消费为 owner follow-through route。`observed_ref_count`、handoff decision count、reconcile count、open evidence count 和 owner checklist 都是 live projection counters，只能重跑命令读取。真实 memory retrieval/writeback、accepted/rejected receipt、artifact mutation receipt、package/export lifecycle receipt、cleanup/restore/retention 对账仍缺 domain-owned receipt。 | Domain-owned surface 产生真实 memory/artifact/lifecycle receipts；OPL 不保存 body、不判定 verdict。`open_count=0`、refs observed、reconcile zero issue、OPL cleanup apply available、typed blocker ref、本轮 App 顶层投影、owner work-order checklist 或 maturity `open_evidence_count=0` 本身都不代表 memory accepted、artifact mutated、package/export ready、physical delete authorized 或 production ready。 |
| `private_platform_retirement` | default-caller deletion-evidence worklist and App drilldown cleanup-retirement are distinct live read surfaces. Their counts can drift with refs-only ledgers; the stable truth is that physical-delete read model remains refs-only and `physical_delete_authorized=false` until a domain owner decision appears. | 没有 domain owner physical delete authorization / keep-as-authority-adapter / typed blocker 时不删 retained surface；empty cleanup lane、open requirement 0、owner decision refs-only observed 或 App 顶层 projection 都不是 delete-ready。 | Domain repo owners；`physical_delete_authorization_ref`、`keep_as_authority_adapter_ref` 或 `typed_blocker_ref`。 | `opl agents default-callers --family-defaults --json`、`opl agents residue-decisions --family-defaults --json`、`opl runtime app-operator-drilldown --json`、domain repo tombstone/provenance refs。 |
| `stage_transition_authority_machine_primitive` | 目标态和 false-authority 边界已折回 active support；StageRun / closeout binding / current owner delta read models 已覆盖当前主链路。 | 完整 append-only Stage Transition Authority 机器 primitive 仍是后续实现项；目前不能把支持文档写成“Stage transition authority fully landed”。 | OPL Framework；append-only authority event log、derived current pointer、StageRun terminal state、`current_owner_delta` projection tests。 | source/contracts/tests when implemented；本文只保留目标和禁止越权口径。 |
| `docs_active_single_truth` | `docs/active/README.md` 和本文明确唯一 active owner；其余 active 文件均为 support/reference/index。 | 后续仍需防止 support 文档追加 dated proof、long checklist、worktree closeout 或第二 backlog。 | OPL docs governance；active gap 必须折回本文，dated evidence 进 history/runtime ledger。 | docs lifecycle policy、active docs scan、review。 |

## 抓大放小风险审计

这组风险从 workspace v2 的物理根 / alias / generated projection 治理问题扩展到 OPL Framework、App 和 MAS/MAG/RCA/OMA。目标不是减少约束，而是让约束只卡住真正会走歪的地方。

| risk surface | 风险 | 当前分类 | 优化口径 |
| --- | --- | --- | --- |
| `workspace_governance_detail` | profile binding、topology event、root mirror、fleet report 和 resource inventory 继续加细后，可能把 workspace 管理本身变成用户推进 stage 的前置卡点。 | `needs_demotion` for ordinary path / `meets_target` for L4 governance | Project Unit、Stage Artifact Unit、owner receipt / typed blocker 和 default `projects/<project-id>` 是大边界；alias、mirror、inventory、fleet drift 和 resource provenance 只进 validate/doctor/report，不能阻断 ordinary domain owner delta，除非它会导致错误 project、越权写入、无法 closeout 或不可恢复。 |
| `stage_admission_bloat` | StageRun Kernel / conformance 继续把 strategy refs、tool refs、quality hints 和 controlled canary 扩成 launch hard gate，会牺牲 executor 自主性。 | `needs_demotion` for advisory refs | Launch 只卡 identity、owner、scope、executor binding、authority、required role slot、receipt/blocker shape、forbidden write 和 replay/audit lineage；strategy completeness、reviewer note、rubric detail 和 optional knowledge 缺失默认 advisory / route-back。 |
| `read_model_worklist_root` | raw worklist、open counter、typed-blocker group、evidence ledger 或 provider repair lane 重新成为 planning root，会把用户带回平台修复循环。 | `meets_target` with no-resurrection watch | `current_owner_delta` 是唯一 ordinary planning root；worklist/raw refs 只支持 full detail、audit 和 queue diagnosis。open worklist 为 0 不等于完成；raw worklist 非 0 也不自动成为普通下一步。 |
| `pack_os_overclaim` | Pack descriptor valid、pack lock written、review receipt refs observed 或 provider completion 若被写成 pack quality ready、domain ready、publication ready、export ready 或 App release ready，会把 refs transport 误升为 domain authority。 | `meets_target` for transport landing / `needs_watch` for overclaim prevention | Pack OS 只保留 descriptor、lock、content hash refs、artifact lifecycle refs、review receipt refs、provenance 和 false-authority boundary。任何 quality/export/review verdict、artifact body mutation、owner receipt、typed blocker、publication/grant/visual/App readiness 仍必须回 domain/App owner。 |
| `stage_transition_authority_split_brain` | domain module、provider observation、App cockpit 或 read-model projection 如果能直接写 current pointer / terminal state / owner delta，会重新制造多 truth source。 | `active_foldback_target` | 多模块只提交 transition intent、owner answer、typed blocker、human gate 或 provider observation；Stage Transition Authority 是 OPL-owned generic primitive，只有 append-only authority event log 可派生 Stage current pointer、StageRun terminal state 和 `current_owner_delta`。 |
| `domain_adoption_contract_depth` | 标准 agent pack、stage operating principles、series profile、controlled canary 和 L5 evidence matrix 过细后，可能替代真实 domain owner evidence。 | `needs_live_owner_evidence` | Structural conformance 只保证不走歪；domain progression 仍只由 owner receipt、quality/export/review receipt、human gate、typed blocker、no-regression ref 或 long-soak ref 关闭。 |
| `cleanup_delete_gate` | wrapper / facade / alias / helper 清理门如果进入 ordinary progress，会让交付线被 delete accounting 抢占。 | `needs_retirement` but explicit lane only | Cleanup lane 保留 replacement parity、no-active-caller、owner decision、no-forbidden-write 和 tombstone/provenance；只有显式进入 cleanup lane 或 domain owner 给出 physical delete authorization / keep / typed blocker 时执行。 |
| `app_release_evidence_detail` | App release cohort、Homebrew checksum、clean-VM、operator bundle 和 updater detail 过多后，可能污染 App 普通 cockpit 或 domain readiness。 | `needs_release_user_path_evidence` | Release gate 归 App release owner；ordinary cockpit 只显示 release owner blocker/receipt 和 user-path fact，不把 release diagnostics 推成 domain / framework next action。 |
| `agent_lab_improvement_loop` | Agent Lab 的 eval、root cause、variant、promotion 和 rollback refs 如果升格为 domain verdict，会制造第二质量权威。 | `needs_demotion` unless owner work order accepted | Agent Lab 只产出 improvement candidate、work order、acceptance evidence 或 typed blocker；domain owner 仍决定 artifact、quality/export/review verdict 和 production acceptance。 |
| `foundry_lab_rho_workflow_backend` | RHO backend 与 dynamic workflow 需要从 plan/template 口径收紧为可执行后端 / runner，同时保留 no-authority 边界。 | `contract_acceptance_landed` / `needs_fresh_execution_receipts` | `contracts/opl-framework/agent-lab-contract.json#rho_backend_surface` 定义 RHO 为 `executable_no_apply_harness_backend`；`#dynamic_workflow_runner_surface` 定义 workflow 为 `executable_suite_topology_work_order_runner`。验收要求 required artifacts、readiness gate、forbidden claims、authority boundary 和 end-to-end acceptance 均为 machine-readable；后续 production 或 L5 只能由 fresh `opl agent-lab rho run --project <dir> --sessions <codex-sessions-dir> --output <rho-run-dir> --json`、`opl agent-lab workflow-template run --template <id> --project <target-agent-dir> --output <workflow-run-dir> --json`、work-order execution receipt、target owner receipt / typed blocker 关闭，不能由 docs、contract pass、RHO winner、candidate diff、runner receipt 或 template pass 声明 domain ready、owner receipt、runtime substrate、ordinary workflow compiler、default promotion 或 production ready。 |
| `provider_long_soak_ops` | Temporal worker、SLO、supervisor、retry/dead-letter 和 production proof 可能重新抢占 domain owner answer。 | `production evidence lane` | Provider liveness 缺 provider attempt / lease / authorization 时可以成为 OPL runtime blocker；只缺 domain owner answer 时，provider ops 退到 full detail / production evidence。 |

新增或复审任何 surface 时，必须显式回答五个问题：它保护哪个大边界、默认 lane 是什么、什么条件升级为 hard blocker、什么条件降为 advisory / audit、accepted owner answer shape 是什么。答不出时，默认不能进入 ordinary path。

## 功能 / 结构差距

| area | 当前状态 | 当前 owner | 下一步 |
| --- | --- | --- | --- |
| `framework_control_plane` | `landed_with_evidence_tail`。Temporal provider、typed queue、stage attempt ledger、safe action shell、refs-only evidence ledger、App/operator drilldown、Agent Lab、pack compiler、owner-delta handoff / compact projection、default read surface policy 和 conformance/readiness 读面已经是 framework 主干。 | OPL Framework | 保持同源 read model；新增 CLI/App/docs surface 必须从同一机器 payload 派生，不制造第二 truth。 |
| `brand_module_taxonomy_and_maturity` | `meets_target_for_top_level_awareness` / `l5_evidence_matrix_landed` / `l5_refs_only_ledger_landed` / `needs_live_operating_evidence`。`docs/references/brand-modules/*` 已给出当前十模块 north-star，核心五件套已把 `Charter / Atlas / Workspace / Pack / Stagecraft / Runway / Vault / Console / Foundry Lab / Connect` 上提为 OPL Framework 顶层 taxonomy。当前十模块都有 registry + CLI governance + module-owned read-only command surface：非 Workspace 模块走 `opl <brand-module> status|inspect|interfaces|validate|doctor --json`，Workspace 只新增 `opl workspace status|inspect --json` 作为品牌读面；domain-agent internal module spine 由 `opl agents modules * --json` 投影。`OPL Pack` 承接 domain pack、authority ABI、pack compiler 和 generated/hosted surfaces 的独立边界。Foundry Lab 本轮把 RHO `executable_no_apply_harness_backend` 与 dynamic workflow `executable_suite_topology_work_order_runner` 写入 `contracts/opl-framework/agent-lab-contract.json` 和验收测试，但这只关闭 contract/docs acceptance，不关闭 live operating evidence。L5 operating-evidence gate 已由 `contracts/opl-framework/brand-module-l5-operating-evidence.json`、`opl brand-modules l5-status|l5-validate|l5-interfaces --json`、`opl <module> l5-status --json` 和 `opl runtime brand-module-l5-evidence record|verify|list --json` 机器化；ledger 只回投 observed / verified refs count，不改 contract completion。当前 contract 已记录 40 条 OPL-controlled evidence refs 与 10 条 owner acceptance typed blocker refs，`.brand_module_l5_status.evidence_ledger.verified_receipt_count=3`：Runway `long_soak_recovery`、Console `release_install_evidence`、Connect `release_install_evidence`。`l5_complete_module_count=0`，十个模块仍全部 `evidence_required`。 | OPL Framework docs governance + module owners | 下一步只推进真实 L5 运营证据：`Pack` 走 cross-agent pack/ABI/generated-surface owner acceptance，`Console` 继续补 App user path / operator repair / owner acceptance，`Foundry Lab` 走 agent improvement loop、RHO/workflow fresh execution receipts、work-order acceptance 和 target owner receipt / typed blocker，`Connect` 继续补 install/release drift matrix与 owner acceptance，`Runway` 继续补 provider worker/supervisor 长窗口、operator repair loop、owner acceptance 与 no-second-truth evidence。不把品牌文档、contract validation、conformance、provider completion、verified ledger、contract refs-only evidence、RHO winner、candidate diff、workflow runner receipt、template pass、App projection 或 CLI governance pass 写成 production maturity。 |
| `brand_system_profile_freeze` | `machine_contract_landed` / `needs_design_system_evidence`。`contracts/opl-framework/brand-system-profile.json` 已把三层产品认知、品牌模块 product grammar、Foundry Agent 命名、App 状态语言、visual pattern group、receipt/blocker 文案和 false-authority boundary 冻结为 required framework contract；`loadFrameworkContracts`、`contract validate` 和品牌模块 focused tests 已把该 profile 纳入默认 contract summary。 | OPL Charter + Console + module owners | 后续 UI token、icon/card/status pattern、App 文案和各 agent public surface 必须从该 profile 对齐；profile validation 只证明品牌系统基线可机器读取，不声明 App release ready、L5 maturity、domain ready 或 production ready。 |
| `standard_agent_source_shape` | `structural_clean_with_no_resurrection_guard`。MAS/MAG/RCA/OMA 按标准 Foundry Agent 读为 source-purity / default-caller deletion-evidence clean；这不是 physical delete 授权。 | OPL + domain owner | 后续只按 domain owner receipt / typed blocker、no-forbidden-write、tombstone/provenance 和 no-active-caller 证据执行物理删除；不保留兼容 facade、alias、wrapper 或 compatibility-only test。 |
| `standard_agent_pack_abi` | `frozen_machine_verifiable_baseline`。Standard Agent Pack ABI 已进入 `standard-domain-agent-skeleton-contract.json`、pack compiler input projection、stage pack v2 validation、scaffold generator 和 conformance tests；必需 repo layout 固定为 `agent/`、`contracts/`、`runtime/authority_functions/` 等标准边界，L4/L5 gate 继续明确 conformance pass 不能算 L5。 | OPL Foundry Lab + pack compiler | 新 agent 或 domain repo 迁移必须先通过 ABI / stage pack v2 gate；ABI 只冻结标准源码和 authority function 入口，不替 domain 产生 owner receipt、quality/export verdict、artifact authority 或 production acceptance。 |
| `standard_agent_admission_gate` | `machine_contract_landed` / `live_probe_landed`。`contracts/opl-framework/standard-agent-admission-gates.json` 已把 candidate standard OPL domain agent 的 formal admission package 冻结为 identity、domain truth owner、generated default entry、standard pack ABI、stage artifact contract、execution model、authority boundary、owner receipt boundary、typed blocker boundary 与 human gate false-authority 十个 gate。`opl agents conformance --family-defaults --json` 现在输出 `family_live_conformance_probe`，把 generated interfaces、action catalog、stage plane 与 admission gate 合成同一 live readout。 | OPL Framework + candidate domain owner | 新 agent / existing agent re-admission 必须按该 gate 提供 refs；gate/probe pass 只允许 formal inclusion 或 live gate readout，不声明 domain ready、production ready、quality verdict、artifact authority、owner receipt、typed blocker 或 physical delete authorization。 |
| `generated_surface_consumption` | `default_entry_baseline_landed` / `source_of_work_lineage_landed` / `no_resurrection_gate_landed` / `ready_for_production_consumption_scaleout`。Generated/hosted surfaces、domain handler target、pack compiler 与 conformance surface 已能承接标准 agent。`domain-pack-compiler-contract.json` 已声明 `default_entry_policy`、`source_of_work_lineage` 与 `generated_default_entry_no_resurrection_gate`，默认派生面包括 `cli`、`mcp`、`openai_tool`、`ai_sdk`、`skill_plugin`、`app_action`、`status_read_model` 和 `workbench`；`family-action-catalog.schema.json` 要求 action catalog 至少声明 `cli`、`mcp`、`skill`、`product_entry`、`openai`、`ai_sdk` 六类 surface slot，并可携带 canonical `source_of_work` lineage。App/workbench canonical metadata feed 是 `opl agents interfaces --family-defaults --format product-entry`；`opl agents pack-compiler --family-defaults` 只做同一批默认标准 agent repo contracts 的 generated-surface projection / drift 诊断，默认 `opl agents pack-compiler` 仍是 admitted manifest 诊断面；`opl framework readiness --family-defaults` 的 pack compiler section 必须消费前者，不能把默认诊断面投成 framework hard blocker。 | OPL generated surface owner | 用真实 MAS/MAG/RCA/OMA default caller、direct skill path、App path 和 owner refs 反复验证；不把 descriptor ready、family-default feed ready、generated default policy ready、source-of-work lineage ready、no-resurrection gate ready 或 pack-compiler ready 写成 domain ready、App GUI 完成证据或 production caller migration。 |
| `app_workbench_runtime` | `ordinary_cockpit_default_landed` / `needs_release_user_path_evidence`。App/workbench 能消费 provider readiness、stage attempt、StageRun cockpit、route graph、workorder、SLO、repair、source/artifact/memory refs 和 owner-aware action；StageRun cockpit 只从 `current_owner_delta` 派生 launch/closeout/advisory/authority 投影。`opl app state --profile fast --json` 的 default operator payload 已收薄为 `ordinary_cockpit`，普通读面只保留 purpose、task、current owner、next action、artifact 或 blocker；provider、ledger、raw receipts、worklist、MCP 和 release evidence 留在 full/developer detail。`runtime app-operator-drilldown` 顶层现在也有同源 owner-delta / StageRun cockpit alias，full refs 仍通过 `attention_first_payload` 和 `--detail full` 下钻。 | OPL App + OPL product surface | App 只展示和介入，不持有 runtime truth、domain truth、release verdict、owner receipt、typed blocker 或 artifact authority；App release truth 回 App 仓合同、release artifact 和实机 evidence，release diagnostics 不回到 ordinary cockpit。 |
| `domain_private_residue_retirement` | `private_platform_cleanup_gate_landed` / `owner_decision_ledger_landed`。旧 repo-local scheduler、runner、queue/session store、status/workbench shell、sidecar、domain wrapper、runtimeWatch、agent-lab materializer、generic wrapper、compat path 只能作为迁移输入、diagnostic 或 history/tombstone。`wrapper-retirement-gate-policy.json`、`functional_privatization_audit.modules[].private_platform_residue_gate` 与 `private-platform-residue-owner-decisions.json` 已把 cleanup lane 拆成 `retain_authority_function`、`absorb_opl_primitive`、`no_active_caller_delete`、`tombstone`、`owner_typed_blocker` 五类处置；`opl agents residue-decisions --family-defaults --json` 投影 refs-only owner decision ledger。 | OPL cleanup gate + domain owner | 满足 replacement parity、no-active-caller、owner receipt / typed blocker、no-forbidden-write 与 provenance 后直接删除或 tombstone；不新增兼容入口。该 gate 只给 operator 提供分类和 refs-only owner decision shape，不授权 OPL 物理删除，也不声明 domain ready 或 production ready。 |
| `docs_lifecycle` | `active_docs_not_ledger`。Core five、active plan、reference、history 的职责已经明确，但 active 文档仍需持续防止 dated evidence 和长清单回流。 | OPL docs governance | Active 只保留 owner / current state / gate / next action；dated proof、coverage tranche、receipt 流水、branch/worktree closeout 进入 `docs/history/**`。 |
| `ideal_operating_model_redesign` | `reference_folded_back`。跨 OPL/App/MAS/MAG/RCA/OMA/support repo 的理想 operating model 已按 `目的反推必要性，MVP 检查阻碍性` 折成 `meets_target` / `needs_demotion` / `needs_retirement` 审计口径，并完成本轮 App cockpit guard、MAS owner-currentness、MAS terminal handoff callable、support entry demotion 和 active line consolidation。该 redesign doc 已非 active backlog owner，只保留评估口径；当前 owner / gate / next action 由本文维护。 | OPL Framework + domain/App owners | 后续只保留 domain owner evidence、App release/user-path cohort、OMA script-to-pack machine gate、support no-authority no-resurrection tail；不新增第二 worklist、第二 App bridge 或第二 Agent Lab；不把 docs foldback 写成 domain ready、App release ready、production ready 或 physical delete 授权。 |

## Active Docs / Gate Foldback

| foldback item | current owner | gate | next action |
| --- | --- | --- | --- |
| `docs_foldback` | OPL docs governance | `current-state-vs-ideal-gap.md` 是唯一 active owner；`opl-family-ideal-operating-model-redesign.md` 只保留 `active_reference` 评估口径；support docs 不维护独立 backlog、dated counter、branch/worktree closeout 或执行顺序。 | 后续 docs-only lane 只把 current owner / current state / gate / next action 折回本文；dated proof、coverage tranche、receipt 流水和 closeout 进入 `docs/history/**`、runtime ledger、提交历史或 domain-owned provenance。 |
| `ideal_top_level_redesign_owner` | OPL docs governance + OPL Framework | `TASTE.md` 持有长期 taste，`docs/decisions.md` 持有决策口径，`surface-budget-policy.json` 持有机器预算，`target-operating-architecture-contract.json` 持有目标态 compiler policy 镜像，本文持有 active owner / gap / next action。抓大守 owner、authority、stage、workspace、executor、single ordinary route、admission、accepted owner answer shape、App release、delete 和 no-second-truth；放小把 advisory / audit / diagnostic / cleanup / production evidence 留在显式 lane。 | 后续 redesign、support doc 或 contract 只能折回这些 owner 面；新增 surface 先回答 protected boundary、default lane、upgrade/demotion condition 和 accepted answer shape，不能让 diagnostic、cleanup 或 production evidence tail 抢占 `current_owner_delta`。 |
| `stage_transition_authority_docs_foldback` | OPL Framework | Stage Transition Authority 已作为目标态 foldback 进入 active/support docs：它是 OPL-owned generic primitive，多模块可提议、单 authority 裁决，Stage current pointer / StageRun terminal state / `current_owner_delta` 从 append-only authority event log 派生。 | 后续若落机器合同或源码，只能把 docs 语义转成 OPL-owned primitive；不把 docs foldback、provider observation、transition intent、App projection 或 conformance pass 写成 domain ready、production ready、App release ready 或 owner receipt authority。 |
| `domain_wrapper_delete_gate` | OPL cleanup gate + domain owners | `contracts/opl-framework/wrapper-retirement-gate-policy.json` 要求 replacement parity、no-active-caller、domain owner receipt 或 typed blocker、no-forbidden-write、tombstone/provenance；docs foldback、delete-gate read-model、conformance pass、tests pass、default-caller readiness 和 refs-only observed 都不是 physical delete authority。 | 逐 surface 由 domain owner 提供 cleanup receipt、typed blocker、no-regression ref 或 explicit physical delete owner receipt；OPL 只记录 refs、投影 false authority flags 和 no-resurrection guard，不物理删除 domain wrapper。 |
| `real_owner_delta_tail` | MAS/MAG/RCA/OMA domain owners + OPL runtime | 功能面 callable / StageRun / conformance / refs-only transport / strict identity preflight / StageRun cockpit closeout binding 已收口；anti-loop stop-loss successor admission 已从 `task_progress_first_anti_spin_blocked` 的明确 successor policy 派生：同一 exhausted work-unit / fingerprint redrive 保持 blocked；identity-different `publishability_repair_sprint`、domain-owned typed blocker、human / operator gate 或 provider hard-gate clearance 才能进入 queue event 与 `current_control_state.stop_loss_successor_admission` read model。live domain progression evidence 仍必须来自 domain-owned owner receipt、typed blocker、human gate、quality/export/review receipt 或 no-regression ref。 | OPL 继续把 provider attempt / active lease / execution authorization decision / closeout binding / stop-loss successor admission guard 作为 refs-only ledger 和 cockpit 投影，不替 domain 创建 receipt、typed blocker 或 readiness claim。MAS/domain owner 后续处理 paper readiness surface 并给出 owner receipt、typed blocker、human gate、quality gate receipt 或 route-back evidence；MAG/RCA/OMA 下一步分别提供真实 grant / visual / target-agent owner evidence，不用 OPL docs、provider lane 或 refs-only ledger 替代。 |

## Operating Model Foldback 状态

| lane | 状态 | 当前 owner | 下一步 |
| --- | --- | --- | --- |
| `multi_plane_os_split` | `active_baton_landed_in_this_doc`。redesign 文档只定义 north-star / 验收标准；本文承接 ordinary progress、durable runtime、artifact、authority decision、telemetry、reconciler、App cockpit 和 improvement 平面的 active gap / owner / next action。 | OPL docs governance | 新增或复审 plane 时写清 default lane、owner、source of truth、accepted answer shape、forbidden OPL claims 和验证命令；不在 redesign 文档维护 backlog。 |
| `runway_console_vault_false_authority` | `meets_target_for_boundary_wording` / `needs_machine_regression_when_surface_changes`。Runway 只输出 provider readiness、desired/current reconcile、handoff gate、repair plan、唯一下一 safe action 或 OPL runtime blocker；Console 只展示 `current_owner_delta`、owner action 和 refs；Vault 只保存 refs-only evidence / telemetry / audit packet。 | OPL Runway + Console + Vault owners | 后续任何 `runway*`、`app state`、`operator drilldown`、`evidence-worklist`、`brand-module-l5` 或 Vault ledger 变更都要证明它不签 owner receipt、不创建 typed blocker、不判 quality/export/review、不写 domain truth、不声明 ready。 |
| `current_owner_delta_single_ordinary_route` | `meets_target_for_default_root`。ordinary App/CLI/operator path 只能从 fresh `current_owner_delta` 读取当前 owner、required delta、accepted answer shape 和 hard gate；provider/transport safe action 只有在没有 current owner delta 时才可成为默认下一步。 | OPL Console + Stage Transition Authority | 继续防止 raw worklist、provider trace、telemetry、release cohort、L5 evidence、cleanup work order、stale cache 或 external-learning refs 变成 ordinary planning root。 |
| `opl_base_optimization` | `target_state_folded_back`。基座优化只保留能缩短 owner delta、保护 authority、承载 durable execution、物化 Stage Artifact Unit、产生 safe action 或保存 passive evidence 的 primitive。 | OPL Framework module owners | 对每个新增 primitive 先走 surface budget；domain owner answer、typed blocker、quality verdict、ready declaration 和 physical delete authorization 始终回 domain/App/brand owner。 |
| `summary_de_noise` | `meets_target_for_framework_default`。App fast state、Aion shell default payload 和 surface-budget contract 已把首屏答案收敛到 owner delta / next action / hard gate；`count_summary` 只作为 diagnostic-only answer。 | OPL Framework | 后续新增 summary 必须继承同一 policy；raw count、replay、typed-blocker group 和 private residue 不能回到 first screen。 |
| `current_owner_delta_cutover` | `meets_target_for_default_root` / `meets_target_for_execution_authorization_single_root_guard` / `meets_target_for_projection_cache_currentness_guard`。`current_owner_delta` 是 App/CLI/operator 默认 root；`compact_owner_delta_projection` 不再作为 active compatibility alias，audit/full-detail refs 由 `current_owner_delta_read_model` 承载。当前 `framework readiness`、App drilldown 和 evidence-worklist 的顶层 owner-delta topline 已同源暴露 owner、domain、stage、payload requirement、三种 accepted answer shape、default next action 和 default next-action authority boundary，避免 operator 看到 null alias、strict typed-blocker fallback、review attention safe action 或 handoff/audit tail 的宽泛 return-shape union。StageRun blocker 缺 provider attempt / active lease / execution authorization decision 时，默认 operator next action 指向 owner=`one-person-lab` 的 OPL runtime blocker；若这些 refs 已存在、只缺 owner answer / closeout binding refs，默认 operator next action 回到 `current_owner_delta.current_owner`，StageRun cockpit 只保留 refs-only closeout binding gate。App fast fallback 的 `current_owner_delta_read_model` cache 只在 source surface、时效和 currentness identity 匹配时可用；stale cache miss 会要求刷新 read model，而不会替代 `current_execution_envelope` / fresh owner delta。 | OPL Framework | 继续让 downstream App contracts 和 support shell 消费 `current_owner_delta`，不再推广 `compact_*` 命名；broader domain success refs 只表达 accepted owner answer shape，不授予 OPL receipt/blocker authority。 |
| `stage_transition_authority_foldback` | `active_support_docs_landed` / `needs_machine_primitive_when_implemented`。目标态已明确为多模块可提交 transition intent、owner answer、typed blocker、human gate 和 provider observation，但只有 OPL Stage Transition Authority 从 append-only authority event log 派生 Stage current pointer、StageRun terminal state 和 `current_owner_delta`。该 foldback 只收紧目标边界，不声明 live domain progression、production ready、App release ready 或 current machine primitive 已完整落地。 | OPL Framework | 后续机器落地时保持 controller desired/status、Temporal event history/message passing、event sourcing append-only stream 和 structured agent handoff 的映射；domain truth、receipt authority、typed blocker authority、quality/export/review verdict 仍回 MAS/MAG/RCA/OMA owner。 |
| `active_line_consolidation` | `meets_target_for_docs_owner`。`current-state-vs-ideal-gap.md` 是唯一 active owner；路线图、production closure matrix、purpose-first audit、target architecture、ideal operating model、docs portfolio 和 private platform inventory 只保留 active support / support index / classification role。 | OPL docs governance | active docs 只写当前 owner / gate / next action；support docs 不维护独立 backlog、执行顺序、dated counter 或 worktree closeout；dated proof、receipt 流水和 closeout 进 history 或 runtime ledger。 |
| `domain_wrapper_delete_gate` | `owner_decision_refs_observed_not_delete_authorized` / `app_drilldown_cleanup_retirement_projection_landed` / `meets_target_for_cleanup_lane_demotion`。Machine-readable delete gate、default-caller audit、physical-delete read model、repo deletion gate summary 和 App drilldown `.cleanup_retirement` 已把 replacement/no-active-caller/no-forbidden-write/tombstone/provenance 前置证据、delete/keep prerequisite 状态、accepted refs-only result shape 与 false-authority flags 暴露到顶层；这些仍只是 cleanup lane 输入。 | OPL cleanup gate + domain owners | 普通 owner-delta 路径只显示 deliverable / runtime hard gate；cleanup lane 显式进入后由 domain owner 对每个 surface 给出 physical delete authorization、keep-as-authority-adapter 或 typed blocker ref。没有 physical delete authorization 时继续保留或 tombstone，不由 OPL 删除。 |
| `real_owner_delta_tail` | `meets_target_for_stage_run_closeout_transport` / `meets_target_for_mas_current_control_provider_admission_currentness` / `needs_live_domain_progression_and_scaleout_tail`。StageRun closeout binding、strict identity preflight、provider admission currentness 和 App/Framework cockpit read model 已能把 OPL transport/read-model 缺口收回当前 owner delta；同一 dedupe-key 下 stale `queued` / `waiting_approval` MAS current-control admission 已能被 fresh work-unit/source currentness identity 原地刷新，且保留既有 approval gate。post-refactor currentness identity 已把 provider admission `route_identity_key` / `attempt_idempotency_key` 与 selected stage packet identity 升为必需字段；`dispatch_ref` 继续作为 dispatch 载荷 / 诊断 ref，但不能兜底为 `stage_packet_ref`。通用 `idempotency_key` 不能兜底 provider attempt identity；缺显式 route identity、attempt identity、`stage_packet_ref` 或非空 `stage_packet_refs` 时 current-control admission fail closed，并抑制同 study stale sidecar default-executor pending task；StageRun live skip / terminal closeout reconcile 仍要求 selected dispatch / stage packet identity 同时匹配。refs-only checkpoint 缺 launch authorization refs 时投影 `blocked_missing_launch_execution_authorization`，只能走 provider-transport redrive 补齐 OPL launch authorization refs；补齐后仍投影为 `checkpointed_refs_only_domain_handler_receipt`，不能当 running proof 或 domain owner receipt。fresh readiness 仍指向 MAS owner-answer / typed-blocker gate，domain-dispatch route 仍要求 domain/App payload。MAG/RCA/OMA 的 owner-chain canary、blocker 或 human-gate evidence tail 只作为各自 domain owner 输入。 | MAS/MAG/RCA/OMA domain owners + OPL runtime | OPL 只承载 provider attempt、active lease、execution authorization decision、closeout binding、refs-only ledger、queued admission refresh 和 cockpit 投影，不写 domain truth、不生成 owner receipt 或 typed blocker、不刷新 publication eval / artifact gate / package，也不把 provider completion解释为 domain completion。后续仍需 domain owner 产出 owner receipt、quality gate receipt、typed blocker、human gate、route-back evidence 或 no-regression evidence。 |
| `app_contract_compaction` | `meets_target_for_contract_guard` / `release_user_path_evidence_refs_observed_not_release_ready`。App ordinary cockpit surface budget、release-boundary guard、release-owner typed blocker path 和 App drilldown evidence 已进入 OPL maturity 输入；这些只防止 release/user-path false-positive。OPL App release evidence ledger 现在把 same-cohort user-path success refs、release-owner verdict refs、typed blocker refs 三条 path 拆开：`install_evidence_refs` 属于 release-owner verdict path，可被 maturity 读取为 owner evidence，但不关闭 release-ready。 | One Person Lab App | App release/user-path refs 只关闭 cohort evidence lane；install evidence、release-owner receipt refs 或 typed blocker refs 必须继续回 App release owner verdict / receipt / blocker 关闭 release-ready，不能进入 ordinary cockpit，也不由 OPL Framework 声明。 |
| `oma_script_to_pack_hygiene` | `meets_target_for_script_gate_coverage`。OMA docs 已改成 fresh-read / live evidence / no second Framework 口径；repo-local TS scripts、developer work-order policy、standard Foundry policy bundle 和 stage-decomposition parts 已进入 contract-backed / split-part machine gate。 | OPL Meta Agent + OPL Framework | 后续新增或继续稳定下来的 policy 继续进入 `agent/`、contracts、authority functions 或 OPL primitive；retained helper/materializer 退役仍需 OPL primitive parity、no-active-caller、no-forbidden-write 和 tombstone/provenance refs，新增/收薄脚本必须更新 machine gate。 |
| `support_entry_clarity` | `meets_target_for_support_entry_boundary`。Aion shell 已按 App contract carrier 处理 initial-message warmup / Full runtime trust；OPL Doc 强化 doctor/profile/family-plan no-authority boundary。 | App shell / OPL Doc support owners | support repo 保持不反向定义 OPL/App/domain truth；后续只防止 implementation detail 或 doctor-clean 重新进入 readiness 叙事。 |

## 测试 / 证据差距

| gap | 仍缺什么 | 完成口径 |
| --- | --- | --- |
| `domain_owner_chain_scaleout` | 真实 MAS paper、MAG grant、RCA visual 和 OMA target-agent stage 在 OPL-hosted path 下持续返回 owner receipt、typed blocker、human gate、quality/export/review receipt 或 no-regression evidence。 | Domain-owned receipt / typed blocker 关闭对应 stage / transition / owner-chain 缺口；OPL 只承载 transport、ledger 和 projection。 |
| `brand_module_l5_operating_maturity` | `l5_evidence_matrix_landed` / `l5_refs_only_ledger_landed` / `needs_live_operating_evidence`。十个品牌模块的 L5 证据矩阵、contract validation、聚合 CLI/read-model、模块级 `l5-status` 和 runtime refs-only ledger 已落地；fresh `l5-status` 在 `.brand_module_l5_status` 下仍显示 `evidence_required_module_count=10`、`l5_complete_module_count=0`、`.evidence_ledger.l5_claim_status=ledger_refs_only_not_l5_claimed`。本轮 contract 记录的可消费 owner-evidence refs 覆盖 `current_owner_delta_default_read`、`domain_authority_false_boundary`、`no_second_truth_regression`、`pack_compile_parity` 四类 requirement，并把每个模块其余 requirement 标成 owner-needed typed blocker。runtime ledger 已验证 3 条 receipt：Runway `long_soak_recovery`、Console `release_install_evidence`、Connect `release_install_evidence`；Console/Connect 的 release/install evidence 复用 App-owned `v26.6.12` release-owner receipt 与 install-evidence ref，仍为 `owner_evidence_recorded_not_l5_claim`。`foundry_agent_os_production_evidence_gate.owner_route_work_orders[lane=brand_module_l5_operating_maturity]` 现在把 130 条 per-requirement work order 折成 10 个模块级 `owner_action_checklist`、130 个 `missing_owner_action_ids` 和 `next_evidence_action=record_or_resolve_brand_module_l5_owner_evidence_for_missing_module_requirements`，让 active-goal 后续候选可直接按模块 owner evidence / typed blocker / acceptance ref 推进。 | L5 只能由模块 owner 提供可重复的 live path、release/install、long-soak、owner receipt / typed blocker / acceptance 和运营闭环证据关闭；Workspace L4、docs foldback、contract/conformance pass、provider completion、verified ledger、accepted module id guard、owner-route work order、top-level owner checklist、contract refs-only evidence、App projection 或 App release-owner receipt ref 只能作为 L5 输入，不能作为 L5 结论。 |
| `stage_artifact_unit_domain_adoption` | MAS stage-native materializer、terminal owner callable、route/policy/lifecycle contract 和 OPL refs-only dispatch transport 已落到可验证主干；MAG/RCA/OMA 已补齐 advisory cognitive-kernel adoption、tool affordance refs、golden path profile 和 stage-level independent gate policy。 | MAS stage folder closure、terminal owner callable、OPL refs-only dispatch transport 和 StageRun closeout read-model 只关闭 missing-output / missing-callable / recordability / transport tail；terminal publication handoff是否完成仍必须回 MAS live owner receipt / typed blocker / human gate。MAG/RCA/OMA 下一步是把这些 stage-internal declarations跑成真实 owner receipt / typed blocker / no-regression evidence，而不是再补 generic wrapper。 |
| `pack_os_domain_pack_transport` | `meets_target_for_framework_transport`。OPL Pack OS 已能从 generic capability pack descriptor 派生 refs-only pack lock、registry entry、content-addressed cache manifest 与 distribution bundle，并运输 artifact lifecycle refs 与 review receipt refs；MAS Display Pack v2 等 domain-declared refs 可以作为 transport input。 | Pack OS install、registry、cache、distribution、验证、lock 写入和 review receipt refs 只证明 transport/read-model 可用；真实 MAS publication figure quality、MAG grant quality、RCA visual export quality、artifact body mutation、owner receipt、typed blocker、App release verdict 或 production maturity 仍由对应 owner 关闭。 |
| `stage_native_kernel_rollout` | OPL 标准层已有 StageRun Kernel contract、StageRun profile / controlled canary conformance、App/operator cockpit、execution authorization gate、closeout binding read model、stage replay binding gate 和 complete StageRun currentness identity guard；四个 domain 的标准接入证明仍是 controlled evidence shape，不是 live domain progression。currentness guard 要求 provider admission route identity、attempt idempotency 与 selected dispatch / stage packet identity 同时存在并匹配。 | Controlled canary evidence、StageRun adoption read model、App cockpit、complete currentness identity guard 和 closeout-binding gate 关闭标准接入与 false-authority 投影缺口。真实 domain progression 仍要由 MAS/MAG/RCA/OMA 的 live owner receipt、typed blocker、human gate、quality/export/review receipt 或 no-regression evidence 关闭；满足 role artifact / owner receipt / typed blocker / no-active-caller / tombstone gates 后再退役旧 progress/read-model/controller compensation path。 |
| `app_operator_drilldown_source_boundary` | `maintenance_slice_landed`。`runtime-tray-app-operator-drilldown.ts` 现在只保留约 `612` 行稳定入口，core refs 已移动到 `runtime-tray-app-operator-drilldown-parts/core-refs.ts`，fresh `family:structure-advisory` 不再把该入口列入 design-pass queue。 | 该条只证明 source boundary 更易维护；不改变 App drilldown 的 refs-only 投影性质，不授权 memory/artifact body mutation、cleanup physical delete、App release-ready、Brand L5 或 production-ready。 |
| `family_runtime_enqueue_source_boundary` | `maintenance_slice_landed`。`existing-dedupe-reconcile.ts` 已通过行为保持的语义拆分从 `989` 行降到约 `712` 行；新增 `existing-dedupe-requeue.ts` 约 `118` 行封装 existing-task requeue apply 逻辑。fresh scoped `family:structure-advisory --repo one-person-lab=<current checkout>` 显示 OPL 自身 `needs_design_pass=0`、`mechanical_residue=0`、`public_surface_risk=8`。 | 该 lane 只服务可维护性和 currentness/reconcile 源码边界；完成并验证后也不能替代 owner evidence、Brand L5、App release-ready、provider long-soak、memory/artifact lifecycle receipt、physical delete authorization 或 production-ready。 |
| `memory_artifact_lifecycle_apply` | `runtime app-operator-drilldown` 已在 `.app_operator_drilldown.memory_artifact_lifecycle` 顶层输出 memory/artifact/lifecycle evidence，并被 `operating-maturity` 消费为 owner follow-through route。`observed_ref_count`、handoff decision count、reconcile count、open evidence count 和 owner checklist 都是 live projection counters，只能重跑命令读取。真实 memory retrieval/writeback、accepted/rejected receipt、artifact mutation receipt、package/export lifecycle receipt、cleanup/restore/retention 对账仍缺 domain-owned receipt。 | Domain-owned surface 产生真实 memory/artifact/lifecycle receipts；OPL 不保存 body、不判定 verdict。`open_count=0`、refs observed、reconcile zero issue、OPL cleanup apply available、typed blocker ref、本轮 App 顶层投影、owner work-order checklist 或 maturity `open_evidence_count=0` 本身都不代表 memory accepted、artifact mutated、package/export ready、physical delete authorized 或 production ready。 |
| `provider_long_soak` | `operating-maturity` 现在消费 provider cadence window / capability projection 与 provider long-soak refs-only ledger，并在 provider long-soak owner gate 暴露可执行 first slice：OPL runtime owner 用 `opl runtime provider-long-soak-evidence record --payload ...` 记录 `long_soak_ref` / `recovery_ref` / `dead_letter_ref` / `provider_blocker_ref` / `typed_blocker_ref` / `owner_acceptance_ref`，也可用 `capability_requirement_ids` 把 provider blocker / typed blocker / refs-only evidence 绑定到具体 capability requirement，再用 `verify --receipt-ref` 验证，最后用 `list --json`、App/operator drilldown 和 maturity readout 回读。本轮真实 OPL state 已新增并验证 per-capability refs-only blocker receipt；fresh readout 为 `.framework_operating_maturity.summary.provider_long_soak_open_count=1` / `provider_long_soak.open_evidence_count=1`，ledger `verified_receipt_ref_count=3`，observed ref shapes include `provider_blocker_ref` and `typed_blocker_ref`，`long_evidence_ready=false`，`capability_status=capability_slo_blocked`。`provider_long_soak.capability_evidence_observed_requirement_ids` 现在记录 `restart_requery_ready`、`signal_history_ready`、`typed_closeout_required_ready`、`missing_closeout_block_ready`、`retry_dead_letter_boundary_ready`，真实 readout 的 `capability_missing_requirement_ids=[]` / `capability_open_requirement_count=0`；`foundry_agent_os_production_evidence_gate.owner_route_work_orders[lane=provider_long_soak]` 同步暴露空 `missing_owner_action_ids`、`owner_action_checklist` 和 `next_evidence_action=capability_slo_requirements_observed_not_production_ready_claim`。 | Stop-loss：provider blocker、typed blocker、per-capability refs-only evidence 或 capability checklist 可以解释 / park provider evidence tail，但不等于 provider production ready；若后续 claimed long window 后 `long_evidence_ready=false`，重新回 OPL runtime owner；provider completion、verified ledger、typed blocker ref、capability checklist、per-capability observed refs 或 open count 0/1 仍不能外推为 provider production ready、production ready 或 domain ready。 |
| `app_release_user_path` | App first-conversation warmup、Full runtime native trust、release-boundary gate、release-owner typed blocker path 和 same-cohort operator evidence bundle 已能被 OPL maturity 读取；`app_release_user_path.execution_runbook` 暴露可执行 first slice：App release owner 用 `opl runtime app-release-evidence record --payload ...` 记录 same-cohort release / operator refs，或用 release-owner verdict path 记录 `release_owner_receipt_refs` / `install_evidence_refs`，也可单独记录 release-owner typed blocker，再用 `verify --receipt-ref` 验证，并用 `list --json`、App/operator drilldown 和 maturity readout 回读。fresh readout 中 App release-owner verdict handoff 已由 `release_owner_receipt_ref://one-person-lab-app/release-owner/v26.6.12/receipt-20260612-owner-verdict` 关闭为 `release_owner_receipt_recorded_not_release_ready_claim`，same-cohort user path 已达到 `open_gate_count=0`、`typed_blocker_ref_count=0`、`production_user_path_ready=true`，maturity 顶层 `app_release_user_path_open_count=0`。 | Stop-loss：当前仍不是 release-ready；App release-owner receipt 和 same-cohort user-path evidence 只关闭 release/user-path evidence lane；release-ready / production-ready verdict 仍回 App release 流程。success refs、release-owner verdict refs 和 typed blocker refs 不混在同一 payload；新 cohort 未完整时继续 false。 |
| `no_resurrection` | 后续代码和文档不重新引入 retired executor / entry / local-manager vocabulary、compat alias、facade、wrapper、old CLI alias 或 compatibility-only tests 作为 active surface。本轮把 `tests/src/active-path-residue-scan.test.ts` 的 active docs 扫描从固定少数文件扩到全部 `docs/active/**/*.md`，focused guard 已通过，当前未发现 active 文档把旧路径重新声明成默认面。 | Focused guard、docs scan、contract/source tests 和 review 一起阻断旧路径复活；保留的旧词必须处在 history/provenance/negative-guard 语境。该 guard 只证明 no-resurrection 扫描覆盖，不授予 physical delete、domain ready、App release ready 或 production ready。 |

`opl framework operating-maturity --family-defaults --json` 是当前 evidence lane 聚合读面：它先在 `owner_evidence_intake` 中读取 domain owner payload summary、Brand L5、App release 和 Codex App runtime evidence 的 refs-only ledger，再把 `observed_receipt_refs`、`observed_ref_shapes`、`observed_ref_counts` 与 per-domain observed status 回投到 `foundry_agent_os_production_evidence_gate.owner_route_work_orders` 和 `domain_owner_chain_scaleout.domain_owner_evidence_routes`。domain owner payload summary 会保留 human gate、quality/export、reviewer 和 long-soak concrete refs，便于 per-domain owner route 精确读出“已有 refs，但仍不是 ready claim”。Work order 的 `owner_evidence_closure_state` 只区分 `owner_evidence_required`、`owner_evidence_recorded_not_ready_claim` 和 `owner_acceptance_or_typed_blocker_required`；`open_count_semantics` 明确 `open_count=0` 不是 ready 授权。新增 `unresolved_owner_gates` 是 owner gate inventory：它把 current owner answer handoff 和 production evidence work orders 聚成单一可执行清单，但 completion policy 固定要求 owner-native refs，且 `refs_only_projection_closes_ready=false` / `opl_can_close_owner_gate=false`。该读面只定位下一 owner、accepted ref shape、observed ref shape、closing source、verify path 和 false-authority boundary；不能把 L4 structural baseline、controlled canary、verified refs-only ledger、provider completion、App projection、owner-route work order projection、unresolved gate projection 或 docs foldback 升级成 domain ready、App release ready、Brand module L5 或 production ready。具体 counter、receipt 和 runtime ledger refs 每轮从 fresh CLI/read-model 读取。

## Active-Goal Agent Prompt

- Primary lane: `W7-owner-evidence-and-production-tail`，来源是 [Foundry Agent OS Family Target Implementation Plan](./foundry-agent-os-family-target-implementation-plan.md) 的 `owner-evidence` lane。本轮 fresh foldback 已确认 OPL 侧落地的是 owner-route work order 投影、字段和 guard；下一步目标不是再证明 W0/W1/W3/W5/W6 结构面，也不是把 work order 投影当 ready，而是逐项消费 `framework operating-maturity` 的 owner-route work orders，推动 domain/App/brand owner 给出真实 closing ref 或 typed blocker。若同时处理 OPL source-boundary lane，例如 `existing-dedupe-reconcile.ts` semantic split，必须把它标为 `hygiene_only_supporting_active_gap`，不得和 W7 completion 混账。
- Write scope: 默认 OPL 只改 refs-only intake / projection / regression guard / 必要 docs foldback；MAS/MAG/RCA/OMA/App/brand 仓只有在对应 owner evidence 的 source of truth、写集、验证命令和禁止范围明确时才写。不要在 MAS 或其他 domain 仓新增 external-learning selector、第二 active backlog、always-on sidecar 或默认 preflight。
- Non-goals: 不让 OPL 生成 MAS/MAG/RCA/OMA owner receipt、typed blocker、quality/export/review receipt、human gate、App release verdict、Brand L5 verdict、physical delete authorization 或 production-ready claim；不把 conformance pass、generated parity、verified refs-only ledger、provider completion、App projection 或 docs foldback 写成理想生产态完成。
- Live truth inputs: 先重读 live source、contracts、tests、CLI/read-model、
  runtime ledger、provider receipt、domain-owned manifest、App evidence、git
  worktree/branch/PR state 和下列 OPL readouts，不从本文继承旧 counters：

   ```bash
   rtk opl framework readiness --family-defaults --json
   # read payload under .framework_readiness
   rtk opl agents conformance --family-defaults --json
   # read payload under .standard_domain_agent_conformance; domain reports live in .standard_domain_agent_conformance.reports[]
   rtk opl agents interfaces --family-defaults --json
   # read generated_direct_parity as regression guard only
   rtk opl framework operating-maturity --family-defaults --json
   # read payload under .framework_operating_maturity; then read owner_evidence_intake, foundry_agent_os_production_evidence_gate.owner_route_work_orders, domain_owner_chain_scaleout.domain_owner_evidence_routes, unresolved_owner_gates.ready_claim_authorized, summary.provider_long_soak_open_count, and summary.ready_claim_authorized
   rtk opl runtime app-operator-drilldown --json
   # read payload under .app_operator_drilldown
   rtk opl brand-modules l5-status --json
   # read payload under .brand_module_l5_status; L5 remains evidence_required input only
   ```

- Execution discipline: 每次只选一个 owner-route work order 或一组写集完全不相交的 work orders。能安全并行时为 MAS/MAG/RCA/OMA/App/brand owner evidence 分 worktree / subagent；主会话必须复核 diff、验证、吸收、push 和清理。已有并发 worktree / dirty 文件一律按外部 owner lane 处理，除非用户明确授权，不得吸收、清理或覆盖。

- Required actions:
  1. Fresh read `unresolved_owner_gates.gates`、`foundry_agent_os_production_evidence_gate.owner_route_work_orders`、`domain_owner_chain_scaleout.domain_owner_evidence_routes`、Brand L5 evidence-required modules、App release/user-path evidence 和 private-platform retirement work orders。
  2. 对每个 open work order 标注 `owner_repo`、`accepted_ref_shapes`、`closing_ref_source`、`typed_blocker_source`、`forbidden_opl_claims` 和 `verification_command`。
  3. 对 MAS current owner lane，先用 read-only dry-run 确认 managed execution envelope、current work unit fingerprint、runtime recovery permission、provider admission candidates、publication gate blockers 和 current-control residue。若 envelope 是 executable owner action，则下一步是 MAS owner 产出 `publication_gate_replay_delta_or_typed_blocker`；若只剩 current-control typed blocker residue，不能由 OPL 文档或 owner-payload summary 代签 readiness pointer。
  4. 先处理能由真实 owner 立即给出 receipt / blocker / human gate / release / long-soak / private delete decision 的 work order；不能处理的写成 owner-needed typed work order，不用 OPL 文档代签。
  5. 若只需 OPL readout/regression guard，保持 domain repos read-only；若必须写 domain/App/brand 仓，先确认 source owner、consumer owner、写集和 repo-native 验证。
  6. 更新 focused tests / contracts / docs 中必要的机器边界，确保 conformance pass、generated surface ready、verified refs-only receipt、provider completion 和 L4/L5 counter 都不能声明 domain ready。
- Verification commands: OPL readout/regression lane 最小验证为 `rtk npm run typecheck`、`rtk npm run test:fast`、`rtk npm run test:meta`；domain/App/brand owner evidence lane 还必须跑对应 repo-native verification；若只做 docs baton 更新，最小验证为 `rtk git diff --check` 和 `rtk rg -n '^(<<<<<<<|=======|>>>>>>>)' docs/active docs/status.md docs/decisions.md`。
- Completion gate: 只能归类为 `owner_evidence_recorded`、`owner_typed_blocker_recorded`、`owner_needed_work_order_projected`、`real_external_blocker` 或 `hygiene_only_supporting_active_gap`；不得关闭全局理想生产态，也不得用 W0-W6 structural landing 代替 W7 production evidence。
- Foldback target: W7/owner-evidence 结论折回本文、Foundry plan、必要 contracts/tests 和 runtime/conformance readout；dated proof、worktree/branch closeout 和 receipt 流水进入 runtime ledger、提交历史或 domain-owned provenance。

## Forbidden Claims

- Descriptor ready、conformance passed、generated bundle ready、queue completion、suite pass、file existence 或 test pass 不能写成 domain quality verdict。
- OPL ledger receipt、stage evidence workorder、provider proof 或 App drilldown projection 不能写成 MAS paper closure、MAG grant-ready、RCA visual-ready、OMA default promotion、App release ready 或 production ready。
- Runway safe action、Console owner-action projection、Vault verified refs、multi-plane health 或 operating-model docs foldback 不能写成 domain owner answer、domain typed blocker、quality/export/review verdict、physical delete authorization、App release verdict、Brand L5 或 ready declaration。
- 同一个 executor 不能在同一上下文中先执行再自审并关闭 AI-first quality gate。
- Retained refs-only adapter、diagnostic shell、tombstone/provenance code path、compatibility facade、re-export wrapper 或 default-caller duplicate 不是标准 agent 完成态。
- 文档归档不能替代实现清理；旧模块、旧接口、旧测试和旧文档入口被当前 owner surface 替代后，按 direct retirement 删除或 tombstone。

## 验证入口

Docs-only inventory updates:

- `rtk git diff --check`
- `rtk rg -n '^(<<<<<<<|=======|>>>>>>>)' docs`
- targeted stale wording scan for retired entry/runtime vocabulary outside `docs/history/**`

触及 source / contract / runtime / App 行为时，按 owner repo 验证：

- OPL: `rtk ./scripts/verify.sh` 或 focused `npm run test:fast` / `npm run test:meta`
- MAS: `rtk ./scripts/verify.sh` 或 MAS repo-local focused tests
- MAG: `rtk ./scripts/verify.sh`、`rtk make test-meta` 或 focused product-entry/autonomy tests
- RCA: `rtk npm run test:fast` 或 focused product-entry/sidecar/native helper tests
- OMA: `rtk npm test`、`rtk npm run typecheck`
