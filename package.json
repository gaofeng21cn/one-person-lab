{
  "name": "opl-readonly-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "opl": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "node ./scripts/lint.mjs",
    "test": "NODE_NO_WARNINGS=1 node --experimental-strip-types --test test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.8.3"
  }
}
